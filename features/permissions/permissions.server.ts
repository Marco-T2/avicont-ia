/** Re-exports moved to hex (§13.APPLICATION canonical home). Named re-exports for spy/mock surface preservation. */
export {
  requirePermission,
  canAccess,
  canPost,
} from "@/modules/permissions/application/permissions.server";
