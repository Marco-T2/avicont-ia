## 3. Reglas duras (las inviolables)

| # | Regla | Enforcement |
|---|-------|-------------|
| **R1** | `domain/` NO importa de `infrastructure/`, `application/`, `presentation/`, ni de `@/generated/prisma/client`, ni de `@prisma/client`. | ESLint + review |
| **R2** | `application/` SOLO importa de `domain/` (suyo o de `shared/domain/`). | ESLint + review |
| **R3** | `infrastructure/` implementa interfaces definidas en `domain/`. Nunca al revés. | Review |
| **R4** | `presentation/` SOLO habla con `application/`. Nunca toca `infrastructure/` ni Prisma. | ESLint + review |
| **R5** | Tipos generados por Prisma (`MortalityLog`, `AccountSubtype`, etc.) viven SOLO en `infrastructure/`. Si la presentation o el dominio necesitan un tipo equivalente, se define propio. | ESLint + review |
| **R6** | Repositorios devuelven **entidades de dominio**, no rows de Prisma. La conversión la hace el `mapper`. | Review |
| **R7** | Use cases / services tienen UN propósito claro. Si el archivo pasa de ~200 líneas o tiene >5 dependencias inyectadas, se parte. | Review |
| **R8** | Errores de dominio son explícitos (`MortalityCountExceedsAlive`), no `throw new Error("...")`. Reutilizar la jerarquía existente (`AppError`, `ValidationError`, etc.). | Review |
| **R9** | Entities con métodos de transición de estado son **inmutables** — el método retorna nueva instancia, nunca muta in-place. **Invariantes enforzadas dentro del método** (no afuera). Sobre estados terminales (VOIDED, etc.) los métodos arrojan `DomainError`. El use case filtra entities en estado terminal **antes** de invocar (orquestación legítima, no lógica de negocio — ver §8.6). | Review |

**Las flechas SIEMPRE van hacia `domain/`.** Si una flecha apunta hacia afuera, está mal.

---

## 9. Reglas de linting (ACTIVAS)

Implementadas en `eslint.config.mjs` usando `no-restricted-imports` (built-in, sin plugin nuevo) — consistente con el patrón existente del repo para `serverBarrelPatterns`.

**Reglas activas:**

| Layer | Bloquea | Citado en error |
|-------|---------|-----------------|
| `modules/*/domain/**` | `@prisma/client`, `@/generated/prisma/*`, `@/lib/prisma`, `**/infrastructure/*`, `**/application/*`, `**/presentation/*` | R1, R5 |
| `modules/*/application/**` | Lo mismo + `**/presentation/*` | R2, R5 |
| `modules/*/presentation/**` | `@prisma/client`, `@/generated/prisma/*`, `@/lib/prisma`, `**/infrastructure/*` | R4, R5 |

**Excepción documentada**: `modules/*/presentation/composition-root.ts` está excluido de la regla R4 vía `ignores`. Es el ÚNICO archivo en presentation/ que puede importar de infrastructure/, porque su rol explícito es wirear adapters concretos al service.

**Verificado**: violaciones deliberadas (archivo de prueba con import prohibido) disparan errores ESLint claros con cita de regla y referencia al doc. Cero falsos positivos en `modules/mortality/`.

**Sin lint, la disciplina se degrada en 6 semanas.** No es opcional.

---

## 12. Convención naming infrastructure adapters

Convención **emergente** — descubierta inspeccionando los archivos existentes en `modules/*/infrastructure/` durante POC #10 C3-C, no inventada. Cumple retroactivamente sin excepciones en los 32 archivos de scope (3 `.repo.ts` + 8 `.repository.ts` + 21 `.adapter.ts`). Aplica a adapters y repositories; mappers, unit-of-work y tests siguen patrones propios fuera de este scope (ver §12.5).

### 12.1. Regla — owner vs consumer

| Rol | Sufijo de archivo | Cuándo aplicar |
|---|---|---|
| **Owner** del aggregate del módulo (persiste sus filas) | `.repo.ts` o `.repository.ts` | El módulo posee el aggregate y lo persiste |
| **Consumer** de un concept externo (legacy, otro módulo, lookup) | `.adapter.ts` | Wrappea concept ajeno o consume port de otro módulo |

La distinción **NO es DB vs no-DB**. `prisma-payment-credit.adapter.ts` y `prisma-lot-inquiry.adapter.ts` tocan Prisma directo y aún así son `.adapter.ts` porque el módulo no posee ese aggregate — son consumers que leen/agregan datos de otros módulos.

### 12.2. Sub-prefijos para `.adapter.ts`

| Prefijo | Significado | Ejemplo |
|---|---|---|
| `legacy-` | Wrappea código de `features/` (era pre-modularización) | `legacy-journal-entries-read.adapter.ts` |
| `prisma-` | Toca Prisma directo, sin wrapper legacy intermediario | `prisma-payment-credit.adapter.ts` |
| (sin prefijo) | Wrappea port de otro módulo ya migrado | `contacts-read.adapter.ts` |

Distribución actual de los 21 `.adapter.ts`: 11 `legacy-`, 2 `prisma-`, 8 sin prefijo.

Para `.repo.ts` / `.repository.ts` de owner: siempre con prefijo `prisma-` — es la única implementación productiva en el repo a la fecha.

### 12.3. Sub-convención `.repo.ts` vs `.repository.ts`

Coexisten dos sub-convenciones para owner-aggregate; **ambas válidas**. NO se uniforma retroactivamente.

| Sufijo | Origen | Módulos |
|---|---|---|
| `.repository.ts` | Convención anterior (módulos pre-`shared/`) | `contacts`, `fiscal-periods`, `mortality`, `org-settings`, `payables`, `payment`, `receivables`, `voucher-types` |
| `.repo.ts` | Convención posterior (`accounting` + `shared/`) | `accounting/prisma-account-balances`, `accounting/prisma-journal-entries`, `shared/prisma-fiscal-periods-tx` |

Distribución actual: 8 `.repository.ts`, 3 `.repo.ts`. Adoptar `.repo.ts` para módulos nuevos; mantener `.repository.ts` en los 8 existentes.

### 12.4. Class name match — sub-prefix

El sub-prefix del filename (`prisma-`, `legacy-`, sin prefijo) se preserva en el prefix de la class. Regla determinística sin excepciones en los 32 archivos de scope.

| Filename | Class |
|---|---|
| `prisma-foo.{adapter\|repo\|repository}.ts` | `PrismaFoo<Suffix>` |
| `legacy-foo.adapter.ts` | `LegacyFoo<Suffix>` |
| `foo.adapter.ts` (sin prefijo) | `Foo<Suffix>` (sin prefijo) |

Ejemplos:

- `prisma-payment-credit.adapter.ts` → `PrismaPaymentCreditAdapter`
- `legacy-permissions.adapter.ts` → `LegacyPermissionsAdapter`
- `contacts-read.adapter.ts` → `ContactsReadAdapter`

El concept central de la class, la pluralidad y la translation del file-suffix (`.repo` → `Repo` vs `Repository`) **no son determinísticos** — siguen patrones heurísticos contextuales declarados en §12.5.

### 12.5. Out of scope

La convención cubre **solo adapters y repositories** en `modules/*/infrastructure/`. Los siguientes archivos del mismo directorio quedan fuera del scope (siguen patrones propios):

- **Mappers** (`*.mapper.ts`) — lógica de mapeo entre tipos de dominio y rows de Prisma. Anomalía menor de naming: `accounting/journal-mapping.ts` (no `.mapper.ts`).
- **Unit of Work** (`*-unit-of-work.ts`) — `shared/prisma-unit-of-work.ts`, `accounting/prisma-accounting-unit-of-work.ts`.
- **Tests** (`__tests__/*.{test,integration.test}.ts`) — sufijo derivado del archivo testeado.

Si se introduce un archivo nuevo en `modules/*/infrastructure/` que no encaja en las categorías de §12.1, documentar la categoría nueva explícitamente antes de adoptarla.

**Heurísticas contextuales (no normadas)**: el concept central de la class, la pluralidad y la translation del file-suffix tienen variabilidad legítima en el inventario actual:

- **Concept central**: a veces alineado al filename (`legacy-fiscal-periods.adapter.ts` → `LegacyFiscalPeriodsAdapter`, descartando el `Read` del port `FiscalPeriodsReadPort`), a veces al port (`payables.adapter.ts` → `PayablesQueryAdapter`, agregando el `Query` del port).
- **Pluralidad**: la class suele seguir el filename, no el port (`prisma-payables.repository.ts` con port `PayableRepository` singular → class `PrismaPayablesRepository` plural).
- **File-suffix**: `.repo.ts` mapea a class suffix `Repo` en 2/3 casos (`PrismaAccountBalancesRepo`, `PrismaFiscalPeriodsTxRepo`) y a `Repository` en 1/3 (`PrismaJournalEntriesRepository`). Coexistencia válida — NO uniformizar retroactivamente.

Estas heurísticas no son violaciones de §12.4 — la regla literal cristalizada (sub-prefix match) sigue determinística. Cuando un archivo nuevo presente ambigüedad de concept/pluralidad/suffix, decidir en contexto y documentar la elección en el commit body si la decisión no es obvia.

---

## 17. Carve-out: cross-module imports en `infrastructure/`

Excepción contextual a la convención implícita "`infrastructure/` no importa concretos cross-module". Cubre el caso documentado en §15 ("Cómo manejar transacciones que cruzan módulos") cuando R3 sigue vigente — la flecha apunta al dominio porque el adapter concreto importado implementa un port definido en `domain/` del módulo dueño.

### 17.1. Scope acotado — qué cubre y qué NO

El carve-out aplica **únicamente** a:

| Categoría | Filename pattern | Razón estructural |
|---|---|---|
| **Unit of Work** | `*-unit-of-work.ts` | El UoW debe construir adapters tx-bound dentro del callback `withAuditTx`. La `Prisma.TransactionClient` no existe pre-tx — un singleton en composition-root no puede capturar el `tx` per-run. Sin esta excepción, el UoW pattern (§4.3) es inejecutable. |
| **Helpers puros de mapping** | `*-mapping.ts`, `*.mapper.ts`, funciones `hydrate*FromRow` / `to*Persistence` | Funciones puras row ↔ entity, sin estado ni efectos. Reutilizar evita duplicar mapeos byte-equivalentes (mismo tradeoff que rule-of-three §11.1). |

**NO aplica** a adapters generales (`*.adapter.ts`, `*.repository.ts`, `*.repo.ts`). Si un adapter regular necesita lógica de otro módulo, va vía port (R3 + R6) o wrap-thin de legacy (`features/`). El carve-out NO es justificación universal.

### 17.2. Direccionalidad — R3 sigue vigente

Los adapters concretos importados **implementan ports definidos en `domain/`** del módulo dueño:

```
sale/infrastructure/prisma-sale-unit-of-work.ts
   ↓ imports concrete (carve-out §17)
accounting/infrastructure/prisma-journal-entries.repo.ts
   ↓ implements (R3)
accounting/domain/ports/journal-entries.repo.ts (port)
```

El UoW de `sale` no acopla con la **lógica** de `accounting` — solo con la **construcción** de un adapter que ya respeta el contrato del port. Sustitución por test fake se hace inyectando otro `UnitOfWork` completo, no patcheando el UoW concreto.

### 17.3. Cita obligatoria en JSDoc

Todo import cross-module concreto cubierto por este carve-out **debe citar la regla en el JSDoc del archivo** con la forma:

```
§17 carve-out: <razón estructural concreta>
```

Ejemplo:

```ts
/**
 * Postgres-backed adapter for the sale UnitOfWork port.
 *
 * §17 carve-out: UoW construye adapters tx-bound dentro de `withAuditTx` —
 * `Prisma.TransactionClient` no existe pre-tx, singleton en composition root
 * no puede capturar `tx` per-run. Cross-module concrete imports:
 * `accounting/PrismaAccountBalancesRepo`,
 * `accounting/PrismaJournalEntriesRepository`,
 * `receivables/PrismaReceivablesRepository`,
 * `shared/PrismaFiscalPeriodsTxRepo`.
 */
```

La cita es **auditable**: cualquier import cross-module concreto en `infrastructure/` que no cite §17 es violación, no excepción.

### 17.4. Precedentes

| POC | Adapter | Cross-module imports cubiertos |
|---|---|---|
| #10 | `accounting/infrastructure/prisma-accounting-unit-of-work.ts` | `shared/PrismaFiscalPeriodsTxRepo` |
| #11.0a Ciclo 6 | `sale/infrastructure/prisma-sale-unit-of-work.ts` | `shared/PrismaFiscalPeriodsTxRepo`, `accounting/PrismaAccountBalancesRepo`, `accounting/PrismaJournalEntriesRepository`, `receivables/PrismaReceivablesRepository` |
| #11.0a Ciclo 4 | `sale/infrastructure/prisma-journal-entry-factory.adapter.ts` | `accounting/journal-mapping.hydrateJournalFromRow` (helper puro) |

### 17.5. Cross-ref

- **Cierra**: §15 ítem abierto "Cómo manejar transacciones que cruzan módulos".
- **Complementa**: R3 (dirección de dependencia), §11.1 (rule of three para promoción a `shared/`).
- **NO debilita**: R1, R2, R4 — `domain/`, `application/`, `presentation/` siguen sin importar cross-module infrastructure (ESLint §9 lo enforça).

---

## 18. Reducción de scope en cutover por dependencia con decisiones lockeadas

Cuando un POC de cutover (sustitución de un módulo legacy por su equivalente hex) descubre que parte del scope original depende de un **bridge o interface cuya forma viola una decisión arquitectónica lockeada en POCs anteriores** (e.g., un path que ya fue retirado intencionalmente), la respuesta correcta es **reducir scope y diferir la parte afectada** al POC donde la decisión deje de aplicar — NO forzar el cutover.

**Por qué**: forzar el cutover en presencia de esta colisión deja sólo tres caminos, todos malos:

- **(α) Adaptador wrap-thin que ignora la decisión** — el bridge re-implementa el path retirado en el adapter (e.g., reabre tx interna por shape, hidrata entidades cross-aggregate por loop). Resultado: decisión lockeada queda viva en infraestructura, contradiciendo la decisión documentada. Suele también violar fidelidad regla #1 (atomicidad, performance, consistencia observable).
- **(β) Revertir la decisión lockeada** — re-codificar el path retirado en `application/` o `domain/`. Pierde el progreso del POC anterior; descalibra la conversación arquitectónica.
- **(δ) Path lateral compensatorio** (saga, eventual consistency, N+1 hidratación) — introduce un patrón nuevo sin precedente que va a propagar.

La opción correcta — **(γ) reducir scope y diferir** — preserva la decisión lockeada y deja la deuda explícita, con destino y forma de cierre conocidos.

### 18.1. Cuándo aplicar

Aplica cuando se cumplen los tres criterios:

1. **Decisión lockeada anterior**: el cutover de la parte afectada requeriría revivir un path que un POC anterior retiró intencionalmente (con cita en commit, JSDoc, o §X de este documento).
2. **Bridge inevitable**: el legacy consumer expone una interface (`*ServiceForBridge`, `*ServiceForHub`, etc.) cuyo contrato sólo se cumple revivendo ese path.
3. **POC futuro identificable**: existe un POC en el roadmap donde la interface deja de existir (porque su consumer migra a hex también) o se rediseña.

Si el criterio 3 falla — no hay POC futuro donde el bloqueador desaparezca — entonces la opción γ no resuelve, sólo posterga. En ese caso, el problema es la decisión lockeada, no el cutover; reabrir la decisión.

### 18.2. Cómo documentar

Toda reducción de scope cubierta por §18 debe registrar **en el commit y en el bookmark del POC**:

1. **Scope original** (lo que el bookmark prometió): "POC #X cutover N consumers".
2. **Scope reducido** (lo que se entrega): "POC #X cutover M consumers; (N − M) diferidos a POC #Y".
3. **Razón estructural concreta** — qué decisión lockeada bloquea, con cita: "§5.5 retired Ciclo 6 elimina `externalTx` en `regenerateJournalForIvaChange`; bridge `SaleServiceForBridge` requiere `externalTx` para atomicidad write-IVA + regen".
4. **POC destino** + **acceptance del cierre**: "POC #Y debe cutover los (N − M) diferidos como pre-condición de cierre. El bridge se rediseña en POC #Y porque su consumer (IVA) migra a hex y el contrato deja de necesitar `externalTx`".

Sin los 4 puntos, la deuda se vuelve invisible y se transmite como "el scope era distinto al bookmark, no me acuerdo por qué".

### 18.3. Direccionalidad — el POC destino hereda la deuda explícita

El POC destino **NO** es libre de cerrar sin honrar el cutover diferido. La deuda `(N − M)` es pre-condición. El bookmark del POC destino debe abrir con esa deuda en su lista de scope, no descubrirla al cerrarlo.

### 18.4. Precedentes

| POC | Bookmark scope | Scope entregado | Diferido a | Razón estructural |
|---|---|---|---|---|
| #11.0a A4 | Cutover 12 consumers formales de legacy `SaleService` (3 core API + 8 iva-books + 1 dispatches-hub)¹ | Cutover 3 consumers (A4-a: 3 core API routes) | POC #11.0c | Bridges `SaleServiceForBridge` (8 iva-books) y `SaleServiceForHub` (1 hub) requieren shape y contracts que sólo hacen sentido cuando IVA migre a hex y los bridges se rediseñen sin `externalTx` (§5.5 retired Ciclo 6 hex `regenerateJournalForIvaChange`) ni shape-adapter (`Sale` aggregate hex no expone `period` hidratado ni `displayCode`/`contact` hidratado para hub). En POC #11.0c, el consumer (IVA) migra a hex; los bridges legacy desaparecen y los 10 consumers diferidos hacen cutover atómico contra puertos hex equivalentes. |
| #11.0b A4 γ | Cutover 12 consumers formales de legacy `PurchaseService` (3 core API + 4 iva-books/purchases + 3 server components + 2 client components)² | Cutover 1 consumer (A4-a γ: `purchases/route.ts` GET + POST) | POC #11.0c | Asimetría legítima vs sale A4-a (3 core routes): purchase entrega scope mínimo γ porque (a) shape arquitectónico hex `makePurchaseService()` factory ya validado por sale A4-a precedent — re-validar 3 core API routes purchase es trabajo redundante; (b) bridge cross-module `iva-books.service.ts:618 recomputeFromPurchaseCascade` paralelo simétrico al sale `recomputeFromSaleCascade:559` requiere cutover atómico cuando IVA migre a hex; (c) `PurchaseWithDetails` legacy DTO migration paralelo sale A5 β Ciclo 3 diferida POC #11.0c — server components + client components consumen el DTO legacy directamente. γ entrega validación asimetrías purchase-specific (object DI spread vs sale positional, factory cross-module sale-side reusado `PrismaJournalEntryFactoryAdapter`, 5+1 cross-module imports §17, UoW 9 scope members) — destapó drift heredado A4-a γ simétrico al sale (legacy Zod parsea `date: string + lineAmount: number` vs hex `CreateDraftInput {date: Date, lineAmount: MonetaryAmount}`, runtime safe via JS coerción, tsc baseline 14 → 16 con 2 callsites POST createDraft + createAndPost vs sale 1 callsite). |
| #11.0c A4 + A5 | Hereda 10 cutovers sale-side diferidos POC #11.0a A4 (8 iva-books + 1 hub + `dispatches/page`) + 11 cutovers purchase-side diferidos POC #11.0b A4 γ (2 core API + 4 iva-books/purchases + 3 server components + 2 client components) + cleanup `features/sale/` y `features/purchase/` load-bearing (POC #11.0a A5 β y POC #11.0b A5 cleanup completo diferido)³ | A4 entera 18 cutovers runtime entregados (A4-a 8 routes core + A4-b 10 iva-books cascade + A4-c cycle-break + cutover hex contract + cleanup integration mirror) + retroactive REQ-FMB.5 fix corrector (entity-to-dto barrel re-route) + A5 sanitize correlationId-api-leak (8 prod routes + 8 tests + 8 JSDoc atomic revoke) | POC futuro pre-IVA-CRUD-hex-migration | 4 bridges teardown DEFER IVA-CRUD-hex-migration (2 inbound `SaleServiceForBridge`, `PurchaseServiceForBridge` + 2 outbound `IvaBooksServiceForSaleCascade`, `IvaBooksServiceForPurchaseCascade`) — pre-condición pre-IVA-CRUD-hex-migration: contracts bridge sólo se rediseñan cuando IVA migre a hex y el shape `externalTx` (§5.5 retired Ciclo 6) deja de ser pre-condición. (Bridge `SaleServiceForHub` defer separado: hub-migration POC, NO IVA-CRUD; shape-adapter cross-aggregate hidratación `Sale.period`/`displayCode`/`contact` permanece §18.4 fila POC #11.0a contexto histórico.) Cleanup `features/sale/` + `features/purchase/` load-bearing también se difiere al mismo POC futuro — load-bearing por bridges activos. Detalle estructural §19. |
| **POC siguiente** (IVA CRUD hex migration + bridges teardown atómico) | A1 (4 bridges teardown atómico: 2 inbound `SaleServiceForBridge` + `PurchaseServiceForBridge` deletion + 2 outbound `IvaBooksServiceFor{Sale,Purchase}Cascade` asimetría C1 purchase + C2 sale) + A2 (TASA_IVA Decimal migration legacy → `iva-calc.utils.ts` + REQ-FMB.5 re-export top-level barrel + vi.mock factory legacy cleanup 8 archivos surgical Cat A 6 + Cat B 2 + atomic delete legacy class structure 2 source classes `IvaBooksService`/`IvaBooksRepository` + 4 legacy tests + 2 Q5 integration tests + `server.ts` trim) — pre-condición §19 puntos 1 + 3 CUMPLIDA | A1 (3 commits master `c7805f3` + bridges) + A2 (5 commits cumulative `9964739`/`972c884` C1 + `d01089c`/`ee5f461` C1.5 + `1fe6874`/`66be172` C2 + `b1c429d`/`349ea9a` C3) + A4 doc-only (this commit batch — JSDoc sweep `37b2301` + architecture.md cementación) | POC nuevo dedicado A3 | A3 cleanup `features/{sale,purchase}/` load-bearing es **trabajo distinto, NO cleanup atómico** (§14 boundary natural): (a) migración hex `modules/purchase/presentation/dto+schemas` precondición — asimetría material §13.R (sale tiene `presentation/{dto,schemas}/`, purchase NO); (b) 8 callers cutover (4 sale `new SaleService()` no-args + HubService dep en `dispatches/page` + `dispatches-hub/route` + 4 purchase `new PurchaseService()` no-args en 2 pages + 2 routes — NO 3 como prompt original §13.Q count drift); (c) DTO mappers presentation pages cutover (§13.T shape divergence hex `Sale[]` entity vs legacy `SaleWithDetails[]` DTO — pages requieren mapper, NO trivial swap); (d) HubService refactor decisión (Sale↔Dispatch hub callers); (e) 20 legacy tests deletion final (11 sale + 9 purchase). Pre-recon expand reveló 5 §13 emergentes Q-U scope honest desde inicio POC nuevo (mirror A2 strategy β refinement precedent). Engram heredado `poc-siguiente/a3/pre-recon-deferred-new-poc` (#1517) — POC nuevo Step 0 leerá este bookmark + scope-locked-bookmark inicial Marco lock granularity 6-8 ciclos (A3-C1 build hex purchase presentation/dto+schemas + A3-C2 sale callers cutover + A3-C3 purchase callers cutover + A3-C4 types/schemas consumers + A3-C5 HubService decisión + A3-C6 atomic delete features/sale + A3-C7 atomic delete features/purchase + A3-C8 doc-only post-mortem). |
| **POC nuevo dedicado A3** (cleanup `features/{sale,purchase}/` load-bearing + cumulative cutover hex modules/{sale,purchase}/ presentation) | 8 ciclos originales heredados engram `poc-siguiente/a3/pre-recon-deferred-new-poc` (#1517) — A3-C1 build hex purchase presentation/dto+schemas + A3-C2 sale callers cutover + A3-C3 purchase callers cutover + A3-C4 types/schemas consumers + A3-C5 HubService decisión + A3-C6 atomic delete features/sale + A3-C7 atomic delete features/purchase + A3-C8 doc-only post-mortem | 15 ciclos cumulative entregados (granularity natural §14 expandida — paired follow-ups RETROACTIVE C1.5+C3.5+C4a.5+C4b.5+C5.5 evidencias #12 1+2 cumulative + PROACTIVE pattern matures C6a+C6b+C6c evidencias #12 3+4 + #13 1+2+3) + atomic delete A3-C7 features/sale wholesale + A3-C8 features/purchase wholesale (Marco Opción A1 archivo bridges-teardown DELETE wholesale §13.A3-C8-γ) + A3 Parte 2 doc-only post-mortem A3-D1+D2+D3+D4. **Suite 4942/6/2 runtime verified cementación A3-D1** (delta +14 net cumulative POC nuevo A3 vs A2 closure 4928 — engram cumulative claim 5043 stale drift +101 §13.A3-C8-δ formal cementación §19.6). **TSC 17 baseline EXACT preserved** (correction §13.A3-C7-γ — drift cumulative arithmetic discovery A3-C7 GREEN runtime verify, NO regression POC nuevo A3 — engram chain A2-C3 closure → A3-C* cumulative sin re-verify runtime cycle-by-cycle origen drift). **ESLint 10e/13w preserved** (improvement -1 errors vs POC siguiente baseline 11e/13w — error vivía en archivo deleted A3-C7+C8 wholesale, NO regression). **REQ-FMB.5 0 violations preserved**. **Test files 481 cumulative** (delta -10 net vs A2 closure 491 — `features/sale/__tests__/` × 11 + `features/purchase/__tests__/` × 9 + `bridges-teardown-shape.poc-siguiente-a1.test.ts` × 1 deleted = 21 cumulative offset por +11 NEW shape tests cumulative ciclos C2-C8). 32 commits cumulative pre-push + 3 doc-only D1+D2+D3 + push D4 = 35 commits batch al cierre POC entero. | POC futuro dedicado paridad fix routes 3+4 PATCH/POST mismatch sale + purchase paired (D5 sub-fase A3-D3 doc-only post-mortem cementación + cross-ref defer) | A3 cleanup `features/{sale,purchase}/` load-bearing fue entregado cumulative cutover hex modules/{sale,purchase}/ presentation precondición — granularity expandida natural §14 (8 → 15 ciclos paired follow-ups + atomic delete wholesale) NO scope creep, evidencias documentadas lecciones #12 + #13 PROACTIVE pattern matures cumulative. Bookmark scope original NO lockeado granularity ciclos final inicio POC nuevo A3 — Marco lock granularity expandida progresiva post-discovery latent bugs RETROACTIVE (#12 evidencias 1+2 A3-C4a.5+C4b.5) + PROACTIVE pattern matures (#12 evidencia 3 A3-C6a + #13 evidencias 1+2+3 A3-C6c+C7+C8). Pre-recon expand cumulative aplicó lecciones #1-#9 heredadas POC siguiente starting state — POC nuevo A3 cementa #10-#14 + #10-skippable sub-precedent doc-only A3-D1 (asymmetric cost-benefit Marco lock PROACTIVE 1ra evidencia paired #14 sister §13.A3-C7-γ + §13.A3-C8-δ runtime verify mandate). |
| **POC nuevo dedicado A4** (cleanup `features/org-settings/` load-bearing + cumulative cutover hex modules/org-settings/ presentation cross-feature/cross-module) | Bookmark scope engrams `poc-nuevo/a4/pre-recon-comprehensive` (#1564) + paired sister `poc-futuro/a4-org-settings/pre-recon-comprehensive` (#1565) — 16 ubicaciones cross-categoría: Cat 1 (6 app pages + 1 api route) + Cat 2 (4 page-rbac vi.mock factory) + Cat 3 (3 source cross-feature/cross-module + 2 paired tests) + atomic delete `features/org-settings/` wholesale (server.ts + index.ts + rmdir, sin __tests__/ collateral). Marco lock granularity Opción α'' Path α'' merge atomic Cat 1+Cat 2 single ciclo A4-C1 post Step 0 PROACTIVE pre-RED descubrió mock factory load-bearing render path coverage NO orphan suposición §13.A4-η formal cementación post-RED (mirror lección #13 PROACTIVE pattern). | 4 ciclos cumulative entregados (granularity natural §14 expandida — Path α'' merge atomic A4-C1 Cat 1+Cat 2 paired + atomic delete A4-C3 wholesale): A4-C1 atomic Cat 1+Cat 2 cutover hex pages/route + 4 vi.mock factory paired (Path α'' merge §13.A4-η — commits `07c9462` RED 30 assertions α + `a6de933` GREEN 10 archivos / 25 changes — 6 source pages/route + 4 vi.mock factory paired) + A4-C2 atomic Cat 3 cleanup cross-feature/cross-module hex 5 archivos / 13 source changes (commits `3951d05` RED 21 assertions α granular per callsite + `2a33993` GREEN, RED scope Test 15 method-target correction NAMED §13.A4-ι NO-formal mid-cycle) + A4-C3 atomic delete `features/org-settings/` wholesale (commits `0bae9f2` RED 5 assertions α 3 transition + 2 safety net per `feedback_red_acceptance_failure_mode` + `31ff403` GREEN 2 archivos + rmdir, mirror A3-C7+C8 simpler shape sin __tests__/ collateral) + A4-D1 doc-only post-mortem cementación cumulative architecture.md §18.4 fila A4 + §13.7 contadores expand 5 lecciones + §19.7 status closure (esta fila + §19.7) + §13.A4-γ engram-only Alt A. **Suite 4998/6/2 runtime verified cementación A4-D1** (delta +56 net cumulative POC nuevo A4 vs A3 closure 4942 — runtime ground truth confirmed clean baseline cumulative arithmetic claims engram chain — preventive evidence vs drift discovery §13.A3-C7-γ + §13.A3-C8-δ pattern, lección #14 3ra paired sister formal). **TSC 17 baseline EXACT preserved** (NO regression POC nuevo A4 vs A3 closure 17). **ESLint 10e/13w preserved** (NO regression POC nuevo A4). **REQ-FMB.5 0 violations preserved**. **Test files 484 cumulative** (delta +3 vs A3 closure 481 — shape tests NEW POC A4 ciclos C1-C3 cumulative). 6 commits cumulative pre-push + A4-D1 doc-only commit cumulative cierre A4 entero (defer push batch A4 closure mirror precedent A1+A2+A3). | Ninguno (POC entero CLOSED A4-D1 cementación) | A4 cleanup `features/org-settings/` load-bearing fue entregado cumulative cutover hex modules/org-settings/ presentation precondición — granularity expandida natural §14 (5 ciclos α' originales propuesta agente pre-recon → 4 ciclos final Path α'' merge atomic post Step 0 PROACTIVE descubrió Cat 1/Cat 2 mock-source coupling estructural §13.A4-η). Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable heredadas POC nuevo A3 starting state — POC nuevo A4 NO cementa lecciones nuevas, expand contadores evidencias cumulative #10-#14 + sub-precedent (§13.7). 7 §13 emergentes A4 cumulative (5 PROACTIVE pre-RED engram-only formal α DTO divergence entity vs snapshot + β callers no-args 9 cumulative + γ drift textual §18.4 línea 787 + δ cross-module dep break payment adapter + ε cross-feature dep break dispatch + ai-agent + 1 formal post-RED §13.A4-η vi.mock factory load-bearing render path coverage NO orphan + 3 NO-formal inventory accuracy paired sisters cumulative cross-cycle ζ JSDoc `@deprecated` claim adapter inferential incorrect + θ + ι Test 15 method-target correction NAMED A4-C2 RED scope mid-cycle). §13.A4-γ engram-only Alt A drift textual §18.4 línea 787 nominal "A3-C2 sale callers cutover" vs entregado real (engram #1522 — A3-C2 cutover 6 consumers `@/features/purchase` barrel → hex modules/purchase/presentation deep paths) — preservación historical accuracy mirror precedent §13.A3-D3-α (3ra evidencia drift textual line ref §20.5). Pattern paired sister cumulative drift docs cumulative — 4 evidencias formales lección #14 (§13.A3-C7-γ + §13.A3-C8-δ + §13.A3-D3-α + §13.A4-γ). |
| **POC nuevo dedicado A5** (cleanup `features/voucher-types/` load-bearing + cumulative cutover hex modules/voucher-types/ presentation cross-feature/cross-module) | Bookmark scope engrams `poc-nuevo/a5/pre-recon-comprehensive` (#1579) + paired sister `poc-futuro/a5-voucher-types/pre-recon-comprehensive` (#1580) — Cat 1 (9 source pages/routes + 1 paired test) + Cat 2 (10 vi.mock factory paired) + Cat 3 (5 archivos cross-feature/cross-module α/β/γ + paired test cascade Issue #1) + Cat 4 (3 cross-module integration tests cleanup hex Shape α uniform — drift correction retroactivo 4→3 §13.A5-ζ formal pre-RED) + atomic delete `features/voucher-types/` wholesale (server.ts + index.ts + voucher-types.types.ts + rmdir auto-cleaned, sin __tests__/ collateral). Marco lock granularity Path α'' merge atomic Cat 1+Cat 2 single ciclo A5-C1 mirror A4-C1 precedent + 3 ciclos paired sister Cat 3 split A5-C2a/A5-C2b/A5-C2c (granularity expandida natural §14 5ta paired sister cumulative §13.A5-α/ε/ζ matures cross-§13 same POC). | 5 ciclos cumulative entregados (granularity natural §14 expandida cumulative — Path α'' merge atomic A5-C1 Cat 1+Cat 2 paired + paired sister Cat 3 split A5-C2a/C2b/C2c cross-feature/cross-module 5ta evidencia cumulative §13.A5-α/ε/ζ + atomic delete A5-C3 wholesale): A5-C1 atomic Cat 1+Cat 2 cutover hex pages/routes + 10 vi.mock factory paired (Path α'' merge §13.A5-γ formal cementación PROACTIVE pre-RED 8 callsites Opción C — commits `9a4c51b` RED 54 assertions α + `cfab7aa` GREEN 19 archivos / +117/-100 — 9 source pages/routes + 10 vi.mock factory paired) + A5-C2a atomic Cat 3 cross-feature cleanup hex 9 archivos (commits `b853164` RED 13 assertions α §13.A5-α formal cementación PROACTIVE pre-RED hex factory addition Option B Marco lock + `f1b9d9d` GREEN Path α'' merge §13.A5-α factory + §13.A5-ε method-on-class Option D-3 final lock dynamic import bypass R2 + Parameters introspection bypass R5) + A5-C2b atomic Cat 3 cross-module cleanup hex 5 archivos α/β/γ + paired test cascade Issue #1 (commits `d9e9517` RED 13 assertions α §13.A5-α 4ta evidencia + `14605bc` GREEN matures cumulative §13.A5-α factory cross-§13) + A5-C2c atomic Cat 4 cross-module integration tests cleanup hex 3 archivos uniform Shape α (commits `40cf286` RED 10 tests/13 assertions α §13.A5-ζ formal cementación PROACTIVE pre-RED 5ta evidencia paired sister A5-C2a/C2b precedent EXACT + `5b91a63` GREEN Path swap mecánico Shape α uniform 5ta evidencia matures classification by-target-type) + A5-C3 atomic delete `features/voucher-types/` wholesale (commits `47d7bfb` RED 5 assertions α 3 transition + 2 safety net per `feedback_red_acceptance_failure_mode` mirror A4-C3 EXACT estricto Opción B + `f9a1e06` GREEN 3 archivos + rmdir, simpler shape sin __tests__/ collateral) + A5-D1 doc-only post-mortem cementación cumulative architecture.md §18.4 fila A5 + §13.7 contadores expand 5 lecciones + §19.8 status closure (esta fila + §19.8) + §13.A5-α/γ/ε/ζ formal cementación canonical + §20.6 verify retroactivo + §13.A3-D4-α 10na evidencia matures cumulative POC A5. **Suite 5092/7/2 cumulative invariant 5101 preserved runtime verified cementación A5-D1** (delta +94 net cumulative POC nuevo A5 vs A4 closure 4998 — runtime ground truth confirmed cumulative invariant arithmetic preservation `passed+failed+skipped=5101` invariant {7,8} margin §13.A3-D4-α 10na evidencia env-dependent toggle 5ta paired sister A5-D1 lección #14 PROACTIVE pre-cementación). **TSC 17 baseline EXACT preserved** (NO regression POC nuevo A5 vs A4 closure 17). **ESLint 10e/13w preserved** (NO regression POC nuevo A5). **REQ-FMB.5 0 violations preserved**. **Test files 482/489 cumulative** (delta -2 passed vs A4 closure 484 — features/voucher-types/__tests__/ deletes offset por NEW shape tests cumulative POC A5 ciclos C1-C3). 10 commits cumulative pre-push + A5-D1 doc-only commit cumulative cierre A5 entero (defer push batch A5 closure mirror precedent A1+A2+A3+A4 EXACT). | Ninguno (POC entero CLOSED A5-D1 cementación) | A5 cleanup `features/voucher-types/` load-bearing fue entregado cumulative cutover hex modules/voucher-types/ presentation precondición — granularity expandida natural §14 (originalmente propuesta agente pre-recon ~5 ciclos α' → 5 ciclos final con granularity Cat 3 split paired sister §13.A5-α/ε/ζ matures cross-§13 same POC). Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable heredadas POC nuevo A4 starting state — POC nuevo A5 NO cementa lecciones nuevas, expand contadores evidencias cumulative #10-#14 + sub-precedent (§13.7) + cementa 4 §13 emergentes formales canonical α/γ/ε/ζ + 1 evidencia recurrente §13.A3-D4-α 10na matures. 4 §13 emergentes formales POC nuevo A5 cumulative: §13.A5-α multi-level composition-root delegation factory `makeVoucherTypeRepository` 5 evidencias cumulative cross-§13 same POC + §13.A5-γ DTO divergence runtime path coverage 8 callsites Opción C 4× magnitud vs §13.A4-α + §13.A5-ε method-on-class shim signature divergence Option D-3 final lock + §13.A5-ζ classification by-target-type drift correction retroactivo 5 evidencias cumulative cross-§13 same POC. §13.A3-D4-α 10na evidencia matures cumulative POC A5 — env-dependent toggle dispatches-hub flake suite-full FAIL vs isolated 10/10 PASS divergence confirmed cross-cycle session A5-D1 PROACTIVE pre-cementación 3 consecutive runs in-session stable. Pattern paired sister cumulative drift docs cumulative — 5 evidencias formales lección #14 (§13.A3-C7-γ + §13.A3-C8-δ + §13.A3-D3-α + §13.A4-γ + A5-D1 PROACTIVE pre-cementación). |
| **POC paired payables↔receivables** (cleanup `features/{payables,receivables}/` load-bearing + cumulative cutover hex modules/{payables,receivables}/ presentation cross-feature/cross-module paired sister axis) | Bookmark scope engrams `paired/payables-receivables/pre-recon-comprehensive` (#1612) + paired sister `paired/payables-receivables/step-0-closed` (#1613) — Cat 1 paired (UI pages + API routes paired sister cxp+cxc 6 source) + Cat 2 paired (vi.mock factory paired 4 page-rbac) + Cat 3 paired (cross-feature/cross-module α/β/γ + paired test cascade — boundary attachContact Option A push INTO infrastructure/contact-attacher.ts via composition-root.ts barrel chain) + Cat 4 paired (drop legacy POJO type defs + DTO divergence paired axis hex Snapshot+Contact composition shape replacement) + barrel sub-imports residuales schemas zod cutover prerequisite (4 símbolos per side post C0+C1a+C1b-α+C3-C4+C5-C6 cumulative absorbed) + atomic delete `features/{payables,receivables}/` wholesale paired sister (12 archivos paired 6 per side × 2 sides + 2 dir rmdir auto-cleaned). Marco lock granularity refined cycle-start preceding sesión pre-recon comprehensive 9 ciclos initial granularity propose → 7 ciclos final con C7-pre split prerequisite §13.A5-ζ-prerequisite NEW classification PROACTIVE pre-RED canonical home + scope expand Opción B atomic c5-c6 cleanup superseded auto-declarado JSDoc origen single batch. | 7 ciclos cumulative entregados (granularity natural §14 refined cumulative — 9 ciclos initial → 7 ciclos final con C7-pre split prerequisite + scope expand Opción B atomic c5-c6 cleanup superseded auto-declarado JSDoc): C0 dispatch receivables RESIDUAL cleanup §13.A5-ζ partial + §13.A5-α paired sister (commits `d6b9f4d` RED + `5f18aac` GREEN — 6 swaps mecánicos atomic single batch) + C1a Cat 3 cross-module §13.A5-α path swap (commits `5ca99cf` RED 10 assertions α + `47449d8` GREEN 6 swaps + 2 JSDoc revoke atomic single batch — paired sister sub-cycle 6ta evidencia matures Path α direct Option B inverso 2da aplicación post-cementación) + C1b-α boundary attachContact Option A push INTO infrastructure/contact-attacher.ts via composition-root.ts barrel chain (commits `ec83d7c` RED-α SUPERSEDING `ee87364` invariant collision elevation R5 8 assertions α + `89e6441` GREEN 8 archivos atomic single batch §13.A5-α 7ma evidencia + R4+R5 invariant honor + α-A3.B canonical exception path) + C3-C4 cutover paired UI pages + API routes hex (commits `a610ef6` RED 26 assertions α 13/13 per side + `2278b11` GREEN 10 archivos atomic single batch §13.A5-γ Opción A bridge NEW pattern emergent + §13.A4-η vi.mock load-bearing render path coverage MATERIAL + 8va evidencia §13.A5-α paired sister sub-cycle 4ta aplicación post-cementación cumulative) + C5-C6 drop legacy POJO type defs + DTO divergence paired axis hex (commits `d5e626e` RED 26 assertions α 13/13 per side + `235b4d1` GREEN 14 archivos atomic single batch §13.B-paired NEW classification 'DTO drop axis paired' emergent + Snapshot+Contact hex DTO + bridge mapper simplification `.toSnapshot()` Opción C precedent A5-C1 4ta aplicación post-cementación cumulative + 9na evidencia §13.A5-α paired sister sub-cycle 5ta aplicación post-cementación cumulative + 17ª evidencia §13.A3-D4-α envelope expansion {6,9} NEW flake source self-lock-integration emergent + 7ma evidencia diagnostic stash gate cross-POC) + C7-pre barrel sub-import migration prerequisite cutover schemas zod + dead aspirational vi.mock cleanup (commits `179da6d` RED 16 assertions α 8/8 per side + `fbb66e3` GREEN 8 archivos atomic single batch §13.A5-ζ-prerequisite NEW classification 1ra evidencia formal cementación PROACTIVE pre-RED canonical home + Option A DELETE dead setters cleanup mock hygiene per `feedback_aspirational_mock_signals_unimplemented_contract` + 10ma evidencia §13.A5-α paired sister sub-cycle 6ta aplicación post-cementación cumulative + 8va evidencia diagnostic stash gate cross-POC) + C7 atomic delete features/{payables,receivables}/ wholesale paired axis hex + cascade cleanup c5-c6 superseded paired axis (commits `b57bee5` RED 16 assertions α 8/8 per side + `60fa450` GREEN 12 archivos delete + 8 tests cleanup atomic single batch scope expand Opción B Marco lock confirmed pre-commit §13.A5-ζ wholesale 6ta evidencia matures cumulative paired sister 3rd wholesale precedent A4-C3 + A5-C3 + C7-paired Opción A stricto + 11ma evidencia §13.A5-α paired sister sub-cycle 7ma aplicación post-cementación cumulative + 9na evidencia diagnostic stash gate pattern matures cumulative cross-POC) + C8 doc-only post-mortem cementación cumulative architecture.md §18.4 fila POC paired (esta fila) + §13.7 contadores expand 5 lecciones + §19.9 status closure + §13.A5-α/γ/ζ-prerequisite formal cementación canonical matures + §13.B-paired NEW formal cementación canonical top-level letter sibling §13.A1...§13.A5 + §20.7 verify retroactivo. **Suite 5199/7/2 cumulative invariant 5199 preserved runtime verified cementación C8** (delta +98 net cumulative POC paired vs A5 closure 5101 — runtime ground truth confirmed cumulative invariant arithmetic preservation `passed+failed+skipped=5199` invariant {6,9} margin §13.A3-D4-α 17ª evidencia matures envelope expansion 5101→5199 trajectory 8 commits cumulative + scope expand Opción B atomic single batch c5-c6 cleanup auto-declarado JSDoc origen — lección #14 6ta paired sister C8 PROACTIVE pre-cementación). **TSC 17 baseline EXACT preserved** (NO regression POC paired vs A5 closure 17). **ESLint 10e/13w preserved** (NO regression POC paired). **REQ-FMB.5 0 violations preserved** (delta-POC paired 0 — verify §20.7 PROD app/ paths). **Test files 501 cumulative** (delta +12 vs A5 closure 489 — features/{payables,receivables}/__tests__/ deletes paired offset por NEW shape tests cumulative POC paired ciclos C0-C7 14 paired-pr.test.ts files 7 per side cumulative paired sister + 8 paired-pr collateral c5-c6 cleanup post C7 GREEN auto-declarado JSDoc origen). 15 commits cumulative pre-push + C8 doc-only commit cumulative cierre POC paired entero (defer push batch POC paired closure mirror precedent A1+A2+A3+A4+A5 EXACT cumulative push deferred al cierre POC entero — D8 push cumulative final 16 commits batch single). | Ninguno (POC paired entero CLOSED C8 cementación + D8 push closure 16 commits cumulative batch) | POC paired payables↔receivables cleanup `features/{payables,receivables}/` load-bearing fue entregado cumulative cutover hex modules/{payables,receivables}/ presentation cross-feature/cross-module paired sister axis precondición — granularity expandida natural §14 (9 ciclos initial refined → 7 ciclos final con C7-pre split prerequisite §13.A5-ζ-prerequisite NEW classification 1ra evidencia formal cementación PROACTIVE pre-RED canonical home + scope expand Opción B atomic c5-c6 cleanup superseded). Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable heredadas POC nuevo A5 starting state — POC paired NO cementa lecciones nuevas, expand contadores evidencias cumulative #10-#14 + sub-precedent (§13.7) + cementa 1 §13 emergente formal canonical NEW classification §13.A5-ζ-prerequisite + 1 letter NEW top-level §13 sibling §13.A1...§13.A5 = §13.B-paired (DTO drop axis paired UNIQUE legacy A1+A2 pre-hex pattern) + matures cumulative cross-POC §13.A5-α paired sister sub-cycle 11ma evidencia + 7ma aplicación post-cementación + §13.A5-γ Opción A bridge NEW pattern emergent C3-C4 + Opción C `.toSnapshot()` 4ta aplicación post-cementación cumulative C5-C6 + §13.A5-ζ wholesale 6ta evidencia matures cumulative cross-track + 1 evidencia recurrente §13.A3-D4-α 17ª matures envelope expansion {6,9} NEW flake source self-lock-integration emergent. **5 §13 emergentes cumulative POC paired**: 1 NEW formal canonical (§13.A5-ζ-prerequisite barrel sub-import migration prerequisite to wholesale delete) + 1 NEW top-level letter (§13.B-paired DTO drop axis paired) + 3 matures (§13.A5-α + §13.A5-γ + §13.A5-ζ) + 1 recurrente matures (§13.A3-D4-α 17ª). Pattern paired sister cumulative drift docs cumulative — 6 evidencias formales lección #14 (§13.A3-C7-γ + §13.A3-C8-δ + §13.A3-D3-α + §13.A4-γ + A5-D1 + POC paired C8 PROACTIVE pre-cementación 6ta paired sister) + 9 evidencias diagnostic stash gate pattern cumulative cross-POC + 11ma evidencia §13.A5-α paired sister sub-cycle 7ma aplicación post-cementación cumulative cross-POC. |

| **POC nuevo dedicado payment** (cleanup `features/payment/` load-bearing + cumulative cutover hex modules/payment/ presentation cross-feature/cross-module) | Bookmark scope engrams `poc-nuevo/payment/c*/closed` cumulative + `arch/§13/r-name-collision-type-vs-value-shadowing` #1638 + `arch/§13/A-features-legacy-type-only-import` #1640 — 6 sub-cycles cumulative POC payment (C0-pre barrel sub-import migration prerequisite §13.A5-ζ-prerequisite 2da evidencia + C1 cross-feature/cross-module presentation cutover routes + pages + vi.mock + §13 R-name-collision NEW 1ra evidencia + C2 DTO mapper centralizado payment-with-relations Path γ §13.A NEW emergent 1ra + C3 drop type axis PaymentWithRelations + hex local DTO Path β-prod scope §13.A 2da + C4-α Adapter Layer presentation/ reader port composition-root chain α-A3.B EXACT mirror 2da evidencia post C1b-α paired-pr 1ra + C4-β wholesale delete features/payment/* atomic + cross-feature TYPE swap LOCAL DTO Path a §13.A WHOLESALE RESOLUCIÓN cumulative + Opción α absorption Test 14 c4-α 3ra evidencia). Marco lock granularity refined cycle-start preceding sesión pre-recon comprehensive 6 ciclos initial granularity propose verified clean cumulative. | 6 ciclos cumulative entregados (granularity natural §14 cumulative — 6 ciclos refined granularity preserved): C0-pre barrel sub-import migration prerequisite cutover schemas zod (commits `7f61154` RED + `8102acb` GREEN — 6 archivos atomic single batch §13.A5-ζ-prerequisite NEW classification 2da evidencia matures cumulative cross-POC post-cementación canonical 1ra paired-pr C7-pre `b57bee5` precedent + 12ma evidencia §13.A5-α multi-level composition delegation cumulative) + C1 cross-feature/cross-module presentation cutover routes + pages + vi.mock targets PaymentService + PaymentRepository hex barrel single feature axis (commits `1f27120` RED 14 assertions α + `a14505d` GREEN 11 archivos atomic single batch §13.A5-α 13ma evidencia matures cumulative + §13.A5-ε method-on-class shim signature divergence drop alias 2da evidencia matures cumulative post-cementación canonical + §13.A4-η vi.mock factory load-bearing render path coverage MATERIAL Opción A re-export class identity preserved + §13 R-name-collision NEW invariant collision category 1ra evidencia formal cementación target D1 doc-only) + C2 DTO mapper centralizado payment-with-relations extraction Path γ scope mapper extraction only NO drop type axis defer C3 (commits `6c35779` RED 13 assertions α + `0c79740` GREEN 2 archivos atomic single batch §13.A NEW emergent classification "hex presentation TYPE-only import desde legacy features/" 1ra evidencia formal cementación target D1 + §13.A5-γ DTO divergence runtime path coverage 5ta aplicación matures cumulative + lección REFINED feedback/step-0-expand-eslint-restricted-imports-grep 8th axis cross-module type-only import direction hex→legacy features) + C3 drop type axis PaymentWithRelations + hex local DTO canonical home Path β-prod scope mirror A3-C3 sale-with-details EXACT precedent (commits `5d2aa20` RED 15 assertions α + `f93dbd4` GREEN 7 archivos atomic single batch §13.A NEW emergent classification 2da evidencia formal post-cementación canonical PROACTIVE pre-D1 + §13.A5-γ DTO divergence runtime path coverage 6ta aplicación matures cumulative + lección REFINED feedback/red-regex-discipline NEW canonical home) + C4-α Adapter Layer presentation/ delegate via reader port + composition-root chain canonical R4 exception path EXACT mirror α-A3.B paired C1b-α `89e6441` precedent (commits `f606863` RED 14 assertions α + `fcfc7e1` GREEN 14 archivos atomic single batch §13.A5-α multi-level composition delegation NEW evidencia matures + §13.A5-ε signature divergence drop alias 3ra evidencia post-cementación canonical applied + §13 NEW classification "Adapter Layer presentation/ delegate via reader port + composition-root chain canonical R4 exception path EXACT mirror α-A3.B" 1ra evidencia POC payment cumulative 2da evidencia post paired C1b-α canonical PROACTIVE pre-D1 + 2 invariant collisions retroactive resolution Path C + Opción α + cascade superseded absorption + Snapshot LOCAL inline def mirror iva-books precedent EXACT cumulative cross-module 7 reader ports + makePaymentServiceAdapter naming convention NEW canonical + JSDoc atomic revoke + Opción α absorption 2da evidencia matures cross-POC) + C4-β wholesale delete features/payment/* atomic + cross-feature TYPE swap consumers via LOCAL DTO presentation/dto/ canonical home Path a (commits `3bc8fec` RED 12 assertions α + `b1cd3a5` GREEN 11 archivos atomic single batch §13.A5-ε signature divergence MATERIAL 3ra evidencia post-cementación canonical mirror PaymentWithRelations C3 EXACT + §13 R-name-collision RESOLVES double pair CreatePaymentInput/CreatePaymentServiceInput vía namespace path + §13.A WHOLESALE RESOLUCIÓN cumulative drops cross-module type-only import vector entire 1ra C2 + 2da C3 + 3ra C4-α evidencias resueltas wholesale + Opción α absorption Test 14 c4-α legacy shim cascade superseded 3ra evidencia matures cross-POC mirror paired C7 Opción B + C4-α GREEN EXACT cumulative + diagnostic stash gate 11ma evidencia anticipated bookmark accuracy MATERIAL) + D1 doc-only post-mortem cementación cumulative architecture.md §18.4 fila POC payment (esta fila) + §13.7 contadores expand 5 lecciones + §19.10 status closure + 6 NEW formal cementación canonical §13 + 1 sub-pattern matures + §13.A5-ε 3ra evidencia matures + §13.A3-D4-α 22ª evidencia counter + §20.8 verify retroactivo + sub-finding NEW WithCorrelation envelope passthrough emergente + §21.2 defer placeholder POC futuro paridad fix payment routes raw entity preservation. **Suite 5242/6/2 cumulative invariant 5242 preserved runtime verified cementación D1** (delta +43 net cumulative POC payment vs POC paired closure 5199 — runtime ground truth confirmed cumulative invariant arithmetic preservation `passed+failed+skipped=5242` invariant {6,9} margin §13.A3-D4-α 22ª evidencia matures envelope expansion env-dependent toggle within margin per `arch/lecciones/dispatches-hub-flake-recurrente`). **TSC 17 baseline EXACT preserved** (NO regression POC payment vs POC paired closure 17). **ESLint 10e/13w preserved** (NO regression POC payment). **REQ-FMB.5 0 violations preserved** (delta-POC payment 0 — verify §20.8 PROD app/ paths §20.1 EXACT regex grep 0 hits cumple + sub-finding NEW WithCorrelation envelope passthrough pre-existing legacy shim defer §21.2). 12 commits cumulative pre-push + D1 doc-only commit cumulative cierre POC payment entero (defer push batch POC payment closure mirror precedent A1+A2+A3+A4+A5+POC paired EXACT cumulative push deferred al cierre POC entero — push cumulative final 13 commits batch single). | Ninguno (POC payment entero CLOSED D1 cementación + push closure 13 commits cumulative batch) | POC nuevo dedicado payment cleanup `features/payment/` load-bearing fue entregado cumulative cutover hex modules/payment/ presentation cross-feature/cross-module precondición — granularity natural §14 6 ciclos refined preserved cumulative. Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable heredadas POC paired starting state + 1 NEW formal canonical §13.A5-ζ-prerequisite + 1 NEW top-level letter §13.B-paired heredadas POC paired — POC payment NO cementa lecciones nuevas, expand contadores evidencias cumulative #10-#14 + sub-precedent (§13.7) + cementa 6 §13 emergentes formales canonical NEW (R-name-collision + §13.A features-legacy-type-only-import + Adapter Layer presentation/ reader port composition-root chain α-A3.B EXACT + Reader port Snapshot LOCAL definition + makeXAdapter() factory naming + Opción α absorption cleanup superseded tests atomic) + 1 sub-pattern matures (§13 mapper move presentation→infrastructure data-access concern 2da evidencia) + matures cumulative cross-POC §13.A5-α 13ma evidencia + §13.A5-ε signature divergence drop alias 3ra evidencia post-cementación canonical + §13.A4-η vi.mock factory load-bearing render path coverage MATERIAL Opción A re-export class identity preserved + 1 evidencia recurrente §13.A3-D4-α 22ª matures envelope expansion {6,9} env-dependent toggle. **9 §13 emergentes cumulative POC payment**: 6 NEW formal canonical + 1 sub-pattern matures + 2 matures cumulative cross-POC (§13.A5-ε 3ra + §13.A4-η post C4-α) + 1 recurrente matures (§13.A3-D4-α 22ª). Pattern paired sister cumulative drift docs cumulative — 7 evidencias formales lección #14 (§13.A3-C7-γ + §13.A3-C8-δ + §13.A3-D3-α + §13.A4-γ + A5-D1 + POC paired C8 PROACTIVE pre-cementación 6ta paired sister + POC payment D1 PROACTIVE pre-cementación 7ma paired sister 12ma evidencia paired sister cumulative cross-POC) + 11 evidencias diagnostic stash gate pattern cumulative cross-POC + 13ma evidencia §13.A5-α paired sister sub-cycle 8va aplicación post-cementación cumulative cross-POC. **Sub-finding NEW emergente cementación target D1** (§20.8 NEW + §21.2 NEW): WithCorrelation envelope passthrough pre-existing legacy shim pattern 4 routes (POST + PATCH x2 + PUT) — POC payment cycles preserved legacy contract via Adapter C4-α NO new leak introduced; §20.1 EXACT regex pattern cumple ✅ (0 hits literal `{ ...x, correlationId }` spread route handler), strict spirit §20 incluye envelope passthrough (lección REFINED feedback/§20-strict-spirit-vs-regex-literal NEW canonical home — verificación PROACTIVE pre-D1 MANDATORY future POCs cuando service retorna WithCorrelation envelope shape) — defer §21.2 POC futuro paridad fix payment routes raw entity preservation simétrico al precedent A3 sale + paired payable EXACT (`Response.json(rawEntity)`). Mirror precedent A3 D5 PATCH/POST mismatch defer NEW POC dedicated. |

| **POC nuevo dedicado mortality** (cleanup `features/mortality/` load-bearing + cumulative cutover hex modules/mortality/ presentation cross-feature/cross-module Path simplificado v2 RSC boundary serialization adapter via `.toJSON()` POJO server-side pre-boundary) | Bookmark scope cycle-start Step 0 PROACTIVE expand pre-recon comprehensive — 5 consumers Cat 1 cross-feature/cross-module (1 client component lot-detail-client.tsx + 1 page dashboard lots/[lotId]/page.tsx + 2 api routes mortality/route.ts + agent/route.ts + 1 cross-feature pricing.service.ts) + 1 vi.mock factory page-rbac (route.confirm-journal-entry.test.ts) + atomic delete `features/mortality/` wholesale 4 archivos (index.ts + mortality.types.ts + mortality.validation.ts + server.ts class shim + toLegacyShape mapper). Marco lock granularity 1 ciclo C1 atomic single batch single feature axis (asimétrico vs payment 6 ciclos refined — scope reducido sin paired DTO divergence MATERIAL). | 1 ciclo C1 atomic + D1 doc-only entregados (granularity natural §14 cumulative — 1 ciclo refined preserved): C1 wholesale delete features/mortality/* atomic + cross-consumer cutover hex Path simplificado v2 entity-direct .toJSON() POJO server-side pre-RSC boundary adapter sub-finding NEW emergente lockable pre-GREEN (commits `f74a1d8` RED 9 assertions α + `3f54aa1` GREEN 10 archivos atomic single batch — 6 modified + 4 deleted §13.A 4ta evidencia post-cementación canonical RESOLUCIÓN wholesale drops cross-module type-only import vector entire single feature axis cumulative cross-POC + §13.A5-ε NO MATERIAL TYPE-only divergence drop sin LOCAL DTO Path simplificado v2 NEW asimetría material vs payment C4-β Path a (1 field TYPE-only vs 5 fields runtime divergent) + §13.A5-ζ wholesale 2da evidencia matures cumulative cross-POC POC mortality + diagnostic stash gate 12ma evidencia anticipated bookmark feature-boundaries.test.ts isolated re-run 8 violations all NON-mortality cross-feature pairs verified) + D1 doc-only post-mortem cementación cumulative architecture.md §18.4 fila POC mortality (esta fila) + §13.7 contadores expand 5 lecciones + §19.11 status closure + 1 NEW formal cementación canonical §13 (Path simplificado v2 RSC boundary serialization adapter via .toJSON() POJO server-side pre-boundary) + matures cumulative cross-POC §13.A 4ta evidencia + §13.A5-ζ 2da evidencia + §13.A3-D4-α 23ra evidencia counter + §20.9 verify retroactivo CLEAN. **Suite 5242/7/2 cumulative invariant 5251 preserved runtime verified cementación D1** (delta +0 net cumulative POC mortality vs POC payment closure 5242 — RED test 9/9 PASS post-GREEN absorbed into cumulative passing count via wholesale features/mortality/__tests__/ delta -9 offset; runtime ground truth confirmed cumulative invariant arithmetic preservation `passed+failed+skipped=5251` invariant {6,9} margin §13.A3-D4-α 23ra evidencia matures envelope expansion env-dependent toggle within margin per `arch/lecciones/dispatches-hub-flake-recurrente`). **TSC 17 baseline EXACT preserved** (NO regression POC mortality vs POC payment closure 17). **ESLint 10e/13w preserved** (NO regression POC mortality). **REQ-FMB.5 0 violations preserved** (delta-POC mortality 0 — verify §20.9 PROD app/ paths §20.1 EXACT regex grep 0 hits cumple + asimétrico vs POC payment §20.8 sub-finding WithCorrelation envelope passthrough — mortality NO usa WithCorrelation/correlationId/logStructured operational domain). 2 commits cumulative pre-push + D1 doc-only commit cumulative cierre POC mortality entero (push batch POC mortality closure 2 commits batch single C1 GREEN + D1 doc-only — granularity reducida vs precedent A1+A2+A3+A4+A5+POC paired+payment cumulative push deferred al cierre POC entero asimétrico scope reducido). | Ninguno (POC mortality entero CLOSED D1 cementación + push closure 2 commits cumulative batch single) | POC nuevo dedicado mortality cleanup `features/mortality/` load-bearing fue entregado cumulative cutover hex modules/mortality/ presentation cross-feature/cross-module precondición — granularity natural §14 1 ciclo refined preserved cumulative (asimétrico vs payment 6 ciclos refined — scope reducido sin paired DTO divergence MATERIAL). Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable heredadas POC payment starting state + 1 NEW formal canonical §13.A5-ζ-prerequisite + 1 NEW top-level letter §13.B-paired + 6 NEW formal canonical POC payment + 1 sub-pattern matures POC payment heredadas POC payment — POC mortality NO cementa lecciones nuevas, expand contadores evidencias cumulative #10-#14 + sub-precedent (§13.7) + cementa 1 NEW formal canonical §13 (Path simplificado v2 RSC boundary serialization adapter via .toJSON() POJO server-side pre-boundary asimétrico vs payment Path a LOCAL DTO presentation/dto/ canonical home — Path simplificado v2 inferred type derivation `ReturnType<X["toJSON"]>[]` annotation drops type entirely sin LOCAL DTO file cuando divergence TYPE-only NO MATERIAL) + matures cumulative cross-POC §13.A 4ta evidencia post-cementación canonical RESOLVED wholesale (cumulative 1ra C2-payment + 2da C3-payment + 3ra C4-α-payment + 4ta C1-mortality cumulative cross-POC) + §13.A5-ζ wholesale 2da evidencia matures cumulative cross-POC (1ra payment C4-β + 2da mortality C1) + 1 evidencia recurrente §13.A3-D4-α 23ra matures envelope expansion {6,9} env-dependent toggle. **3 §13 emergentes cumulative POC mortality**: 1 NEW formal canonical (Path simplificado v2 RSC boundary serialization adapter) + 2 matures cumulative cross-POC (§13.A 4ta + §13.A5-ζ 2da) + 1 recurrente matures (§13.A3-D4-α 23ra). Pattern paired sister cumulative drift docs cumulative — 8 evidencias formales lección #14 (§13.A3-C7-γ + §13.A3-C8-δ + §13.A3-D3-α + §13.A4-γ + A5-D1 + POC paired C8 + POC payment D1 7ma + POC mortality D1 PROACTIVE pre-cementación 8va paired sister 13ma evidencia paired sister cumulative cross-POC) + 12 evidencias diagnostic stash gate pattern cumulative cross-POC + 14ma evidencia §13.A5-α paired sister sub-cycle (NO emerge POC mortality — counter preserved cumulative cross-POC payment 13ma sin progression POC mortality scope sub-feature single granularity). **Sub-finding NEW emergente cementación target D1** (§20.9 CLEAN — asimétrico vs §20.8 payment): RSC boundary serialization constraint pre-GREEN sub-finding emergente Marco lock surface — lock pre-RED original "consumer entity directo" missing premise lot-detail-client.tsx is `"use client"` → entity con métodos NO atraviesa Server→Client RSC serialization boundary Next.js + `log.count` getter retorna `MortalityCount` VO NO number JSX renderiza `[object Object]` broken UI. Marco lock final Path simplificado v2 (NO LOCAL DTO file + .toJSON() server-side adapter pre-boundary + `ReturnType<Mortality["toJSON"]>[]` inferred type derivation). Lección directa per `feedback_premise_verification_canonical_claim` aplicación recursiva — premise "consumer entity directo" verified post-RED revealed RSC boundary constraint material missing pre-RED Marco lock. Forward-applicable cross-POC: future POCs cuando consumer es client component "use client" + entity serialization fails RSC boundary + divergence NO MATERIAL (TYPE-only sin runtime read) → Path simplificado v2 canonical home (vs Path a payment cuando MATERIAL multi-field runtime divergent → LOCAL DTO presentation/dto/ canonical home). |

| **POC nuevo dedicado contacts** (cleanup `features/contacts/` load-bearing + cumulative cutover hex modules/contacts/ presentation cross-feature/cross-module + Balance methods cross-module hex modules/contact-balances/ presentation Path simplificado v2 RSC boundary serialization adapter via `.toSnapshot()` POJO server-side pre-boundary) | Bookmark scope cycle-start preceding sesión pre-recon comprehensive 6 sub-cycles initial granularity propose (C0-pre + C1 + C2 + C3 + C4-pre + C4 + D1) — Marco lock granularity refined cumulative reorder C5 antes C4 (POC contacts secuencia revised C0-pre + C1 + C2 + C3 + C4-pre + C5 + C4 + D1 — 8 sub-fases) + 3-axis recon gap surface iterativa cumulative C4-bis + C4-ter Marco lock D-β-c4 5 commits totales preserves git history honest progressive recon NO destructive. Cat 1 routes API + pages + Cat 2 vi.mock factory + Cat 3 cross-feature/cross-module adapter + Cat 4 wholesale delete features/contacts/* atomic + DROP línea 13 modules/contacts/presentation/server.ts Opción A re-export. | 7 ciclos cumulative entregados + D1 doc-only (granularity natural §14 expandida cumulative — 6 ciclos initial → 7 ciclos final con C5 reorder antes C4 dependency chain page.tsx listWithBalances consumer transitorio Marco lock 1 + 3-axis recon gap surface iterativa cumulative C4-bis + C4-ter Marco lock D-β-c4): C0-pre barrel sub-import migration prerequisite cutover schemas zod + DEEP path import migration components/payments/payment-form.tsx PendingDocument (commits `c7584d7` RED + `e0a279c` GREEN — 3 archivos atomic single batch §13.A5-ζ-prerequisite 3ra evidencia matures cumulative cross-POC) + C1 cross-feature/cross-module presentation cutover ContactsService hex barrel atomic single-axis Path α'' merge atomic Opción A re-export legacy shim class identity preserved (commits `4ce9701` RED 14α + `5e3dc3b` GREEN 33 archivos atomic single batch §13.A5-α 14ma evidencia matures cumulative + §13.A4-η LOAD-BEARING render path coverage MATERIAL N+1ma matures cumulative + cascade-NEW absorption Opción α mismo batch) + C2 cross-module accounting infrastructure adapter cutover ContactsReadAdapter dependency hex factory makeContactsService (commits `b123a3e` RED 6α + `0de1b48` GREEN 2 archivos atomic single batch §13.A reverse direction TYPE-only/existence-only 5ta evidencia matures cumulative cross-POC Q1-α retroactive reclassification VALUE→TYPE-only premise verification recursive aplicación + Adapter Layer cross-module 3ra evidencia matures) + C3 cross-module routes API Balance methods cutover 3 routes legacy shim ContactsService → hex factory makeContactBalancesService cross-module atomic single-axis (commits `40276d3` C3-pre retire C1 Tests 3-5 skip+comment archaeology + `ed390af` RED 8α + `0834aa3` GREEN 3 archivos atomic single batch §13.A5-α 18ma evidencia matures cumulative + Adapter Layer cross-module 3ra evidencia + cross-cycle-red-test-cementacion-gate 1ra evidencia retroactive surface post-edits lección NEW canonical home target D1) + C4-pre prerequisite gate dual-axis prep work re-exports Contact POJO + ContactFilters + ContactWithBalance flat POJO interface NEW isomorphic + retire C1 Test 14 (commit `15aeb89` 2 archivos atomic single batch +52/-6 — Marco lock P-δ revised dual-axis absorb prerequisite gate + cross-cycle-red-test-cementacion-gate 2da evidencia PROACTIVE pre-RED matures cumulative cross-POC) + C5 page.tsx callsite + page.test.ts vi.mock SHAPE cutover VALUE-axis cross-module legacy shim ContactsService → hex factory makeContactBalancesService().listWithBalancesFlat() atomic single-axis Path α single ciclo (commits `1ea5bb7` C5-pre retire C1 Tests 6+12 skip+comment archaeology + `70880f0` RED 8α + `c979666` GREEN 3 archivos atomic single batch §13.A5-α 21ma evidencia matures cumulative + Sub-α ADD listWithBalancesFlat factory método dependency simultaneous mirror C2/C3 GREEN precedent EXACT + ContactWithBalanceFlat interface NEW canonical home extends ContactSnapshot type derivation pattern §13 path simplificado v2 idea consistent + cross-cycle-red-test-cementacion-gate 3ra evidencia PROACTIVE pre-RED matures cumulative cross-POC + Path simplificado v2 RSC boundary serialization adapter mortality C1 precedent §13 1ra evidencia + POC contacts C5 2da evidencia entity-direct .toSnapshot() POJO server-side pre-RSC boundary cleanup) + C4 wholesale delete features/contacts/* atomic + DROP línea 13 modules/contacts/presentation/server.ts Opción A re-export + cumulative cutover cross-feature/cross-module Marco lock D-β-c4 5 commits totales 3 surface iterativa recon gap progresiva (commits `1129c14` C4 RED 18α + `e6104be` C4-bis RED 8α + `0252624` C4-ter RED 44α + `1bf7c34` C4 GREEN cumulative single batch 38 archivos atomic single batch +143/-327 — 3 DELETE features/contacts/* + rmdir + 1 DROP línea 13 + 5 components TYPE swap + 2 ai-agent tests Sub-B inline + 4 features/* VALUE consumers Sub-B-bis factory swap + 12 production app/.../* VALUE consumers factory swap + 10 vi.mock factory declarations swap class → arrow factory pattern + retire C1 Tests 1+2+7-10 cementación same-axis archaeology preserved + cross-cycle-red-test-cementacion-gate 4ta evidencia PROACTIVE matures cumulative cross-POC + §13.A5-α 25ma evidencia matures cumulative cross-POC sub-cycle continuation + §13.A WHOLESALE RESOLUCIÓN 5ta evidencia matures cumulative cross-POC post-cementación canonical RESOLVED wholesale + §13.A5-ζ wholesale 3ra evidencia matures cumulative cross-POC) + D1 doc-only post-mortem cementación cumulative architecture.md §18.4 fila POC contacts (esta fila) + §13.7 contadores expand 5 lecciones + §19.12 status closure + 9 §13 matures cumulative cross-POC (§13.A 5ta + §13.A5-ζ 3ra + §13.A5-ζ-prerequisite 3ra + §13.A reverse direction 5ta + §13.A4-η N+2ma + §13 Adapter Layer 3ra + §13 Path simplificado v2 2da + §13.A5-α 25ma + §13.A3-D4-α 28ª) + §20.10 verify retroactivo CLEAN mirror mortality §20.9 paired sister symmetric + 3 NEW lecciones canonical homes engram body POC contacts (10mo class symbol grep + 11mo vi.mock factory grep + Step 0 expand REFINED 11 axes + cross-cycle-red-test-cementacion-gate 4ta evidencia matures). **Suite 5342/7/14 cumulative invariant 5363 preserved runtime verified cementación D1** (delta +112 net cumulative POC contacts vs POC mortality closure 5251 — runtime ground truth confirmed cumulative invariant arithmetic preservation `passed+failed+skipped=5363` invariant {6,9} margin §13.A3-D4-α 28ª evidencia matures envelope expansion env-dependent toggle within margin per `arch/lecciones/dispatches-hub-flake-recurrente`). **TSC 13 baseline NEW** (delta -4 vs POC mortality closure 17 — ground truth supersedes engram cumulative claim chain stale; 4 errors resueltos commits no-mio anteriores cumulative pre-POC contacts; files con errors actuales NINGUNO en diff POC contacts 17 commits cumulative outside POC contacts modification domain — `app/api/.../sales/route.ts` 2 + `modules/org-settings/__tests__/org-settings.service.test.ts` 2 + `modules/sale/application/__tests__/{fakes/in-memory-account-lookup.ts, sale.service.test.ts}` 1+8 = 13 EXACT). **ESLint 10e/13w preserved** (NO regression POC contacts). **REQ-FMB.5 0 violations preserved** (delta-POC contacts 0 — verify §20.10 PROD app/ paths). **Test files 514 cumulative** (delta +13 vs POC mortality closure 501 — 8 NEW shape tests POC contacts ciclos C0-pre+C1+C2+C3+C5+C4+C4-bis+C4-ter cumulative + 5 paired cascade tests). 17 commits cumulative pre-push + D1 doc-only commit cumulative cierre POC contacts entero (push batch POC contacts closure 18 commits batch single — granularity expandida natural §14 vs precedent A1+A2+A3+A4+A5+POC paired+payment+mortality cumulative push deferred al cierre POC entero — 7 ciclos atomic + D1 cumulative 8 sub-fases mirror payment 6 ciclos refined + paired sister axis cross-POC sub-cycle continuation pattern matures). | Ninguno (POC contacts entero CLOSED D1 cementación + push closure 18 commits cumulative batch single) | POC nuevo dedicado contacts cleanup `features/contacts/` load-bearing + cumulative cutover hex modules/contacts/ presentation cross-feature/cross-module + Balance methods cross-module hex modules/contact-balances/ presentation precondición fue entregado cumulative — granularity expandida natural §14 (6 ciclos initial granularity propose → 7 ciclos final con C5 reorder antes C4 Marco lock 1 dependency chain + 3-axis recon gap surface iterativa cumulative C4-bis + C4-ter Marco lock D-β-c4 5 commits totales preserves git history honest progressive recon NO destructive). Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable heredadas POC mortality starting state + 1 NEW formal canonical §13.A5-ζ-prerequisite + 1 NEW top-level letter §13.B-paired + 6 NEW formal canonical POC payment + 1 sub-pattern matures POC payment + 1 NEW formal canonical Path simplificado v2 POC mortality heredadas — POC contacts NO cementa lecciones nuevas §13 formal canonical (Marco lock Q2 pre-D1 — todas matures cumulative; 3-axis recon gap surface iterativa cementadas como lecciones canonical homes feedback/* engram body NO §13 formal) — pero sí origina **3 NEW lecciones canonical homes engram body** POC contacts (1er POC origina lecciones nuevas post-A2 cumulative cross-POC — distintos POCs anteriores A4+A5+paired+payment+mortality NO origina lecciones nuevas): (1) `feedback/retirement-reinventory-gate-class-symbol-grep` 10mo axis 1ra evidencia POC contacts C4-bis emergent post-3-archivo features/* VALUE consumers cascade detected post-edits — class symbol grep PROJECT-scope MANDATORY cuando wholesale delete + DROP re-export bridge + (2) `feedback/retirement-reinventory-gate-vimock-factory-grep` 11mo axis 1ra evidencia POC contacts C4-ter emergent post-12-archivo production app/.../* + 10 vi.mock factory declarations cascade detected post-edits — vi.mock factory class declarations grep PROJECT-scope MANDATORY axis separado de production VALUE consumers axis + (3) `feedback/cross-cycle-red-test-cementacion-gate` 4ta evidencia matures cumulative cross-POC (1ra C3 retroactive + 2da C4-pre PROACTIVE + 3ra C5-pre PROACTIVE + 4ta C4 PROACTIVE — gate funcionó forward cumulative). Adicional Step 0 expand REFINED 11 axes cumulative cross-POC future POCs (vs 9-axis previo POCs anteriores) — 10mo class symbol grep + 11mo vi.mock factory grep cumulative. Engram body canónico `feedback/step-0-expand-eslint-restricted-imports-grep` REFINED 11 axes cumulative cross-POC update. POC contacts cementa 9 §13 emergentes formales canonical matures cumulative cross-POC (§13.A 5ta evidencia post-cementación canonical RESOLVED wholesale + §13.A5-ζ wholesale 3ra + §13.A5-ζ-prerequisite barrel sub-import 3ra + §13.A reverse direction TYPE-only/existence-only 5ta + §13.A4-η factory return shape sub-pattern N+2ma matures + §13 Adapter Layer cross-module 3ra + §13 Path simplificado v2 RSC boundary 2da + §13.A5-α multi-level composition delegation 25ma + §13.A3-D4-α env-dependent toggle 28ª) + 1 evidencia recurrente §13.A3-D4-α 28ª matures envelope expansion {6,9} env-dependent toggle. **9 §13 emergentes cumulative POC contacts** (todos matures cumulative cross-POC Marco lock Q2 — 0 NEW formal canonical). Pattern paired sister cumulative drift docs cumulative — 9 evidencias formales lección #14 (§13.A3-C7-γ + §13.A3-C8-δ + §13.A3-D3-α + §13.A4-γ + A5-D1 + POC paired C8 + POC payment D1 7ma + POC mortality D1 8va + POC contacts D1 PROACTIVE pre-cementación 9na paired sister 14ma evidencia paired sister cumulative cross-POC) + 13 evidencias diagnostic stash gate pattern cumulative cross-POC + 25ma evidencia §13.A5-α multi-level composition delegation cumulative cross-POC. **TSC 13 baseline NEW honest documented** (delta -4 vs heredado 17 — ground truth supersedes engram cumulative claim chain stale; mejora NO regresión, 4 errors resueltos commits no-mio anteriores cumulative pre-POC contacts outside modification domain — bookmark closure D1 cementa baseline NEW 13 ground truth wins). |

| **POC nuevo dedicado fiscal-periods** (cleanup `features/fiscal-periods/` load-bearing + cumulative cutover hex modules/fiscal-periods/ presentation cross-feature/cross-module Path simplificado v2 RSC boundary serialization adapter via `.toSnapshot()` POJO server-side pre-boundary) | Bookmark scope cycle-start preceding sesión pre-recon comprehensive 8 sub-cycles initial granularity propose (C1-α + C-bis + C2 + C3 + C4 + C5 + C6 + C7 + D1) — Marco locks granularity preserved cumulative VACUOUS-CLOSED partial scope reduction C5 + Opción ε identifier-preserving alias barrel re-export NEW canonical home pattern C6 + Opción C mixto retire schedule wholesale + skip+comment intra-file symmetry preservation NEW canonical home pattern C7. Cat 1 routes API + pages + Cat 2 vi.mock factory + Cat 3 cross-feature/cross-module adapter + Cat 4 wholesale delete features/fiscal-periods/* atomic + DROP línea 9 modules/fiscal-periods/presentation/server.ts Opción A re-export. | 8 ciclos cumulative entregados + D1 doc-only (granularity natural §14 — 8 sub-cycles initial granularity propose → 8 cumulative final con C5 VACUOUS-CLOSED partial scope reduction cumulative absorb pattern + C6 Opción ε identifier-preserving alias barrel re-export NEW canonical home pattern + C7 Opción C mixto retire schedule wholesale + skip+comment intra-file symmetry preservation NEW canonical home pattern): C1-α Opción A re-export bridge mecánico puro path swap 3 cross-feature services FiscalPeriodsService class hex barrel atomic single batch (commits `a793cdc` RED + `099553b` GREEN — §13.A5-α 15ma evidencia matures cumulative + Marco lock L1-estricto axis-distinct collision retroactive permitido split scope Opción 4) + C-bis cumulative absorb single batch real class→factory swap + entity API adjustments + 1 production hit fix + retire C1-α T1-T3 + Sub-B cast 11 test files + Sub-D mock data entity-shape paired + integration mock source refactor (commits `bb751e7` RED + `216ce83` GREEN — §13.A4-η factory return shape N+3ma matures cumulative + Marco lock D-β-c4 surface iterativa recon gap progresiva precedent EXACT) + C2 Opción 1 homogeneous VALUE-pure mecánico puro path swap 2 components isomorphic barrel (commits `f8d2ff5` RED + `3234b9e` GREEN — §13.A4-η N+4ma matures cumulative + §13.A reverse 6ta evidencia matures + premise-verification-canonical-claim 4ta evidencia + Marco lock Opción 1 homogeneous VALUE-pure + 4 métricas baseline preserved EXACT) + C3 Opción E .toSnapshot() bridge + field type alias `ReturnType<typeof makeFiscalPeriodsService>` 3 cross-module adapters class→factory swap + 2 vi.mock paired Sub-B same batch (commits `e070a64` RED + `7b374a2` GREEN — §13 Adapter Layer cross-module 4ta evidencia matures + §13.A4-η N+5ma matures cumulative + Marco lock C3 #3 unlock formal axis-distinct collision retroactive elevation + 4 métricas baseline preserved EXACT) + C4 cumulative absorb single batch 17 page.tsx + 3 route.ts production VALUE swap class→factory makeFiscalPeriodsService() + .toSnapshot() bridge per Categ A/A'/B + 1 createFiscalPeriodSchema isomorphic barrel deferred C2 finalización + 21 test files Sub-B mock target swap + Sub-D mock data entity-shape paired (commits `df20e3d` RED + `aa6c5a6` GREEN — §13 Path simplificado v2 RSC boundary serialization adapter 5ta evidencia matures cumulative + §13.A4-η factory return shape Sub-D mock entity-shape paired 6ta evidencia matures + §13 Adapter Layer cross-module 5ta evidencia matures + Marco lock Opción 1 cumulative absorb Sub-B+Sub-D mismo GREEN batch + pre-phase-audit-gate 2da evidencia matures + invariant-collision-elevation audit-detected-pre-RED variant NEW + 4 métricas baseline EXACT preserved cumulative) + C5 VACUOUS-CLOSED partial scope reduction cumulative absorb upstream colapsa downstream (canonical home VACUOUS-CLOSED cycle 2da evidencia C5 full + C6 partial cumulative POC fiscal-periods) + C6 Opción ε identifier-preserving alias barrel re-export 2 archivos diff mínimo isomorphic barrel ADD export type FiscalPeriodSnapshot as FiscalPeriod + period-list.tsx:11 path swap mecánico TYPE-only (commits `cd96a92` RED + `c43032f` GREEN — §13.A4-η N+7ma matures cumulative cross-POC + §13 RSC boundary serialization adapter 7ma evidencia matures resolved C6 + §13.A reverse direction TYPE-only/existence-only 6ta evidencia resolved deferred C2 → resolved C6 PROACTIVE + Opción ε identifier-preserving alias barrel re-export pattern NEW canonical home 1ra evidencia formal cementada + VACUOUS-CLOSED partial scope reduction canonical home 2da evidencia matures cumulative C5 full + C6 partial + premise-verification-canonical-claim 6ta evidencia bookmark drift count 7 test files projected → 0 reales + cross-cycle-red-test-cementacion-gate 8va evidencia POC fiscal-periods C6 PROACTIVE + 4 métricas baseline EXACT preserved cumulative) + C7 wholesale delete features/fiscal-periods/* (3 archivos: index.ts + fiscal-periods.types.ts + server.ts) atomic + DROP línea 9 bridge re-export modules/fiscal-periods/presentation/server.ts Opción A class identity preservada cementada C1-α GREEN cumulative absorb cross-cycle 0 residual consumers post C1-α through C6 verified 10mo + 11mo grep gates CLEAN PROACTIVE pre-RED + Marco lock Opción C mixto retire 5 archivos delete wholesale + 2α intra-file skip+comment C1-α T4+T5 (commits `ff9603a` RED + `20c8054` GREEN — §13.A5-ζ wholesale 4ta evidencia matures cumulative cross-POC sub-cycle + retirement-strategy-mixto NEW canonical home 1ra evidencia POC fiscal-periods C7 + retirement-reinventory-gate-class-symbol-grep 10mo axis 2da evidencia CLEAN matures cumulative cross-POC + retirement-reinventory-gate-vimock-factory-grep 11mo axis 2da evidencia CLEAN matures cumulative + cross-cycle-red-test-cementacion-gate 9na evidencia matures cumulative POC fiscal-periods C7 retire schedule mixto + 4 métricas baseline EXACT preserved cumulative) + D1 doc-only post-mortem cementación cumulative architecture.md §18.4 fila POC fiscal-periods (esta fila) + §13.7 contadores expand 2 NEW lecciones canonical homes + 1 NEW Marco-lock variant + §19.13 status closure + 9 §13 matures cumulative cross-POC (§13.A 6ta + §13.A5-ζ 4ta + §13.A5-ζ-prerequisite 4ta + §13.A reverse direction 6ta + §13.A4-η N+7ma + §13 Adapter Layer 5ta + §13 Path simplificado v2 7ma + §13.A5-α 26ma + §13.A3-D4-α 29ª) + §20.11 verify retroactivo CLEAN paired sister symmetric mortality §20.9 + contacts §20.10 + 2 NEW lecciones canonical homes engram body POC fiscal-periods (retirement-strategy-mixto + Opción ε identifier-preserving alias barrel re-export) + 1 NEW Marco-lock variant `feedback/invariant-collision-elevation#audit-detected-variant` 3 variants formales total cumulative cross-POC. **Suite 5347/7/19 cumulative invariant 5373 preserved runtime verified cementación D1** (delta +10 net cumulative POC fiscal-periods vs POC contacts closure 5363 — runtime ground truth confirmed cumulative invariant arithmetic preservation `passed+failed+skipped=5373` invariant {6,9} margin §13.A3-D4-α 29ª evidencia matures envelope expansion env-dependent toggle within margin per `arch/lecciones/dispatches-hub-flake-recurrente`). **TSC 17 baseline NEW** (delta +4 vs POC contacts closure 13 — ground truth supersedes engram cumulative claim chain stale; **regression** honest documented mirror contacts D1 -4 pattern recursive aplicación lección #14, 4 errors NEW commits no-mio anteriores cumulative pre-POC fiscal-periods outside POC fiscal-periods modification domain — `app/api/[orgSlug]/purchases/route.ts` 2 + `app/api/[orgSlug]/sales/[saleId]/route.ts` 1 + `app/api/[orgSlug]/purchases/[purchaseId]/route.ts` 1 = 4 NEW; files heredados sin cambio: `sale.service.test.ts` 8 + `org-settings.service.test.ts` 2 + `sales/route.ts` 2 + `in-memory-account-lookup.ts` 1 = 13 EXACT cumulative; total 13 + 4 = 17 EXACT). **ESLint 10e/13w preserved** (NO regression POC fiscal-periods). **REQ-FMB.5 0 violations preserved** (delta-POC fiscal-periods 0 — verify §20.11 PROD app/ paths). **Test files 516 cumulative** (delta +2 vs POC contacts closure 514 — 5 NEW shape tests POC fiscal-periods C1-α + C-bis + C2 + C3 + C4 + C6 + C7 cumulative + 5 NEW deleted RED test files cumulative C7 wholesale retire + 2α intra-file C1-α T4+T5 skip absorbed). 14 commits cumulative pre-push + D1 doc-only commit cumulative cierre POC fiscal-periods entero (push batch POC fiscal-periods closure 15 commits batch single — granularity natural §14 — 8 ciclos atomic + D1 cumulative 9 sub-fases mirror contacts 7 ciclos refined + paired sister axis cross-POC sub-cycle continuation pattern matures). | Ninguno (POC fiscal-periods entero CLOSED D1 cementación + push closure 15 commits cumulative batch single) | POC nuevo dedicado fiscal-periods cleanup `features/fiscal-periods/` load-bearing + cumulative cutover hex modules/fiscal-periods/ presentation cross-feature/cross-module precondición fue entregado cumulative — granularity natural §14 (8 sub-cycles initial granularity propose → 8 cumulative final con C5 VACUOUS-CLOSED partial scope reduction cumulative absorb pattern + C6 Opción ε identifier-preserving alias barrel re-export NEW canonical home pattern + C7 Opción C mixto retire schedule wholesale + skip+comment intra-file symmetry preservation NEW canonical home pattern). Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable heredadas POC contacts starting state + 5 §13 emergentes formales canonical α/γ/ε/ζ + ζ-prerequisite + 1 NEW top-level letter §13.B-paired + 6 NEW formal canonical POC payment + 1 sub-pattern matures POC payment + 1 NEW formal canonical Path simplificado v2 POC mortality + 3 NEW lecciones canonical homes POC contacts heredadas — POC fiscal-periods NO cementa lecciones nuevas §13 formal canonical (Marco lock D1 — todas matures cumulative cross-POC) — pero sí origina **2 NEW lecciones canonical homes engram body + 1 NEW Marco-lock variant** POC fiscal-periods (POC fiscal-periods 2do POC origina lecciones nuevas post-contacts cumulative cross-POC — distinto POC contacts originó 3 lecciones diferentes): (1) `feedback/retirement-strategy-mixto` 1ra evidencia POC fiscal-periods C7 emergent — Opción C mixto delete wholesale (5 archivos RED tests c-bis + c2 + c3 + c4 + c6) + skip+comment intra-file (2α C1-α T4+T5) symmetry preservation cuando precedent intra-file existente → delete file completo rompería symmetry forward-applicable cross-POC futuro retire schedule mixto + (2) `arch/§13/A5-zeta-classification-by-target-type#opcion-epsilon-identifier-preserving-alias` 1ra evidencia POC fiscal-periods C6 emergent — Opción ε identifier-preserving alias barrel re-export pattern NEW canonical home distingue de §13.A5-ζ wholesale por preserve identifier (`export type FiscalPeriodSnapshot as FiscalPeriod`) cuando consumer references Y mientras canonical home internal = X evita rename cascade forward-applicable cross-POC futuro TYPE swap diff mínimo + (3) `feedback/invariant-collision-elevation#audit-detected-variant` 3 variants formales total cumulative cross-POC POC fiscal-periods (pre-RED + audit-pre-RED + post-GREEN cascade) — Marco-lock variant audit-detected formal post C4 pre-phase-audit-gate 2da evidencia matures cumulative. Adicional `feedback/cross-cycle-red-test-cementacion-gate` 9na evidencia matures cumulative cross-POC (8va C6 PROACTIVE + 9na C7 PROACTIVE retire schedule mixto verify) — gate funcionó forward cumulative POC fiscal-periods. POC fiscal-periods cementa 9 §13 emergentes formales canonical matures cumulative cross-POC (§13.A 6ta evidencia post-cementación canonical RESOLVED wholesale + §13.A5-ζ wholesale 4ta + §13.A5-ζ-prerequisite barrel sub-import 4ta + §13.A reverse direction TYPE-only/existence-only 6ta evidencia resolved deferred C2 → resolved C6 PROACTIVE + §13.A4-η factory return shape sub-pattern N+7ma matures + §13 Adapter Layer cross-module 5ta + §13 Path simplificado v2 RSC boundary 7ma matures resolved C6 + §13.A5-α multi-level composition delegation 26ma + §13.A3-D4-α 29ª env-dependent toggle envelope expansion {6,9}) + 1 evidencia recurrente §13.A3-D4-α 29ª matures envelope expansion. **9 §13 emergentes cumulative POC fiscal-periods** (todos matures cumulative cross-POC Marco lock D1 — 0 NEW formal canonical). Pattern paired sister cumulative drift docs cumulative — 15ma evidencia paired sister lección #14 (POC fiscal-periods D1 PROACTIVE pre-cementación cumulative invariant 5373 preserved {6,9} margin clean baseline preventive evidence) + 14 evidencias diagnostic stash gate pattern cumulative cross-POC + 26ma evidencia §13.A5-α multi-level composition delegation cumulative cross-POC. **TSC 17 baseline NEW honest documented** (delta +4 vs heredado contacts closure 13 — ground truth supersedes engram cumulative claim chain stale; regression NEW commits no-mio anteriores cumulative pre-POC fiscal-periods outside modification domain — bookmark closure D1 cementa baseline NEW 17 ground truth wins). |

| **POC nuevo dedicado monthly-close** (cleanup `features/monthly-close/` load-bearing + cumulative build hex modules/monthly-close/ presentation cross-feature/cross-module + cross-module §17 carve-out FiscalPeriodReader + AccountingReader + DraftDocumentsReader + PeriodLockingWriter + UoW assemble 4 adapters tx-bound dentro `withAuditTx` callback timeout 30_000 + Path α single ciclo per stratum domain/application/infrastructure/presentation/cutover/wholesale-delete) | Bookmark scope cycle-start preceding sesión Step 0 PROACTIVE expand pre-recon comprehensive — 12 sub-cycles refined granularity (C0 skeleton barrels + C1 domain entity-less ports cross-module 2 readers + 1 single bundle errors file precedent mirror cumulative 13 evidencias + C2.1 application ports outbound cross-entity + UoW shape generic `UnitOfWork<TScope extends BaseScope>` POC #9 shared + C2.2 service class orquestador puro + C2.5 `getSummary` outside-scope read-only outside-tx use case completion hex application surface gap axis-distinct deferred sub-cycle + C3 infrastructure adapters Prisma concretes + UoW implementación §17 carve-out cross-module 5 archivos NEW + C4 presentation layer composition root factory `makeMonthlyCloseService()` zero-arg + C5 integration tests Postgres-real refactor preservante PASS-first VACUOUS-CLOSED + C6-a routes `route.ts` hex factory atomic + C6-b summary route `summary/route.ts` hex factory cementado C2.5 paridad EXACT pre-existing signature alignment + C7 wholesale delete features/monthly-close/* atomic 17 archivos 5 src + 12 tests dir __tests__ NO bridge DROP). Marco lock granularity refined cycle-start preceding sesión pre-recon comprehensive 12 ciclos initial granularity propose verified clean cumulative + retirement-strategy-preserve canonical home NEW (paired sister fiscal-periods C7 retirement-strategy-mixto). | 12 ciclos cumulative entregados (granularity natural §14 cumulative — 12 ciclos refined preserved + C2.5 deferred sub-cycle bookmark gap surface honest mid-cycle): C0 skeleton presentation barrels placeholder vacíos isomorphic + server-only mirror fiscal-periods precedent EXACT (commits `4bdde18` RED 3α + `fd456bc` GREEN 2 archivos NEW skeleton clean cutover) + C1 domain layer entity-less driver-anchored 2 reader ports outbound cross-module + 1 single bundle errors file 14ª evidencia precedent EXACT (commits `758975c` RED 5α superseded → `ff71dc0` RED-α refactor in-place 3α scope ajustado pre-RED redact gate MISSED → `be4fee8` GREEN 3 archivos NEW — `FiscalPeriodReaderPort` Snapshot LOCAL primitive-typed mirror iva-books + `AccountingReaderPort` Snapshot LOCAL Money VO reuse §13 sub-evidencia variant + `PeriodAlreadyClosedError` extends ConflictError + `BalanceNotZeroError` extends ValidationError wrap shared codes — textual-rule-verification recursive structural conventions 1ra evidencia POC monthly-close lección NEW canonical home paired sister recursive métricas Step 0) + C2.1 application layer ports outbound cross-entity + UoW shape minimal driver-anchored cumulative-precedent EXACT (commits `e0f55c5` RED 3α + `32331f8` GREEN 3 archivos NEW + 1 MOD UoW shape generic `UnitOfWork<TScope extends BaseScope>` 4 evidencias type alias + 5 evidencias XScope cross-module + UoW location application/ 3:1 majority — sub-cycle adjustment 4α → 3α IN-PLACE pre-RED redact gate mirror C1 lección NO RED-time content assertions cumulative-precedent recursive 3ra evidencia matures fiscal-periods C1-α 1ra + monthly-close C1-α 2da + C2.1 RED-α 3ra) + C2.2 application layer service class orquestador puro driver-anchored (commits `df21a07` RED 1α + `7359cb8` GREEN 1 archivo NEW — sale + payment + purchase + iva-book service class 13 evidencias absoluto + 4 evidencias deps interface + 4 evidencias inline DTO + 4 evidencias decomposed args + 4 evidencias spread correlationId UoW.run + Money.equals() 1ra POC monthly-close paired sister Money VO 4ta cementación cross-POC + evidence-supersedes-assumption-lock 3ra evidencia matures cumulative cross-POC + textual-rule-verification recursive structural conventions 2da evidencia matures C1 cementación textual JSDoc supersede wording loose posterior) + C2.5 application layer `getSummary` outside-scope read-only outside-tx use case completion hex application surface gap axis-distinct deferred sub-cycle bookmark gap canonical home NEW (commits `97b5780` RED 5α existence-only POS + `ae6321f` GREEN 4 archivos 2 NEW + 2 MOD — outside-scope read-only outside-tx use case completion hex application surface gap axis-distinct cumulative-precedent EXACT driver-anchored + `MonthlyCloseSummary` DTO inline service file 2da evidencia matures cumulative + summary reader port outside-scope axis-distinct 5ta evidencia matures + moneyToFixed adapter-side helper bit-perfect pattern NEW canonical home 1ra evidencia POC monthly-close C2.5) + C3 infrastructure layer adapters Prisma concretes + UoW implementación §17 carve-out cross-module driver-anchored cumulative-precedent EXACT (commits `52a1f8d` RED 5α + `a80e6c3` GREEN 5 archivos NEW — `PrismaFiscalPeriodReader` factory-wrap `makeFiscalPeriodsService` NO-prisma-prefix opción b mirror accounting precedent variant 3ra evidencia matures factory-wrap convention canonical iva-books + accounting + monthly-close + `PrismaDraftDocumentsReader` cross-module Prisma direct 5 entities `Promise.all` count outside-scope read-only §17 carve-out cite + `PrismaAccountingReader` cross-module Prisma direct tx-bound at construction raw SQL JOIN journal_lines + journal_entries POSTED COALESCE numeric(18,2) Money VO 4ta cementación cross-POC §17 carve-out cite + `PrismaPeriodLockingWriter` cross-module Prisma direct tx-bound at construction 5 updateMany {POSTED → LOCKED} cross-entity §17 carve-out cite + Writer adapter naming `prisma-<X>-writer.adapter.ts` NEW canonical 1ra evidencia POC monthly-close paired sister Reader pattern + `PrismaMonthlyCloseUnitOfWork` assemble 4 adapters tx-bound dentro `withAuditTx` callback + BaseScope.fiscalPeriods.markClosed cumulative POC #9 shared + UoW timeout 30_000 wiring `withAuditTx` 4to arg signature `audit-tx.ts:32` legacy parity preservation NEW 1ra evidencia POC monthly-close + Riesgo C lock cascade rationale JSDoc paired adapter + UoW pattern opción c ambas ref design.md §Lock order FK direction Sale↔JE archive cementado Lock cascade STRICT ORDER assertion NEW 1ra evidencia POC monthly-close + Money VO consistency tx-bound/NoTx variant NEW 1ra evidencia POC monthly-close paired sister) + C4 presentation layer composition root factory `makeMonthlyCloseService()` zero-arg cumulative-precedent EXACT supersede absoluto driver-anchored (commits `a11e3a2` RED 1α + `54043d7` GREEN 1 archivo NEW + 1 archivo MOD — sale + payment + fiscal-periods + iva-books + accounting 5 evidencias zero-arg signature 6ta evidencia matures monthly-close NEW + R4 carve-out cite composition-root JSDoc 5ta evidencia matures + NO §17 cite at composition-root 6ta evidencia absoluto + repoLike pattern 3 evidencias EXACT cumulative + 3 deps wiring directos own-module + cross-module via FiscalPeriodReaderAdapter factory-wrap default-init `makeFiscalPeriodsService` cementado C3 + NO memoización mirror sale + payment + fiscal-periods + accounting 4 evidencias supersede solo iva-books cycle-break + server.ts barrel populate `export * from "./validation"` 3ra evidencia EXACT mirror fiscal-periods + payment + monthly-close + textual-rule-verification recursive structural conventions 4ta evidencia matures C1 + C2.2 + C3 + C4 recursive 7 conventions verified ≥3 evidencias EXACT pre-RED redact gate composition root layer + evidence-supersedes-assumption-lock 5ta evidencia matures cumulative cross-POC bookmark wording "(prisma)" param loose superseded por cumulative-precedent EXACT zero-arg 5 evidencias supersede absoluto) + C5 integration tests Postgres-real refactor preservante PASS-first VACUOUS-CLOSED canonical home NEW 1ra evidencia POC monthly-close (commit `81be1b7` RED-α 9α PASS-first 2 archivos NEW integration tests Postgres-real driver-anchored — VACUOUS-CLOSED vs refactor preservante PASS-first axis-distinct distinction NEW canonical home 1ra evidencia POC monthly-close C5 paired sister VACUOUS-CLOSED cycle canonical home cumulative fiscal-periods C5 full + C6 partial — distinción canonical: VACUOUS-CLOSED = upstream colapsa downstream sin GREEN required vs refactor preservante PASS-first = RED tests assert refactor invariants pasan contra legacy current state, no GREEN needed semantic) + C6-a routes hex factory atomic migration to hex modules/monthly-close/presentation/validation.ts + adapt close() signature object→positional 4 args (commits `67903c5` RED 4α + `37ba4cb` GREEN 4 archivos 1 NEW + 3 MOD — cutover route.ts hex factory `makeMonthlyCloseService()` + closeRequestSchema atomic + close() signature divergence object→positional intentional design 4ta evidencia matures cumulative + hex barrel populate `export * from "./validation"` 3ra evidencia EXACT mirror fiscal-periods + payment + monthly-close cumulative) + C6-b summary route hex factory cementado C2.5 paridad EXACT pre-existing signature alignment (commits `c5ea687` RED 4α + `c6c8ca0` GREEN 2 MOD — cutover summary route hex factory `makeMonthlyCloseService()` consume `getSummary` cementado C2.5 paridad + importOriginal preserve barrel re-export cross-route consistency variant 1ra evidencia + JournalSummary float drift Riesgo H DEFER §13 D1 documented surface honest pre-GREEN inferential JournalSummary numeric drift detección sub-finding) + C7 wholesale delete features/monthly-close/* atomic 17 archivos 5 src + 12 tests dir __tests__ NO bridge DROP hex barrel autónomo post-C6-a + C6-b cumulative (commits `2b48b25` RED-α 7α: 5 NEG src files + 1 NEG dir __tests__ + 1 POS preservation guard hex canonical exports + `d5b5059` GREEN 17 archivos delete atomic single batch — `feedback/retirement-strategy-preserve` NEW canonical home 1ra evidencia POC monthly-close C7 paired sister fiscal-periods C7 retirement-strategy-mixto + retirement-reinventory-gate-class-symbol-grep 10mo axis 3ra evidencia CLEAN matures cumulative + retirement-reinventory-gate-vimock-factory-grep 11mo axis 3ra evidencia CLEAN matures cumulative + cross-cycle-red-test-cementacion-gate 10ma evidencia matures cumulative + 4 métricas baseline EXACT preserved cumulative). 22 commits cumulative pre-push + D1 doc-only commit cumulative cierre POC monthly-close entero (push batch POC monthly-close closure 23 commits batch single — granularity natural §14 — 12 ciclos atomic + D1 cumulative 13 sub-fases vs precedent A1+A2+A3+A4+A5+POC paired+payment+mortality+contacts+fiscal-periods cumulative push deferred al cierre POC entero EXACT). | Ninguno (POC monthly-close entero CLOSED D1 cementación + push closure 23 commits cumulative batch) | POC nuevo dedicado monthly-close cleanup `features/monthly-close/` load-bearing + cumulative build hex modules/monthly-close/ presentation cross-feature/cross-module precondición — granularity natural §14 12 ciclos refined preserved cumulative (asimétrico vs payment 6 ciclos refined + fiscal-periods 8 ciclos — scope expandido por strata-per-cycle granularity natural §14 + C2.5 deferred sub-cycle bookmark gap surface honest). Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable + 3 NEW lecciones canonical homes POC contacts + 2 NEW lecciones canonical homes POC fiscal-periods + 1 NEW Marco-lock variant heredadas POC fiscal-periods starting state — POC monthly-close ORIGINA 5 NEW lecciones canonical homes engram body (3er POC origina lecciones nuevas post-contacts + fiscal-periods cumulative cross-POC) + cementa 0 NEW formal canonical §13 (Marco lock D1 — todas matures cumulative cross-POC) + matures cumulative cross-POC ~14 §13 emergentes matures cumulative ver §19.14 detail. **Suite 5334/7/19 cumulative invariant 5360 preserved runtime verified cementación D1** (delta -13 net cumulative POC monthly-close vs POC fiscal-periods closure 5373 — runtime ground truth confirmed cumulative invariant arithmetic preservation `passed+failed+skipped=5360` invariant {6,9} margin §13.A3-D4-α 30ª evidencia matures envelope expansion env-dependent toggle within margin per `arch/lecciones/dispatches-hub-flake-recurrente`). **TSC 17 baseline EXACT preserved** (NO regression POC monthly-close vs POC fiscal-periods closure 17). **ESLint 26 (10e/16w) baseline NEW honest delta +3w vs POC fiscal-periods closure 10e/13w** outside POC modification domain (3 warnings NEW commits no-mio anteriores cumulative pre-POC monthly-close `tests/stress/*.js import/no-anonymous-default-export` untracked dir tests/ — recursive aplicación lección #14 pattern paired sister 14ma TSC -4 contacts D1 + 15ma TSC +4 fiscal-periods D1 + 16ma ESLint +3w monthly-close D1 honest documented). **REQ-FMB.5 0 violations preserved** (delta-POC monthly-close 0 — verify §20.12 PROD app/ paths). |
| **POC nuevo dedicado pagination-sale** (POC pilot pagination Sale Opción 2 ~6 ciclos initial → 3 ciclos refined Opción C ADDITIVE NEW método paralelo transitional dual-method `findAll+findPaginated` + `list+listPaginated` + adapter Prisma skip/take + count Promise.all parallel + service NEW método return generic VO `PaginatedResult<T>` + route Response.json wrap PaginatedResult<SaleSnapshot> shape + UI shadcn Pagination + URL search params sync RSC + page Next 16 RSC `searchParams: Promise<{...}>` + 2 D1 adapt direct-consumer-tests cascade) | POC pilot pagination Sale ~6 ciclos initial bookmark recon `discovery/pagination-recon-fcdb399` #1782 (C0 shared VO + C1 port + C2 adapter + C3 service + C4 route + C5 UI + D1) | 3 ciclos refined cumulative — C0 shared canonical home (3 archivos NEW VO `modules/shared/domain/value-objects/pagination.ts` + presentation schema `modules/shared/presentation/pagination.schema.ts` + shared route helper `modules/shared/presentation/route-helpers/parse-pagination-params.ts`) + C1-MACRO atomic batch cross-layer cutover ADDITIVE NEW métodos paralelos `findPaginated`+`listPaginated` (10 archivos: 1 NEW shadcn `components/ui/pagination.tsx` + 7 modify port/fake/adapter/service/route/page/sale-list + 2 D1 adapt direct-consumer-tests cascade `page.test.ts`+`sale-list-date-format.test.tsx`) + D1 doc-only cementación cumulative architecture.md (5 secciones expand mirror precedent monthly-close `0943f1f` EXACT) | POCs siguientes inmediatos ~3 features remaining (HubService cleanup pending engram canonical home `feedback/sale-find-all-legacy-vs-find-paginated-dual-method-cleanup-pending` 4to cumulative paired sister 3 heredados — `feedback/json-parse-stringify-cleanup-pending` + `contact-snapshot-vs-prisma-contact-type-alignment` + `radix-dialog-aria` — Opción C ADDITIVE preserves DEFER + zero orphan period + future migration trigger condition cumulative paired sister) + Contacts+Balances N+1 sub-POC architecture decision separate (Marco lock E DEFER post-pilot recon #1782) + Journal legacy migration journal hex POC dedicado (Marco lock D DEFER post-pilot Riesgo H recon #1782) | Opción C ADDITIVE NEW método paralelo dual-API transitional pattern cross-POC reusable cuando BREAKING signature change collide con cross-feature consumer DEFER lock (`features/dispatch/hub.service.ts:141,151` axis-η Categ C++ DEFER recon #1782). L1 BREAKING signature change strategy abandoned retroactive Hub callsite emergente Step 0 expand cycle-resume cold cascade analysis (evidence-supersedes-assumption-lock 9na matures + 2da intra-POC mid-cycle L1 BREAKING superseded retroactive). Distinción canonical direct-consumer-tests cascade variant (page.test + sale-list-date-format adapt mismo batch — tests test the changing surface) vs cross-feature consumer cascade variant (Hub Opción C ADDITIVE preserves legacy zero orphan period). Hex-correctness preserved dual API transitional axis-distinct paralelo (findAll legacy + findPaginated NEW). Pre-phase-audit-gate scope expand 7ma matures cumulative cross-POC cascade analysis MANDATORY include direct-consumer tests pre-execute (NOT solo cross-feature). Recursive aplicación lección #14 17ma paired sister D1 PROACTIVE pre-cementación cumulative invariant 5399 preserved {6,9} margin §13.A3-D4-α 31ª evidencia matures envelope expansion clean baseline preventive evidence. |
| **POC nuevo dedicado pagination-replication** (POC replication multi-módulo Purchase + Payment Opción C ADDITIVE NEW método paralelo transitional dual-method `findAll+findPaginated` + `list+listPaginated` heredado pagination-sale pilot canonical + adapter Prisma skip/take + count Promise.all parallel + service NEW método return generic VO `PaginatedResult<T>` + route Response.json wrap PaginatedResult<{Purchase,PaymentWithRelations}> shape + UI shadcn Pagination + URL search params sync RSC + page Next 16 RSC `searchParams: Promise<{...}>` + Path 2 PurchaseFilters interface extension `purchaseTypeIn` array unification axis-distinct + Payment reader port extension `findPaginatedWithRelations` + adapter wrap distintivo direct factory + 5 D1 adapt direct-consumer-tests cascade Purchase 3 + Payment 2) | POC pilot replication pattern Sale pilot canonical heredado #1799 a Purchase + Payment hex modules — bookmark recon Step 0 expand cycle-start cold cumulative pre-RED Purchase verified baseline 5373p/7f/19s/5399 envelope {6,9} ∋ 7 + bookmark Payment cycle-start cold checklist file+assumption pairs + recon driver-anchored Purchase Path 2 unification + Payment Path 1 simple | 3 ciclos refined cumulative — C1-MACRO Purchase atomic batch cross-layer cutover ADDITIVE NEW métodos paralelos `findPaginated`+`listPaginated` (11 archivos: 8 plan + 3 D1 adapt direct-consumer-tests cascade `page.test.ts` + `purchase-list-date-format.test.tsx` + `purchase-list-unification.test.tsx`) + Path 2 PurchaseFilters interface extension `purchaseTypeIn` array unification axis-distinct + intra-GREEN micro-fix TS2698 spread undefined `page.tsx` atomic same commit + C1-MACRO Payment atomic batch cross-layer cutover ADDITIVE NEW métodos paralelos (12 archivos: 9 plan + 2 D1 adapt direct-consumer-tests cascade `page.test.ts` + `payment-list.test.tsx` + 1 intra-GREEN cleanup InMemoryPaymentRepository fake findPaginated impl scope-recon-supersede-by-emergent-tsc-propagation 4ta evidencia matures) + reader port extension `findPaginatedWithRelations` + adapter wrap distintivo Payment vs Sale+Purchase symmetric direct factory + intra-GREEN micro-fix red-regex-discipline 11ma `skip:`/`take:` explicit literal vs shorthand object literal + D1 doc-only cementación cumulative architecture.md (5 secciones expand mirror precedent pagination-sale `f6146d6` EXACT) | POCs siguientes inmediatos ~3 features remaining (HubService cleanup pending engram heredado pagination-sale `feedback/sale-find-all-legacy-vs-find-paginated-dual-method-cleanup-pending` + 4 NEW cleanup pending engrams cumulative cross-POC POC pagination-replication: 5to `feedback/dispatch-pagination-defer-pending-hex-migration` + 6to `feedback/purchase-period-id-filter-drift-domain-fake-cleanup-pending` + 7mo `feedback/payment-composition-root-direct-instance-vs-factory-make-cleanup-pending` + 8vo `feedback/payment-port-location-asymmetry-domain-vs-domain-ports-cleanup-pending`) + Contacts+Balances N+1 sub-POC architecture decision separate (Marco lock E DEFER post-pilot recon #1782 heredado) + Journal legacy migration journal hex POC dedicado (Marco lock D DEFER post-pilot Riesgo H recon #1782 heredado) | Opción C ADDITIVE NEW método paralelo dual-API transitional pattern cross-POC reusable replicación heredado pagination-sale pilot canonical Purchase + Payment hex modules. Path 2 PurchaseFilters interface dual single+array fields backwards-compat preserved 1ra evidencia POC pagination-replication (paired sister dual-method ADDITIVE pattern interface field-level variant — `purchaseType` single + `purchaseTypeIn` array mutuamente exclusivos via zod refine — REQ-C.2 unification COMPRA_GENERAL_O_SERVICIO maps a [COMPRA_GENERAL, SERVICIO]). Payment structural drift cumulative 3ra evidencia matures (port location asymmetry `domain/payment.repository.ts` directo vs Sale/Purchase `domain/ports/X.repository.ts` 2da + composition-root `new PaymentService()` direct instance vs Sale/Purchase `makeXService()` factory 2da + adapter wrap distintivo Payment vs Sale+Purchase symmetric direct factory 1ra) — 4 NEW cleanup pending engrams heredados Opción C ADDITIVE preserves DEFER + zero orphan period. Reader-port-extension `findPaginatedWithRelations` 1ra evidencia matures cumulative cross-POC necesaria preserve hex encapsulation §13 vs adapter bypass `inner.list` usa `reader.findAllWithRelations` directo. Direct-consumer-tests cascade scope-truncado asymmetry per módulo tests-presence Payment 2 archivos vs Sale+Purchase 3 archivos 1ra evidencia (NO `payment-list-format.test.tsx` ni `payment-list-unification.test.tsx` heredados pre-existentes — honest asymmetry per módulo tests-presence diferent). Pre-phase-audit-gate scope cumulative cross-POC 9na matures + cross-cycle red-test cementación gate forward-only zero collision verified (Sale RED + Purchase RED + Payment RED paths divergent). diagnostic-stash-gate-pattern 4ta evidencia matures cumulative cross-POC (stash RED-α + isolated baseline re-run distinguishes deterministic from flake variance). Recursive aplicación lección #14 18ma paired sister D1 PROACTIVE pre-cementación cumulative invariant 5419 preserved envelope {6,9} ∋ 9 upper bound + ESLint 26 (10e/16w) baseline EXACT preserved + TSC 17 EXACT preserved §13.A3-D4-α 32ª evidencia matures envelope expansion env-dependent toggle. |

| **POC paired farms+lots** (POC paired greenfield migración hexagonal Farm + Lot domain entity-less greenfield C0/C1/C2/C3/C4/C5/C6/C7 paired sister payables/receivables canonical reference EXACT mirror precedent + hotfix retroactivo pre-D1 runtime RSC boundary serialization fix LotSummary.toJSON() + expenses Decimal→Number page-level map + lot-detail-client.tsx prop types update + NEW type alias export LotSummaryShape + cross-feature ports migration mortality+pricing+AI-agent consolidate canonical + wholesale delete features/{farms,lots}/* 12 archivos atomic + Path α single ciclo per stratum paired sister payables/receivables EXACT) | Bookmark scope cycle-start POC paired greenfield hex migración 8 ciclos atomic paired sister payables/receivables canonical reference EXACT mirror precedent + 4 §13 NEW emergentes C2 + 1 NEW C3 + 1 NEW C4 + 3 NEW C5 + 2 NEW C6 + hotfix retroactivo pre-D1 ADDENDUM RSC boundary serialization fix runtime path coverage detected in-the-wild Marco-side smoke `/lots/[lotId]` page render 2 errors + 1 NEW hotfix §13 emergente invariant-collision-elevation-prefers-new-type-export | 8 ciclos atomic + hotfix retroactivo RED+GREEN cumulative + D1 doc-only post-mortem (granularity natural §14 cumulative — 8 ciclos refined + hotfix retroactivo runtime ground truth verified ahead-of-doc precedent §19.16.1 EXACT mirror): C0 domain greenfield + C1 application service class + ports + InMemory fakes + C2 infrastructure Prisma adapters + mappers + LocalMemberInquiryAdapter + C3 presentation composition root + validation + server barrels + C4 routes API cutover hex factory + schemas swap + C5 UI pages cutover hex + NEW attachLots bridge + NEW DTO + LotSummary VO refactor + consumer client type migration + C6 cross-feature ports migration consolidate canonical + C7 wholesale delete features/{farms,lots}/* 12 archivos + hotfix retroactivo pre-D1 RED-α + GREEN-α 4 archivos modify atomic single batch + D1 doc-only cementación cumulative architecture.md (5 secciones expand mirror precedent pagination-replication EXACT) + ADDENDUM §19.17.1 bundle MISMO D1 commit paired sister c0-hotfix §19.16.1 bundle precedent EXACT mirror. 18 commits cumulative pre-push + D1 doc-only commit cumulative cierre POC paired farms+lots entero (push batch POC paired farms+lots closure 19 commits batch single — granularity natural §14 — 8 ciclos atomic + hotfix retroactivo RED+GREEN + D1 cumulative push deferred al cierre POC entero EXACT) | Ninguno (POC paired farms+lots entero CLOSED D1 cementación + hotfix retroactivo pre-D1 ADDENDUM bundle MISMO D1 commit + push closure 19 commits cumulative batch) | POC paired farms+lots paired greenfield migración hexagonal Farm + Lot domain entity-less greenfield cumulative build hex modules/{farm,lot}/ presentation cross-feature/cross-module precondición — granularity natural §14 8 ciclos atomic + hotfix retroactivo pre-D1 cumulative (asimétrico vs paired payables/receivables 8 ciclos EXACT mirror precedent + hotfix retroactivo runtime ground truth verified ahead-of-doc precedent §19.16.1 EXACT mirror). Pre-recon expand cumulative aplicó lecciones #1-#14 + #10-skippable + 3 NEW lecciones canonical homes POC contacts + 2 NEW lecciones canonical homes POC fiscal-periods + 1 NEW Marco-lock variant heredadas + 5 NEW lecciones canonical homes POC monthly-close + 8 NEW lecciones canonical homes POC pagination-sale + 12 NEW lecciones canonical homes POC pagination-replication heredadas starting state — POC paired farms+lots ORIGINA 4 NEW lecciones canonical homes engram body (6to POC origina lecciones nuevas post-pagination-replication cumulative cross-POC) + cementa 11 NEW formal canonical §13 (Marco lock D1 — 4 C2 + 1 C3 + 1 C4 + 3 C5 + 2 C6 + 1 hotfix) + matures cumulative cross-POC ~12 §13 emergentes matures cumulative ver §19.17 detail. **Suite 5606p/7f/19s/5632 cumulative invariant preserved runtime verified cementación D1** (delta +213p net cumulative POC paired farms+lots vs POC pagination-replication closure 5419 — runtime ground truth confirmed cumulative invariant arithmetic preservation `passed+failed+skipped=5632` invariant {6,9} margin §13.A3-D4-α 22ma matures envelope expansion env-dependent toggle within margin per `arch/lecciones/dispatches-hub-flake-recurrente`). **TSC 17 baseline EXACT preserved** (NO regression POC paired farms+lots vs POC pagination-replication closure 17 — paired greenfield + wholesale delete + hotfix retroactivo Opción A4 NEW type alias export LotSummaryShape bypass TS2344 private constructor preserves cumulative invariant). **ESLint 26 (10e/16w) baseline EXACT preserved** (paired greenfield + wholesale delete + hotfix retroactivo preservación zero impact ESLint baseline cumulative — recursive aplicación lección #14 pattern paired sister 19na EXACT preserved POC paired farms+lots D1 honest documented). **REQ-FMB.5 0 violations preserved** (delta-POC paired farms+lots 0 — verify §20.15 PROD app/ paths). |

¹ **Nota correctiva (POC #11.0a A5 β Step 0 D-Step0#2)**: pre-recon A5 β reveló 3 server components adicionales consumiendo `SaleService` legacy NO listados en el scope formal del bookmark histórico A4-a: `app/(dashboard)/[orgSlug]/sales/page.tsx`, `sales/[saleId]/page.tsx`, `dispatches/page.tsx`. POC #11.0c hereda **10 cutovers explícitos** (8 iva-books + 1 hub + `dispatches/page` — el surface nuevo que A5 β agrega al scope heredado), no 9. Los otros 2 server components (`sales/page` + `sales/[saleId]/page`) son consumers conocidos pre-existentes pero quedan para auditoría retroactiva POC #11.0c. El bookmark histórico se preserva sin reescribir — la corrección vive aquí (auditabilidad).

² **Nota correctiva (POC #11.0b post-A3 audit-6 Marco challenge)**: bookmark scope POC #11.0b nunca se lockeó explícitamente al inicio del POC (asumido implícito mirror sale POC #11.0a 5 sub-fases). Cuando A1+A2+A3 cerraron, reporte interpretó "POC #11.0b CLOSED completo" — Marco surfaceó el gap (A4 + A5 pendientes mirror sale precedent). Bookmark `poc-11/0b/scope-locked-bookmark` (engram) se creó retroactivamente con γ lockeada Marco (A4-a γ minimal + A5 cleanup completo deferral). POC #11.0c hereda **11 cutovers purchase-side explícitos** post A4-a γ (2 core API restantes + 4 iva-books/purchases + 3 server components + 2 client components) + cleanup completo `features/purchase/` (mirror sale cleanup completo diferido). Tsc baseline POC #11.0c heredado actualizado: 16 errores (3 sales/route + 2 purchases/route + 11 findManyByCodes) — drift heredado A4-a γ simétrico al sale precedente. El bookmark POC #11.0b se preserva con la corrección retroactiva en `poc-11/0b/scope-locked-bookmark` engram (auditabilidad). Lección operacional: bookmark scope-locked-bookmark al INICIO de un POC es check obligatorio — sin él, cierre prematuro pasa silently incluso con auditoría retroactiva por sub-fase.

³ **Nota emergente (POC #11.0c A4-c)**: bridges teardown surfaceó como NEW §13 emergente durante A4-c P-items recon — blast radius excedió expectativa "validación estructural sale + purchase". Las 4 interfaces IVA-bridge (2 inbound `SaleServiceForBridge`, `PurchaseServiceForBridge` consumidas por IVA legacy + 2 outbound `IvaBooksServiceForSaleCascade`, `IvaBooksServiceForPurchaseCascade` consumidas por Sale/Purchase legacy cascade) son cross-module IVA↔Sale/Purchase y NO migran en POC #11.0c — sus contracts dependen de `externalTx` (§5.5 retired Ciclo 6 hex). (Bridge `SaleServiceForHub` defer separado: hub-migration POC, shape-adapter no expuesto por aggregate hex `Sale.period` hidratado + `displayCode`/`contact` permanece §18.4 fila POC #11.0a contexto histórico — NO IVA-CRUD pre-condición.) Lock D-Step0 Marco: NO A4-c, NO A5, NO POC #11.0c entero — defer atómico POC futuro pre-IVA-CRUD-hex-migration cuando consumer (IVA) migre a hex y bridges desaparezcan en cutover atómico (§19). Adicionalmente A5 cerró sanitize `correlationId-api-leak` (Opción C ejecutada cleanup pre-decidido per engram `poc-11/0c/a4/section-13/correlationid-api-leak`) — convención formal codificada §20. Métricas POC #11.0c finales: 5030/6/2 (5038 tests) + tsc 16 + ESLint 11e/15w heredados pre-existentes — POC futuro retire #6 ESLint warnings legacy `features/` + #9 drift externo R5 Prisma value-objects.

### 18.5. Cleanup parcial honesto — caso inverso

§18.4 cubre el caso donde el cutover NO se puede hacer (bridge bloquea, scope se difiere). Hay un caso simétrico inverso: el cutover principal está bloqueado (parte del scope diferida a POC futuro), **pero porciones del legacy son fachada-presentation pura — cero consumers internos del propio legacy** y se pueden DELETE/migrar sin esperar el cutover completo.

**Cuándo aplica**:

1. El POC actual está cerrando con parte del scope diferida a POC futuro vía §18.4.
2. Existen archivos o sub-modules legacy donde el pre-recon (grep cross-cutting **PROJECT-scope**) confirma cero consumers internos del propio legacy: schemas Zod no consumidos por el service, tipos DTO presentation no referenciados por dominio, helpers no llamados desde otras capas legacy.
3. Los consumers cross-module se pueden re-rutear ahora (sin esperar al POC destino) porque el shape migrado es bit-exact.

**Por qué entregarlo en el POC actual**:

- Reduce surface legacy ahora — menos código que el POC destino tenga que tocar.
- Entrega valor incremental observable: el POC actual no cierra "vacío" cuando hay cleanup factible.
- Establece precedente: cleanup honesto sólo aplica donde el legacy NO es load-bearing. NO es atajo para evadir §18.4.

**Cómo distinguir cleanup factible de scope creep**:

- Pre-recon honesto **PROJECT-scope** (no sólo `app/`) — incluyendo `__tests__/` legacy y mocks. Excluir directorios da falsos negativos.
- Validación post-GREEN con `tsc --noEmit` para cambios type-only. Las imports `import type` se elidan en runtime vitest — los smoke tests pasan trivialmente y NO producen RED genuino para tipos puros. La validación honesta es tsc.
- Si surgen consumers no detectados, update in-place sólo si el shape migrado es bit-exact (regla #1) y el cambio es type-only.

**Precedentes**:

| POC | Cleanup entregado | Status del archivo |
|---|---|---|
| #11.0a A5 β Ciclo 1 | Drift A3 paridad bit-exact: `prisma-iva-book-regen-notifier.adapter.ts` drop `status: "ACTIVE"` filter (alinear con legacy SIN filter — adapter NO inventa "defensive" filter; precedente Ciclo 3 `getNextSequenceNumber`). | Adapter intacto, drift cerrado |
| #11.0a A5 β Ciclo 2 | DELETE `features/sale/sale.validation.ts` (4 schemas Zod fachada-presentation pura, cero consumers internos legacy). 3 routes core re-routeadas a `modules/sale/presentation/schemas/`. | Archivo eliminado |
| #11.0a A5 β Ciclo 3 | Migrate `SaleWithDetails` + 3 tipos (`PaymentAllocationSummary`, `ReceivableSummary`, `SaleDetailRow`) a `modules/sale/presentation/dto/`. ELIMINATE `IvaSalesBookDTO` re-export muerto. 8 imports re-routed (4 producción + 4 tests legacy). | `sale.types.ts` reducido a input types |
| #11.0a A5 β (cleanup completo `features/sale/`) | — | Diferido POC #11.0c (load-bearing por runtime consumers heredados §18.4) |

### 18.6. Cross-ref

- **Complementa**: §13 (auditoría retroactiva — surface drift hacia atrás), §14 (componente mínimo — completar la decisión actual), §18.5 (cleanup parcial honesto — caso inverso). §18 cubre el caso simétrico hacia adelante: cuando la decisión actual choca con scope prometido, mover el scope, no la decisión.
- **NO debilita**: regla #1 fidelidad legacy (la deuda explícita no es atajo — es el costo conocido y declarado de la decisión lockeada anterior).
- **Pre-condición POC destino**: el POC que herede la deuda diferida la lista en su bookmark de apertura como scope obligatorio. Sin eso, el patrón se rompe.

---

## 20. correlationId NO en API surface

Las hex routes Next.js que retornan `Response.json` (o `NextResponse.json`) **NO** deben spread `correlationId` en el body de la response:

```ts
// ❌ INCORRECTO — leak audit telemetry interno en API surface
return Response.json({ ...result.entry, correlationId: result.correlationId });

// ✅ CORRECTO — sólo entrega el aggregate/DTO público
return Response.json(result.entry);
```

**Razón estructural**: `correlationId` es identificador de correlation tracking entre transacción + audit log. Es **telemetría interna del backend** — no parte del contrato API público. Filtrarlo al body acopla consumers externos a un campo audit-only que puede cambiar shape (UUID v4 → ULID → trace-id distributed) sin notice, y expone el modelo audit a clients que NO deben razonar sobre él.

El shape canónico hex `{ entry, correlationId }` (retornado por `service.execute()`) **separa concerns correctamente**: `entry` es el aggregate público, `correlationId` es metadata audit. La capa HTTP route es responsable de elegir qué se publica — y `correlationId` no se publica.

### 20.1. Auditoría retroactiva esperable pre-merge POC futuro

Toda hex route nueva — y toda hex route existente que entre a un POC futuro de cutover/refactor — debe auditarse contra el leak shape:

```bash
# pattern grep pre-merge — hits = leak
grep -rE "Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId" app/
grep -rE "NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId" app/
```

Strip atómico en mismo edit que toque la route — no diferir a "cleanup commit posterior". Si requiere RED+GREEN paired (assertions test acopladas), precedent A5 split TDD aplica (RED separado del GREEN+REFACTOR atomic, per `feedback_jsdoc_atomic_revoke`).

### 20.2. Precedente — POC #11.0c A5

Surfaceó como `§13 emergente correlationId-api-leak` durante POC #11.0c A4-a Ciclo 1 Q4 verify gate: 8 routes iva-books `{sales,purchases}` × `{POST, PATCH, void, reactivate}` retornaban `Response.json({ ...result.entry, correlationId: result.correlationId })`. Lock decisional fue Opción C (preservation-during-cutover, cleanup futuro pre-decidido) en POC #11.0c A4-a; ejecutado en POC #11.0c A5 (sanitize TDD split: 8 tests inversion RED en `e55e551` + 8 routes strip + 8 JSDoc atomic revoke GREEN+REFACTOR en `6ed613d`).

### 20.3. Cross-ref

- **Origen**: §13 (auditoría retroactiva) — la convención emerge de un drift sistémico audit-only filtrado a API; auditoría retroactiva (§20.1) extiende detección hacia atrás.
- **Engram**: `poc-11/0c/a4/section-13/correlationid-api-leak` (lock textual original Opción C → CLOSED A5), `poc-11/0c/a5/closed` (sanitize ejecución A5).

### 20.4. Verify retroactivo POC siguiente

POC siguiente "IVA CRUD hex migration + bridges teardown atómico" verify retroactivo §20.1 pattern grep ejecutado pre-cierre (A4 doc-only post-mortem):

- **Routes nuevas added POC siguiente**: 0 — POC siguiente fue cleanup atómico (deletions + migrations), NO surface API growth.
- **Routes editadas POC siguiente**: 0 routes añaden `correlationId` al body. Modificaciones se limitaron a (a) drop legacy class instantiations (`new IvaBooksService()` deletion), (b) re-import paths post-A2-C1.5 redirect (TASA_IVA top-level barrel), (c) vi.mock factory cleanup A2-C2.
- **Convención §20 preservada**: leak shape NO regressed. 8 commits cumulative POC siguiente verify clean contra `Response.json({ ...result, correlationId })` pattern.

Pattern application coherente §20.1: cualquier hex route futura POC nuevo A3 (cutover legacy → hex) DEBE auditarse contra leak shape pre-merge. Strip atómico mismo edit que toque la route.

### 20.5. Verify retroactivo POC nuevo A3

POC nuevo dedicado A3 "cleanup `features/{sale,purchase}/` load-bearing + cumulative cutover hex modules/{sale,purchase}/ presentation" verify retroactivo §20.1 pattern grep ejecutado pre-cierre A3-D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 runtime verify pre-cementación):

- **Routes nuevas added POC nuevo A3**: 0 — POC nuevo A3 fue cumulative cutover (composition root `makeService` factory + DTO mappers + DELETE legacy class instantiations), NO surface API growth.
- **Routes editadas POC nuevo A3**: sale routes 3+4 (A3-C5 cutover `sales/[saleId]/route.ts` PATCH + `sales/[saleId]/status/route.ts` POST → hex composition root) + purchase routes 3+4 (A3-C6c cutover `purchases/[purchaseId]/route.ts` PATCH + `purchases/[purchaseId]/status/route.ts` POST → hex composition root). 0 routes añaden `correlationId` al body — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones se limitaron a (a) replace `new SaleService()`/`new PurchaseService()` legacy con `makeSaleService()`/`makePurchaseService()` composition root + (b) replace import `@/features/sale/server`/`@/features/purchase/server` con `@/modules/{sale,purchase}/presentation/composition-root` + (c) signature swaps object-DI vs positional + (d) `Response.json` raw entity preserved (NO mapper invocation routes mutation per consumer behavior `purchase-form.tsx`/`sale-form.tsx` mutation handlers NO consume body solo `response.ok`).
- **Convención §20 preservada**: leak shape NO regressed. 32 commits cumulative POC nuevo A3 verify clean contra `Response.json({ ...result, correlationId })` pattern.
- **D5 PATCH/POST mismatch defer POC futuro paridad fix sale + purchase paired**: sale `sales/[saleId]/status/route.ts:10` POST vs client `sale-form.tsx:344,454` PATCH (handlePost + handleVoid) + purchase `purchases/[purchaseId]/status/route.ts:15` POST vs client `purchase-form.tsx:564,590` PATCH (handlePost + handleVoid) mismatches paired. Documentation A3-D3 sub-fase doc-only post-mortem ✅ §21.1 (line refs sale corregidos `:15`→`:10` retroactivo §13.A3-D3-α drift cumulative arithmetic engram chain — 3ra evidencia paired sister §13.A3-C7-γ + §13.A3-C8-δ engram-only). Cross-ref §21 + engram `poc-futuro/d5-patch-post-mismatch-paridad-fix` POC futuro candidate.

### 20.6. Verify retroactivo POC nuevo A5

POC nuevo dedicado A5 "cleanup `features/voucher-types/` load-bearing + cumulative cutover hex modules/voucher-types/ presentation cross-feature/cross-module" verify retroactivo §20.1 pattern grep ejecutado pre-cierre A5-D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 5ta paired sister runtime verify pre-cementación):

- **Routes nuevas added POC nuevo A5**: 0 — POC nuevo A5 fue cumulative cutover (composition root `makeVoucherTypeRepository` factory + DTO mappers + DELETE legacy class instantiations features/voucher-types/), NO surface API growth.
- **Routes editadas POC nuevo A5**: 9 source pages/routes A5-C1 (Cat 1 cutover voucher-types hex composition root) + 5 archivos α/β/γ A5-C2a/C2b cross-feature/cross-module cleanup hex + 3 archivos uniform Shape α A5-C2c integration tests cleanup. 0 routes añaden `correlationId` al body — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones se limitaron a (a) replace legacy `VoucherTypesService` instantiations con composition root `makeVoucherTypeRepository(prisma)` factory + (b) replace import `@/features/voucher-types/server` con `@/modules/voucher-types/presentation/composition-root` + (c) signature swaps method-on-class shim Option D-3 (dynamic import bypass R2 + Parameters introspection bypass R5) + (d) DTO divergence runtime path coverage 8 callsites Opción C 4 representative patterns hex VoucherType entity con VOs vs legacy string+enum + (e) Response.json raw entity preserved (NO mapper invocation routes mutation per consumer behavior).
- **Convención §20 preservada**: leak shape NO regressed. 10 commits cumulative POC nuevo A5 verify clean contra `Response.json({ ...result, correlationId })` pattern.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~10 features remaining post-A5: IVA books / payables / receivables / journal entries / fiscal periods / accounts / contacts / dispatches / shipments / wage / etc.) DEBE auditarse contra leak shape pre-merge. Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts).

### 20.7. Verify retroactivo POC paired payables↔receivables

POC paired payables↔receivables "cleanup `features/{payables,receivables}/` load-bearing + cumulative cutover hex modules/{payables,receivables}/ presentation cross-feature/cross-module paired sister axis" verify retroactivo §20.1 pattern grep ejecutado pre-cierre C8 doc-only post-mortem (PROACTIVE Marco mandate lección #14 6ta paired sister runtime verify pre-cementación):

- **Routes nuevas added POC paired**: 0 — POC paired fue cumulative cutover paired sister axis (composition root `make{Payables,Receivables}Service` factories + DTO mappers paired + DELETE legacy class instantiations features/{payables,receivables}/ wholesale + barrel sub-import migration prerequisite cutover schemas zod paired), NO surface API growth.
- **Routes editadas POC paired**: 6 source API routes paired (3 cxp + 3 cxc — `cxp/route.ts` + `cxp/[payableId]/route.ts` + `cxp/[payableId]/status/route.ts` + cxc mirror simétrico) C3-C4 cutover hex composition root + 4 page-rbac vi.mock factory paired C7-pre cleanup dead aspirational + 4 components consumer C5-C6 swap legacy import → hex barrel type-only erased at compile + 6 schemas zod barrel sub-imports cutover paired C7-pre prerequisite. 0 routes añaden `correlationId` al body — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones se limitaron a (a) replace legacy `PayablesService`/`ReceivablesService` instantiations con composition root `make{Payables,Receivables}Service()` factories paired + (b) replace import `@/features/{payables,receivables}/server` con `@/modules/{payables,receivables}/presentation/server` hex barrel type-only erased at compile (server-only safe banServerBarrels rule scope `@/features/*/server` only) + (c) signature swaps bridge mapper simplification `.toSnapshot()` Opción C precedent A5-C1 4ta aplicación post-cementación cumulative drop Decimal reconstruction overhead + (d) DTO divergence paired axis hex `{X}SnapshotWithContact = {X}Snapshot & { contact: Contact }` composition shape Snapshot+Contact hex DTO replacement legacy POJO `{X}WithContact` drop wholesale §13.B-paired NEW classification 'DTO drop axis paired' + (e) Response.json raw entity hex preserved (NO mapper invocation routes mutation per consumer behavior `{payable,receivable}-form.tsx` mutation handlers NO consume body solo `response.ok`).
- **Convención §20 preservada**: leak shape NO regressed. 15 commits cumulative POC paired verify clean contra `Response.json({ ...result, correlationId })` pattern.

### 20.8. Verify retroactivo POC nuevo dedicado payment

POC nuevo dedicado payment "cleanup `features/payment/` load-bearing + cumulative cutover hex modules/payment/ presentation cross-feature/cross-module" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC payment D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 12ma paired sister runtime verify pre-cementación):

- **Routes nuevas added POC nuevo payment**: 0 — POC nuevo payment fue cumulative cutover (cross-feature/cross-module presentation cutover routes + pages + vi.mock + DTO mapper centralizado + drop type axis hex local DTO + Adapter Layer reader port composition-root chain + wholesale delete features/payment/* atomic + cross-feature TYPE swap LOCAL DTO Path a), NO surface API growth.
- **Routes editadas POC nuevo payment**: 5 source API routes payment (`payments/route.ts` GET+POST + `payments/[paymentId]/route.ts` GET+PATCH+DELETE + `payments/[paymentId]/status/route.ts` PATCH + `payments/[paymentId]/allocations/route.ts` PUT + `payments/apply-credits/route.ts` POST) C1 cutover hex barrel + 2 source pages payment (`payments/page.tsx` + `payments/[paymentId]/page.tsx`) C1 cutover hex barrel + 1 source unapplied-payments route (`contacts/[contactId]/unapplied-payments/route.ts`) C1 cutover PaymentRepository → PrismaPaymentsRepository hex barrel + 2 client components consumer C4-β swap legacy import → hex barrel type-only erased at compile (`payment-list.tsx` + `payment-form.tsx`) + 4 vi.mock factory page-rbac C1 swap target hex barrel paired Opción A re-export class identity preserved §13.A4-η MATERIAL. **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones POC payment se limitaron a (a) replace import path `@/features/payment/server` con `@/modules/payment/presentation/server` hex barrel C1 + (b) cross-feature TYPE swap legacy `@/features/payment/payment.types` → LOCAL DTO `./dto/payment-input-types` (Adapter) + hex barrel `@/modules/payment/presentation/server` (components + Adapter PaymentFilters + CreditAllocationSource) C4-β + (c) `Response.json(payment)` envelope passthrough preserved heredado legacy shim contract pre-existing — NO route handler edit Response.json shape changed.
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC payment cycles. 12 commits cumulative POC nuevo payment verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**Sub-finding NEW emergente cementación target D1** — "WithCorrelation envelope passthrough pre-existing legacy shim pattern": durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, descubrió 4 payment routes pasan `WithCorrelation<PaymentWithRelations>` envelope from service directly via `Response.json(payment)` — `payment` shape internal `{ ...row, correlationId }` constructed por Adapter `paymentService.update/create/createAndPost/post/void/updateAllocations` returns. Routes: `payments/route.ts:61` POST + `payments/[paymentId]/route.ts:43` PATCH + `payments/[paymentId]/status/route.ts:40` PATCH + `payments/[paymentId]/allocations/route.ts:37` PUT. **POC payment cycles preserved legacy contract via Adapter C4-α NO new leak introduced** — Adapter wraps inner hex `PaymentsService` returns + spreads `correlationId` mismo shape legacy shim contract pre-existing (legacy `features/payment/payment.service.ts` ALSO returned WithCorrelation envelopes preserved EXACT). C1 GREEN solo SWAP IMPORT path (`@/features/payment/server` → `@/modules/payment/presentation/server`), NO Response.json behavior changed cycles.

**§20.1 EXACT regex pattern grep cumple ✅** (literal spread route handler shape detection): POC payment NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes. **Sub-finding distinción strict spirit §20**: regex pattern detecta literal spread route handler line, NO captura indirect leak via service-returned WithCorrelation envelope passthrough. Strict spirit §20 (correlationId es telemetría interna del backend, NO parte del contrato API público) incluye envelope passthrough — body de `Response.json(payment)` cuando `payment` es WithCorrelation envelope leakea correlationId indirect, mismo concern semantic que §20 strict spirit aunque NO matches §20.1 regex pattern.

**Cross-ref defer §21.2 POC futuro paridad fix payment routes raw entity preservation**: pattern emergent surface D1 verify retroactivo — payment routes deben unwrap WithCorrelation envelope pre-Response.json honor §20 strict spirit (NOT solo §20.1 literal regex). Mirror precedent A3 sale + paired payable EXACT routes raw entity preservation (`Response.json(rawEntity)`) — destructure `const { correlationId, ...rest } = await paymentService.X(...)` + `Response.json(rest)`. Defer POC futuro dedicated paridad fix simétrico al precedent A3 D5 PATCH/POST mismatch defer separate POC. NO scope creep retroactive mid-D1 doc-only (D1 es post-mortem cumulative cementación canonical, NO RED+GREEN cycle).

**Lección REFINED `feedback/§20-strict-spirit-vs-regex-literal` NEW canonical home**: strict spirit §20 incluye envelope passthrough (NO solo literal spread regex) — verificación PROACTIVE pre-D1 MANDATORY future POCs cuando service retorna WithCorrelation envelope shape. Forward-applicable cross-POC futuro: ~7 features remaining post-payment heredan strict spirit verify pre-D1 cementación POC closure runtime — si service retorna WithCorrelation envelope (indirect leak passthrough), surface honest D1 verify retroactivo + defer §21.X POC futuro dedicated paridad fix raw entity preservation.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~7 features remaining post-payment) DEBE auditarse contra leak shape pre-merge — strict spirit §20 (incluido envelope passthrough indirect leak) + §20.1 EXACT regex pattern literal spread direct leak. Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts).

### 20.9. Verify retroactivo POC nuevo dedicado mortality

POC nuevo dedicado mortality "cleanup `features/mortality/` load-bearing + cumulative cutover hex modules/mortality/ presentation cross-feature/cross-module Path simplificado v2 RSC boundary serialization adapter" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC mortality D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 13ma paired sister runtime verify pre-cementación):

- **Routes nuevas added POC nuevo mortality**: 0 — POC nuevo mortality fue cumulative cutover (cross-consumer cutover hex Path simplificado v2 entity-direct .toJSON() POJO server-side pre-RSC boundary adapter + wholesale delete features/mortality/* atomic), NO surface API growth.
- **Routes editadas POC nuevo mortality**: 2 source API routes mortality (`mortality/route.ts` GET+POST + `agent/route.ts` POST mortality confirm handler scope) C1 cutover hex barrel + 1 source page dashboard (`lots/[lotId]/page.tsx`) C1 cutover hex barrel makeMortalityService factory + .map(e => e.toJSON()) server-side pre-RSC boundary serialization adapter + 1 client component consumer (`lot-detail-client.tsx`) C1 swap legacy import → hex barrel type-only erased at compile + ReturnType<Mortality["toJSON"]>[] inferred type derivation Path simplificado v2 + 1 cross-feature consumer (`features/pricing/pricing.service.ts`) C1 cutover hex barrel makeMortalityService factory + ReturnType<typeof makeMortalityService> type alias MortalityServiceImpl DI annotation + 1 vi.mock factory page-rbac (`agent/__tests__/route.confirm-journal-entry.test.ts`) C1 swap target hex barrel + makeMortalityService factory mock shape (vs MortalityService class constructor mock pre-cutover). **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones POC mortality se limitaron a (a) replace import path `@/features/mortality(/server)?` con `@/modules/mortality/presentation/server` hex barrel C1 + (b) replace `new MortalityService()` legacy class instantiations con `makeMortalityService()` composition root factory C1 + (c) `.toJSON()` server-side adapter pre-RSC boundary serialization (page.tsx `.map(e => e.toJSON())` + agent/route.ts `data: log.toJSON()` + mortality/route.ts `logs.map(e => e.toJSON())` + `log.toJSON()` Response.json shape preserved POJO) — NO route handler edit Response.json shape changed legacy contract (operational domain, no envelope passthrough).
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC mortality cycles. 2 commits cumulative POC nuevo mortality verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.9 CLEAN — asimétrico vs §20.8 payment sub-finding NEW WithCorrelation envelope passthrough**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, mortality scope grep `correlationId|correlation_id|x-correlation` (modules/mortality/ + features/mortality/) returned 0 matches + grep `WithCorrelation|withCorrelation` (modules/mortality/) returned 0 matches + grep `logStructured` (modules/mortality/) returned 0 matches. **Mortality NO usa WithCorrelation envelope ni correlationId tracking ni logStructured** — operational domain (lot mortality logs), NO money domain. Asimétrico vs POC payment §20.8 sub-finding NEW WithCorrelation envelope passthrough indirect leak — mortality clean por nature operational domain (NO journal entry creation NO audit-tracked transactional state mutation requiring correlation tracking). NO sub-finding emergente §20.9 — verify retroactivo CLEAN single batch.

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean): POC mortality NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape (NO indirect passthrough leak). Strict spirit §20 honored single batch — operational domain pure, no audit telemetry leak vector cross-POC mortality.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~6 features remaining post-mortality) DEBE auditarse contra leak shape pre-merge — strict spirit §20 (incluido envelope passthrough indirect leak per §20.8 payment sub-finding NEW canonical home `feedback/§20-strict-spirit-vs-regex-literal`) + §20.1 EXACT regex pattern literal spread direct leak. Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts).

### 20.10. Verify retroactivo POC nuevo dedicado contacts

POC nuevo dedicado contacts "cleanup `features/contacts/` load-bearing + cumulative cutover hex modules/contacts/ presentation cross-feature/cross-module + Balance methods cross-module hex modules/contact-balances/ presentation Path simplificado v2 RSC boundary serialization adapter via `.toSnapshot()` POJO server-side pre-boundary" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC contacts D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 14ma paired sister runtime verify pre-cementación):

- **Routes nuevas added POC nuevo contacts**: 0 — POC nuevo contacts fue cumulative cutover (cross-feature/cross-module presentation cutover routes + pages + vi.mock + adapter cross-module accounting infrastructure + Balance methods cross-module hex contact-balances + page.tsx callsite + page.test.ts vi.mock SHAPE + wholesale delete features/contacts/* atomic + 5 components TYPE swap hex barrel), NO surface API growth.
- **Routes editadas POC nuevo contacts**: 2 source API routes contacts (`contacts/route.ts` GET+POST + `contacts/[contactId]/route.ts` GET+PATCH+DELETE) C4 cutover hex barrel + 3 source routes Balance methods cross-module (`contacts/[contactId]/balance/route.ts` + `credit-balance/route.ts` + `pending-documents/route.ts`) C3 cutover legacy shim ContactsService → hex factory makeContactBalancesService cross-module + 2 source pages contacts (`accounting/contacts/page.tsx` + `accounting/contacts/[contactId]/page.tsx`) C5+C4 cutover hex factory + cross-module makeContactBalancesService getBalanceSummary mirror C5 precedent Path simplificado v2 entity-direct .toSnapshot() POJO server-side pre-RSC boundary + 5 client components consumer C4 TYPE swap legacy import → hex barrel type-only erased at compile (`contact-detail.tsx` + `contact-filters.tsx` + `contact-form.tsx` + `contact-list.tsx` + `contact-selector.tsx`) + 10 vi.mock factory page-rbac C4-ter swap class identity → factory pattern hex MATERIAL §13.A4-η (sales/new + sales/[saleId] + payments/page.test + payments/[paymentId] + payments/new + accounting/contacts/[contactId] + purchases/[purchaseId] + purchases/new + dispatches/[dispatchId] + dispatches/new) + 4 features/* VALUE consumers C4-bis factory swap (`journal.service.ts` + `find-contact.ts` + `parse-operation.ts` + `dispatch.service.ts`) + 2 ai-agent tests TYPE swap Sub-B inline `ReturnType<typeof makeContactsService>` cast-silenced (`tools.find-contact.test.ts` + `tools.parse-operation.test.ts`) + 1 cross-module accounting infrastructure adapter C2 cutover (`contacts-read.adapter.ts` ContactsReadAdapter dependency hex factory makeContactsService). **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones POC contacts se limitaron a (a) replace import path `@/features/contacts/server` con `@/modules/contacts/presentation/server` hex barrel C1 + (b) cross-feature TYPE swap legacy `@/features/contacts/contacts.types` + `@/features/contacts` → hex barrel `@/modules/contacts/presentation/index` (5 components + 2 ai-agent tests + 4 features/* + 12 production app/.../*) C4 + (c) Balance methods cross-module path swap `@/modules/contacts/presentation/server` → `@/modules/contact-balances/presentation/server` C3+C5 + (d) wholesale delete features/contacts/* atomic single batch + DROP línea 13 modules/contacts/presentation/server.ts Opción A re-export C4 + (e) `Response.json(contact)` / `Response.json({ contacts })` / `Response.json(balance)` shape preserved heredado legacy operational read-mostly contract pre-existing — NO route handler edit Response.json shape changed.
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC contacts cycles. 17 commits cumulative POC nuevo contacts verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.10 CLEAN — asimétrico vs §20.8 payment sub-finding NEW WithCorrelation envelope passthrough**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, contacts scope grep `correlationId|correlation_id|x-correlation` (modules/contacts/ + modules/contact-balances/ + features/contacts/) returned 0 matches + grep `WithCorrelation|withCorrelation` (modules/contacts/ + modules/contact-balances/) returned 0 matches + grep `logStructured` (modules/contacts/ + modules/contact-balances/) returned 0 matches. **Contacts NO usa WithCorrelation envelope ni correlationId tracking ni logStructured** — operational read-mostly domain (contact CRUD + Balance read methods cross-module hex contact-balances), NO money domain transactional state mutation requiring correlation tracking. Asimétrico vs POC payment §20.8 sub-finding NEW WithCorrelation envelope passthrough indirect leak — contacts clean por nature operational read-mostly domain (NO journal entry creation NO audit-tracked transactional state mutation requiring correlation tracking). Mirror mortality §20.9 CLEAN precedent EXACT (operational domain check) — paired sister symmetric clean cross-POC. NO sub-finding emergente §20.10 — verify retroactivo CLEAN single batch.

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean): POC contacts NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape (NO indirect passthrough leak). Strict spirit §20 honored cumulative — operational read-mostly domain pure, no audit telemetry leak vector cross-POC contacts.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~5 features remaining post-contacts: IVA books / journal entries / fiscal periods / accounts / dispatches / shipments / wage / etc.) DEBE auditarse contra leak shape pre-merge — strict spirit §20 (incluido envelope passthrough indirect leak per §20.8 payment sub-finding NEW canonical home `feedback/§20-strict-spirit-vs-regex-literal`) + §20.1 EXACT regex pattern literal spread direct leak. Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts).

### 20.11. Verify retroactivo POC nuevo dedicado fiscal-periods

POC nuevo dedicado fiscal-periods "cleanup `features/fiscal-periods/` load-bearing + cumulative cutover hex modules/fiscal-periods/ presentation cross-feature/cross-module Path simplificado v2 RSC boundary serialization adapter via `.toSnapshot()` POJO server-side pre-boundary" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC fiscal-periods D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 15ma paired sister runtime verify pre-cementación):

- **Routes nuevas added POC nuevo fiscal-periods**: 0 — POC nuevo fiscal-periods fue cumulative cutover (cross-feature/cross-module presentation cutover routes + pages + vi.mock + adapter + page.tsx callsite + page.test.ts vi.mock SHAPE + wholesale delete features/fiscal-periods/* atomic + components TYPE swap hex barrel), NO surface API growth.
- **Routes editadas POC nuevo fiscal-periods**: 3 source API routes fiscal-periods C4 cutover hex barrel + 17 source pages cumulative cross-feature/cross-module C4 cutover hex factory makeFiscalPeriodsService() + .toSnapshot() bridge per Categ A/A'/B + 1 component period-list.tsx C6 swap legacy isomorphic barrel `@/features/fiscal-periods` → hex isomorphic barrel `@/modules/fiscal-periods/presentation/index` Opción ε identifier-preserving alias `FiscalPeriodSnapshot as FiscalPeriod` + 11 vi.mock factory page-rbac C-bis swap class identity → factory pattern hex MATERIAL §13.A4-η + 21 test files Sub-B mock target swap + Sub-D mock data entity-shape paired C4 + 3 cross-module adapters C3 cutover (FiscalPeriodsReadAdapter dependency hex factory makeFiscalPeriodsService) + 2 components isomorphic barrel C2 swap + 3 cross-feature services C1-α path swap class hex barrel atomic Opción A re-export bridge mecánico puro. **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones POC fiscal-periods se limitaron a (a) replace import path `@/features/fiscal-periods/server` con `@/modules/fiscal-periods/presentation/server` hex barrel C1-α + (b) cross-feature class→factory swap legacy `FiscalPeriodsService` instantiations con `makeFiscalPeriodsService()` composition root factory C-bis through C7 + (c) `.toSnapshot()` server-side adapter pre-RSC boundary serialization (C3 + C4 + C6 cumulative) + (d) Opción ε identifier-preserving alias barrel re-export pattern C6 (`export type FiscalPeriodSnapshot as FiscalPeriod`) NEW canonical home pattern + (e) wholesale delete features/fiscal-periods/* atomic single batch (3 archivos: index.ts + fiscal-periods.types.ts + server.ts) + DROP línea 9 modules/fiscal-periods/presentation/server.ts Opción A re-export legacy shim C7 + (f) `Response.json(period)` / `Response.json({ periods })` shape preserved heredado legacy operational read-mostly contract pre-existing — NO route handler edit Response.json shape changed.
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC fiscal-periods cycles. 14 commits cumulative POC nuevo fiscal-periods verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.11 CLEAN — asimétrico vs §20.8 payment sub-finding NEW WithCorrelation envelope passthrough**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, fiscal-periods scope grep `correlationId|correlation_id|x-correlation` (modules/fiscal-periods/ + features/fiscal-periods/) returned 0 matches + grep `WithCorrelation|withCorrelation` (modules/fiscal-periods/) returned 0 matches + grep `logStructured` (modules/fiscal-periods/) returned 0 matches. **Fiscal-periods NO usa WithCorrelation envelope ni correlationId tracking ni logStructured** — operational read-mostly domain (fiscal-periods CRUD + open/close period state mutation), NO money domain transactional state mutation requiring correlation tracking. Asimétrico vs POC payment §20.8 sub-finding NEW WithCorrelation envelope passthrough indirect leak — fiscal-periods clean por nature operational read-mostly domain. Mirror mortality §20.9 + contacts §20.10 CLEAN precedent EXACT (operational domain check) — paired sister symmetric clean cross-POC cumulative. NO sub-finding emergente §20.11 — verify retroactivo CLEAN single batch.

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean): POC fiscal-periods NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape (NO indirect passthrough leak). Strict spirit §20 honored cumulative — operational read-mostly domain pure, no audit telemetry leak vector cross-POC fiscal-periods.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~4 features remaining post-fiscal-periods: IVA books / journal entries / accounts / dispatches / shipments / wage / etc.) DEBE auditarse contra leak shape pre-merge — strict spirit §20 (incluido envelope passthrough indirect leak per §20.8 payment sub-finding NEW canonical home `feedback/§20-strict-spirit-vs-regex-literal`) + §20.1 EXACT regex pattern literal spread direct leak. Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts).

### 20.12. Verify retroactivo POC nuevo dedicado monthly-close

POC nuevo dedicado monthly-close "cleanup `features/monthly-close/` load-bearing + cumulative build hex modules/monthly-close/ presentation cross-feature/cross-module + cross-module §17 carve-out FiscalPeriodReader + AccountingReader + DraftDocumentsReader + PeriodLockingWriter + UoW assemble 4 adapters tx-bound dentro `withAuditTx` callback timeout 30_000" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC monthly-close D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 16ma paired sister runtime verify pre-cementación):

- **Routes nuevas added POC nuevo monthly-close**: 0 — POC nuevo monthly-close fue cumulative build hex (skeleton presentation barrels + domain ports + application ports + service class + infrastructure adapters Prisma + UoW + composition root factory + integration tests + cutover routes + summary route + wholesale delete features/monthly-close/* atomic), NO surface API growth.
- **Routes editadas POC nuevo monthly-close**: 2 source API routes monthly-close (`monthly-close/route.ts` POST close + `monthly-close/summary/route.ts` GET summary) C6-a + C6-b cutover hex factory `makeMonthlyCloseService()` consume `close()` + `getSummary()` + 1 source API route monthly-close audit-trail (`monthly-close/audit-trail/route.ts` GET) NO edit (preserved pre-existing legacy contract — consume correlationId como input search param para audit query legitimate read direction NO output envelope passthrough leak asymmetric direction). **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones POC monthly-close se limitaron a (a) replace import path `@/features/monthly-close/server` con `@/modules/monthly-close/presentation/server` hex barrel C6-a + (b) factory `makeMonthlyCloseService()` zero-arg consume `close()` signature object→positional 4 args atomic C6-a + (c) factory `makeMonthlyCloseService()` consume `getSummary()` cementado C2.5 paridad EXACT pre-existing signature alignment C6-b + (d) wholesale delete features/monthly-close/* atomic 17 archivos single batch NO bridge DROP hex barrel autónomo C7 + (e) `Response.json({ ...body, correlationId })` shape preserved heredado paridad legacy contract pre-existing TX-internal `withAuditTx` 4 invariantes (correlationId pre-tx + SET LOCAL inside + fn invoke + return shape inherited) NO leak presentation/route layer — NO route handler edit Response.json shape changed.
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC monthly-close cycles. 22 commits cumulative POC nuevo monthly-close verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.12 CLEAN — asimétrico vs §20.8 payment sub-finding NEW WithCorrelation envelope passthrough + asimétrico vs §20.11 fiscal-periods CLEAN operational read-mostly**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, monthly-close scope grep `correlationId|correlation_id|x-correlation` (modules/monthly-close/) returned 4 archivos / 8 hits TX-internal UoW pattern paridad legacy preserved (composition-root.ts JSDoc + `prisma-monthly-close-unit-of-work.ts` return shape `{ result, correlationId }` + monthly-close.service.ts JSDoc + spread post-`UoW.run`) — TX-internal NO leak presentation/route layer + grep `WithCorrelation|withCorrelation` (modules/monthly-close/) returned 0 matches + grep `logStructured` (modules/monthly-close/) returned 0 matches + grep `correlationId` PROD app/ paths returned 1 archivo `app/api/organizations/[orgSlug]/monthly-close/audit-trail/route.ts:14-24` consume correlationId como input search param + WHERE clause filter audit query (input direction NOT output direction — legitimate audit read query parámetro NO leak output envelope passthrough). **Monthly-close ES money domain transactional state mutation CON correlationId tracking TX-internal preservado paridad legacy** `withAuditTx` 4 invariantes (correlationId pre-tx + SET LOCAL inside + fn invoke + return shape inherited) — distinto de POC payment §20.8 sub-finding NEW WithCorrelation envelope passthrough output direction indirect leak (asimétrico direction — input vs output) + distinto de POC fiscal-periods §20.11 + contacts §20.10 + mortality §20.9 CLEAN operational read-mostly domain (monthly-close ES money domain transactional state mutation, NO operational read-mostly). NO sub-finding emergente §20.12 — verify retroactivo CLEAN single batch (audit-trail input-direction consumption legitimate read query NOT output-direction leak shape regression).

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean output direction): POC monthly-close NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape (NO indirect passthrough leak output direction) + audit-trail input-direction consumption NOT output-direction leak. Strict spirit §20 honored cumulative — money domain transactional state mutation CON correlationId tracking TX-internal preservado paridad legacy NO leak presentation/route layer + audit-trail legitimate read query parámetro consumption asymmetric direction.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~3 features remaining post-monthly-close: IVA books / journal entries / accounts / dispatches / shipments / wage / etc.) DEBE auditarse contra leak shape pre-merge — strict spirit §20 (incluido envelope passthrough indirect leak per §20.8 payment sub-finding NEW canonical home `feedback/§20-strict-spirit-vs-regex-literal`) + §20.1 EXACT regex pattern literal spread direct leak + asymmetric direction distinction (input vs output direction — input consumption legitimate audit query parámetro vs output leak envelope passthrough). Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts) — recursive aplicación pattern paired sister 14ma TSC -4 contacts D1 + 15ma TSC +4 fiscal-periods D1 + 16ma ESLint +3w monthly-close D1 honest documented.

- **Complementa**: §3 (reglas duras) — convención HTTP-layer specific, no reemplaza R1-R9; §8 (anti-patrones) — leak abstracción audit en API surface.

### 20.13. Verify retroactivo POC nuevo dedicado pagination-sale

POC nuevo dedicado pagination-sale "POC pilot pagination Sale Opción 2 ~6 ciclos initial → 3 ciclos refined Opción C ADDITIVE NEW método paralelo transitional dual-method `findAll+findPaginated` + `list+listPaginated` + adapter Prisma skip/take + count Promise.all parallel + service NEW método return generic VO `PaginatedResult<T>` + route Response.json wrap PaginatedResult<SaleSnapshot> shape + UI shadcn Pagination + URL search params sync RSC + page Next 16 RSC `searchParams: Promise<{...}>` + 2 D1 adapt direct-consumer-tests cascade — Path α single ciclo per stratum atomic batch cross-layer cutover ADDITIVE preservación legacy zero orphan period" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC pagination-sale D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 17ma paired sister runtime verify pre-cementación):

- **Routes nuevas added POC nuevo pagination-sale**: 0 — POC nuevo pagination-sale fue cumulative cutover ADDITIVE NEW método paralelo (port + adapter + service + route handler edit + UI page + sale-list component + 2 direct-consumer-tests cascade adapt), NO surface API growth.
- **Routes editadas POC nuevo pagination-sale**: 1 source API route sales (`app/api/organizations/[orgSlug]/sales/route.ts` GET) C1-MACRO cutover hex factory `makeSaleService()` consume `listPaginated()` paralelo a `list()` legacy ADDITIVE Response.json wrap PaginatedResult<SaleSnapshot> shape (page + pageSize + sortBy + sortOrder query params parse via parsePaginationParams URLSearchParams variant route helper canonical home) + 1 source page sales (`app/(dashboard)/[orgSlug]/sales/page.tsx`) C1-MACRO cutover Next 16 RSC `searchParams: Promise<{[key]: string|string[]|undefined}>` async prop verified textual `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md` — plain JS object NOT URLSearchParams instance, parsing via paginationQuerySchema directly NOT parsePaginationParams helper (URLSearchParams variant for route) + 1 client component sales (`components/sales/sale-list.tsx`) C1-MACRO cutover URL search params sync RSC server-side pagination state in URL via Link href + buildHref helper + drop useState client-side + shadcn Pagination component (Pagination/Content/Item/Link/Previous/Next/Ellipsis exports) + 2 D1 adapt direct-consumer-tests cascade (`page.test.ts` + `sale-list-date-format.test.tsx`) NEW emergente Step 0 expand cycle-resume cold cascade analysis incomplete superseded direct-consumer-tests emergente runtime + 1 NEW shadcn `components/ui/pagination.tsx` via `pnpm dlx shadcn@latest add pagination --yes` con `printf "n\n" | ...` decline overwrite existing button preserves dependencies + creates pagination.tsx 130 líneas componentes Pagination/Content/Item/Link/Previous/Next/Ellipsis exports. **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones POC pagination-sale se limitaron a (a) ADDITIVE NEW método paralelo `findPaginated`+`listPaginated` paralelo a `findAll`+`list` legacy preservados Opción C zero orphan period — `features/dispatch/hub.service.ts:141,151` axis-η Categ C++ DEFER recon #1782 preserved consume legacy `saleService.findAll()` + `saleService.list()` + (b) Prisma adapter `findPaginated` impl `Promise.all([findMany skip/take, count])` NOT `$transaction` (DbClient type Pick<PrismaClient,"sale"|"saleDetail"> NO incluye $transaction; read-only count+items sin tx isolation aceptable offset pagination expected behavior page-shift bajo writes) + (c) HTTP route Response.json wrap PaginatedResult<SaleSnapshot> shape NEW canonical (items + total + page + pageSize + totalPages) — NO route handler edit Response.json shape backward-incompatible (legacy `list()` legacy callers preserved zero orphan period via dual-method ADDITIVE).
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC pagination-sale cycles. 5 commits cumulative POC nuevo pagination-sale verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.13 CLEAN — asimétrico vs §20.12 monthly-close money-domain TX-internal correlationId tracking + asimétrico vs §20.8 payment WithCorrelation envelope passthrough output direction + symmetric clean vs §20.11 fiscal-periods + §20.10 contacts + §20.9 mortality CLEAN operational read-mostly**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, pagination-sale scope grep `correlationId|correlation_id|x-correlation` (modules/sale/ + modules/shared/) returned 0 matches + grep `WithCorrelation|withCorrelation` (modules/sale/ + modules/shared/) returned 0 matches + grep `logStructured` (modules/sale/ + modules/shared/) returned 0 matches. **Pagination-sale ES read-only operational pagination domain NOT money domain transactional state mutation requiring correlation tracking** (find/list paginated read query + offset pagination canonical home). Asimétrico vs POC monthly-close §20.12 money-domain transactional state mutation CON correlationId tracking TX-internal preservado paridad legacy `withAuditTx` 4 invariantes + asimétrico vs POC payment §20.8 sub-finding NEW WithCorrelation envelope passthrough indirect leak output direction — pagination-sale clean por nature read-only operational pagination domain (find/list paginated NO journal entry creation NO audit-tracked transactional state mutation requiring correlation tracking). Mirror fiscal-periods §20.11 + contacts §20.10 + mortality §20.9 CLEAN precedent EXACT (operational read-only domain check) — paired sister symmetric clean cross-POC. NO sub-finding emergente §20.13 — verify retroactivo CLEAN single batch.

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean output direction): POC pagination-sale NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape (NO indirect passthrough leak output direction). Strict spirit §20 honored cumulative — read-only operational pagination domain pure, no audit telemetry leak vector cross-POC pagination-sale.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~3 features remaining post-pagination-sale: IVA books / journal entries / accounts / dispatches / shipments / wage / etc.) DEBE auditarse contra leak shape pre-merge — strict spirit §20 (incluido envelope passthrough indirect leak per §20.8 payment sub-finding NEW canonical home `feedback/§20-strict-spirit-vs-regex-literal`) + §20.1 EXACT regex pattern literal spread direct leak + asymmetric direction distinction (input vs output direction — input consumption legitimate audit query parámetro vs output leak envelope passthrough per §20.12 monthly-close audit-trail variant). Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts) — recursive aplicación pattern paired sister 14ma TSC -4 contacts D1 + 15ma TSC +4 fiscal-periods D1 + 16ma ESLint +3w monthly-close D1 + 17ma EXACT preserved pagination-sale D1 honest documented.

- **Complementa**: §3 (reglas duras) — convención HTTP-layer specific, no reemplaza R1-R9; §8 (anti-patrones) — leak abstracción audit en API surface.

---

### 20.14. Verify retroactivo POC nuevo dedicado pagination-replication

POC nuevo dedicado pagination-replication "POC replication multi-módulo Purchase + Payment Opción C ADDITIVE NEW método paralelo transitional dual-method `findAll+findPaginated` + `list+listPaginated` heredado pagination-sale pilot canonical + adapter Prisma skip/take + count Promise.all parallel + service NEW método return generic VO `PaginatedResult<T>` + route Response.json wrap PaginatedResult<{Purchase,PaymentWithRelations}> shape + UI shadcn Pagination + URL search params sync RSC + page Next 16 RSC `searchParams: Promise<{...}>` + Path 2 PurchaseFilters interface extension `purchaseTypeIn` array unification axis-distinct + Payment reader port extension `findPaginatedWithRelations` + adapter wrap distintivo direct factory + 5 D1 adapt direct-consumer-tests cascade — Path α single ciclo per módulo atomic batch cross-layer cutover ADDITIVE preservación legacy zero orphan period" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC pagination-replication D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 18ma paired sister runtime verify pre-cementación):

- **Routes nuevas added POC nuevo pagination-replication**: 0 — POC nuevo pagination-replication fue cumulative cutover ADDITIVE NEW métodos paralelos (Purchase port + adapter + service + route handler edit + UI page + purchase-list component + 3 direct-consumer-tests cascade adapt + Payment port + adapter + service + route handler edit + UI page + payment-list component + adapter wrap + 2 direct-consumer-tests cascade adapt), NO surface API growth.
- **Routes editadas POC nuevo pagination-replication**: 2 source API routes (`app/api/organizations/[orgSlug]/purchases/route.ts` GET + `app/api/organizations/[orgSlug]/payments/route.ts` GET) C1-MACRO cutover hex factory `makePurchaseService()` + class instance `new PaymentService()` consume `listPaginated()` paralelo a `list()` legacy ADDITIVE Response.json wrap PaginatedResult<{Purchase,PaymentWithRelations}> shape (page + pageSize + sortBy + sortOrder query params parse via parsePaginationParams URLSearchParams variant route helper canonical home heredado pagination-sale pilot) + 2 source pages (`app/(dashboard)/[orgSlug]/purchases/page.tsx` + `app/(dashboard)/[orgSlug]/payments/page.tsx`) C1-MACRO cutover Next 16 RSC `searchParams: Promise<{[key]: string|string[]|undefined}>` async prop heredado pagination-sale pilot precedent EXACT + 2 client components (`components/purchases/purchase-list.tsx` + `components/payments/payment-list.tsx`) C1-MACRO cutover URL search params sync RSC server-side pagination state in URL via Link href + buildHref helper + drop useState client-side filters + shadcn Pagination component heredado pagination-sale pilot + 5 D1 adapt direct-consumer-tests cascade Purchase 3 (`page.test` + `purchase-list-date-format.test` + `purchase-list-unification.test`) + Payment 2 (`page.test` + `payment-list.test`) NEW emergente Step 0 expand cycle-start cold cumulative cross-POC pre-execute MANDATORY identified scope-truncado asymmetry per módulo tests-presence honest documented. **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones POC pagination-replication se limitaron a (a) ADDITIVE NEW método paralelo `findPaginated`+`listPaginated` paralelo a `findAll`+`list` legacy preservados Opción C zero orphan period — `features/dispatch/hub.service.ts` axis-η Categ C++ DEFER recon #1782 heredado pagination-sale preserved + (b) Prisma adapter `findPaginated` impl `Promise.all([findMany skip/take, count])` NOT `$transaction` (Purchase DbClient type Pick<PrismaClient,"purchase"|"purchaseDetail"> NO incluye $transaction; Payment DbClient type Pick<PrismaClient,"payment"|"paymentAllocation"|"accountsReceivable"|"$transaction"> HAS $transaction availability NO ejercida pagination read-only `arch/§13/payment-adapter-transaction-axis-distinct-not-used-by-pagination-read-only` 2da evidencia matures cumulative cross-POC; read-only count+items sin tx isolation aceptable offset pagination expected behavior page-shift bajo writes) + (c) HTTP route Response.json wrap PaginatedResult<{Purchase,PaymentWithRelations}> shape canonical (items + total + page + pageSize + totalPages) — NO route handler edit Response.json shape backward-incompatible (legacy `list()` legacy callers preserved zero orphan period via dual-method ADDITIVE) + (d) Payment adapter wrap distintivo `listPaginated` PaymentWithRelations cast vs Sale+Purchase symmetric direct factory — reader port extension `findPaginatedWithRelations` necesaria preserve hex encapsulation §13 vs adapter bypass `inner.list` usa `reader.findAllWithRelations` directo `arch/§13/reader-port-extension-find-paginated-with-relations` 1ra evidencia POC pagination-replication Payment.
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC pagination-replication cycles. 4 commits cumulative POC nuevo pagination-replication verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.14 CLEAN — asimétrico vs §20.12 monthly-close money-domain TX-internal correlationId tracking + asimétrico vs §20.8 payment WithCorrelation envelope passthrough output direction + symmetric clean vs §20.13 pagination-sale + §20.11 fiscal-periods + §20.10 contacts + §20.9 mortality CLEAN operational read-mostly**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, pagination-replication scope grep `correlationId|correlation_id|x-correlation` (modules/purchase/ + modules/payment/ + modules/shared/) returned 0 matches + grep `WithCorrelation|withCorrelation` (modules/purchase/ + modules/payment/ + modules/shared/) returned 0 matches + grep `logStructured` (modules/purchase/ + modules/payment/ + modules/shared/) returned 0 matches. **Pagination-replication ES read-only operational pagination domain NOT money domain transactional state mutation requiring correlation tracking** (find/list paginated read query + offset pagination canonical heredado pagination-sale pilot precedent EXACT). Asimétrico vs POC monthly-close §20.12 money-domain transactional state mutation CON correlationId tracking TX-internal preservado paridad legacy `withAuditTx` 4 invariantes + asimétrico vs POC payment §20.8 sub-finding NEW WithCorrelation envelope passthrough indirect leak output direction (POC pagination-replication Payment cycle 2/3 read-only pagination NO toca CRUD WithCorrelation envelope routes — POST/PATCH/PUT separate concerns scope-distinct §21.2 DEFER POC futuro paridad fix payment routes raw entity preservation) — pagination-replication clean por nature read-only operational pagination domain (find/list paginated NO journal entry creation NO audit-tracked transactional state mutation requiring correlation tracking). Mirror pagination-sale §20.13 + fiscal-periods §20.11 + contacts §20.10 + mortality §20.9 CLEAN precedent EXACT (operational read-only domain check) — paired sister symmetric clean cross-POC. NO sub-finding emergente §20.14 — verify retroactivo CLEAN single batch.

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean output direction): POC pagination-replication NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape (NO indirect passthrough leak output direction) — Payment cycle 2/3 GET route read-only pagination NO touches POST/PATCH/PUT WithCorrelation envelope routes pre-existing §21.2 DEFER POC futuro paridad fix scope-distinct. Strict spirit §20 honored cumulative — read-only operational pagination domain pure, no audit telemetry leak vector cross-POC pagination-replication.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~3 features remaining post-pagination-replication: HubService cleanup pending engram trigger condition + Contacts+Balances N+1 sub-POC + Journal legacy migration journal hex POC dedicado heredados pagination-sale precedent) DEBE auditarse contra leak shape pre-merge — strict spirit §20 (incluido envelope passthrough indirect leak per §20.8 payment sub-finding NEW canonical home `feedback/§20-strict-spirit-vs-regex-literal`) + §20.1 EXACT regex pattern literal spread direct leak + asymmetric direction distinction (input vs output direction — input consumption legitimate audit query parámetro vs output leak envelope passthrough per §20.12 monthly-close audit-trail variant). Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts) — recursive aplicación pattern paired sister 14ma TSC -4 contacts D1 + 15ma TSC +4 fiscal-periods D1 + 16ma ESLint +3w monthly-close D1 + 17ma EXACT preserved pagination-sale D1 + 18ma EXACT preserved pagination-replication D1 honest documented.

- **Complementa**: §3 (reglas duras) — convención HTTP-layer specific, no reemplaza R1-R9; §8 (anti-patrones) — leak abstracción audit en API surface.

---

### 20.15. Verify retroactivo POC paired farms+lots

POC paired farms+lots "POC paired greenfield migración hexagonal Farm + Lot domain entity-less greenfield C0/C1/C2/C3/C4/C5/C6/C7 paired sister payables/receivables canonical reference EXACT mirror precedent + cumulative cross-POC matures forward functioning — domain entities Farm + Lot + Snapshots + Repository ports + Service classes + composition roots `makeFarmService()`+`makeLotService()` + routes API cutover hex factory + UI pages cutover hex Q7A `{ lot, summary }` return shape + NEW infrastructure attacher `lots-attacher.ts` Prisma raw + NEW DTO presentation `FarmSnapshotWithLots` Prisma raw composition + cross-feature ports migration mortality+pricing+AI-agent consolidate canonical + wholesale delete `features/{farms,lots}/*` 12 archivos atomic + hotfix retroactivo pre-D1 runtime RSC boundary serialization fix LotSummary.toJSON() + expenses Decimal→Number page-level map + lot-detail-client.tsx prop types update + NEW type alias export LotSummaryShape + Path α single ciclo per stratum domain/application/infrastructure/presentation/cutover/wholesale-delete paired sister payables/receivables EXACT mirror" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC paired farms+lots D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 19na paired sister runtime verify pre-cementación):

- **Routes nuevas added POC paired farms+lots**: 0 — POC paired farms+lots fue paired greenfield migración hexagonal cumulative C0-C7 + wholesale delete features/{farms,lots}/* + hotfix retroactivo pre-D1 runtime RSC boundary serialization fix, NO surface API growth.
- **Routes editadas POC paired farms+lots**: 4 source API routes (`app/api/organizations/[orgSlug]/farms/route.ts` + `app/api/organizations/[orgSlug]/farms/[farmId]/route.ts` + `app/api/organizations/[orgSlug]/lots/route.ts` + `app/api/organizations/[orgSlug]/lots/[lotId]/route.ts`) C4 cutover hex factory `makeFarmService()`/`makeLotService()` consume domain entity methods paralelo a legacy class `new FarmsService()`/`new LotsService()` REPLACE (Q7A `{ lot, summary }` return shape getSummary hex confirmed) + 4 source pages (`app/(dashboard)/[orgSlug]/farms/page.tsx` + `app/(dashboard)/[orgSlug]/farms/[farmId]/page.tsx` + `app/(dashboard)/[orgSlug]/lots/[lotId]/page.tsx` + `app/(dashboard)/[orgSlug]/farms/farms-client.tsx` + `app/(dashboard)/[orgSlug]/farms/[farmId]/farm-detail-client.tsx` + `app/(dashboard)/[orgSlug]/lots/[lotId]/lot-detail-client.tsx`) C5 cutover hex factory + `.toSnapshot()` bridge per call site MANDATORY + consumer client type migration `FarmSnapshot` + `LotSnapshot` + `FarmSnapshotWithLots` + `LotSummary` import preservation paired sister C5 α22 cementación textual + hotfix retroactivo D1 ADDENDUM page.tsx + lot-detail-client.tsx prop types update `LotSummaryShape` NEW canonical type export pattern §13 invariant-collision-elevation-prefers-new-type-export-vs-modify-cemented-test-axis-distinct 1ra evidencia matures. **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths. Modificaciones POC paired farms+lots se limitaron a (a) C4 cutover hex factory `makeFarmService()`+`makeLotService()` REPLACE legacy class instantiations `new FarmsService()`+`new LotsService()` 4 callsites cross-routes (asymmetric-auth-pattern-legacy-preserve-vs-canonical-require-permission RBAC-EXCEPTION cross-module auth-only NO resource frozen Resource union Decision rbac-legacy-auth-chain-migration 2026-04-19 preserved) + (b) C5 cutover hex factory UI pages `.toSnapshot()` bridge per call site MANDATORY (consumer client type migration `FarmSnapshot` + `LotSnapshot` + `FarmSnapshotWithLots` + `LotSummary` import preservation paired sister C5 α22 cementación textual forward-only canonical heredado 8va matures) + (c) C6 cross-feature ports migration mortality+pricing+AI-agent consolidate canonical (port direction provider vs consumer axis-distinct 1ra evidencia + port name canonical collision MortalityLotInquiryPort vs LotInquiryPort axis-distinct 1ra evidencia cumulative cross-POC matures forward) + (d) C7 wholesale delete features/{farms,lots}/* 12 archivos atomic single batch (-433 LOC §13.A5-ζ wholesale 6ta heredada matures cumulative cross-POC) + (e) hotfix retroactivo pre-D1 ADDENDUM runtime RSC boundary serialization fix `LotSummary.toJSON()` method + `LotSummaryShape` NEW type alias export + page.tsx `summary.toJSON()` JSX call site + `expenses.map((e) => ({ ...e, amount: Number(e.amount) }))` page-level Decimal→Number conversion + lot-detail-client.tsx prop types update inline literal `LotSummaryShape` + `Omit<ExpenseWithRelations, "amount"> & { amount: number }` (paired sister hotfix-correctivo-contacts retroactive precedent EXACT mirror cumulative cross-POC 2da aplicación matures forward).
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC paired farms+lots cycles. 18 commits cumulative POC paired farms+lots verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.15 CLEAN — symmetric clean vs §20.14 pagination-replication + §20.13 pagination-sale + §20.11 fiscal-periods + §20.10 contacts + §20.9 mortality CLEAN operational read-mostly + asimétrico vs §20.12 monthly-close money-domain TX-internal correlationId tracking + asimétrico vs §20.8 payment WithCorrelation envelope passthrough output direction**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, POC paired farms+lots scope grep `correlationId|correlation_id|x-correlation` (modules/farm/ + modules/lot/) returned 0 matches + grep `WithCorrelation|withCorrelation` (modules/farm/ + modules/lot/) returned 0 matches + grep `logStructured` (modules/farm/ + modules/lot/) returned 0 matches. **Paired farms+lots ES operational livestock domain NOT money domain transactional state mutation requiring correlation tracking** (farm + lot + chicken count CRUD + expenses + mortality logs aggregate read + summary compute pure domain logic). Asimétrico vs POC monthly-close §20.12 money-domain transactional state mutation CON correlationId tracking TX-internal preservado paridad legacy `withAuditTx` 4 invariantes + asimétrico vs POC payment §20.8 sub-finding NEW WithCorrelation envelope passthrough indirect leak output direction (POC paired farms+lots NO toca payment routes WithCorrelation envelope `correlationId` pre-existing §21.2 DEFER POC futuro paridad fix scope-distinct) — paired farms+lots clean por nature operational livestock domain pure (NO journal entry creation NO audit-tracked transactional state mutation requiring correlation tracking). Mirror pagination-replication §20.14 + pagination-sale §20.13 + fiscal-periods §20.11 + contacts §20.10 + mortality §20.9 CLEAN precedent EXACT (operational read-mostly domain check) — paired sister symmetric clean cross-POC. NO sub-finding emergente §20.15 — verify retroactivo CLEAN single batch.

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean output direction): POC paired farms+lots NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape (NO indirect passthrough leak output direction) — paired farms+lots cycles C0-C7 + hotfix retroactivo D1 ADDENDUM NO touch WithCorrelation envelope routes pre-existing §21.2 DEFER POC futuro paridad fix scope-distinct. Strict spirit §20 honored cumulative — operational livestock domain pure, no audit telemetry leak vector cross-POC paired farms+lots.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes inmediatos (~3 features remaining post-paired farms+lots: HubService cleanup pending engram heredado pagination-sale `feedback/sale-find-all-legacy-vs-find-paginated-dual-method-cleanup-pending` + Contacts+Balances N+1 sub-POC + Journal legacy migration journal hex POC dedicado heredados pagination-sale pilot precedent) DEBE auditarse contra leak shape pre-merge — strict spirit §20 (incluido envelope passthrough indirect leak per §20.8 payment sub-finding NEW canonical home `feedback/§20-strict-spirit-vs-regex-literal`) + §20.1 EXACT regex pattern literal spread direct leak + asymmetric direction distinction (input vs output direction — input consumption legitimate audit query parámetro vs output leak envelope passthrough per §20.12 monthly-close audit-trail variant). Strip atómico mismo edit que toque la route. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts) — recursive aplicación pattern paired sister 14ma TSC -4 contacts D1 + 15ma TSC +4 fiscal-periods D1 + 16ma ESLint +3w monthly-close D1 + 17ma EXACT preserved pagination-sale D1 + 18ma EXACT preserved pagination-replication D1 + 19na EXACT preserved POC paired farms+lots D1 honest documented.

- **Complementa**: §3 (reglas duras) — convención HTTP-layer specific, no reemplaza R1-R9; §8 (anti-patrones) — leak abstracción audit en API surface.

---

### 20.16. Verify retroactivo POC #2 AI tools writing granjas

POC #2 "AI tools writing granjas frontend wire-up — modal greenfield chat-like simple conversational + state mgmt useState multi-state + backend integration wire-up + botón inject pages 2 contextos + props drilling contextHints + hotfix retroactivo bundle (contextHints injection + brevity + tool selection write-vs-read + UX requiresConfirmation discriminator + fechas relativas resolución Spanish Bolivia + temporal context Intl.DateTimeFormat es-BO + ambiguous date clarification)" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC #2 D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 20ma matures cumulative cross-POC runtime verify pre-cementación):

- **Routes nuevas added POC #2**: 0 — POC #2 fue frontend wire-up + backend AI agent hotfix retroactivo, NO surface API growth (preserved existing `app/api/organizations/[orgSlug]/agent/route.ts` POST handler heredado pre-POC).
- **Routes editadas POC #2**: 0 source API routes (`app/api/organizations/[orgSlug]/agent/route.ts` NO touched POC #2 cycles — contextHints propagation upstream via `agent.service.ts:70-85` executeChatMode call propagates contextHints arg + chat.ts buildSystemPrompt extend without route layer change). Modificaciones POC #2 se limitaron a (a) frontend modal greenfield + wire-up backend chat mode integration confirm card structured render + (b) botón inject pages 2 contextos reusable single SOT + props drilling contextHints + (c) hotfix retroactivo bundle MISMO D1 contextHints injection chat mode + 5 REGLAS directives + frontend discriminator requiresConfirmation flag — NO touch payment routes WithCorrelation envelope pre-existing §21.2 DEFER POC futuro paridad fix scope-distinct. **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths.
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC #2 cycles. 14 commits cumulative POC #2 verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.16 CLEAN — symmetric clean vs §20.15 paired farms+lots + §20.14 pagination-replication + §20.13 pagination-sale + §20.11 fiscal-periods + §20.10 contacts + §20.9 mortality CLEAN operational read-mostly + asimétrico vs §20.12 monthly-close money-domain TX-internal correlationId tracking + asimétrico vs §20.8 payment WithCorrelation envelope passthrough output direction**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, POC #2 scope grep `correlationId|correlation_id|x-correlation` (features/ai-agent/ + components/agent/) returned 0 matches + grep `WithCorrelation|withCorrelation` returned 0 matches. **AI tools writing granjas ES frontend wire-up + AI agent chat mode integration NOT money domain transactional state mutation requiring correlation tracking** (createExpense + logMortality suggestions LLM-generated → confirm flow → existing route handler invokes write services preserved pre-POC). Mirror paired farms+lots §20.15 + pagination-replication §20.14 + pagination-sale §20.13 + fiscal-periods §20.11 + contacts §20.10 + mortality §20.9 CLEAN precedent EXACT (operational read-mostly domain check + frontend wire-up cumulative cross-POC). NO sub-finding emergente §20.16 — verify retroactivo CLEAN single batch.

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean output direction): POC #2 NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape — POC #2 cycles Pre-C0+C0+C1+C2 + hotfix retroactivo bundle NO touch WithCorrelation envelope routes pre-existing §21.2 DEFER POC futuro paridad fix scope-distinct. Strict spirit §20 honored cumulative — frontend wire-up + AI agent chat mode integration domain pure, no audit telemetry leak vector cross-POC #2.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes (~3 features remaining post-POC #2 + heredados §21.10-§21.13 POC #2 cleanup pending: multi-turn conversation history retrieval+inject LLM + anti-hallucination directive + category inference Spanish keywords mapping + per-tool consistency directives logMortality vs createExpense) DEBE auditarse contra leak shape pre-merge. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts) — recursive aplicación pattern paired sister 14ma TSC -4 contacts D1 + 15ma TSC +4 fiscal-periods D1 + 16ma ESLint +3w monthly-close D1 + 17ma EXACT preserved pagination-sale D1 + 18ma EXACT preserved pagination-replication D1 + 19na EXACT preserved POC paired farms+lots D1 + 20ma EXACT preserved POC #2 D1 honest documented.

- **Complementa**: §3 (reglas duras) — convención HTTP-layer specific, no reemplaza R1-R9; §8 (anti-patrones) — leak abstracción audit en API surface.

### 20.17. Verify retroactivo POC #1 UX accordion granjero mayor

POC #1 "UX accordion granjero mayor — farm-detail-client.tsx Accordion lotes expandable 3 granja-header + 4 métricas collapsed per-lote + AccordionContent expanded 3 botones + 10 expenses + 5 mortality + Ver más route preservation + responsive Tailwind breakpoints mobile-first stack + hotfix retroactivo C1h layout full-width edge-to-edge paired sister convention restored + hotfix retroactivo C2h remove header farm-level AI button single source of truth contextHints per-lote scope única paired Opt D SPLIT POC #2 cementado test α29 deleted" verify retroactivo §20.1 pattern grep ejecutado pre-cierre POC #1 D1 doc-only post-mortem (PROACTIVE Marco mandate lección #14 21ma matures cumulative cross-POC runtime verify pre-cementación):

- **Routes nuevas added POC #1**: 0 — POC #1 fue frontend UX accordion lotes + responsive breakpoints + 2 hotfix retroactivo bundle, NO surface API growth (preserved existing `app/api/organizations/[orgSlug]/agent/route.ts` POST handler heredado POC #2 cumulative + preserved existing routes farms/lots heredado paired farms+lots cumulative).
- **Routes editadas POC #1**: 0 source API routes. Modificaciones POC #1 se limitaron a (a) `app/(dashboard)/[orgSlug]/farms/[farmId]/page.tsx` server-slice Promise.all per-lote summary + recentExpenses 10 + recentMortality 5 + farmMetrics current-month aggregation + (b) `app/(dashboard)/[orgSlug]/farms/[farmId]/farm-detail-client.tsx` Accordion render greenfield 3 granja-header + 4 métricas collapsed per-lote + AccordionContent expanded 3 botones + 10 expenses + 5 mortality + Ver más + responsive breakpoints hotfix C2 + C1h layout convention + C2h remove header AI button + (c) `components/agent/__tests__/c2-presentation-shape.poc-ai-tools-writing-granjas.test.ts` paired Opt D SPLIT α29 deleted + RENDER_BOTON_FARM_RE removed unused per-lote scope única preserved. **0 routes añaden `correlationId` al body literal spread** — verify runtime grep `Response\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths + grep `NextResponse\.json\(\s*\{\s*\.\.\.[^,]+,\s*correlationId` returned 0 matches PROD app/ paths.
- **Convención §20.1 EXACT regex pattern preservada**: leak shape NO regressed por POC #1 cycles. 12 commits cumulative POC #1 verify clean contra `Response.json({ ...result, correlationId })` pattern literal spread.

**§20.17 CLEAN — symmetric clean vs §20.16 POC #2 + §20.15 paired farms+lots + §20.14 pagination-replication + §20.13 pagination-sale + §20.11 fiscal-periods + §20.10 contacts + §20.9 mortality CLEAN operational read-mostly + asimétrico vs §20.12 monthly-close money-domain TX-internal correlationId tracking + asimétrico vs §20.8 payment WithCorrelation envelope passthrough output direction**: durante D1 PROACTIVE verify retroactivo §20.1 pattern grep ejecución, POC #1 scope grep `correlationId|correlation_id|x-correlation` (app/(dashboard)/[orgSlug]/farms/[farmId]/ + components/farms/ + components/agent/__tests__/) returned 0 matches + grep `WithCorrelation|withCorrelation` returned 0 matches. **UX accordion granjero ES frontend UX domain pure NOT money domain transactional state mutation requiring correlation tracking** (Accordion render lotes + per-lote AI button + manual gasto + manual mortalidad → existing route handlers heredado POC #2 + paired farms+lots cumulative). Mirror POC #2 §20.16 + paired farms+lots §20.15 + pagination-replication §20.14 + pagination-sale §20.13 + fiscal-periods §20.11 + contacts §20.10 + mortality §20.9 CLEAN precedent EXACT (operational read-mostly domain check + frontend UX cumulative cross-POC). NO sub-finding emergente §20.17 — verify retroactivo CLEAN single batch.

**§20.1 EXACT regex pattern grep cumple ✅ + strict spirit §20 cumple ✅** (literal spread + envelope passthrough both clean output direction): POC #1 NO introduces literal `{ ...x, correlationId }` spread leaks por cycle changes + NO retorna WithCorrelation envelope shape — POC #1 cycles Pre-C0+C0+C1+C1h+C2+C2h NO touch WithCorrelation envelope routes pre-existing §21.2 DEFER POC futuro paridad fix scope-distinct. Strict spirit §20 honored cumulative — frontend UX accordion granjero mayor domain pure, no audit telemetry leak vector cross-POC #1.

Pattern application coherente §20.1 forward: cualquier hex route futura POCs siguientes (~3 features remaining post-POC #1 + heredados §21.10-§21.13 POC #2 cleanup pending + heredados §21.14-§21.18 POC #1 cleanup pending NEW) DEBE auditarse contra leak shape pre-merge. Lección #14 runtime verify pre-cementación architecture.md aplicable cumulative arithmetic claims POC future closures (Suite/TSC/ESLint/file counts) — recursive aplicación pattern paired sister 14ma TSC -4 contacts D1 + 15ma TSC +4 fiscal-periods D1 + 16ma ESLint +3w monthly-close D1 + 17ma EXACT preserved pagination-sale D1 + 18ma EXACT preserved pagination-replication D1 + 19na EXACT preserved POC paired farms+lots D1 + 20ma EXACT preserved POC #2 D1 + 21ma EXACT preserved POC #1 D1 honest documented.

- **Complementa**: §3 (reglas duras) — convención HTTP-layer specific, no reemplaza R1-R9; §8 (anti-patrones) — leak abstracción audit en API surface.

---

