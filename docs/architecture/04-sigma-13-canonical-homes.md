# 04. §13 Canonical Homes — Engram Cementación Retroactiva

> **Cementación**: POC docs-refactor — drift acknowledgment retroactivo.
> **Honest correction cumulative cross-POC matures heredado**:
> Engram catálogo cumulative cross-POC matures menciona `§13.UX`, `§13.UI`, `§13.baseline`, `§13.DOCS` como canonical homes — pero §13 doc original (`architecture.md.deprecated`) tenía título "Auditoría retroactiva de POCs" SIN subsecciones textuales `### §13.X`.
> Esta drift detectada en POC docs-refactor Step 0 coherence gate. Resolución: §13.X engram canonical homes cementación retroactiva NEW aquí (Opt C hybrid lock D-DOCS-7).
> §13 doc original (51 LOC "Auditoría retroactiva POCs") migrated → [07-poc-history.md](07-poc-history.md).

## Aplicación recursiva textual-rule-verification 28ma matures

Esta drift es 4ta evidencia cumulative cross-POC matures de `evidence-supersedes-assumption-lock` heredado cumulative cross-POC recursive aplicación:
- 50ma: tail-N truncation masks footer summary errors
- 51ma: recursive aplicación textual rule verification
- 52ma: desktop-layout drift visual material
- 53ma: D2 drift bookmark assumption
- 55ma: docs size claim ">5000 LOC" SUPERSEDED
- 56ma: R1-R8 naming SUPERSEDED por R1-R9 textual existence

## §13.UX — Canonical Homes UX (paired sister POC #1 cumulative)

### §13.UX scoped-AI-button-per-context-vs-global-sidebar-bookmark-assumption-superseded-axis-distinct
- **1ra evidencia**: POC #1 D-POC-1
- **Source**: Engram observation cumulative cross-POC matures
- **Decisión**: scoped AI button per-context (`RegistrarConIABoton` inject pages farm/lot/lot-accordion-expanded 2-3 contextos) vs global sidebar AgentChat pattern axis-distinct
- **Cementado**: POC #2 scoped per-context (granjero mayor UX simplicidad mobile-friendly)

### §13.UX granja-header-3-metricas-globales-big-picture-vs-minimal-axis-distinct
- **1ra evidencia**: POC #1 D-POC-1-GRANJA-HEADER-METRICS
- **Decisión**: 3 cards arriba accordion lotes mobile-friendly (pollos totales suma `aliveCount` lots + ...) vs minimal nombre-only
- **Opt B aprobado**: 3 métricas globales context awareness pre-accordion deep-dive

### §13.UX single-source-of-truth context-hints per-lote vs farm-level redundant axis-distinct
- **1ra evidencia**: POC #1 hotfix C2h
- **Decisión**: contextHints AI agent prompt construction single source of truth per-action (per-lote scope NO farm-level redundant)
- **Cementado**: POC #1 hotfix C2h Opt D SPLIT POC #2 test α29 deleted paired single batch atomic

### §13.UX button-context-ambiguity farm-level vs lote-level axis-distinct
- **1ra evidencia**: POC #1 hotfix C2h
- **Decisión**: Marco visual intuition runtime smoke catch UX context ambiguity latente que tests unitarios shape regex Opt A NO detectan
- **Cementado**: invariant collision elevation 8va matures cumulative cross-POC pattern matures forward

## §13.UI/PATTERN — Canonical Homes UI/Pattern (paired sister POC #1 cumulative)

### §13.UI shadcn-wrapper-component-install-canonical-pattern-vs-custom-build-axis-distinct
- **1ra evidencia**: POC #1 D-POC-1-NEW-ACCORDION-WRAPPER
- **Decisión**: `npx shadcn add <component>` wrapper @radix-ui/react-* primitives vs custom build axis-distinct
- **Opt A aprobado**: shadcn add accordion → `components/ui/accordion.tsx`

### §13.UI/PATTERN max-width centered SUPERSEDED full-width edge-to-edge paired sister convention
- **Codebase verified evidence** (filesystem read 2026-05-11): `/farms` paired sister convention full-width edge-to-edge NO `max-w-*` NO `mx-auto` root wrapper. Vertical rhythm via `space-y-{n}` standalone.
- **Cementado**: POC #1 hotfix C1h retroactivo full-width edge-to-edge paired sister codebase convention restored

### §13.UI/PATTERN Pre-write paired sister codebase filesystem verify MANDATORY visual UX locks
- **Aplicación recursiva**: textual-rule-verification 27ma matures (visual UX subset). Lock conceptual aislado (sin codebase verify) = drift latent
- **Cementado**: POC #1 hotfix C1h evidence-supersedes-assumption-lock

### §13.UI/PATTERN Regex Opt A — type alias extraction + trailing comma atomic same-commit
- **Source**: POC #1 sensitivity gotchas regex Opt A shape verification readFileSync source pattern
- **2 gotchas**: (1) Type alias extraction inline `Omit<T, "k"> & { k: U }` inside Array/object type breaks `[^}]*` negated character class traversal; (2) Trailing comma atomic same-commit discipline mandatory

## §13.baseline — Canonical Homes Baseline (TSC/suite/lint verify)

### §13.baseline tsc-tail-N-truncation-masks-footer-summary-errors-full-output-verify-MANDATORY
- **Aplicación recursiva**: 2 evidencias POC #1 cumulative cross-POC matures
  - 1ra: Initial Step 0 baseline "TSC OK exit 0" tail-10 SUPERSEDED por full output 17 errors
  - 2da: cleanup pending 38mo modules/sale TSC drift 9→17 errors detected ortogonal POC #1
- **Cementado**: TSC + suite + lint pre-write baseline lock MANDATORY verify completo output NO `| tail -N` truncation

## §13.DOCS — Canonical Homes Docs (NEW 1ra evidencia POC docs-refactor)

### §13.DOCS naming-convention-forward-compatible-NO-sufijo-numerico-vs-rename-retroactivo-axis-distinct
- **1ra evidencia**: POC docs-refactor D-DOCS-8 lock
- **Decisión**: forward-compatible NO sufijo numérico (e.g. `03-rules-hard-rules.md`) vs rename retroactivo per POC nuevo que agregue Rn (e.g. `03-rules-R1-R9.md`)
- **Opt B aprobado**: forward-compat preserve future POCs add R10..Rn sin rename overhead cumulative cross-POC matures

## §13.DOMAIN/PORTS/APPLICATION/INFRASTRUCTURE/PRESENTATION — Canonical Homes POC expense hex (NEW 7 evidencias cumulative cross-POC closure)

### §13.DOMAIN R5-absoluta-zero-prisma-imports-const-array-pattern
- **1ra evidencia**: POC expense hex C0
- **Decisión**: domain MUST NOT import Prisma — enums Prisma↔domain via local `export const FOO_VALUES = [...] as const; export type Foo = typeof FOO_VALUES[number]` paired sister Lot LOT_STATUSES precedent EXACT mirror
- **Cementado**: D2 Opt B "re-export Prisma enum" SUPERSEDED por verificación textual `modules/{lot,mortality,farm}/domain/` ZERO Prisma imports — Path A LOT_STATUSES pattern EXACT mirror. Mapper Prisma↔domain bidirectional en infrastructure layer C2.

### §13.PORTS expenses-inquiry-port-creation-R7-read-non-tx-separation
- **1ra evidencia**: POC expense hex C0
- **Decisión**: ExpensesRepository (write-tx) + ExpensesInquiryPort (read-non-tx) separation R7 paired sister farms+lots cementado cumulative cross-POC matures heredado absoluto
- **Cementado**: NEW ExpensesInquiryPort + LocalExpensesInquiryAdapter wraps ExpenseService para cross-feature consumers (pricing.service.ts C4 cutover)

### §13.DOMAIN expense-amount-number-paired-sister-lot-lossy-boundary
- **1ra evidencia**: POC expense hex C0 (D1 Opt B Marco lock)
- **Decisión**: Expense.amount = `number` paired sister Lot precedent EXACT mirror (vs Opt A Money VO). Precision lossy boundary aceptable scope MVP. Decimal→number conversion en mapper boundary C2 via Number() coercion.
- **Cementado**: paired sister `LotSummary.compute(expenses: { amount: number }[], ...)` precedent EXACT mirror — Money VO NO scope creep domain

### §13.APPLICATION expense-service-7-methods-legacy-parity-divergent-vs-paired-lot-totals
- **1ra evidencia**: POC expense hex C1 (Path b hotfix)
- **Decisión**: ExpenseService 7 methods legacy parity (vs paired Lot 5 methods). Service expone getTotalByLot + getTotalsByCategory delegando ExpensesRepository (sumByLot + totalsByCategory) thin pass-through. Path b revert L-C1-AGGREGATIONS-SCOPE Opt 1 drop totals → Opt 2 keep totals legacy parity completo.
- **Cementado**: pricing.service.ts cross-module DI consume getTotalByLot (legacy parity preserve — Marco repo interface expand intentional post-C1 GREEN initial)

### §13.INFRASTRUCTURE expense-mapper-asymmetric-decimal-number-boundary
- **1ra evidencia**: POC expense hex C2
- **Decisión**: expense.mapper.ts toDomain + toPersistence bidirectional paired sister lot.mapper.ts EXACT mirror reduced (no toWithRelationsSnapshot — single aggregate). Decimal→number via Number() coercion at boundary (works real Prisma.Decimal + test mock { toString: () => "..." } since Number() valueOf→toString fallback).
- **Cementado**: Schema enum runtime safety guarantee allows direct cast `row.category as ExpenseCategory` mapper boundary (avoid parseExpenseCategory scope creep — schema NOT NULL constraint enforces)

### §13.PRESENTATION expense-cutover-toSnapshot-canonical-bridge-supersedes-legacy-number-workaround
- **1ra evidencia**: POC expense hex C4 (Path X resolution)
- **Decisión**: Hex `.toSnapshot()` ES canonical bridge Decimal→number boundary D1 Opt B paired sister cementado heredado matures SUPERSEDES legacy `amount: Number(...)` workaround. Hotfix-retroactivo test α3 regex update legacy `/amount:\s*Number\(/` → hex `/\.toSnapshot\(\)/`.
- **Cementado**: evidence-supersedes-assumption-lock 59ma matures cumulative cross-POC — hex canonical patterns SUPERSEDE legacy workarounds atomic cascade resolution mismo batch GREEN

### §13.A5-ζ wholesale-7ma-evidencia-matures-cumulative-cross-POC
- **7ma evidencia matures cumulative cross-POC**: POC expense hex C5 (paired farms+lots C7 6ta + 5 anteriores)
- **Decisión**: Wholesale delete features/expenses/ atomic single batch 6 archivos DELETE -ϕ LOC + DIR auto-removed por git rm (zero residual) paired sister farms+lots C7 EXACT mirror reduced single-aggregate
- **Cementado**: retirement-reinventory-gate PROJECT-scope grep classification 5-axis APPLIED pre-RED ZERO CONSUMER PROD verified post-C4 cutover cumulative (5 hits TEST-SHAPE-ASSERTION-NEGATIVE absorbed self-references inside __tests__/ excluded por walkProductionSources)

### §13.DOMAIN shared-errors-class-rich-relocation-paired-sister-account-subtype-axis-distinct
- **1ra evidencia**: POC poc-shared-errors C2 (OLEADA 2 transversal foundational)
- **Decisión**: `features/shared/errors.ts` (215 LOC, 7 classes, 78 const codes, 2 types) → `modules/shared/domain/errors/index.ts` canonical home single file 1:1 verbatim mirror + 2-line SHIM at original location (§13.X precedent placeholder). Resolves 1 stranded fwd-dep `modules/shared/domain/errors/monetary-errors.ts` (modules/* → features/* violation) atomic same GREEN commit.
- **Cementado**: paired sister poc-#2c account-subtype-to-hex EXACT mirror — single source file relocated 1:1, 2-line SHIM shape literal verified textual, sibling relative import convention (`./index`) preserved. 154 consumers (149 deep + 5 barrel) transit via SHIM zero touch — future cleanup POC #10 (retirement-reinventory-gate v3).

### §13.PRESENTATION shared-middleware-auth-cross-cutting-presentation-paired-sister-shared-errors-axis-distinct
- **1ra evidencia**: POC poc-shared-middleware-auth C2 (OLEADA 2 transversal foundational sub-POC #2 of 4)
- **Decisión**: `features/shared/{middleware,http-error-serializer}.ts` (36 LOC total: 10 middleware + 26 http-serializer, 3 exports: requireAuth + handleError re-export + handleError fn) → `modules/shared/presentation/{middleware,http-error-serializer}.ts` flat siblings 1:1 verbatim mirror + 2 × 2-line SHIM at original locations (§13.X precedent placeholder). Cross-layer imports canonicalized to `@/modules/shared/domain/errors` (presentation/ → domain/errors/ alias). Internal re-export `export { handleError } from "./http-error-serializer"` preserved sibling-relative inside hex middleware.ts.
- **Cementado**: paired sister poc-shared-errors EXACT mirror — relocate-1:1 + SHIM-bridge pattern preserved, axis-distinct on file count (2 vs 1) + layer (presentation flat-siblings vs domain/errors/ single-file aggregator) + internal-re-export-chain (NEW REQ-003 vs n/a). 90 prod consumers + 39 vi.mock declarations transit via SHIM zero touch (PATH-ONLY-SUFFICIENT verdict locked explore) — future cleanup POC #10.

### §13.INFRASTRUCTURE shared-audit-infrastructure-cross-cutting-relocation-paired-sister-shared-errors-axis-distinct-shim-option-b-divergence
- **1ra evidencia**: POC poc-shared-audit C2 (OLEADA 2 transversal foundational sub-POC #3 of 4)
- **Decisión**: `features/shared/{audit-context,audit-tx}.ts` (110 LOC total: 42 audit-context + 68 audit-tx, 3 value exports: setAuditContext + withAuditTx + assertAuditContextSet + 1 type export: WithCorrelation) → `modules/shared/infrastructure/{audit-context,audit-tx}.ts` flat siblings 1:1 verbatim mirror + 2 SHIM at original locations (Option B named re-exports, NOT `export *` — DIVERGES from sub-POC #1/#2 precedent). Internal sibling import `import { setAuditContext } from "./audit-context"` preserved literal inside hex audit-tx.ts. Zero PROD consumer touches (10 prod files + 1 stranded fwd-dep `prisma-unit-of-work.ts:3` auto-covered via SHIM — no explicit edit). 1 spy consumer `features/shared/__tests__/audit-tx.test.ts` required import path update to hex namespace (cascade-NEW: SHIM spy can't intercept hex-to-hex closed-over import; diagnostic stash gate pattern confirmed).
- **Cementado**: paired sister poc-shared-errors PATTERN preserved (relocate-1:1 + SHIM-bridge) — axis-distinct on file count (2 vs 1) + layer (infrastructure/ flat-siblings vs domain/errors/ aggregator) + SHIM shape (named re-exports + `export type` vs `export *`). NEW invariant LOCKED: when SHIM target has `vi.spyOn`-via-namespace consumers (2 sites: prisma-unit-of-work.test.ts spy + audit-tx.test.ts internal namespace spy), SHIM MUST use named re-exports for mutable binding compatibility — `export *` bindings are immutable per ES module spec. Spy consumer that calls hex function via SHIM must spy on hex namespace directly (SHIM namespace spy does NOT intercept hex-to-hex closed-over import). Type-only re-export discipline: `export type { WithCorrelation }` mandatory under `tsconfig.isolatedModules: true`. 10 prod consumers transit via SHIM zero touch — future cleanup POC #10.

### §13.INFRASTRUCTURE shared-base-repo-prisma-errors-infrastructure-relocation-paired-sister-audit-axis-distinct-shim-option-a-restored
- **2da evidencia**: POC poc-shared-base-repo C2 (OLEADA 2 transversal foundational sub-POC #4 of 4 — FINAL)
- **Decisión**: `features/shared/{base.repository,prisma-errors}.ts` (55 LOC total: 31 + 24, 2 exports: BaseRepository abstract class + isPrismaUniqueViolation fn) → `modules/shared/infrastructure/{base.repository,prisma-errors}.ts` flat siblings 1:1 verbatim mirror + 2 SHIM at original locations (Option A `export *`, RESTORES sub-POC #1/#2 simplest pattern — diverges from sub-POC #3 Option B). server-only directive preserved in hex base.repository.ts. Zero prod consumer touches (18 import lines across 17 files). Zero spy consumers (spy gate PASS: 0 vi.spyOn sites, 0 namespace imports, no closed-over siblings → Option B not needed).
- **Cementado**: paired sister poc-shared-audit INFRASTRUCTURE layer pattern confirmed (flat siblings, verbatim 1:1, SHIM bridge) — axis-distinct on SHIM strategy (Option A vs B per spy-gate result) + no stranded fwd-deps (vs #3's prisma-unit-of-work.ts). Option A SHIM restored: `export *` valid when no mutable-binding spy consumers exist. spy-consumer-namespace-closed-over-imports invariant gate applied proactively — PASS confirmed clean. No `export type` needed: BaseRepository=class (value+type unified), isPrismaUniqueViolation=fn value. Vitest baseline IMPROVED post-C2 (57 fail / 6348 pass vs ~59/6329 pre-POC). poc-shared-canonical umbrella COMPLETE (4/4 sub-POCs archived).

### §13.DOMAIN permissions-domain-relocation-paired-sister-shared-errors-axis-distinct-shim-option-b-isolatedmodules
- **1ra evidencia**: POC poc-permissions-hex B1 C2 (OLEADA 2 sub-POC #6/6 — final)
- **Decisión**: `features/permissions/{permissions,index}.ts` (181 LOC total: 180 permissions + 1 index, 10 value exports: SYSTEM_ROLES + isSystemRole + 4 PERMISSIONS_ matrices + getPostAllowedRoles + getRagScopes + getUploadScopes + canUploadToScope, 6 type exports: Role + SystemRole + Resource + Action + DocumentScope + PostableResource) → `modules/permissions/domain/{permissions,index}.ts` flat siblings 1:1 verbatim mirror + 2 SHIM at original locations (Option B named re-exports + `export type` for permissions.ts SHIM due to isolatedModules; Option A `export *` for index.ts barrel forwarding to SHIM). Test relocation INCLUDED in C2 atomic: 3 test files co-located to `modules/permissions/domain/__tests__/`. 7-file atomic (2 hex + 3 test + 2 SHIM) above sister 4-file ceiling — justified by multi-test count vs sister 0-test sub-POCs.
- **Cementado**: paired sister poc-shared-errors EXACT mirror SHIM-bridge — axis-distinct on file count (2 vs 1) + layer (domain flat-siblings vs domain/errors/ single-file aggregator) + tests-co-located (NEW vs sister tests-stayed-in-features). Option B forced by `isolatedModules: true` mixing values + types (6 type exports require explicit `export type` re-export). Cross-layer test imports use `@/features/permissions/permissions.cache` and `@/features/permissions/permissions.server` aliases (NOT cross-layer relative `../../infrastructure/...` per design §4) — divergence justified: at B1 GREEN time B2/B3 hex paths do not exist; alias-stable across all sub-POCs (source pre-relocation → SHIM post-relocation). 9 type-only client consumers + service-layer transit via SHIM zero touch — future cleanup POC #10.

### §13.INFRASTRUCTURE permissions-cache-infrastructure-relocation-paired-sister-audit-axis-distinct-shim-option-b-module-scope-singleton-preserved
- **3ra evidencia**: POC poc-permissions-hex B2 C2 (OLEADA 2 sub-POC #6/6 — INFRA-cumulative w/ #3 audit + #4 base-repo)
- **Decisión**: `features/permissions/{permissions.cache,cache}.ts` (212 LOC total: 202 + 10, 5 value exports: getMatrix + revalidateOrgMatrix + ensureOrgSeeded + _setLoader + _resetCache + 1 type export: OrgMatrix) → `modules/permissions/infrastructure/{permissions.cache,cache}.ts` flat siblings 1:1 verbatim mirror + 2 SHIM (Option B named re-exports + `export type` for permissions.cache.ts SHIM; Option A `export *` for cache.ts barrel forwarding to SHIM, `import "server-only"` preserved). Internal sibling import `./permissions` rewritten cross-layer `../domain/permissions` inside hex permissions.cache.ts. Test relocation INCLUDED (permissions.cache.test.ts → infrastructure/__tests__/; `../permissions` rewritten to `../../domain/permissions`). 5-file atomic (2 hex + 1 test + 2 SHIM).
- **Cementado**: paired sister poc-shared-audit INFRASTRUCTURE Option B PATTERN preserved (named re-exports + `export type` under isolatedModules). Module-scope Map singleton invariant preserved via pure re-export SHIM (no second module instance — same cache Map). Axis-distinct on test-co-location (NEW vs sister tests-stayed) + cross-layer internal-import-rewrite (NEW: `./permissions` sibling becomes `../domain/permissions` cross-layer alias). 1 legacy adapter consumer transit via SHIM zero touch.

### §13.APPLICATION permissions-application-relocation-paired-sister-shared-audit-axis-distinct-shim-option-b-spy-safe-cross-module-boundary-84-mocks-preserved
- **1ra evidencia**: POC poc-permissions-hex B3 C2 (OLEADA 2 sub-POC #6/6 — UMBRELLA closure)
- **Decisión**: `features/permissions/{permissions.server,client-matrix,server}.ts` (168 LOC total: 102 + 49 + 17, 6 value exports: requirePermission + canAccess + canPost + buildClientMatrixSnapshot + _setLoader-re-export + _resetCache-re-export + 1 type re-export: OrgMatrix) → `modules/permissions/application/{permissions.server,client-matrix,server}.ts` flat siblings 1:1 verbatim mirror + 3 SHIM at original locations (Option B named re-exports for ALL THREE — server.ts critical for spy coherence + 84-vi.mock surface). Internal sibling imports rewritten cross-layer (`./permissions.cache` → `../infrastructure/permissions.cache`; `./permissions` → `../domain/permissions`). Test relocation INCLUDED (require-permission.test.ts + client-matrix.test.ts → application/__tests__/; vi.mock("../permissions.cache") → vi.mock("../../infrastructure/permissions.cache")). 9-file atomic (3 hex + 2 test + 3 SHIM + 1 B1-mock-hygiene fix on permissions.test.ts) — LARGEST in OLEADA 2 (above sister 4-file ceiling), driven by 3-source-file × 3-fold artifact (hex + test + SHIM) + 1 cross-cutover mock-target hygiene fix per [[mock_hygiene_commit_scope]] (B1 permissions.test.ts mock target updated from SHIM alias to hex infrastructure path, discovered via [[diagnostic_stash_gate_pattern]]).
- **Cementado**: NEW invariant LOCKED — `cross-module-boundary` spy vs `closed-over-sibling` spy distinction. Sub-POC #3 (audit) proved closed-over-sibling-imports-bypass-SHIM-namespace; HERE journal.service.ts imports `canPost` from `@/features/permissions/server` (cross-module path, not closed-over relative), so `vi.spyOn(permissions, "canPost")` on `import * as permissions from "@/features/permissions/server"` intercepts correctly because both spy and subject resolve to SHIM module namespace, and Option B `export { canPost } from "@/modules/permissions/application/permissions.server"` preserves the mutable binding. NO spy-consumer redirect required (DIFFERS from sub-POC #3). NEW invariant LOCKED — `cross-module-boundary mock target rewrite at cutover`: when a hex sibling internal import (`../infrastructure/permissions.cache`) supersedes a SHIM alias path (`@/features/permissions/permissions.cache`), tests in lower-layer modules that mock the SHIM alias MUST update mock target to hex path atomically with the cutover. SHIM alias and hex path resolve to DIFFERENT module entries even when they re-export same symbols (separate Vitest module-graph nodes). 84 `vi.mock("@/features/permissions/server")` decls preserved via shape parity of Option B SHIM (PATH-ONLY-SUFFICIENT verdict; raw grep returns 86 = 84 consumer + 2 self-references in c1-shape.poc-permissions-hex-b3.test.ts). `_setLoader` + `_resetCache` test hooks transit via `server.ts` SHIM (logically belong on `/cache` — minor debt deferred POC #10, OQ-5). poc-permissions-hex UMBRELLA COMPLETE (3/3 sub-POCs archived). OLEADA 2 COMPLETE (6/6).

### §13.APPLICATION auth-relocation-paired-sister-users-hex-axis-distinct-application-only-flat-dual-mock-test-wiring
- **1ra evidencia**: POC poc-auth-hex C2 (OLEADA 3 sub-POC #6/6 — FINAL)
- **Decisión**: `features/auth/{sync-user.service,__tests__/sync-user.service.test}.ts` (205 LOC total: 56 src + 146 test + 3 SHIM barrel, 1 value export: `syncUserToDatabase`) → `modules/auth/application/sync-user.service.ts` + `modules/auth/application/__tests__/sync-user.service.test.ts` flat application-only (NO `infrastructure/` layer — auth has 0 Prisma repo, 0 ports, 0 domain entities; DB writes route entirely via UsersService). 4-file atomic GREEN (2 git mv + 1 SHIM rewrite + 1 test canonical-mock add, vs sister poc-users-hex 3-file — +1 for heredado test relocation). Canonical SERVICE rewires DIRECT (REQ-004): `@/modules/users/application/users.service` (NOT via `@/features/users/server` SHIM) + `@/modules/shared/domain/errors` (NOT via `@/features/shared/errors` SHIM). SHIM bridge: `features/auth/server.ts` → Option A static named re-export `export { syncUserToDatabase } from "@/modules/auth/application/sync-user.service"` + preserved `import "server-only"` (3 LOC). Mock-target invariant per [[cross_module_boundary_mock_target_rewrite]]: `@/features/users/server` vi.mock target PRESERVED in heredado test (1 decl) + DUAL mock `@/modules/users/application/users.service` added alongside — `@/features/shared/errors` type import left as-is (mock-target preservation priority over REQ-004 in TEST context). Consumer cementado: 1 runtime (`app/(dashboard)/layout.tsx`) + 0 external test mocks. [[engram_textual_rule_verification]] applied: canonical paths re-verified at apply time.
- **Cementado**: paired sister poc-users-hex C2 (`f5bdbc3f`) EXACT mirror SHIM-bridge — axis-distinct on (a) module shape APPLICATION-ONLY flat (vs sister application+infrastructure cross-layer split), (b) 4-file atomic (+1 test vs sister 3-file), (c) test at `application/__tests__/` (vs sister `infrastructure/__tests__/`), (d) dual-mock wiring (both `@/features/users/server` legacy + `@/modules/users/application/users.service` canonical mocked in same test). 1 runtime consumer + 0 mock targets transit via SHIM zero touch. SHIM deletion deferred bundle (poc-auth-hex + poc-users-hex + poc-account-balances-hex retirement batch per OLEADA 3 pattern). Cross-ref: sister `f5bdbc3f` (poc-users-hex D1) + paired pending `poc-account-balances-hex` (POC #9 territory). C0 RED `ac497dda` · C1 GREEN `693a51c4` · C2 D1 (this commit).

### §13.APPLICATION reports-relocation-paired-sister-auth-hex-axis-distinct-application-only-flat-no-server-only-types-in-shim
- **1ra evidencia**: POC poc-reports-hex C2 (OLEADA 4 POC #1/4)
- **Decisión**: `features/reports/{catalog,index,__tests__/catalog,__tests__/catalog-icons}.ts` (712 LOC total: 325 src + 326+56 test + 5 barrel, 2 value exports: reportCategories+reportRegistry, 3 type exports: ReportCategory+ReportEntry+ReportStatus) → `modules/reports/application/catalog.ts` (canonical, values+types co-located) + `modules/reports/application/__tests__/{catalog,catalog-icons}.test.ts` (2 heredado tests relocated) + SHIM at `features/reports/index.ts` (Option A static named re-export, values+types, NO server-only — 2 LOC). 6-op atomic GREEN (3 CREATE + 1 SHIM rewrite + 2 DELETE). REQ-004 N/A (catalog.ts has ZERO imports — pure static data). [[cross_module_boundary_mock_target_rewrite]] NOT triggered (zero vi.mock in either heredado test — plain import-path rewrite only). 4 consumers (components/reports/{catalog-page,category-section,report-card}.tsx + components/reports/__tests__/catalog-page.test.tsx) go through barrel SHIM — zero edits.
- **Cementado**: paired sister poc-auth-hex (`ac497dda` RED / `693a51c4` GREEN / `d9d63849` D1) EXACT mirror SHIM-bridge — axis-distinct on (a) NO server-only (catalog is pure static data, client-safe — sister had server-only on both canonical + SHIM), (b) SHIM exports VALUES + TYPES 2-line (sister was values-only 1-line — NEW precedent for type-bearing SHIMs), (c) 6-op C1 atomic (vs sister 4-op — +2 for 2 heredado tests vs sister 1), (d) zero vi.mock in both heredado tests (sister had 3 vi.mock targets to preserve). [[cross_module_boundary_mock_target_rewrite]] invariant NO trigger: SHIM preserves `@/features/reports` module-graph entry — 0 vi.mock declarations transit via SHIM. SHIM deletion deferred OLEADA 4 retirement bundle. **OLEADA 4: 1/4 CLOSED**. C0 RED `db46f0c0` · C1 GREEN `c5c2caa0` · C2 D1 (this commit).

### §13.APPLICATION/INFRASTRUCTURE users-relocation-paired-sister-shared-base-repo-axis-distinct-single-shim-barrel-named-re-export
- **1ra evidencia**: POC poc-users-hex C2 (OLEADA 3 sub-POC #5/6)
- **Decisión**: `features/users/{users.service,users.repository,server}.ts` (82 LOC total: 43 service + 36 repo + 3 barrel, 1 value export class UsersService + 1 type interface CreateUserInput) → `modules/users/{application/users.service,infrastructure/users.repository}.ts` cross-layer split (NEW divergence from sister poc-shared-base-repo flat-infra-only) + 1 SHIM at `features/users/server.ts` (Option A static named re-export `export { UsersService } from "@/modules/users/application/users.service";` + preserved `import "server-only";` — 3 LOC). 3-file atomic GREEN (2 git mv + 1 SHIM rewrite, vs sister 4-file 2 origin SHIMs). Canonical files rewire imports DIRECT bypassing features/shared SHIMs (REQ-004): users.service.ts imports NotFoundError from `@/modules/shared/domain/errors` (not `@/features/shared/errors`); users.service.ts imports UsersRepository + type CreateUserInput from `@/modules/users/infrastructure/users.repository` (canonical absolute, not sibling relative — application → infrastructure cross-layer explicit); users.repository.ts imports BaseRepository from `@/modules/shared/infrastructure/base.repository` (not `@/features/shared/base.repository`). Zero domain layer (no entity/VO/port — thin coordination service over Prisma adapter; full hex purity OUT of scope per relocation-only mandate). Zero own tests pre-POC (no test migration).
- **Cementado**: paired sister poc-shared-base-repo (`5517966d` RED / `c3b98533` GREEN / `c242f858` D1) EXACT mirror SHIM-bridge — axis-distinct on (a) layer count (application+infrastructure vs infrastructure-only), (b) SHIM count (1 single barrel vs 2 separate), (c) SHIM shape (named re-export + `import "server-only"` preserved vs `export *` no server-only), (d) named re-export rationale: pre-relocation file already named-exported (`export { UsersService }`), preserving named re-export keeps surface 1:1 with zero accidental widening. [[cross_module_boundary_mock_target_rewrite]] invariant NO trigger: SHIM preserves `@/features/users/server` module-graph entry → all 8 `vi.mock("@/features/users/server", ...)` declarations remain valid post-relocation (mock target invariant LOCKED). 21 runtime consumers (19 app routes + 1 features/auth sibling + 1 modules/organizations adapter) + 8 test mocks transit via SHIM zero touch. SHIM deletion deferred retirement bundle (poc-auth-hex NOW CLOSED — features/auth canonical relocated; wholesale delete deferred per OLEADA 3 pattern with poc-users-hex + poc-account-balances-hex retirement batch). 14α shape sentinels paired-sister mirror (13 FAIL + 1 PASS-lock α4 server-only preservation, declared honest per [[red_acceptance_failure_mode]] — sister α7 same shape). [[engram_textual_rule_verification]] applied: NotFoundError re-verified at `modules/shared/domain/errors/index.ts:20` at apply time. C0 RED `6b41ce09` · C1 GREEN `274ecd2e` · C2 D1 (this commit).

### §13.APPLICATION/INFRASTRUCTURE account-balances-relocation-paired-sister-users-hex-axis-distinct-5-file-atomic-types-file-divergence-req-004-circular-dep-broken
- **1ra evidencia**: POC poc-account-balances-hex C2 (OLEADA 3 sub-POC #6/6 — FINAL, wave complete)
- **Decisión**: `features/account-balances/{account-balances.service,account-balances.repository,account-balances.types,server}.ts` (150 LOC total: 61 service + 79 repo + 8 types + 2 barrel, 1 value export class AccountBalancesService + 1 type AccountBalanceWithRelations) → `modules/account-balances/{application/account-balances.service,infrastructure/account-balances.repository,infrastructure/account-balances.types}.ts` cross-layer split + types in infrastructure (Prisma-derived composite, zero domain semantics — mirrors users layout) + 1 SHIM at `features/account-balances/server.ts` (Option A static named re-export `export { AccountBalancesService } from "@/modules/account-balances/application/account-balances.service";` + preserved `import "server-only";` — 3 LOC). 5-file atomic GREEN (3 git mv + 1 SHIM rewrite, vs sister 3-file +1 types file divergence). Canonical files rewire imports DIRECT bypassing features/ SHIMs (REQ-004, 2 bypasses +1 vs sister): account-balances.service.ts imports JournalEntryWithLines from `@/modules/accounting/presentation/dto/journal.types` (not `@/features/accounting/server` — breaks type-level circular dep accounting ↔ account-balances at hex layer); account-balances.service.ts imports AccountBalancesRepository + type AccountBalanceWithRelations from `@/modules/account-balances/infrastructure/*` (canonical absolute, not sibling relative — application → infrastructure cross-layer explicit); account-balances.repository.ts imports BaseRepository from `@/modules/shared/infrastructure/base.repository` (not `@/features/shared/base.repository`). Zero domain layer (AccountBalanceWithRelations is pure Prisma composite Pick<...> — no entity/VO/port; full hex purity OUT of scope per relocation-only mandate). Zero own tests pre-POC (no test migration, same as sister).
- **Cementado**: paired sister poc-users-hex (`6b41ce09` RED / `274ecd2e` GREEN / `f5bdbc3f` D1) EXACT mirror SHIM-bridge — axis-distinct on (a) C1 atomic file count (5-file vs sister 3-file, +1 types file), (b) REQ-004 bypass count (2 vs sister 2 — same magnitude, but different content: account-balances adds JournalEntryWithLines cross-module type bypass, sister had NotFoundError instead), (c) cross-module concern: JournalEntryWithLines bypass resolves a TYPE-LEVEL circular dependency between account-balances ↔ accounting (TS erases type imports at runtime, no runtime regression — fix structural coupling at hex layer), (d) consumer count (7 = 5 runtime + 2 type-only test fixtures vs sister 21), (e) named re-export rationale: pre-relocation file already named-exported (`export { AccountBalancesService }`), preserving named re-export keeps surface 1:1. [[cross_module_boundary_mock_target_rewrite]] invariant NO trigger: SHIM preserves `@/features/account-balances/server` module-graph entry → 1 `vi.mock("@/features/account-balances/server", ...)` declaration in `app/(dashboard)/[orgSlug]/accounting/balances/__tests__/page-rbac.test.ts` remains valid post-relocation (mock target invariant LOCKED). 7 consumers (5 runtime: app/(dashboard)/.../balances/page.tsx + app/api/.../balances/route.ts + features/accounting/journal.service.ts + features/accounting/ledger.service.ts + modules/dispatch/infrastructure/legacy-account-balances.adapter.ts; 2 type-only test fixtures: features/accounting/__tests__/journal.service.audit.test.ts + features/accounting/__tests__/journal.service.void-guard.test.ts) + 1 vi.mock transit via SHIM zero touch. SHIM deletion deferred OLEADA 3 retirement bundle: poc-auth-hex + poc-users-hex + poc-account-balances-hex ALL CLOSED — wholesale C5 SHIM delete READY for retirement phase (full trio complete). 14α shape sentinels paired-sister mirror (13 FAIL + 1 PASS-lock α4 server-only preservation, declared honest per [[red_acceptance_failure_mode]] — sister α4 same shape; α6 cross-divergence: JournalEntryWithLines DIRECT replaces sister α7 CreateUserInput optional field — different REQ-004 content, same slot count). [[engram_textual_rule_verification]] applied: §13.users-relocation entry textually verified at apply time + JournalEntryWithLines canonical home re-verified at `modules/accounting/presentation/dto/journal.types.ts`. **OLEADA 3: 6/6 CLOSED** (wave complete — final POC). C0 RED `2565077d` · C1 GREEN `96aec947` · C2 D1 (this commit).

## §13.documents — Canonical Homes Documents hex (NEW poc-documents-hex — OLEADA 4 sub-POC #2/4)

### §13.documents BlobStoragePort-upload-del-documents-specific-vs-org-profile-del-only
- **1ra evidencia**: POC poc-documents-hex C0 (OLEADA 4 sub-POC #2/4)
- **Decisión**: `modules/documents/domain/ports/blob-storage.port.ts` — FRESH documents-specific port with `upload(file, organizationId, userId): Promise<{url, pathname}>` + `del(url): Promise<void>`. DIFFERS from `modules/org-profile/domain/ports/blob-storage.port.ts` (del-only). Textually verified at apply time — no reuse possible (paired sister has 1-method interface; documents needs full upload+del lifecycle because the service persists user-supplied files end-to-end).
- **Cementado**: paired sister poc-org-profile-hex C0 (`aa25c052` RED). Documents adapter `VercelBlobStorageAdapter` (modules/documents/infrastructure/blob/) extends the upload+del shape over `@vercel/blob` `put`/`del` — sister adapter is del-only.

### §13.documents REQ-004-rag-cross-module-bypass-canonical
- **1ra evidencia**: POC poc-documents-hex C1 (OLEADA 4 sub-POC #2/4)
- **Decisión**: `DocumentsService` imports `RagService` from `@/features/documents/rag/server` as documented cross-module bypass (REQ-004). rag/ stays at features path — deferred to `poc-rag-hex`. Comment lock `// cross-module canonical-bypass REQ-004 — rag/ stays at features path (poc-rag-hex)` required at import site (both application/documents.service.ts AND presentation/composition-root.ts).
- **Cementado**: 4 `vi.mock("@/features/documents/rag/server", …)` declarations across the codebase (error-propagation test, agent.context.dispatch test, etc.) remain valid post-cutover — mock targets unchanged per [[cross_module_boundary_mock_target_rewrite]] NO-trigger sister precedent. α44 PASS at C4 RED verifies `features/ai-agent/agent.context.ts` rag/ import is UNCHANGED (rag carve-out invariant).

### §13.documents REQ-007-pdfjs-dist-processing-library-accepted-exception-no-PdfPort
- **1ra evidencia**: POC poc-documents-hex C1 (OLEADA 4 sub-POC #2/4)
- **Decisión**: `pdfjs-dist/legacy/build/pdf.mjs` remains as a direct application-layer dependency at `modules/documents/application/documents.service.ts`. Comment lock `// processing library accepted exception — no PdfPort (REQ-007)` required at import site. Rationale: pdfjs-dist is a CPU-bound text-extraction library, NOT an I/O port like @vercel/blob or @prisma/client. The hex purity argument breaks down for in-process computation — same precedent as date-fns and zod (used directly across layers without ports).
- **Cementado**: α15 POSITIVE asserts both `pdfjs-dist/legacy/build/pdf.mjs` import AND `processing library accepted exception` text are present in the application service.

### §13.documents REQ-005-NEGATIVE-server-only-domain-application-isolation
- **1ra evidencia**: POC poc-documents-hex C0+C1+C2 (OLEADA 4 sub-POC #2/4)
- **Decisión**: `modules/documents/domain/**` and `modules/documents/application/**` MUST NOT contain `import "server-only"`. ONLY `modules/documents/presentation/server.ts` carries the marker (canonical hex barrel) AND `features/documents/server.ts` SHIM carries it. Comment text in source files deliberately avoids the literal `import "server-only"` pattern to keep NEGATIVE regex sentinels tight (α8, α13, α28 PASS).
- **Cementado**: 4 NEGATIVE sentinels (α8 domain, α13 application, α28 infrastructure twice) + 2 POSITIVE sentinels (α33 presentation/server.ts, α36 features/documents/server.ts SHIM). REQ-005 enforced across the full hex tree.

### §13.documents C5-wholesale-delete-deferred-rag-blocks-parent-dir
- **1ra evidencia**: POC poc-documents-hex (OLEADA 4 sub-POC #2/4 — partial closure)
- **Decisión**: `features/documents/` directory NOT deleted this POC. `rag/` subfolder still resident (rag.service.ts, chunking.ts, embedding.service.ts, vector.repository.ts, server.ts — 18α PASS shape from poc-quick-cleanup) — blocks parent dir removal. SHIM at `features/documents/server.ts` keeps consumers + vi.mock targets. C5 wholesale delete DEFERRED to `poc-rag-hex` closure (rag/ migrates to its own hex module; then features/documents/ becomes empty + can be removed).
- **Cementado**: 5-cycle chain C0–C4 closed; C5 explicitly deferred (NOT 6-cycle sister mirror). Test file `features/documents/rag/__tests__/poc-quick-cleanup-rag-shape.test.ts` updated atomic to reference the relocated `modules/documents/application/__tests__/documents.service.error-propagation.test.ts` path (cross-POC residual cementación adjusted per [[mock_hygiene_commit_scope]]).

## §13.audit — Canonical Homes Audit hex (NEW poc-audit-hex — OLEADA 4 sub-POC #3/4)

### §13.audit-domain types-classifier-repo-interface-AuditInquiryPort-R5-absoluta-READ-ONLY
- **1ra evidencia**: POC poc-audit-hex C0 (OLEADA 4 sub-POC #3/4)
- **Decisión**: `modules/audit/domain/` contains audit types, event classifier, repo interface, and `AuditInquiryPort` — port surface is READ-ONLY by design (NO create/update/delete methods). Differs from paired sister poc-documents-hex on axis: documents has R/W lifecycle (BlobStoragePort upload+del); audit is inquiry-only (CTE-based read paths). R5 absoluta zero prisma imports enforced.
- **Cementado**: C0 RED `a7036e43` · GREEN `bce5b981`. 8α PASS sentinels (types + classifier + port + repo-interface shape). Paired sister poc-documents-hex C0 EXACT mirror axis-distinct on READ-ONLY (no mutation in port surface).

### §13.audit-application AuditService-UserNameResolver-port-READ-ONLY
- **1ra evidencia**: POC poc-audit-hex C1 (OLEADA 4 sub-POC #3/4)
- **Decisión**: `modules/audit/application/` contains `AuditService` + `UserNameResolver` port — second READ-ONLY port (resolves user display names by id; NO create/update). Both audit ports lack mutation methods — invariant LOCKED for the full hex module (READ-ONLY pattern). Differs from poc-documents-hex (DocumentsService persists files via BlobStoragePort.upload).
- **Cementado**: C1 RED `09ecb23e` · GREEN `154ff73f`. 7α PASS sentinels (service shape + port + R5 absoluta + server-only-free in domain+application). READ-ONLY pattern axis-distinct from documents-hex sister.

### §13.audit-infrastructure PrismaAuditRepository-raw-SQL-CTE-preserved-tenant-guard-PrismaUserNameResolver
- **1ra evidencia**: POC poc-audit-hex C2 (OLEADA 4 sub-POC #3/4)
- **Decisión**: `modules/audit/infrastructure/` contains `PrismaAuditRepository` (raw SQL CTE preserved verbatim from legacy — architectural exception to typical Prisma ORM-only repo pattern; CTE drives correlated-event grouping that ORM can't express cleanly) + `PrismaUserNameResolver` adapter + tenant guard (organizationId scoping in every query — security-relevant). Raw SQL exception accepted same precedent as REQ-007 pdfjs-dist (processing-library carve-out) — boundary is "in-process computation NOT I/O port".
- **Cementado**: C2 RED `cb4f6ec0` · GREEN `2a74c69b`. 7α PASS sentinels (repo shape + raw SQL preservation + tenant guard + UserNameResolver adapter). Raw SQL CTE LOCKED architectural exception; tenant guard LOCKED security invariant.

### §13.audit-presentation composition-root-server-index-validation-barrels
- **1ra evidencia**: POC poc-audit-hex C3 (OLEADA 4 sub-POC #3/4)
- **Decisión**: `modules/audit/presentation/` contains composition-root + `server.ts` (with `import "server-only"` + `makeAuditService` factory) + `index.ts` client barrel + `validation.ts` barrel. Server barrel pattern EXACT mirror of paired sister poc-documents-hex; client barrel exposes types (AuditEvent + helpers) consumed by 4 client components.
- **Cementado**: C3 RED `5aebf296` · GREEN `9f78b9ac`. 5α PASS sentinels (server barrel + client barrel + validation + composition-root shape). Paired sister documents-hex presentation barrel EXACT mirror.

### §13.audit-cross-feature-cutover 12-consumers-atomic-RSC-API-client-tests
- **1ra evidencia**: POC poc-audit-hex C4 (OLEADA 4 sub-POC #3/4)
- **Decisión**: 12 cross-feature consumers cutover atomic in a single GREEN: 2 RSC pages (`app/(dashboard)/[orgSlug]/audit/page.tsx` + `[entityType]/[entityId]/page.tsx`) + 2 API routes + 4 client components (audit-detail-timeline, audit-event-list, audit-diff-viewer, audit-event-badges) + 4 test files. Differs from paired sister documents-hex (3 consumers) — 4× consumer surface absorbed atomic per [[mock_hygiene_commit_scope]].
- **Cementado**: C4 RED `b68aa8f9` · GREEN `939934a9`. 12α PASS sentinels (one per consumer; hex-import POSITIVE + legacy-import NEGATIVE). α36 sentinel for `features/audit/__tests__/audit-types-helpers.test.ts` later guarded with existsSync (commit `67f437d0`) — file deleted at C5 wholesale.

### §13.audit-c5-wholesale-delete 14-files-DELETE-atomic-DIR-auto-removed-first-in-POC-execution
- **1ra evidencia**: POC poc-audit-hex C5 (OLEADA 4 sub-POC #3/4 — FULL CLOSURE)
- **Decisión**: `features/audit/` directory DELETED wholesale atomic (14 files removed: server.ts, index.ts, audit.service.ts, audit.types.ts, audit-event-classifier.ts, audit.repository.ts, user-name-resolver.ts + 7 test files). Directory auto-removed by git when last file deleted. FIRST hex POC to EXECUTE C5 in-POC (sister documents-hex deferred C5 to poc-rag-hex because rag/ subfolder blocked parent dir removal; audit had no carve-out). 6-cycle full closure pattern (vs documents 5-cycle partial).
- **Cementado**: C5 RED `53758f0f` · GREEN `2da91e58`. 9α PASS sentinels (NEGATIVE: 8 deleted-file existsSync false + 1 parent-dir nonexistent). In-POC C5 execution axis-distinct from documents-hex sister. SHIMs NOT required (no deferred consumers — all 12 cutover atomic in C4).

## §13.organizations — Canonical Homes Organizations hex (NEW poc-organizations-hex — OLEADA 4 sub-POC #4/4 — WAVE COMPLETE)

### §13.organizations-domain types-ports-7-adapter-ports-2-repo-ports-R5-absoluta
- **1ra evidencia**: POC poc-organizations-hex C0+C1 (OLEADA 4 sub-POC #4/4)
- **Decisión**: `modules/organizations/domain/` contains organization types (`types.ts`), clerk-error classifiers (`clerk-error-classifiers.ts`), member-clerk-saga logger (`member-clerk-saga.logger.ts`), members validation (`members.validation.ts`), roles validation (`roles.validation.ts`), and 7 adapter ports (`ports/clerk-auth.port.ts`, `ports/user-resolution.port.ts`, `ports/account-seed.port.ts`, `ports/voucher-type-seed.port.ts`, `ports/system-role-seed.port.ts`, `ports/permission-cache.port.ts`) + 2 repo ports (`ports/organizations.repository.port.ts`, `ports/roles.repository.port.ts`). 7 adapter ports is HIGHEST port count in the repo (axis-distinct from paired sister poc-audit-hex READ-ONLY 2-port surface). R5 absoluta zero Prisma in domain. 3 domain aggregates: Organization, Membership/Clerk-saga, RBAC/CustomRole.
- **Cementado**: C0 `f5705a19` · C1 `10552e56`. 7 adapter ports LOCKED invariant (ClerkAuth, UserResolution, AccountSeed, VoucherTypeSeed, SystemRoleSeed, PermissionCache, SystemRoleGuard). Axis-distinct from audit-hex on port count (7 vs 2) and port nature (write-through vs READ-ONLY).

### §13.organizations-application services-member-clerk-saga-R5-absoluta
- **1ra evidencia**: POC poc-organizations-hex C2 (OLEADA 4 sub-POC #4/4)
- **Decisión**: `modules/organizations/application/` contains `OrganizationsService`, `MembersService`, `RolesService` + `member-clerk-saga.ts` generic saga helper. `runMemberClerkSaga<T>` encapsulates 4-branch failure matrix (a: happy path, b: dbWrite fails, c: Clerk fails + compensation succeeds, d: double failure divergent state) with structured logging via domain logger. Services injected with all 7 adapter ports via constructor — no static imports of Prisma or Clerk SDK. Application layer calls `revalidateOrgMatrix` after every role mutation (PermissionCache port). Differs from audit-hex application layer (mutation operations vs READ-ONLY).
- **Cementado**: C2 `f8b498fb`. 139 tests GREEN across 13 test files (12 unit + 1 integration; saga branches a/b/c/d/e all TDD-covered). `runMemberClerkSaga` reusable pattern — template for future external-API integrations.

### §13.organizations-infrastructure 7-legacy-adapters-2-prisma-repos
- **1ra evidencia**: POC poc-organizations-hex C3 (OLEADA 4 sub-POC #4/4)
- **Decisión**: `modules/organizations/infrastructure/` contains `PrismaOrganizationsRepository`, `PrismaRolesRepository` (2 Prisma repos) + 7 legacy adapters (`legacy-clerk-auth.adapter.ts`, `legacy-user-resolution.adapter.ts`, `legacy-account-seed.adapter.ts`, `legacy-voucher-type-seed.adapter.ts`, `legacy-system-role-seed.adapter.ts`, `legacy-permission-cache.adapter.ts` — wrapping `clerkClient`, `UsersService`, `AccountsService`, `VoucherTypesService`, `SystemRolesService`, `revalidateOrgMatrix` respectively). "Legacy" prefix convention: adapters that delegate to sibling feature modules still in `features/` (pending their own hex migration). Differs from audit-hex infrastructure (no legacy adapters — audit's UserNameResolver uses Prisma directly).
- **Cementado**: C3 `1d7ebed5`. 7 legacy-adapter pattern LOCKED naming convention for infrastructure bridging pre-hex modules. `PrismaRolesRepository` covers `CustomRole` entity (RBAC) separate from `PrismaOrganizationsRepository` (memberships). Zero TS errors introduced.

### §13.organizations-presentation composition-root-server-client-middleware-singletons
- **1ra evidencia**: POC poc-organizations-hex C4+C5 (OLEADA 4 sub-POC #4/4 — FULL CLOSURE)
- **Decisión**: `modules/organizations/presentation/` contains `composition-root.ts` (factory fns `makeOrganizationsService`, `makeRolesService` with `getCallerRoleSlug` callback param, `makeReadOnlyRolesService`), `server.ts` (import "server-only" + service re-exports), `index.ts` (client barrel: exports `slugify` pure fn for `components/settings` consumer), `middleware.ts` (exports `requireOrgAccess` + `requireRole` relay — consumed by EVERY protected route ~49 consumers), `roles.service.singleton.ts` (validation singleton via `makeReadOnlyRolesService`). Cross-cutting middleware relay pattern is unique to this module — `requireOrgAccess`/`requireRole` repointed from `features/permissions` to `modules/organizations/presentation/server` at C5 cutover (features/organizations wholesale deleted — no SHIM needed). Axis-distinct from audit-hex presentation (middleware relay + mutable operations vs audit read-only server barrel).
- **Cementado**: C4 `3f2d4ad2` · C5 `a01b4d59` · D1 `3c75ddf7` (initial) + retroactive sigma-13 (this commit). 49 consumer cutover atomic at C5 (features/organizations wholesale delete: 14 source + 13 test + 3 fixture files). SHIMs NOT required. `makeRolesService` callback pattern LOCKED for caller-context injection (differs from audit-hex makeAuditService no-callback factory). **OLEADA 4: 4/4 CLOSED** (WAVE COMPLETE — all 4 medium POCs archived).

## §13.dispatch — Canonical Homes Dispatch hex (NEW poc-dispatch-hex — OLEADA 5 sub-POC #1/3)

### §13.dispatch-domain VOs-discriminated-dispatch-type-state-machine-entities-errors-7-ports-R5-absoluta
- **1ra evidencia**: POC poc-dispatch-hex C0 (OLEADA 5 sub-POC #1/3)
- **Decisión**: `modules/dispatch/domain/` contains discriminated VOs (`dispatch-type.ts`: NOTA_DESPACHO/BOLETA_CERRADA; `dispatch-status.ts`: DRAFT→POSTED→LOCKED→VOIDED state machine; `payment-allocation-summary.ts`; `receivable-summary.ts`), entities (`dispatch.entity.ts` root aggregate 349 LOC with BC-specific header fields + `dispatch-detail.entity.ts` child), pure domain functions (`compute-line-amounts.ts`, `compute-bc-summary.ts` shrinkage/shortage/realNetWeight in BC dispatch-type path, `round-total.ts`), domain errors (`dispatch-errors.ts`), and 7 port interfaces: `DispatchRepository` (R/W Tx-aware), `DispatchJournalEntryFactoryPort`, `DispatchAccountBalancesPort`, `DispatchOrgSettingsReaderPort`, `DispatchContactsPort`, `DispatchFiscalPeriodsPort`, `DispatchReceivablesPort`. R5 absoluta zero Prisma in domain. Axis-distinct from paired sister poc-organizations-hex on discriminated type computations (BC-path shrinkage/shortage absent from organizations) and state machine DRAFT→POSTED→LOCKED→VOIDED (mirror of Sale module, not organizations RBAC aggregate).
- **Cementado**: C0 RED `19345bcb` · GREEN `189492cc`. 33α PASS sentinels (entity shape + VO + errors + all 7 port interfaces + pure functions + R5 absoluta). 7 port invariant LOCKED (DispatchRepository as internal R/W + 6 external adapter ports). Pre-existing S-01: 2 TS2353 errors in c0-domain-shape sentinel for dispatchId — UNCHANGED from before POC, accepted.

### §13.dispatch-application DispatchService-state-machine-accounting-cascade-discriminated-BC-computations
- **1ra evidencia**: POC poc-dispatch-hex C1 (OLEADA 5 sub-POC #1/3)
- **Decisión**: `modules/dispatch/application/dispatch.service.ts` (739 LOC) implements full CRUD + state machine operations (list, getById, create, createAndPost, update, post, void, delete, hardDelete, recreate) with accounting cascade on post/createAndPost (calls JournalEntryFactoryPort to create accounting entries). Discriminated computations in BC path: shrinkage, shortage, realNetWeight differ from NOTA_DESPACHO path — both handled in domain layer but dispatched by DispatchType VO. `recreate()` marked deprecated. Architectural mirror of Sale state machine (DRAFT→POSTED→LOCKED→VOIDED). Axis-distinct from poc-organizations-hex application (mutation with external accounting bridge vs organizations external Clerk sync saga).
- **Cementado**: C1 RED `b2996874` · GREEN `e15b8d25`. 13α PASS sentinels (service method shapes + port injection + R5 absoluta). State machine + accounting cascade LOCKED. `totalAmount` uses `number` type (predates strict Prisma.Decimal money-math rule — accepted per D1 doc).

### §13.dispatch-infrastructure PrismaDispatchRepository-6-legacy-adapters-displayCode-utility
- **1ra evidencia**: POC poc-dispatch-hex C2 (OLEADA 5 sub-POC #1/3)
- **Decisión**: `modules/dispatch/infrastructure/` contains `PrismaDispatchRepository` (491 LOC, full R/W Tx-aware, standard Prisma ORM — NO raw SQL exception unlike poc-audit-hex) + 6 legacy adapters wrapping cross-module hex siblings: `LegacyJournalEntryFactoryAdapter` (bridges `features/accounting` AutoEntryGenerator — TEMPORARY, accounting not yet hex), `LegacyAccountBalancesAdapter` (→ `modules/account-balances/presentation/server`), `LegacyOrgSettingsReaderAdapter` (→ org-settings), `LegacyContactsAdapter` (→ `modules/contacts/presentation/server`), `LegacyFiscalPeriodsAdapter` (→ `modules/fiscal-periods/presentation/server`), `LegacyReceivablesAdapter` (→ `modules/receivables/presentation/server`) + `dispatch-display-code.ts` utility (10 LOC). "Legacy" prefix naming convention inherited from poc-organizations-hex (adapters bridging pre-hex or transitional modules). Differs from poc-audit-hex infrastructure (no legacy adapters — audit uses Prisma directly for UserNameResolver).
- **Cementado**: C2 RED `4dcd9689` · GREEN `b9bdbcf3`. 8α PASS sentinels (repo shape + 6 adapter shapes + displayCode utility). 6 legacy-adapter pattern LOCKED per poc-organizations-hex naming convention. `LegacyJournalEntryFactoryAdapter` cross-module boundary flagged as TEMPORARY (pending poc-accounting-cleanup OLEADA 5 #3/3).

### §13.dispatch-presentation composition-root-HubService-cross-module-aggregation-server-client-barrels-C5-wholesale-delete
- **1ra evidencia**: POC poc-dispatch-hex C3+C4+C5 (OLEADA 5 sub-POC #1/3 — FULL CLOSURE)
- **Decisión**: `modules/dispatch/presentation/` contains `composition-root.ts` (`makeDispatchService()` zero-arg — wires repo + 6 adapters; differs from organizations `makeRolesService(getCallerRoleSlug)` callback pattern), `server.ts` (server barrel: `DispatchService` + `makeDispatchService` + `HubService` + `hubQuerySchema`), `index.ts` (client barrel: `HubFilters`, `HubItem`, `updateDispatchSchema`), `schemas/dispatch.schemas.ts` + `hub.validation.ts`, and `hub.service.ts` (`DispatchHubService` 197 LOC — aggregates `modules/sale` + `modules/dispatch` into unified `HubItem[]`). **Load-bearing arch debt**: `SaleServiceForHub` interface imported from `modules/sale` at presentation layer (NOT port-isolated); coupling accepted as-is — Sale is also a hex module but the interface crosses module boundaries at presentation, not infrastructure. No presentation-layer analog in poc-organizations-hex (organizations has no cross-module aggregation). C4 cutover: 13 cross-feature consumers atomic (7 runtime + 3 vi.mock + 2 type-only + 1 app-route test). C5 wholesale delete: `features/dispatch/` 15 files DELETE (10 source + 5 test) — DIR auto-removed. SHIMs NOT required. OLEADA 5 FIRST in-POC C5 execution.
- **Cementado**: C3 RED `6dcf69be` · GREEN `5fd0cd42`; C4 RED `cb3f7a7d` · GREEN `e3e6ad8b`; C5 RED `80d92b47` · GREEN `0c546df4`; D1 `45f26c75` (initial) + retroactive sigma-13 (this commit). 35α PASS C3+C4+C5 combined (11+13+11). `makeDispatchService()` zero-arg factory pattern LOCKED (NO callback param — axis-distinct from organizations `makeRolesService`). HubService cross-module aggregation co-located in modules/dispatch/presentation LOCKED accepted (load-bearing arch debt surfaced S: SaleServiceForHub not port-isolated). Consumer count 13 (corrected from roadmap estimate 15). **OLEADA 5: 1/3 CLOSED** (WAVE INITIATED).

## §13.ai-agent — Canonical Homes AI Agent hex (NEW poc-ai-agent-hex — OLEADA 5 sub-POC #2/3)

### §13.ai-agent-domain 8-ports-LLMProviderPort-first-non-Prisma-external-API-port-R5-absoluta
- **1ra evidencia**: POC poc-ai-agent-hex C0 (OLEADA 5 sub-POC #2/3)
- **Decisión**: `modules/ai-agent/domain/` contains 8 port interfaces: `LLMProviderPort` (queryWithTools/analyzeDocument — FIRST non-Prisma, non-HTTP external API port in repo), `RateLimitPort`, `AccountsLookupPort`, `ContactsLookupPort`, `MemoryPort`, `AgentContextPort`, `FiscalPeriodPort`, `PricingPort`. Plus types, errors, entities (AgentInvocation aggregate), VOs (InvocationOutcome). R5 absoluta zero Prisma in domain. 8 ports is HIGHEST count in repo among domain layer (axis-distinct from poc-organizations-hex 7 adapter ports — different layer).
- **Cementado**: C0 RED `7a949b64` · GREEN `eb76f91e`. 38α PASS sentinels (domain types + 8 port interfaces + aggregate + VOs + R5 absoluta). LLMProviderPort pattern LOCKED: first hex port wrapping non-I/O external AI SDK (Gemini) — precedent for future AI/ML integrations.

### §13.ai-agent-application AgentService-deps-object-6-port-injection-4-modes-LLM-singleton-elimination
- **1ra evidencia**: POC poc-ai-agent-hex C1 (OLEADA 5 sub-POC #2/3)
- **Decisión**: `modules/ai-agent/application/agent.service.ts` — deps-object ctor pattern `constructor(private deps: AgentServiceDeps)` with 6-port injection (LLMProviderPort, RateLimitPort, AccountsLookupPort, ContactsLookupPort, MemoryPort, AgentContextPort). 4 modes extracted (chat, balance-sheet-analysis, income-statement-analysis, journal-entry-ai). LLM singleton pattern from legacy `features/ai-agent/` ELIMINATED — each factory call creates fresh LLM client via deps injection. `application/tools/` contains find-accounts.ts + find-contact.ts + parse-operation.ts consuming AccountsLookupPort + ContactsLookupPort (NOT direct Prisma repos — REQ-004 insulation).
- **Cementado**: C0 drift: C1 changed AgentService from zero-arg class to deps-object ctor — this BLOCKS naive `git mv` of legacy __tests__/ at C4 (tests coupled to zero-arg ctor signature). **NEW INVARIANT**: API breaking change at C1 blocks naive C4 test migration. [[API_breaking_change_C1_blocks_C4_test_migration]] LOCKED. C1 RED `b1ef1a24` · GREEN `7d6e9ac8`. 30α PASS sentinels.

### §13.ai-agent-infrastructure 6-legacy-adapters-LLMAdapter-AccountsLookupPort-insulation-RAG-carve-out
- **1ra evidencia**: POC poc-ai-agent-hex C2 (OLEADA 5 sub-POC #2/3)
- **Decisión**: `modules/ai-agent/infrastructure/` contains: `GeminiLLMAdapter` (implements LLMProviderPort — wraps Gemini SDK; FIRST LLMProviderPort implementation in repo) + 5 legacy adapters: `legacy-accounts.adapter.ts` (implements AccountsLookupPort via PrismaAccountsRepo — SOLE insulation point for accounting lookup in ai-agent; find-accounts.ts + parse-operation.ts consume the port, NOT direct PrismaAccountsRepo), `legacy-contacts.adapter.ts` (ContactsLookupPort), `legacy-fiscal-periods.adapter.ts` (FiscalPeriodPort), `legacy-pricing.adapter.ts` (PricingPort), `legacy-rag.adapter.ts` (AgentContextPort — bridges features/documents/rag/server; RAG carve-out canonical home). RAG carve-out invariant: `AgentContext` now lives in `modules/ai-agent/infrastructure/legacy-rag.adapter.ts` (NOT features/ai-agent/agent.context.ts which was deleted).
- **Cementado**: C2 RED `9df51130` · GREEN `8f84eb64`. 11α PASS sentinels (6 infra files + R5 absoluta). `legacy-accounts.adapter.ts` LOCKED as sole accounting insulation point — future POCs must NOT re-introduce direct PrismaAccountsRepo imports in ai-agent application or tools layers.

### §13.ai-agent-presentation dual-barrel-D5-use-client-server-only-composition-root
- **1ra evidencia**: POC poc-ai-agent-hex C3 (OLEADA 5 sub-POC #2/3)
- **Decisión**: `modules/ai-agent/presentation/` — FIRST hex with dual-barrel D5 (Next.js 16.2.1 doc-verified): `server.ts` (`import "server-only"` + `analyzeDocument` + `makeAgentService` factory) + `index.ts` (`"use client"` + `useAgentQuery` React hook barrel). Client barrel uses `"use client"` directive (NOT `import "server-only"`). Axis-distinct from all prior hex modules which have server barrel only or no client barrel. `composition-root.ts` wires AgentService with all 6 adapters. `makeAgentService()` zero-arg factory (same as dispatch zero-arg pattern vs organizations callback-param pattern).
- **Cementado**: C3 RED `a37f2c42` · GREEN `c16510ac`. 21α PASS sentinels (dual-barrel shape + composition root + D5 invariants). `"use client"` client barrel pattern LOCKED — first precedent for AI/realtime modules with React hooks. D5 dual-barrel convention documented per PRE-C3 Next.js 16.2.1 doc read.

### §13.ai-agent-c4-cutover-import-type-systematic-pattern-C4-drift-deferred-tests
- **1ra evidencia**: POC poc-ai-agent-hex C4 (OLEADA 5 sub-POC #2/3)
- **Decisión**: C4 consumer cutover (14 consumers: 5 runtime + 4 vi.mock + 5 type-only). Systematic `import type { X }` pattern discovered: all hex modules use type-only imports for cross-boundary factory references (e.g., `import type { makeContactsService } from "@/modules/contacts/presentation/server"`) — only importing at runtime via dynamic `import()` in server components. **CRITICAL**: All sentinel regexes matching hex factory imports MUST accept `(?:type\s+)?` optional qualifier. Pre-existing regex `^import \{ makeX \} from ...` will FALSE-FAIL for hex-module consumers. C4 drift: C1 zero-arg→deps-object ctor break makes all 21 legacy test files impossible to `git mv`; all deferred to C5 wholesale delete.
- **Cementado**: C4 RED `a73c5978` · GREEN `ccab3a77`. 16α PASS sentinels. `import type` pattern for cross-boundary factories LOCKED as systematic hex convention. [[import_type_systematic_hex_pattern]] NEW invariant.

### §13.ai-agent-c5-wholesale-delete-54α-closed-POC-cascade-3rd-category-NEW-invariant
- **1ra evidencia**: POC poc-ai-agent-hex C5 (OLEADA 5 sub-POC #2/3 — FULL CLOSURE)
- **Decisión**: `features/ai-agent/` directory DELETED wholesale (51 files — 16 top-level + 3 llm/ + 4 modes/ + 3 pricing/ + 4 tools/ + 19 __tests__/ + 1 llm/__tests__/ + 1 pricing/__tests__/). LARGEST wholesale delete in repo (51 files vs dispatch 15). C5 scope: 54α (51 FILE-STILL-EXISTS + 1 REQ-001 git grep + 2 runtime path coverage). 10 closed-POC sentinel cascade fixed atomically in GREEN: farm ×2, lot ×3, expense ×1, documents ×1, rag ×2, accounting ×1, contacts ×3 (across 8 sentinel files). **3rd sentinel-cascade category** beyond runtime-consumer and vi.mock-target: **readFileSync against features/X/ paths in closed-POC structural sentinels**. SHIMs NOT required. `+2 FAIL delta` above 71 baseline (73 total) confirmed pre-existing disjoint failures in contacts c4-bis T7/T8.
- **Cementado**: C5 RED `e8b1565c` · GREEN `f84edceb` · D1 (this commit). 54α PASS (51+1+2). NEW INVARIANT LOCKED: [[closed_poc_sentinel_cascade_readFileSync]] — when wholesale deleting features/X/, grep ALL closed-POC sentinels for `readFileSync.*features/X/` patterns PROJECT-scope (not just runtime consumers + vi.mock). Third mandatory grep axis per [[retirement_reinventory_gate]] extension. **OLEADA 5: 2/3 CLOSED**.

## §13.accounting-financial-statements — Canonical Homes Financial Statements hex (NEW poc-financial-statements-hex — OLEADA 5 sub-POC #3/3 — WAVE COMPLETE)

### §13.accounting-financial-statements-domain 2-ports-narrow-surface-R1-permissible-value-type-exception-money.utils-Prisma.Decimal-RUNTIME
- **1ra evidencia**: POC poc-financial-statements-hex C0 (OLEADA 5 sub-POC #3/3)
- **Decisión**: `modules/accounting/financial-statements/domain/` contains 2 port interfaces: `FinancialStatementsQueryPort` (4 methods narrow surface: findFiscalPeriod, findAccountBalances, findAccountsWithSubtype, aggregateJournalLinesInRange) + `AccountSubtypeLabelPort` (R7 single-method formatSubtypeLabel). Plus types (BalanceSheet, IncomeStatement, BalanceSheetCurrent, IncomeStatementCurrent, StatementColumn, BreakdownBy, CompareWith, DatePresetId, AccountMetadata, ResolvedBalance, BuildBalanceSheetInput, MovementAggregation, BuildISInput, SubtypeGroup), VOs (StatementTableRow, SerializedColumn, SerializedBalanceSheetResponse, SerializedIncomeStatementResponse), pure calculators (retained-earnings.calculator.ts, balance-source.resolver.ts, date-presets.utils.ts, balance-sheet.builder.ts, income-statement.builder.ts), validation (Zod schemas), and **money.utils.ts** (formatBolivianAmount, roundHalfUp, sumDecimals, eq, serializeStatement). **R1 PERMISSIBLE VALUE-TYPE EXCEPTION**: `domain/money.utils.ts` imports `Prisma.Decimal` RUNTIME — documented exception with textual precedent. Rationale: `Prisma.Decimal` is a re-export of `decimal.js` (value-type arithmetic engine, like JavaScript's `Number`), NOT a Prisma-generated entity (R5 governs entity types). Textual precedent VERIFIED at `modules/shared/domain/value-objects/money.ts:4-10`: "Decimal-based monetary VO. Uses Prisma.Decimal (re-export of decimal.js). Domain does NOT expose Prisma.Decimal — only the methods on Money." R5 absoluta preserved for ENTITY types — all other domain files are TYPE-ONLY (`import type`) for Prisma.
- **Cementado**: C0 RED `3a550df5` · GREEN `bafcec65`. 42α PASS sentinels (domain types + 2 port interfaces + 5 pure calculators + money.utils R1-permissible-value-type-exception + 122 pure-function tests migrated via `git mv` at C0). **R1-permissible-value-type-exception LOCKED with textual precedent** — sole Prisma RUNTIME import in domain layer; ALL OTHER domain files MUST remain TYPE-ONLY for Prisma.

### §13.accounting-financial-statements-application FinancialStatementsService-deps-object-2-port-injection-snapshot-vs-live-paths
- **1ra evidencia**: POC poc-financial-statements-hex C1 (OLEADA 5 sub-POC #3/3)
- **Decisión**: `modules/accounting/financial-statements/application/financial-statements.service.ts` — deps-object ctor pattern `constructor(deps: FinancialStatementsServiceDeps)` with 2-port injection (`FinancialStatementsQueryPort` for fiscal period + balances + movements + metadata; `AccountSubtypeLabelPort` for R7 subtype labeling via cross-module canonical home). 516 LOC orchestration: `generateBalanceSheet` + `generateIncomeStatement` with snapshot-vs-live path discrimination (closed fiscal periods use AccountBalanceRow snapshot; open periods aggregate JournalLine via aggregateJournalLinesInRange). Sister precedent: ai-agent C1 deps-object pattern. **BREAKING CHANGE vs features/.../financial-statements.service.ts**: ctor was zero-arg with optional repo param; now requires deps-object. All consumers must use `makeFinancialStatementsService()` zero-arg factory (composition root) — 4 routes migrated at C4 GREEN. [[API_breaking_change_C1_blocks_C4_test_migration]] applied: 5 service-coupled tests + 1 exporter test deferred to C5 delete.
- **Cementado**: C1 RED `4b11ba6c` · GREEN `0fe2f58c`. 12α PASS sentinels (service shape + 2-port injection + import "server-only" line 1 + deps-object ctor + R5 absoluta for entity types). Service-coupled test deferral pattern reaffirmed (sister ai-agent precedent). `import "server-only"` line 1 positional INHERITED — kept conservative.

### §13.accounting-financial-statements-infrastructure PrismaFinancialStatementsRepo-LegacyAccountSubtypeLabelAdapter-exporters-pdf-excel
- **1ra evidencia**: POC poc-financial-statements-hex C2 (OLEADA 5 sub-POC #3/3)
- **Decisión**: `modules/accounting/financial-statements/infrastructure/` contains: `PrismaFinancialStatementsRepo` (233 LOC implements FinancialStatementsQueryPort 4 methods + bulk variant `aggregateJournalLinesInRangeBulk`; RENAMED from legacy `FinancialStatementsRepository`) + `LegacyAccountSubtypeLabelAdapter` (implements AccountSubtypeLabelPort — single-line wrap of `formatSubtypeLabel as formatSubtypeLabelImpl` from `@/modules/accounting/domain/account-subtype.utils` — R7 already canonical at modules/accounting/domain; trivial 5-line adapter) + `exporters/` subdir (6 files: pdf.exporter.ts, excel.exporter.ts, sheet.builder.ts, statement-shape.ts, pdf.helpers.ts, pdf.fonts.ts — Node-runtime preserved via route `runtime = "nodejs"` config). LegacyAccountSubtypeLabelAdapter pattern: single-line fix at future poc-accounting-account-subtype-hex if ever needed (already canonical home reached).
- **Cementado**: C2 RED `d01ea335` · GREEN `e7588066`. 11α PASS sentinels (repo + adapter + 6 exporters + R5 absoluta). PrismaFinancialStatementsRepo class rename invariant LOCKED — sibling features (4 consumer files) updated at C5 GREEN per [[retirement_reinventory_gate_features_inclusion]]. Class methods unchanged (legacy zero-arg ctor compat preserved for sibling feature consumers).

### §13.accounting-financial-statements-presentation D5-INVERSE-server-only-no-client-ts-composition-root-zero-arg-factory
- **1ra evidencia**: POC poc-financial-statements-hex C3 (OLEADA 5 sub-POC #3/3)
- **Decisión**: `modules/accounting/financial-statements/presentation/` — **D5 INVERSE** of ai-agent dual-barrel: `server.ts` (`import "server-only"` line 1 POSITIONAL + `FinancialStatementsService` + `makeFinancialStatementsService` factory + Zod schemas + RUNTIME helpers `formatBolivianAmount, roundHalfUp, sumDecimals, eq, serializeStatement`) + `index.ts` (client-safe RUNTIME `serializeStatement` + `buildBalanceSheetTableRows` + `buildIncomeStatementTableRows` + TYPE-only re-exports BalanceSheet/IncomeStatement/StatementColumn/DatePresetId/BreakdownBy/CompareWith/StatementTableRow/SerializedColumn/SerializedBalanceSheetResponse/SerializedIncomeStatementResponse/BalanceSheetCurrent/IncomeStatementCurrent/SubtypeGroup) + `composition-root.ts` (`makeFinancialStatementsService()` zero-arg factory — wires PrismaFinancialStatementsRepo + LegacyAccountSubtypeLabelAdapter; sister pattern dispatch zero-arg, NOT organizations callback). **NO client.ts** (zero React hooks in consumers; dispatch-hex pattern alignment). Client-directive ABSENT from entire module — D5 INVERSE locked at C3. Surface symmetric with old features/.../server.ts (re-exports money.utils contents) — preserves consumer path stability across cutover.
- **Cementado**: C3 RED `2073f1eb` · GREEN `b8b9dcf5`. 18α PASS sentinels (composition root + server.ts line 1 positional + index.ts client-safe + REQ-002 NEGATIVE D5 INVERSE — no client.ts file + no client-directive anywhere). **D5 INVERSE pattern LOCKED** — first hex with zero React hooks; index.ts client-safe barrel suffices. Comment-string gotcha caveat: REQ-002 NEGATIVE walkSources regex scans ALL bytes including JSDoc/strings — module sources must paraphrase ("client-directive bundles") not the literal directive string.

### §13.accounting-financial-statements-c4-cutover-ai-agent-cross-module-debt-closed-23-consumers-makeFactory-mock-shape
- **1ra evidencia**: POC poc-financial-statements-hex C4 (OLEADA 5 sub-POC #3/3)
- **Decisión**: C4 consumer cutover (23 consumers: 12 prod runtime + 5 ai-agent cross-module debt + 6 vi.mock). **ai-agent CROSS-MODULE DEBT CLOSURE**: 5 files in modules/ai-agent/ (agent.service.ts + 2 modes + 2 prompts) repointed atomically from `@/features/accounting/financial-statements/*` to `@/modules/accounting/financial-statements/presentation` — closes follow-up explicitly deferred by ai-agent C4 GREEN `ccab3a77` (financial-statements POC was OLEADA 5 POC 3/3 not yet run at that time). **Mock factory shape pattern**: when consumer uses `makeFinancialStatementsService()` factory not `new FinancialStatementsService()`, vi.mock factory must return BOTH `ClassName` (for type compat) AND `makeClassName` (for runtime resolution): `class Mock {...} return { FinancialStatementsService: Mock, makeFinancialStatementsService: () => new Mock() }` — locked as cross-module-boundary-mock-target-rewrite extension. **Composition root extensions at GREEN**: presentation/server.ts added `serializeStatement` re-export (SYMMETRIC with old features/.../server.ts `export * from "./money.utils"`); presentation/index.ts added 2 RUNTIME re-exports (buildBalanceSheetTableRows + buildIncomeStatementTableRows) + 5 TYPE re-exports (SerializedBalanceSheetResponse + SerializedIncomeStatementResponse + BalanceSheetCurrent + IncomeStatementCurrent + SubtypeGroup) for ai-agent debt + page-clients.
- **Cementado**: C4 RED `03e6f6c1` · GREEN `5e854c7c`. 23α PASS sentinels (assertion-mismatch substring checks satisfied). ai-agent CROSS-MODULE DEBT explicitly CLOSED. Mock factory shape pattern LOCKED — extension of [[cross_module_boundary_mock_target_rewrite]].

### §13.accounting-financial-statements-c5-wholesale-delete-sibling-features-cutover-PRE-C4-grep-gap-closure-NEW-invariant
- **1ra evidencia**: POC poc-financial-statements-hex C5 (OLEADA 5 sub-POC #3/3 — FULL CLOSURE → WAVE COMPLETE)
- **Decisión**: `features/accounting/financial-statements/` directory DELETED wholesale (25 files — 13 top-level src + 6 exporters/ src + 5 __tests__/ + 1 exporters/__tests__/). PLUS **15 sibling-feature cutover files** (PRE-C4 grep gap closure): equity-statement (3 src + 3 tests), initial-balance (2 src), trial-balance (2 src), worksheet (2 src), iva-books (1 src), exporters/voucher-pdf (1), __tests__/contra-backfill (1 test). Sibling consumers repointed: money.utils → `@/modules/.../presentation/server` (5 files); pdf.fonts/pdf.helpers → `@/modules/.../infrastructure/exporters/` (5 files); FinancialStatementsRepository class renamed → PrismaFinancialStatementsRepo (3 files); balance-sheet.builder/income-statement.builder/retained-earnings.calculator → `@/modules/.../domain/` (3 files). **5 service-coupled features tests + 1 exporters/__tests__/pdf.helpers.test deleted** per [[API_breaking_change_C1_blocks_C4_test_migration]] — C1 ctor change blocked naive C4 test migration; -46 behavioral tests accepted loss, coverage maintained by 256α cycle sentinels + sibling integration tests still PASS.
- **Cementado**: C5 RED `e7b4f01f` · GREEN `f1f1d1a9` · D1 (this commit). 28α PASS (25 FILE-STILL-EXISTS + 1 REQ-001 + 2 runtime path coverage). **NEW INVARIANT LOCKED [[retirement_reinventory_gate_features_inclusion]]**: PRE-CN grep MUST NOT exclude `features/` paths when migrating from features/ → modules/. Sibling features in the same legacy tree are valid consumers needing cutover. Caught at delete time via tsc cascade (+25 errors); fixed atomically in C5 GREEN per [[low_cost_verification_asymmetry]]. Suggested patch to [[retirement_reinventory_gate]]: scan `':!modules/X/__tests__/'` only, not `':!features/'`. **OLEADA 5: 3/3 CLOSED (WAVE COMPLETE)** — ai-agent cross-module debt closed; financial-statements lifted to hex; umbrella poc-accounting-cleanup re-framed (8 sibling subfeatures DEFERRED to OLEADA 6 multi-sub-POC).

## §13.accounting-trial-balance — Canonical Homes Trial-Balance hex (NEW poc-accounting-trial-balance-hex — OLEADA 6 sub-POC #1/8)

### §13.accounting-trial-balance-domain 3-ports-narrow-surface-D4-sumDecimals-eq-copy-to-own-money.utils-R1-permissible-value-type-exception
- **1ra evidencia**: POC poc-accounting-trial-balance-hex C0 (OLEADA 6 sub-POC #1/8)
- **Decisión**: `modules/accounting/trial-balance/domain/` contains 1 port interface: `TrialBalanceQueryPort` (3 methods narrow surface: aggregateAllVouchers, findAccounts, getOrgMetadata). Plus types (TrialBalanceRow, TrialBalanceTotals, TrialBalanceReport, TrialBalanceFilters, Serialized*, port-companion DTOs TrialBalanceMovement/TrialBalanceAccountMetadata/TrialBalanceOrgMetadata), pure calculators (trial-balance.builder.ts, trial-balance.validation.ts), and **money.utils.ts** (sumDecimals + eq COPIED verbatim from `modules/accounting/financial-statements/domain/money.utils.ts`). **D4 Option A LOCKED**: `sumDecimals` + `eq` duplicated to own domain/money.utils.ts INSTEAD OF cross-module import. Rationale: C0 builder.ts was importing these from FS presentation/server (cross-module domain→presentation dependency violation per R5 absoluta). Option A resolves by owning the pure helpers locally (2-line duplication acceptable; consolidation deferred until ≥3 modules motivate shared extraction). **R1 PERMISSIBLE VALUE-TYPE EXCEPTION** inherited from FS sister: `domain/money.utils.ts` + `domain/trial-balance.builder.ts` + `infrastructure/prisma-trial-balance.repo.ts` import `Prisma.Decimal` RUNTIME — documented exception with textual precedent. Same rationale as FS: Prisma.Decimal is value-type arithmetic (decimal.js re-export), NOT Prisma entity type.
- **Cementado**: C0 RED `16b3a819` · GREEN `1a3bce65`. 25α PASS sentinels (domain types + 1 port interface + 2 pure calculators + money.utils copy + R1-permissible-value-type-exception + 17α pure-function tests migrated via `git mv`). **D4 Option A (copy sumDecimals + eq) LOCKED** — breaks FS cross-module domain→presentation coupling. REQ-009 CLEAN: `modules/accounting/trial-balance/domain/**` ZERO imports from `@/modules/accounting/financial-statements/**`.

### §13.accounting-trial-balance-application TrialBalanceService-deps-object-1-port-injection-zero-arg-factory
- **1ra evidencia**: POC poc-accounting-trial-balance-hex C1 (OLEADA 6 sub-POC #1/8)
- **Decisión**: `modules/accounting/trial-balance/application/trial-balance.service.ts` — deps-object ctor pattern `constructor(deps: TrialBalanceServiceDeps)` with 1-port injection (`TrialBalanceQueryPort`). SIMPLER than FS sister (1 port vs 2): no `AccountSubtypeLabelPort` needed — trial-balance does not break down by subtype label. 247 LOC orchestration: `generateTrialBalance` single-method service (vs FS multi-statement). Ctor refactor: legacy zero-arg with optional repo param → requires deps-object. All consumers must use `makeTrialBalanceService()` zero-arg factory (composition root) — 1 runtime consumer (route.ts) migrated at C4 GREEN. [[API_breaking_change_C1_blocks_C4_test_migration]] applied: service.test.ts deferred to C5 delete.
- **Cementado**: C1 RED `e6e26f56` · GREEN `47b98180`. 12α PASS sentinels (service shape + 1-port injection + deps-object ctor + R5 absoluta for entity types). Service-coupled test deferral pattern reaffirmed (inherited from FS sister). NO `import "server-only"` in application (spec REQ-005 NEGATIVE — application layer pure, server-only only at presentation).

### §13.accounting-trial-balance-infrastructure PrismaTrialBalanceRepo-exporters-pdf-xlsx-cross-module-infra-import-flagged-D7-tech-debt
- **1ra evidencia**: POC poc-accounting-trial-balance-hex C2 (OLEADA 6 sub-POC #1/8)
- **Decisión**: `modules/accounting/trial-balance/infrastructure/` contains: `PrismaTrialBalanceRepo` (implements TrialBalanceQueryPort 3 methods: aggregateAllVouchers, findAccounts, getOrgMetadata; 212 LOC with raw SQL CTEs for debit/credit aggregation) + `exporters/` subdir (2 files: trial-balance-pdf.exporter.ts, trial-balance-xlsx.exporter.ts). **D7 Option A LOCKED** (tech debt): `infrastructure/exporters/trial-balance-pdf.exporter.ts` imports `registerFonts`, `pdfmakeRuntime`, `fmtDecimal` from `@/modules/accounting/financial-statements/infrastructure/exporters/` — cross-module INFRA→INFRA dependency TOLERATED as transitional. **REQ-010 tech debt flagged**: extraction deferred to **sub-POC 6 (poc-accounting-exporters-cleanup)** — will consolidate shared pdf utilities (pdf.fonts + pdf.helpers) to `modules/accounting/shared/infrastructure/exporters/` or equivalent. Rationale: cross-module DOMAIN→PRESENTATION (D4/Option A) is NOT tolerated; cross-module INFRA→INFRA (D7/Option A) is tolerated AS TRANSITIONAL with explicit tech-debt flag and deferred POC scheduling. Different layer, different tolerance. All 3 exporter dependencies documented in C2 GREEN commit body per [[mock_hygiene_commit_scope]].
- **Cementado**: C2 RED `96acb983` · GREEN `5c747026`. 11α PASS sentinels (repo + 2 exporters + R5 absoluta for entity types). PrismaTrialBalanceRepo class name invariant LOCKED (no sibling consumers yet — FS sibling features cutover was FS-specific due to pre-existing imports). REQ-010 carries forward as RESIDUAL to sub-POC 6.

### §13.accounting-trial-balance-presentation D5-INVERSE-server-only-no-client-ts-composition-root-zero-arg-factory
- **1ra evidencia**: POC poc-accounting-trial-balance-hex C3 (OLEADA 6 sub-POC #1/8)
- **Decisión**: `modules/accounting/trial-balance/presentation/` — **D5 INVERSE** (exact mirror of FS sister C3): `server.ts` (`import "server-only"` line 1 POSITIONAL + TrialBalanceService + makeTrialBalanceService factory + Zod schema + 2 exporters re-exports) + `index.ts` (client-safe TYPE-only re-exports TrialBalanceReport/TrialBalanceRow/TrialBalanceTotals/TrialBalanceFilters/SerializedTrialBalanceResponse) + `composition-root.ts` (`makeTrialBalanceService()` zero-arg factory — wires PrismaTrialBalanceRepo). **NO client.ts** (zero React hooks in consumers). Client-directive ABSENT from entire module — D5 INVERSE locked at C3. Simpler than FS sister (no serializeStatement re-export needed at C4; route.ts imports schema + exporters directly from server.ts). [[paired_sister_default_no_surface]] applied — no surface needed beyond explicit D5 INVERSE confirmation.
- **Cementado**: C3 RED `5aa9281a` · GREEN `2a50c2ca`. 18α PASS sentinels (composition root + server.ts line 1 positional + index.ts client-safe + REQ-002 NEGATIVE D5 INVERSE — no client.ts file + no client-directive anywhere). **D5 INVERSE pattern confirmed inherited** — axis-distinct from FS on composition-root surface (TB simpler: no extension at C4 needed).

### §13.accounting-trial-balance-c4-cutover-1-runtime-3-vi-mock-atomic-rewrite-23-sentinels
- **1ra evidencia**: POC poc-accounting-trial-balance-hex C4 (OLEADA 6 sub-POC #1/8)
- **Decisión**: C4 consumer cutover (1 runtime consumer + 3 vi.mock test declarations in route.test.ts): `app/api/organizations/[orgSlug]/trial-balance/route.ts` repointed from 4 features/ imports to single `@/modules/accounting/trial-balance/presentation/server` import block + NEW separate import `PrismaTrialBalanceRepo` from infrastructure (unique to TB — route.ts needs direct repo reference for getOrgMetadata PDF/XLSX metadata, whereas FS sibling routes use only service factory). **[[cross_module_boundary_mock_target_rewrite]] applied**: route.test.ts 3 vi.mock declarations repointed atomically: `@/modules/accounting/trial-balance/presentation/server` + `@/modules/accounting/trial-balance/infrastructure/exporters/trial-balance-pdf.exporter` + `@/modules/accounting/trial-balance/infrastructure/exporters/trial-balance-xlsx.exporter` + NEW `@/modules/accounting/trial-balance/infrastructure/prisma-trial-balance.repo`. **FS closed-POC sentinel PASS** [[low_cost_verification_asymmetry]]: financial-statements C4 sentinel reads route.ts imports — FS sentinel assertions remain PASS post-TB-C4 (route.ts still imports serializeStatement from FS, NOT broken by TB cutover).
- **Cementado**: C4 RED `52288f52` · GREEN `e99f9c0a`. 23α PASS sentinels (assertion-substring checks satisfied). 1 runtime consumer CLEAN. **Axis-distinct from FS C4**: no composition-root surface extension needed (TB server.ts pre-exports everything route.ts consumes). REQ-001 VERIFIED: 0 production imports @/features/accounting/trial-balance (sentinel α80 PASS).

### §13.accounting-trial-balance-c5-wholesale-delete-no-sibling-consumers-clean-0-cascade-28-sentinels
- **1ra evidencia**: POC poc-accounting-trial-balance-hex C5 (OLEADA 6 sub-POC #1/8 — CLOSURE)
- **Decisión**: `features/accounting/trial-balance/` directory DELETED wholesale (7 src + 7 tests + 2 exporters + 2 exporters tests = 18 files / 2,814 LOC). **CLEAN DELETE — NO sibling consumers** (axis-distinct from FS C5 which had 15 sibling cutover sites). Reason: sibling features (equity-statement, worksheet, initial-balance, iva-books) already imported from FS money.utils at FS C5 cutover — TB was not a dependency for siblings. PRE-C5 grep enumeration [[enumerated_baseline_failure_ledger]]: all 12 file-exists assertions pass; 4 test files already git mv'd at C0/C2; no readFileSync sentinel cascade needed (FS sibling cascade was FS-specific pre-existing import pattern). **[[retirement_reinventory_gate_features_inclusion]] confirmed**: TB sibling features need ZERO repoint (cleaner than FS predecessor). **Behavioral test loss**: service.test.ts (7α) + repository.test.ts (9α) + integration.test.ts (4α) = 20α deleted per [[API_breaking_change_C1_blocks_C4_test_migration]] — C1 ctor change blocked test migration; coverage maintained by 117α cycle sentinels + sibling integration tests (route.test still PASS post-cutover).
- **Cementado**: C5 RED `90f4b82f` · GREEN `12f85a44` · D1 (this commit). 28α PASS (12 FILE-STILL-EXISTS + 1 REQ-001 + 15 integration path coverage). **Sub-POC sibling-consumer complexity AXIS-DISTINCT from FS**: TB withdrawal CLEAN; FS withdrawal required 15 cutover sites. Pattern established: pure-data sub-POCs (trial-balance, equity-statement, worksheet, initial-balance, iva-books per OLEADA 6 pairing) tolerate clean deletes IF predecessors already lifted shared domain patterns (money.utils via FS C5 cutover). **OLEADA 6: 1/8 CLOSED** — trial-balance hex lifted + tests migrated + consumers cutover atomic + features/trial-balance/ deleted. REQ-010 tech debt (cross-module INFRA imports) flagged for sub-POC 6 resolution.

## §13.accounting-equity-statement — Canonical Homes Equity-Statement hex (NEW poc-accounting-equity-statement-hex — OLEADA 6 sub-POC #2/8)

### §13.accounting-equity-statement-domain 6-ports-narrow-surface-D4-sumDecimals-eq-copy-to-own-money.utils-R1-permissible-value-type-exception
- **1ra evidencia**: POC poc-accounting-equity-statement-hex C0 (OLEADA 6 sub-POC #2/8)
- **Decisión**: `modules/accounting/equity-statement/domain/` contains 1 port interface: `EquityStatementQueryPort` (6 methods narrow surface: getPatrimonioBalancesAt, getTypedPatrimonyMovements, getAperturaPatrimonyDelta, findPatrimonioAccounts, getOrgMetadata, isClosedPeriodMatch — LARGER than TB's 3 methods). Plus types (Patrimony*, EquityStatement*, EquityStatementFilters, Serialized*, port-companion DTOs), pure calculators (equity-statement.builder.ts, equity-statement.validation.ts), and **money.utils.ts** (sumDecimals + eq COPIED verbatim — D4 Option A inherited). **D4 Option A LOCKED** (inherited): `sumDecimals` + `eq` duplicated to own domain/money.utils.ts INSTEAD OF cross-module import (now 3 modules: FS original + TB copy + ES copy — consolidation CANDIDATE at sub-POC 3 proposal). **R1 PERMISSIBLE VALUE-TYPE EXCEPTION** inherited: `domain/money.utils.ts` + `domain/equity-statement.builder.ts` + `infrastructure/prisma-equity-statement.repo.ts` import `Prisma.Decimal` RUNTIME — documented exception, Prisma.Decimal is value-type arithmetic NOT entity type.
- **Cementado**: C0 RED `6c923368` · GREEN `386021f6`. 25α PASS sentinels (domain types + 1 port interface + 2 pure calculators + money.utils copy + R1-permissible-value-type-exception + 17α pure-function tests migrated via `git mv`). **D4 Option A (copy sumDecimals + eq) LOCKED** — breaks FS cross-module domain→presentation coupling. REQ-009 CLEAN: `modules/accounting/equity-statement/domain/**` ZERO imports from `@/modules/accounting/financial-statements/**`.

### §13.accounting-equity-statement-application EquityStatementService-deps-object-2-port-injection-IncomeStatementSourcePort-NEW-D9-D10
- **1ra evidencia**: POC poc-accounting-equity-statement-hex C1 (OLEADA 6 sub-POC #2/8)
- **Decisión**: `modules/accounting/equity-statement/application/equity-statement.service.ts` — deps-object ctor pattern `constructor(deps: EquityStatementServiceDeps)` with **2-port injection** — **AXIS-DISTINCT from TB's 1-port**: (a) `EquityStatementQueryPort` (6 methods repo narrowing), (b) `IncomeStatementSourcePort` (NEW, 2-method application-layer port — **D9 Option A LOCKED**). **D9 Option A**: `IncomeStatementSourcePort` returns RAW data (findAccountsWithSubtype, aggregateJournalLinesInRange) — equity-statement service calls FS domain pure functions (buildIncomeStatement, calculateRetainedEarnings) DIRECTLY after receiving raw data. Rationale: testable (port returns data; unit test injects mock port + calls real FS pure functions separately); hex-clean (application imports domain contracts, not infra); FS domain pure functions stable contract. Composition root injects both adapters: `PrismaEquityStatementRepo` + `PrismaIncomeStatementSourceAdapter`. **D10 LOCKED (NEW)**: cross-module DOMAIN→DOMAIN imports of FS pure functions TOLERATED at application layer (buildIncomeStatement, calculateRetainedEarnings from `@/modules/accounting/financial-statements/domain/**`). JSDoc lock required at import site: `// @domain-cross-module: FS pure functions tolerated per D10`. Different from D4 DOMAIN→PRESENTATION (not tolerated) — D10 allows DOMAIN→DOMAIN because pure functions carry zero infra/side-effect coupling.
- **Cementado**: C1 RED `5c2f0898` · GREEN `486efe2d`. 14α PASS sentinels (service shape + 2-port injection + D9 IncomeStatementSourcePort existence + D10 JSDoc lock at FS domain imports + REQ-011 NEGATIVE sentinel for ZERO FS PRESENTATION/INFRA imports in application). NO `import "server-only"` in application (REQ-005 NEGATIVE).

### §13.accounting-equity-statement-infrastructure PrismaEquityStatementRepo-PrismaIncomeStatementSourceAdapter-exporters-pdf-xlsx-D7-tech-debt-D9-D10-axis-distinct
- **1ra evidencia**: POC poc-accounting-equity-statement-hex C2 (OLEADA 6 sub-POC #2/8)
- **Decisión**: `modules/accounting/equity-statement/infrastructure/` contains: **PrismaEquityStatementRepo** (implements EquityStatementQueryPort 6 methods; 380 LOC with patrimony/movement aggregation + equity calculation orchestration) + **PrismaIncomeStatementSourceAdapter** (NEW, thin wrapper implementing IncomeStatementSourcePort — **D9 companion**; delegates findAccountsWithSubtype + aggregateJournalLinesInRange to PrismaFinancialStatementsRepo; ~80 LOC) + `exporters/` subdir (2 files: equity-statement-pdf.exporter.ts, equity-statement-xlsx.exporter.ts). **D7 Option A LOCKED** (inherited, tech debt): `infrastructure/exporters/equity-statement-pdf.exporter.ts` imports `registerFonts`, `pdfmakeRuntime`, `fmtDecimal` from `@/modules/accounting/financial-statements/infrastructure/exporters/` — cross-module INFRA→INFRA dependency TOLERATED as transitional. **REQ-012 (NEW)**: IncomeStatementSourcePort must exist at `modules/accounting/equity-statement/application/income-statement-source.port.ts` with exactly 2 methods (findAccountsWithSubtype, aggregateJournalLinesInRange); adapter must exist at `modules/accounting/equity-statement/infrastructure/prisma-income-statement-source.adapter.ts`. Sentinel at C2 validates port + adapter existence + method names.
- **Cementado**: C2 RED `0ad99fd0` · GREEN `f031a966`. 13α PASS sentinels (repo + adapter + 2 exporters + R5 absoluta for entity types + REQ-012 port/adapter existence + D9/D10 axis-distinct). PrismaEquityStatementRepo + PrismaIncomeStatementSourceAdapter class names LOCKED. REQ-010 carries forward as RESIDUAL to sub-POC 6.

### §13.accounting-equity-statement-presentation D5-INVERSE-server-only-no-client-ts-2-adapter-composition-root
- **1ra evidencia**: POC poc-accounting-equity-statement-hex C3 (OLEADA 6 sub-POC #2/8)
- **Decisión**: `modules/accounting/equity-statement/presentation/` — **D5 INVERSE** (inherited): `server.ts` (`import "server-only"` line 1 POSITIONAL + EquityStatementService + makeEquityStatementService factory + Zod schema + 2 exporters re-exports) + `index.ts` (client-safe TYPE-only re-exports EquityStatementReport/EquityStatementRow/etc.) + `composition-root.ts` (makeEquityStatementService() zero-arg factory — **wires 2 adapters** per D9: PrismaEquityStatementRepo + PrismaIncomeStatementSourceAdapter). **NO client.ts** (zero React hooks — verified explore #2299). Client-directive ABSENT — D5 INVERSE locked. **Axis-distinct from TB**: 2-adapter composition root (TB has 1 adapter). [[paired_sister_default_no_surface]] applied — mirror TB structure, surface only novel D9/D10 axis-distinct.
- **Cementado**: C3 RED `c139a981` · GREEN `34ee0005`. 18α PASS sentinels (composition root + server.ts line 1 positional + index.ts client-safe + REQ-002 NEGATIVE D5 INVERSE — no client.ts + no client-directive). **2-adapter composition root pattern NEW LOCKED** — first in OLEADA 6 wave (TB = 1-adapter, ES = 2-adapter; subsequent sub-POCs inherit single-adapter unless novel ports dictate otherwise).

### §13.accounting-equity-statement-c4-cutover-1-runtime-3-vi-mock-atomic-rewrite-getOrgMetadata-absorption-23-sentinels
- **1ra evidencia**: POC poc-accounting-equity-statement-hex C4 (OLEADA 6 sub-POC #2/8)
- **Decisión**: C4 consumer cutover (1 runtime consumer + 3 vi.mock test declarations): `app/api/organizations/[orgSlug]/equity-statement/route.ts` repointed from features/ imports to `@/modules/accounting/equity-statement/presentation/server`. **AXIS-DISTINCT from TB**: C4 discover — route.ts had separate `const repo = new EquityStatementRepository()` for `getOrgMetadata()` lookup (NOT delegated through service). Option A resolution: absorbed getOrgMetadata into service.generate() orchestration at C4 GREEN. Composition root now exports factory ONLY; route.ts drops direct repo instantiation. **[[cross_module_boundary_mock_target_rewrite]] applied**: route.test.ts 3 vi.mock declarations repointed atomically: `@/modules/accounting/equity-statement/presentation/server` + `@/modules/accounting/equity-statement/infrastructure/exporters/equity-statement-pdf.exporter` + `@/modules/accounting/equity-statement/infrastructure/exporters/equity-statement-xlsx.exporter`. TB closed-POC sentinel PASS [[low_cost_verification_asymmetry]]: trial-balance C4 sentinel reads route.ts imports — TB sentinel assertions remain PASS post-ES-C4 (no cross-import interference).
- **Cementado**: C4 RED `356fb338` · GREEN `061d74de`. 23α PASS sentinels (assertion-substring checks satisfied + getOrgMetadata absorption verified). 1 runtime consumer CLEAN. REQ-001 VERIFIED: 0 production imports @/features/accounting/equity-statement (sentinel).

### §13.accounting-equity-statement-c5-wholesale-delete-no-sibling-consumers-clean-0-cascade-28-sentinels
- **1ra evidencia**: POC poc-accounting-equity-statement-hex C5 (OLEADA 6 sub-POC #2/8 — CLOSURE)
- **Decisión**: `features/accounting/equity-statement/` directory DELETED wholesale (9 src + 6 test + 2 exporters + 2 exporters tests = 17 files / 4,275 LOC). **CLEAN DELETE — NO sibling consumers** (axis-distinct from FS C5 which had 15 sibling cutover sites; mirroring TB clean delete pattern). Reason: sibling features (worksheet, initial-balance, iva-books) already imported from FS + TB patterns at prior cutover cycles — ES was not a dependency. PRE-C5 grep enumeration [[enumerated_baseline_failure_ledger]]: all file-exists assertions pass; test files already git mv'd at C0/C2; no readFileSync sentinel cascade needed. **[[retirement_reinventory_gate_features_inclusion]] confirmed**: ES sibling features need ZERO repoint. **Behavioral test loss**: service.test.ts + repository.test.ts + integration.test.ts deleted per [[API_breaking_change_C1_blocks_C4_test_migration]] — C1 ctor change (zero-arg → deps-object with 2 ports) blocked test migration; coverage maintained by 121α cycle sentinels + sibling integration tests (route.test still PASS).
- **Cementado**: C5 RED `3b57a9d8` · GREEN `68de447d` · D1 (this commit). 28α PASS (FILE-STILL-EXISTS + REQ-001 + integration path coverage). **Sub-POC sibling-consumer pattern CONSISTENT**: ES clean delete matches TB; pure-data mirror template confirmed for OLEADA 6 2-5 subfamily. **OLEADA 6: 2/8 CLOSED** — equity-statement hex lifted + tests migrated + consumers cutover atomic + features/equity-statement/ deleted. D9 (IncomeStatementSourcePort raw-data contract) + D10 (cross-module DOMAIN→DOMAIN tolerance) NEW LOCKED. 2-adapter composition root pattern first in wave. REQ-010 tech debt (cross-module INFRA imports) + REQ-012 NEW (IncomeStatementSourcePort + adapter existence) flagged for sub-POC 6.

## §13.accounting.worksheet — Canonical Homes Worksheet hex (NEW poc-accounting-worksheet-hex — OLEADA 6 sub-POC #3/8)

### §13.accounting.worksheet-domain 3-ports-WorksheetMovementAggregation-type-extraction-WS-D1-D4-copy-sumDecimals-eq-R1-exception
- **1ra evidencia**: POC poc-accounting-worksheet-hex C0 (OLEADA 6 sub-POC #3/8)
- **Decisión**: `modules/accounting/worksheet/domain/` contains 1 port interface: `WorksheetQueryPort` (3 methods narrow surface: findFiscalPeriod, aggregateByAdjustmentFlag, findAccountsWithDetail — EXACT mirror of TB's 3-method port). Plus types (WorksheetMovementAggregation, WorksheetAccountMetadata, WorksheetFilter, Serialized*, port-companion DTOs), pure calculators (worksheet.builder.ts, worksheet.validation.ts), and **money.utils.ts** (sumDecimals + eq COPIED verbatim — D4 Option A inherited). **WS-D1 NEW INVARIANT**: WorksheetMovementAggregation + WorksheetAccountMetadata **EXTRACTED from infrastructure to domain/types.ts at C0**. Legacy code had these types defined inside repository.ts (infra file); port interface requires domain types. Extraction mandatory at C0 — cannot defer because port interface cannot reference infra-defined types. Copy-exact shape from repository.ts to types.ts (tsc validation at C0 GREEN ensures correctness). **D4 Option A LOCKED** (inherited): `sumDecimals` + `eq` duplicated to own domain/money.utils.ts INSTEAD OF cross-module import (now 4 modules: FS original + TB copy + ES copy + WS copy — consolidation MANDATORY at sub-POC 6). **R1 PERMISSIBLE VALUE-TYPE EXCEPTION** inherited: `domain/money.utils.ts` + `domain/worksheet.builder.ts` + `infrastructure/prisma-worksheet.repo.ts` import `Prisma.Decimal` RUNTIME.
- **Cementado**: C0 RED `c5d94070` · GREEN `1b4f6521`. 25α PASS sentinels (domain types + 1 port interface + 2 pure calculators + money.utils copy + WS-D1 type extraction + R1-permissible-value-type-exception + 17α pure-function tests migrated via `git mv`). **WS-D1 (type extraction) + D4 Option A (copy sumDecimals + eq) LOCKED**. REQ-009 CLEAN: `modules/accounting/worksheet/domain/**` ZERO imports from `@/modules/accounting/financial-statements/**`.

### §13.accounting.worksheet-application WorksheetService-deps-object-1-port-injection-EXACT-mirror-TB
- **1ra evidencia**: POC poc-accounting-worksheet-hex C1 (OLEADA 6 sub-POC #3/8)
- **Decisión**: `modules/accounting/worksheet/application/worksheet.service.ts` — deps-object ctor pattern `constructor(deps: WorksheetServiceDeps)` with 1-port injection (`WorksheetQueryPort`). **EXACT mirror of TB architecture** — NO secondary port, NO IncomeStatementSourcePort (axis-distinct from ES). Service self-contained: single-method generateWorksheet orchestration. Ctor refactor: legacy zero-arg with optional repo param → deps-object. All consumers must use `makeWorksheetService()` zero-arg factory (composition root). 1 runtime consumer (route.ts) migrated at C4 GREEN. [[API_breaking_change_C1_blocks_C4_test_migration]] applied: service.test.ts deferred to C5 delete.
- **Cementado**: C1 RED `b55f6f3b` · GREEN `cde818c5`. 12α PASS sentinels (service shape + 1-port injection + deps-object ctor + R5 absoluta). Service-coupled test deferral pattern reaffirmed. NO `import "server-only"` in application (REQ-005 NEGATIVE).

### §13.accounting.worksheet-infrastructure PrismaWorksheetRepo-exporters-pdf-xlsx-cross-module-infra-import-D7-tech-debt
- **1ra evidencia**: POC poc-accounting-worksheet-hex C2 (OLEADA 6 sub-POC #3/8)
- **Decisión**: `modules/accounting/worksheet/infrastructure/` contains: `PrismaWorksheetRepo` (implements WorksheetQueryPort 3 methods: findFiscalPeriod, aggregateByAdjustmentFlag, findAccountsWithDetail) + `exporters/` subdir (2 files: worksheet-pdf.exporter.ts, worksheet-xlsx.exporter.ts). **D7 Option A LOCKED** (inherited, tech debt): `infrastructure/exporters/worksheet-pdf.exporter.ts` imports `registerFonts`, `pdfmakeRuntime`, `fmtDecimal` from `@/modules/accounting/financial-statements/infrastructure/exporters/` — cross-module INFRA→INFRA dependency TOLERATED as transitional. **REQ-010 tech debt flagged**: extraction deferred to **sub-POC 6 (poc-accounting-exporters-cleanup)** alongside shared money.utils consolidation. All 3 exporter dependencies documented in C2 GREEN commit body per [[mock_hygiene_commit_scope]].
- **Cementado**: C2 RED `cf6d31bd` · GREEN `ab494aac`. 11α PASS sentinels (repo + 2 exporters + R5 absoluta). PrismaWorksheetRepo class name LOCKED. REQ-010 carries forward as RESIDUAL to sub-POC 6.

### §13.accounting.worksheet-presentation D5-INVERSE-server-only-line1-ALREADY-PRESENT-cleanest-OLEADA6-composition-root-zero-arg-factory
- **1ra evidencia**: POC poc-accounting-worksheet-hex C3 (OLEADA 6 sub-POC #3/8)
- **Decisión**: `modules/accounting/worksheet/presentation/` — **D5 INVERSE** (inherited): `server.ts` (`import "server-only"` line 1 POSITIONAL + WorksheetService + makeWorksheetService factory + Zod schema + 2 exporters re-exports) + `index.ts` (client-safe TYPE-only re-exports WorksheetMovementAggregation/WorksheetAccountMetadata/WorksheetFilter/Serialized*) + `composition-root.ts` (`makeWorksheetService()` zero-arg factory). **NO client.ts** (zero React hooks). Client-directive ABSENT — D5 INVERSE locked. **AXIS-DISTINCT POSITIVE**: legacy `features/accounting/worksheet/server.ts` ALREADY had `import "server-only"` on line 1 (migration cleanest of OLEADA 6 so far — NO ADD needed at C3, unlike ES which required ADD at C3 GREEN). 1-adapter composition root (mirror TB, NOT 2-adapter ES).
- **Cementado**: C3 RED `980139cd` · GREEN `d2628891`. 18α PASS sentinels (composition root + server.ts line 1 positional + index.ts client-safe + REQ-002 NEGATIVE D5 INVERSE — no client.ts + no client-directive). **D5 INVERSE cleanest variant LOCKED** — server.ts pre-existing server-only import bypasses C3 refactor burden.

### §13.accounting.worksheet-c4-cutover-1-runtime-3-vi-mock-atomic-rewrite-23-sentinels
- **1ra evidencia**: POC poc-accounting-worksheet-hex C4 (OLEADA 6 sub-POC #3/8)
- **Decisión**: C4 consumer cutover (1 runtime consumer + 3 vi.mock test declarations): `app/api/organizations/[orgSlug]/worksheet/route.ts` repointed from features/ imports to `@/modules/accounting/worksheet/presentation/server`. **EXACT mirror of TB C4** — NO getOrgMetadata absorption needed (unlike ES C4 axis-distinct). **[[cross_module_boundary_mock_target_rewrite]] applied**: route.test.ts 3 vi.mock declarations repointed atomically: `@/modules/accounting/worksheet/presentation/server` + `@/modules/accounting/worksheet/infrastructure/exporters/worksheet-pdf.exporter` + `@/modules/accounting/worksheet/infrastructure/exporters/worksheet-xlsx.exporter`. FS+TB+ES closed-POC sentinels PASS [[low_cost_verification_asymmetry]]: all 3 sibling POCs' sentinels remain PASS post-WS-C4 (no cross-import interference).
- **Cementado**: C4 RED `4a7b56b2` · GREEN `cbfa03ef`. 23α PASS sentinels (assertion-substring checks satisfied). 1 runtime consumer CLEAN. REQ-001 VERIFIED: 0 production imports @/features/accounting/worksheet (sentinel).

### §13.accounting.worksheet-c5-wholesale-delete-no-sibling-consumers-clean-0-cascade-28-sentinels
- **1ra evidencia**: POC poc-accounting-worksheet-hex C5 (OLEADA 6 sub-POC #3/8 — CLOSURE)
- **Decisión**: `features/accounting/worksheet/` directory DELETED wholesale (8 src + 7 test + 2 exporters + 2 exporters tests = 15 files / 4,021 LOC). **CLEAN DELETE — NO sibling consumers** (pattern consistent with TB + ES). Reason: sibling features (initial-balance, iva-books) already import from FS + TB + ES patterns — WS not a dependency. PRE-C5 grep enumeration [[enumerated_baseline_failure_ledger]]: all file-exists assertions pass; test files git mv'd at C0/C2; no readFileSync sentinel cascade needed. **[[retirement_reinventory_gate_features_inclusion]] confirmed**: WS sibling features need ZERO repoint. **Behavioral test loss**: service.test.ts + repository.test.ts + integration.test.ts deleted per [[API_breaking_change_C1_blocks_C4_test_migration]]; coverage maintained by 117α cycle sentinels + sibling integration tests (route.test still PASS).
- **Cementado**: C5 RED `bbaaf034` · GREEN `25e0deba` · D1 (this commit). 28α PASS (FILE-STILL-EXISTS + REQ-001 + integration path coverage). **Pure-data mirror template CONFIRMED** for OLEADA 6 1-3 subfamily (TB=1, ES=2, WS=3 all clean, no sibling cascade). **OLEADA 6: 3/8 CLOSED** — worksheet hex lifted + tests migrated + consumers cutover atomic + features/accounting/worksheet/ deleted. WS-D1 (type extraction) NEW LOCKED. 4th sumDecimals copy now triggers MANDATORY consolidation in sub-POC 6. REQ-010 tech debt flagged; sumDecimals consolidation scoped sub-POC 6.

## §13.accounting.initial-balance — Canonical Homes initial-balance hex (NEW poc-accounting-initial-balance-hex — OLEADA 6 sub-POC #4/8)

### §13.accounting.initial-balance-domain 1-port-InitialBalanceQueryPort-4-methods-IB-D1-NO-type-extraction-axis-distinct-WS-D1
- **1ra evidencia**: POC poc-accounting-initial-balance-hex C0 (OLEADA 6 sub-POC #4/8)
- **Decisión**: `modules/accounting/initial-balance/domain/` contains 1 port interface: `InitialBalanceQueryPort` (4 methods narrow surface: findFiscalPeriod, getOpeningBalance, getRawInitialBalance, findAccountsWithDetail) + pure calculators (initial-balance.builder.ts, initial-balance.validation.ts) + **money.utils.ts** (sumDecimals + eq COPIED verbatim — D4 Option A inherited). **IB-D1 NEW INVARIANT: NO type extraction** (axis-distinct from WS-D1). Legacy initial-balance repo had 0 domain-level aggregation types; port interface uses application-layer dto shape (InitialBalanceEntry DTO from presentation layer). Extraction NOT mandatory — port is interface-only, DTO lives in application/presentation boundary. **IB-D4 NEW: service ctor refactor**. Legacy zero-arg ctor with optional injections → deps-object `constructor(deps: InitialBalanceServiceDeps)` port-typed injection at C1. ALL consumers updated atomically at C4 GREEN via composition root factory. **D4 Option A LOCKED** (inherited): `sumDecimals` + `eq` duplicated to own domain/money.utils.ts INSTEAD OF cross-module import (now 5 modules: FS original + TB + ES + WS + IB copies — **MANDATORY consolidation sub-POC 6**). **R1 PERMISSIBLE VALUE-TYPE EXCEPTION** inherited: `domain/money.utils.ts` + `infrastructure/prisma-initial-balance.repo.ts` import `Prisma.Decimal` RUNTIME.
- **Cementado**: C0 RED `07298ed7` · GREEN `ea900774`. 25α PASS sentinels (domain types + 1 port interface + 2 pure calculators + money.utils copy + NO IB-D1 type extraction + R1-permissible-value-type-exception + 18α pure-function tests migrated via `git mv`). **IB-D1 (NO type extraction) NEW LOCKED**. **IB-D4 (service ctor refactor) NEW LOCKED**. REQ-009 CLEAN: `modules/accounting/initial-balance/domain/**` ZERO imports from `@/modules/accounting/financial-statements/**`.

### §13.accounting.initial-balance-application InitialBalanceService-deps-object-1-port-injection-port-typed-IB-D4-ctor-refactor
- **1ra evidencia**: POC poc-accounting-initial-balance-hex C1 (OLEADA 6 sub-POC #4/8)
- **Decisión**: `modules/accounting/initial-balance/application/initial-balance.service.ts` — deps-object ctor pattern `constructor(deps: InitialBalanceServiceDeps)` with 1-port injection (`InitialBalanceQueryPort`). **EXACT mirror of TB/WS architecture** — NO secondary port, NO IncomeStatementSourcePort (axis-distinct from ES). Service single-method `generate()` orchestration, replaced legacy zero-arg ctor. **IB-D4 LOCKED**: ctor signature refactor breaking API — all 0 pre-POC tests deferred to C5 delete (service-coupled test deferral pattern per [[API_breaking_change_C1_blocks_C4_test_migration]]). All consumers must use `makeInitialBalanceService()` zero-arg factory (composition root). 1 runtime consumer (route.ts) migrated at C4 GREEN. Port interface `InitialBalanceQueryPort` is port-typed (types on method signatures) — NEW variant of port definition (vs sister TB/WS/ES ports which are plain object shape).
- **Cementado**: C1 RED `768d537b` · GREEN `db8b4a71`. 12α PASS sentinels (service shape + 1-port injection + deps-object ctor + port-typed signatures + R5 absoluta). Service-coupled test deferral pattern reaffirmed. NO `import "server-only"` in application (REQ-005 NEGATIVE).

### §13.accounting.initial-balance-infrastructure PrismaInitialBalanceRepo-exporters-pdf-xlsx-cross-module-infra-import-D7-tech-debt
- **1ra evidencia**: POC poc-accounting-initial-balance-hex C2 (OLEADA 6 sub-POC #4/8)
- **Decisión**: `modules/accounting/initial-balance/infrastructure/` contains: `PrismaInitialBalanceRepo` (implements InitialBalanceQueryPort 4 methods: findFiscalPeriod, getOpeningBalance, getRawInitialBalance, findAccountsWithDetail) + `exporters/` subdir (2 files: initial-balance-pdf.exporter.ts, initial-balance-xlsx.exporter.ts). **D7 Option A LOCKED** (inherited, tech debt): `infrastructure/exporters/initial-balance-pdf.exporter.ts` imports `registerFonts`, `pdfmakeRuntime`, `fmtDecimal` from `@/modules/accounting/financial-statements/infrastructure/exporters/` — cross-module INFRA→INFRA dependency TOLERATED as transitional. **REQ-010 tech debt flagged**: extraction deferred to **sub-POC 6 (poc-accounting-exporters-cleanup)** alongside shared money.utils consolidation + 5th sumDecimals copy resolution.
- **Cementado**: C2 RED `b88f4161` · GREEN `21ddbd3b`. 13α PASS sentinels (repo + 2 exporters + R5 absoluta + 13α tests migrated). PrismaInitialBalanceRepo class name LOCKED. REQ-010 carries forward as RESIDUAL to sub-POC 6.

### §13.accounting.initial-balance-presentation D5-INVERSE-server-only-at-C3-axis-distinct-ES-composition-root-zero-arg-factory
- **1ra evidencia**: POC poc-accounting-initial-balance-hex C3 (OLEADA 6 sub-POC #4/8)
- **Decisión**: `modules/accounting/initial-balance/presentation/` — **D5 INVERSE** (inherited): `server.ts` (InitialBalanceService + makeInitialBalanceService factory + Zod schema + 2 exporters re-exports) + `index.ts` (client-safe TYPE-only re-exports InitialBalanceEntry/InitialBalanceFilter/Serialized*) + `composition-root.ts` (`makeInitialBalanceService()` zero-arg factory). **NO client.ts** (zero React hooks). Client-directive ABSENT. **AXIS-DISTINCT from ES**: legacy `features/accounting/initial-balance/server.ts` did NOT have `import "server-only"` on line 1 (unlike WS + TB pre-existing marker). **IB server.ts required ADD at C3 GREEN** (same as ES C3 ADD pattern, but DIFFERENT from WS no-ADD variant — honest surface). 1-adapter composition root (mirror TB/WS, NOT 2-adapter ES).
- **Cementado**: C3 RED `c25a5d11` · GREEN `5f936c70`. 18α PASS sentinels (composition root + server.ts `import "server-only"` ADD at C3 + index.ts client-safe + REQ-002 NEGATIVE D5 INVERSE — no client.ts + no client-directive). **D5 INVERSE variant with ADD**. IB presentation mirrors TB/WS architectural structure (1 adapter, zero secondary ports).

### §13.accounting.initial-balance-c4-cutover-1-runtime-3-vi-mock-atomic-rewrite-23-sentinels
- **1ra evidencia**: POC poc-accounting-initial-balance-hex C4 (OLEADA 6 sub-POC #4/8)
- **Decisión**: C4 consumer cutover (1 runtime consumer + 3 vi.mock test declarations): `app/api/organizations/[orgSlug]/initial-balance/route.ts` repointed from features/ imports to `@/modules/accounting/initial-balance/presentation/server`. **EXACT mirror of TB/WS C4** — NO getOrgMetadata absorption (same as TB/WS, unlike ES C4). **[[cross_module_boundary_mock_target_rewrite]] applied**: route.test.ts 3 vi.mock declarations repointed atomically: `@/modules/accounting/initial-balance/presentation/server` + `@/modules/accounting/initial-balance/infrastructure/exporters/initial-balance-pdf.exporter` + `@/modules/accounting/initial-balance/infrastructure/exporters/initial-balance-xlsx.exporter`. FS+TB+ES+WS closed-POC sentinels PASS [[low_cost_verification_asymmetry]]: all 4 sibling POCs' sentinels remain PASS post-IB-C4 (no cross-import interference).
- **Cementado**: C4 RED `41ac0115` · GREEN `704901b1`. 23α PASS sentinels (assertion-substring checks satisfied). 1 runtime consumer CLEAN. REQ-001 VERIFIED: 0 production imports @/features/accounting/initial-balance (sentinel).

### §13.accounting.initial-balance-c5-wholesale-delete-clean-0-cascade-28-sentinels-retrofix-tsc-baseline-33-to-30
- **1ra evidencia**: POC poc-accounting-initial-balance-hex C5 (OLEADA 6 sub-POC #4/8 — CLOSURE)
- **Decisión**: `features/accounting/initial-balance/` directory DELETED wholesale (8 src + 7 test + 2 exporters + 2 exporters tests = 15 files / 3,145 LOC). **CLEAN DELETE — NO sibling consumers** (pattern consistent with TB + ES + WS). Reason: sibling features (iva-books) already import from FS + TB + ES + WS patterns — IB not a dependency of later siblings. PRE-C5 grep enumeration [[enumerated_baseline_failure_ledger]]: all file-exists assertions pass; test files git mv'd at C0/C2; no readFileSync sentinel cascade needed. **[[retirement_reinventory_gate_features_inclusion]] confirmed**: IB sibling features need ZERO repoint. **Behavioral test loss**: service.test.ts + repository.test.ts + integration.test.ts deleted per [[API_breaking_change_C1_blocks_C4_test_migration]]; coverage maintained by 148α cycle sentinels + sibling integration tests (route.test still PASS). **C5→D1 Retrofix**: C5 GREEN tsc drift 33→30 errors detected (cycle-end tsc count enumeration missed per [[low_cost_verification_asymmetry]] oversight). Retrofix `f511ad57` applied: tsc baseline restored to 30 (matching TB/ES/WS precedent). **[[cycle_end_tsc_count_enumeration_mandatory]]** NEW meta-protocol locked — all future sdd-apply cycles MUST enumerate tsc count at cycle end as low-cost verification gate before D1 transition.
- **Cementado**: C5 RED `288aa2cd` · GREEN `7baa839a` · Retrofix `f511ad57` · D1 (this commit). 28α PASS (FILE-STILL-EXISTS + REQ-001 + integration path coverage). **Cycle-end tsc baseline verification asymmetry now LOCKED** — C5→D1 boundary includes mandatory tsc count recount for all OLEADA 6 cycles 4-8. **Pure-data mirror template CONFIRMED** for OLEADA 6 1-4 subfamily (TB=1, ES=2, WS=3, IB=4 all clean, no sibling cascade). **OLEADA 6: 4/8 CLOSED** — initial-balance hex lifted + tests migrated + consumers cutover atomic + features/accounting/initial-balance/ deleted + tsc baseline restored. IB-D1 (NO type extraction) + IB-D4 (service ctor refactor) NEW LOCKED. **5th sumDecimals copy** now triggers MANDATORY consolidation in sub-POC 6 — consolidation scope extended to POC-6 charter. REQ-010 + sumDecimals + cycle-end-tsc-enumeration all scoped sub-POC 6.

## §13.accounting.iva-books — Canonical Homes IVA Books retirement+cutover (NEW poc-accounting-iva-books-hex — OLEADA 6 sub-POC #5/8)

### §13.accounting.iva-books-domain DTO-validation-TASA_IVA-dual-constant-relocation-axis-distinct-3-cycle-compression
- **1ra evidencia**: POC poc-accounting-iva-books-hex C0 (OLEADA 6 sub-POC #5/8)
- **Decisión**: `modules/iva-books/domain/` receives RELOCATION (not greenfield migration): `iva-books.types.ts` (copy-exact from features/ — IvaPurchaseBookDTO, IvaSalesBookDTO, CreatePurchaseInputDto, CreateSaleInputDto, IvaBookMonetaryFields, enums) + `iva-books.validation.ts` (copy-exact from features/ — 5 Zod schemas + 4 Dto type aliases). **ZERO import changes** (only zod dependency preserved). **IVA-D2 LOCKED**: Same-name constant duality — `TASA_IVA` coexists at 2 layers: (1) `modules/iva-books/domain/compute-iva-totals.ts:24` = `number = 0.13` (UNCHANGED, domain math — existing pre-POC), (2) `modules/iva-books/presentation/legacy-bridge-constants.ts` (NEW) = `Prisma.Decimal("0.1300")` P3.4 textual lock (relocated from features/iva-calc.utils.ts). Layer-scoped resolution: entity-to-dto bridges import Decimal from presentation/server; compute-iva-totals + domain tests import number from domain/compute-iva-totals. NO renaming (preserves P3.4 textual lock + minimizes churn). **IVA-D1 LOCKED (axis-distinct)**: `presentation/index.ts` CREATED this POC (vs sister precedent where it pre-exists). First OLEADA 6 sub-POC where presentation/index.ts is delta-NEW deliverable. Client-safe barrel: TYPE re-exports from domain/iva-books.types + schema re-exports from domain/iva-books.validation; NO server-only, NO "use client", NO TASA_IVA Decimal (Decimal requires Prisma client → server-only).
- **Cementado**: C0 RED `71c2393f` · GREEN `0f14c7c7`. 18α PASS sentinels (6 file existence + 5 type re-exports + 4 schema re-exports + 1 P3.4 textual lock + 2 client-safe barrel shape). **Axis-distinct from IB/WS/ES/TB**: 3-cycle structure vs sister 6-cycle. Justification: modules/iva-books hex pre-exists from POC #11.0c A1-A4 (50 files, 8,667 LOC, production-used) — retirement-only removes C0-C3 domain/application/infrastructure/composition-root creation cycles. **tsc baseline 34 PRESERVED** (pre-existing ambient drift 30→34; zero-drift confirmed post-POC). **IVA-D2 + IVA-D1 NEW invariants locked with C0 sentinels**.

### §13.accounting.iva-books-presentation index-ts-client-safe-legacy-bridge-constants-dual-constant-duality-server-ts-re-exports
- **1ra evidencia**: POC poc-accounting-iva-books-hex C0 (OLEADA 6 sub-POC #5/8)
- **Decisión**: `modules/iva-books/presentation/` — **D5 INVERSE** (inherited): `server.ts` (MODIFIED — add 4 export blocks: types + validation re-exports from domain; TASA_IVA Decimal from legacy-bridge-constants; exporter re-export placeholder at C0, real at C1) + `index.ts` (NEW — client-safe barrel: TYPE re-exports + schema re-exports only; NO server-only, NO "use client") + `legacy-bridge-constants.ts` (NEW — `export const TASA_IVA = new Prisma.Decimal("0.1300")` with P3.4 JSDoc lock preserved verbatim from features/iva-calc.utils.ts). **NO client.ts** (confirmed zero React hooks in iva-books module). Client-directive ABSENT. **server.ts line 1 positional `import "server-only"`** already present (PRE-existing, NO ADD needed at C3 — cleanest D5 INVERSE variant). Composition root + makeIvaBookService factory UNCHANGED (already wired at POC #11.0c).
- **Cementado**: C0 RED sentinels verify: `index.ts` exists, re-exports types+schemas, NO server-only, NO client-directive; `legacy-bridge-constants.ts` matches P3.4 regex `/export\s+const\s+TASA_IVA\s*=\s*new\s+Prisma\.Decimal\("0\.1300"\)/m`; `server.ts` line 1 = `import "server-only"` positional + re-exports include Decimal TASA_IVA + 4 type/schema blocks. **presentation/index.ts NEW client-safe barrel pattern replicated from sister precedent** (IB/WS/ES all had pre-existing index.ts; IVA-BOOKS creates it here, flagged as IVA-D1). **Dual constant duality by layer LOCKED** — separate import paths (`domain/compute-iva-totals` vs `presentation/server`) ensure zero ambiguity at type-check time.

### §13.accounting.iva-books-c1-cutover-exporter-relocation-22-consumer-atomic-rewrite-vi-mock-target-rewrite
- **1ra evidencia**: POC poc-accounting-iva-books-hex C1 (OLEADA 6 sub-POC #5/8)
- **Decisión**: C1 scope: Step 1 — `git mv features/accounting/iva-books/exporters/excel.exporter.ts` → `modules/iva-books/infrastructure/exporters/iva-book-xlsx.exporter.ts` (RENAME to clarify Excel format: xlsx suffix); `sheet.builder.ts` co-located (git mv); internal import rewrite `./sheet.builder` → `./iva-book-xlsx.sheet-builder`. Step 2 — `presentation/server.ts` ADD real exporter import (replaces C0 placeholder). Step 3 — ATOMIC cutover 22 consumers: 4 routes + 2 entity-to-dto bridges + 2 pages + 2 components + 4 cross-module (modules/purchase + modules/sale) + 6 test + 2 vi.mock (export route.test.ts files, lines 48-49). All `@/features/accounting/iva-books` → `@/modules/iva-books/presentation/{server,index}` per consumer layer (routes + entity-to-dto = server; pages + components + cross-module = index TYPE-only). **[[cross_module_boundary_mock_target_rewrite]] applied**: 2 vi.mock declarations repointed atomically from `@/features/accounting/iva-books/server` → `@/modules/iva-books/presentation/server` per [[mock_hygiene_commit_scope]] (vi.mock rewrites named in C1 commit body).
- **Cementado**: C1 RED `71e75fa6` · GREEN `44cfabc7`. 25α PASS sentinels (exporter location + 22 consumer imports verified + 2 vi.mock target rewrites + 1 REQ-001 git grep NEGATIVE project-wide [features/iva-books production imports = 0]). Cross-module consumer type imports confirmed safe (presentation/index re-exports only TYPE + schema, no runtime Decimal or server exports). Closed-POC sentinels (IB c5 + ES c5 + WS c5 + TB c5) pre-C1 verified PASS [[low_cost_verification_asymmetry]]: no cascade interference. **22-consumer atomic count EXACT** (matches exploration #2343 verification).

### §13.accounting.iva-books-c2-wholesale-delete-13-files-2-a2-sentinels-retired-collateral-71-cumulative-alpha
- **1ra evidencia**: POC poc-accounting-iva-books-hex C2 (OLEADA 6 sub-POC #5/8 — CLOSURE)
- **Decisión**: `features/accounting/iva-books/` directory DELETED wholesale (8 src + 5 test = 13 files / 680 LOC + 2,012 LOC delete context from prior features/ lifecycle). **INCLUDES collateral retirement**: 2 POC-siguiente-A2 sentinels (`legacy-class-deletion-shape.poc-siguiente-a2.test.ts` 207 LOC + `vi-mock-legacy-cleanup-shape.poc-siguiente-a2.test.ts` 126 LOC) retired (A2 era ceased post-iva-books module migration at POC #11.0c; A2 sentinels no longer valid). Behavioral test loss: `iva-books.validation.test.ts` (303 LOC) + `iva-calc.utils.test.ts` (342 LOC) deleted; no modules/iva-books migration target (validation schemas migrated domain, but test coverage presumed by modules/iva-books domain tests + route integration tests still PASS). `excel-exporter.test.ts` (354 LOC) — optional git mv to infrastructure/exporters/__tests__/ considered at apply phase, deferred as out-of-scope (spec left open; apply found NOT feasible within effort budget — test paths too coupled to features/-specific imports). **PRE-C2 grep enumeration** [[enumerated_baseline_failure_ledger]]: 0 production imports `@/features/accounting/iva-books` post-C1 (already confirmed at C1 α33). **Behavioral loss audited**: iva-calc.utils.ts `calcTotales`, `roundHalfUp` utilities have ZERO module consumers (verified explore #2343) — loss acceptable (features-only utils). A2 sentinels' coverage (P3.4 textual lock + vi.mock cleanup) migrated to NEW C0 sentinels (legacy-bridge-constants-shape + vi-mock-targets-shape at modules/iva-books/__tests__/).
- **Cementado**: C2 RED `4132ef44` · GREEN `992b3feb`. 28α PASS (13 FILE-STILL-EXISTS + 1 dir-non-existence + 14 path-grep negatives across runtime + sibling modules + components). **Cumulative 71α PASS** (18 C0 + 25 C1 + 28 C2). **Axis-distinct from IB/WS/ES/TB**: 3-cycle total (no C3-C5 domain/application/infrastructure/composition-root/sibling-consumers cycles). **tsc baseline 34 PRESERVED** (zero drift confirmed at C2 close). **5 closed-POC sentinels cascade check pre-D1** (IB c5 + ES c5 + WS c5 + TB c5 + FS c5 via trial-balance module) — all PASS [[low_cost_verification_asymmetry]]. **Coverage continuity**: NEW C0 sentinels inherit A2 invariants; domain + presentation barriers maintained (D5 INVERSE server.ts + index.ts, no client.ts); TASA_IVA dual constant locked by layer scope.
- **Cementado**: **OLEADA 6: 5/8 CLOSED** — iva-books hex leveraged (pre-existing POC #11.0c) + retirement (features/ consolidated to modules/) + consumers atomic cutover (22 exact) + wholesale delete (13 files) complete. IVA-D1 (presentation/index.ts NEW) + IVA-D2 (TASA_IVA dual constant duality) + IVA-D3 (3-cycle compression) NEW LOCKED. Inherited: D5 INVERSE, R1 Decimal in presentation (not domain), [[cycle_end_tsc_enumeration_mandatory]] gate (tsc 34 zero-drift). REQ-012 (all 12/12 PASS). Residual: sub-POCs 6, 7, 8 (exporters cleanup, journal-ledger-core, shim retirement).

## §13.accounting.shared — Canonical Homes shared infra extraction (NEW poc-accounting-exporters-cleanup — OLEADA 6 sub-POC #6/8 — CLOSED)

### §13.accounting.shared-domain money-utils-canonical-sumDecimals-eq-EX-D1-EX-D2-EX-D3-R1-exception
- **1ra evidencia**: POC poc-accounting-exporters-cleanup C0 (OLEADA 6 sub-POC #6/8)
- **Decisión**: `modules/accounting/shared/domain/money.utils.ts` — NEW canonical home for `sumDecimals` + `eq` (verbatim copy from FS domain/money.utils.ts, the richest reference). **EX-D1 LOCKED**: `modules/accounting/shared/` NEW namespace, NO barrel index. **EX-D2 LOCKED**: `shared/` is SOURCE only — MUST NOT import from any module under `modules/accounting/{financial-statements,trial-balance,equity-statement,worksheet,initial-balance}`; JSDoc + sentinel α4..α8 lock this invariant. **EX-D3 LOCKED**: D4 consolidation mandate from proposal #2357 — 5 standalone `sumDecimals`+`eq` copies (FS+TB+ES+WS+IB domain/money.utils.ts) consolidated: TB/ES/WS/IB → thin re-export shims `export { sumDecimals, eq } from "@/modules/accounting/shared/domain/money.utils"`; FS → re-exports sumDecimals+eq from shared + KEEPS 6 richer fns (roundHalfUp, formatBolivianAmount, zeroDecimal, toDecimal, isDecimal, serializeStatement). **R1-permissible-value-type-exception**: shared/domain/money.utils.ts runtime-imports `Prisma.Decimal` (value-type arithmetic engine, not entity). JSDoc R1 lock atomic. Precedent: FS archive #2282, IB design #2330 §5 (textually verified [[textual_rule_verification]]). NO barrel index (EX-D1 decision).
- **Cementado**: C0 RED `fe4cde4f` · GREEN `864e9f4f`. 10α sentinel: α1..α8 PASS at C0 GREEN (money.utils.ts existence + sumDecimals/eq exports + 5 NEGATIVE dep-direction guards); α9..α10 intentionally remain FAIL at C0 (pdf files deferred to C1 git mv — honest documented deferral).

### §13.accounting.shared-infrastructure-exporters pdf-fonts-pdf-helpers-git-mv-EX-D4-7-consumer-repoint
- **1ra evidencia**: POC poc-accounting-exporters-cleanup C1 (OLEADA 6 sub-POC #6/8)
- **Decisión**: `modules/accounting/shared/infrastructure/exporters/` — NEW canonical home for `pdf.fonts.ts` (registerFonts, pdfmakeRuntime) + `pdf.helpers.ts` (DecimalLike, fmtDecimal). **EX-D4 LOCKED**: git mv from `modules/accounting/financial-statements/infrastructure/exporters/{pdf.fonts,pdf.helpers}.ts` → shared — history preserved. NO shim at old FS-infra path (consumers updated directly). 7-consumer repoint ATOMIC: (1) `features/accounting/exporters/voucher-pdf.exporter.ts` (pdf.fonts only; file OTHERWISE UNTOUCHED per EX-D7 — sub-POC 7 git-mv's it later); (2) TB `trial-balance-pdf.exporter.ts` (pdf.fonts + pdf.helpers); (3) ES `equity-statement-pdf.exporter.ts` (pdf.fonts + pdf.helpers + JSDoc revoked); (4) WS `worksheet-pdf.exporter.ts` (pdf.fonts + pdf.helpers + JSDoc revoked); (5) IB `initial-balance-pdf.exporter.ts` (pdf.fonts + pdf.helpers + JSDoc revoked); (6) FS `pdf.exporter.ts` (`./pdf.fonts` RELATIVE → `@/modules/accounting/shared/infrastructure/exporters/pdf.fonts` ABSOLUTE). **EX-D7 LOCKED**: `features/accounting/exporters/` INTACT; voucher-pdf migration deferred sub-POC 7. **EX-D5 LOCKED**: R1 exception — shared/domain/money.utils.ts runtime-imports Prisma.Decimal (inherited, not new at C1). **C1 atomicity**: git mv + 7 repoints + 4 money.utils shims + FS re-export + 11 sentinel inversions in ONE commit — broken intermediate state if split.
- **Cementado**: C1 RED `02cf3d8c` · GREEN `451314ff`. 22α NEW c1-repoint-shape sentinel (α11..α32): 32α total shared sentinels (c0 10α + c1 22α) ALL PASS at C1 GREEN. **EX-D6 — 11 sentinel inversions** (DIVERGENCE from tasks #2361 which declared 6 — re-inventory gap; [[retirement_reinventory_gate]] miss named in GREEN commit body [[mock_hygiene_commit_scope]]): **6 REQ-010 c2/c5 inversions** (TB c2 α46, ES c2 α52, WS c2 α46, WS c5 α117, IB c2 α44, IB c5 α117 — `financial-statements\/infrastructure\/exporters` → `shared\/infrastructure\/exporters` regex swap) **+ 5 c0-domain-shape inversions** (TB α6/α7, ES α6/α7, WS α8/α9, IB α7/α8, FS α10/α11 — asserting re-export-from-shared shape instead of standalone `export function sumDecimals`/`export function eq` impl — the EX-D3 shim rewrite collided with these assertions). C0 α9/α10 also flip GREEN at C1 (git mv done). Full suite post-C1: 621 passed / 0 failed / 1 skipped, 6928 tests. tsc 0. REQ-010 CLOSED — all pdf.fonts/pdf.helpers consumers import from shared canonical path.

## §13.accounting.journal-ledger-core — Canonical Homes journal/ledger CORE fold (NEW poc-accounting-journal-ledger-core-hex — OLEADA 6 sub-POC #7/8 — CLOSED)

> **AXIS-DISTINCT from OLEADA 6 sub-POCs 1-5** (pure-data mirror, fresh hex) **and from sub-POC 6** (shared infra extraction): this is a CODE-FOLD into a PRE-EXISTING hex (`modules/accounting/` 4-layer existed before the POC). Legacy files survived additively C0–C4; C5 was the only would-be-irreversible cycle — and it was re-scoped from wholesale-delete to shim-ify (B2a). Sentinel file: `modules/accounting/presentation/__tests__/poc-journal-ledger-core-hex-shape.test.ts` (per-cycle α blocks α01–α27).

### §13.accounting.journal-ledger-core-C0 un-wrap-circular-repo-wrap-document-lifecycle-relocate-VALID_TRANSITIONS-consolidate
- **1ra evidencia**: POC poc-accounting-journal-ledger-core-hex C0 (OLEADA 6 sub-POC #7/8)
- **Decisión**: (1) hex `modules/accounting/infrastructure/` un-wrapped — no longer imports `@/features/accounting/journal.repository`; `prisma-journal-entries.repo.ts` OWNS the `journal_entries` row outright (`createWithRetryTx` retry loop + `MAX_CONTENTION_ATTEMPTS=5` + `journalIncludeLines` shape + `findById`/`findAll`/audit aggregates folded in verbatim). (2) `document-lifecycle.service.ts` (pure validators — validateTransition/Editable/DraftOnly/LockedEdit/PeriodOpen) git-mv'd → `modules/accounting/domain/document-lifecycle.ts`; payment module + hex `journals.service.ts` repointed. EX-D2 rationale: `shared/` is source-only — these validators are accounting-DOMAIN-specific, not shared. (3) `VALID_TRANSITIONS` consolidated to the single canonical `domain/value-objects/journal-entry-status.ts` (`ALLOWED`/`canTransition`); the 2 duplicates (legacy `journal.service.ts` + `document-lifecycle.service.ts`) deleted.
- **Cementado**: C0 RED `7724e6aa` · GREEN `78e5ceba`. 4α (α01–α04 + α04a–α04d behavioral-parity snapshot anchors): hex infra has NO legacy-repo import; document-lifecycle in hex domain; sole canonical VALID_TRANSITIONS; `MAX_CONTENTION_ATTEMPTS=5` + `journalIncludeLines` byte-identical pre/post-fold.

### §13.accounting.journal-ledger-core-C1 5-read-use-cases-LedgerService-JournalLedgerQueryPort (R-money DISCHARGED OLEADA 7 POC #2)
- **1ra evidencia**: POC poc-accounting-journal-ledger-core-hex C1 (OLEADA 6 sub-POC #7/8)
- **Decisión**: 5 read use cases (`list`, `getById`, `getCorrelationAudit` — gap-detection logic ported verbatim, `getLastReferenceNumber`, `getNextNumber`) added to hex `application/journals.service.ts`; NEW `application/ledger.service.ts` (`getAccountLedger` + `getTrialBalance`) — zero hex equivalent before; NEW `domain/ports/journal-ledger-query.port.ts` + `PrismaJournalLedgerQueryAdapter` + InMemory fake. **[DISCHARGED — poc-money-math-decimal-convergence C1 GREEN, OLEADA 7 POC #2]** the prior DEV-1/R-money named deviation (float `Math.round(x*100)` + `Number()` running-balance) has been RETIRED: `LedgerService` running balance + trial balance totals now use `Prisma.Decimal` (`sumDecimals` + `.minus()` chain + `roundHalfUp`) from `modules/accounting/shared/domain/money.utils`; DTOs `LedgerEntry`/`TrialBalanceRow` serialize monetary fields as `string` at the JSON boundary; UI `ledger-page-client.tsx` parses via `parseFloat`. AutoEntryGenerator balance-check converged to `eq(sumDecimals(debits), sumDecimals(credits))` at C2 GREEN. Rebuilt-reads (journals.service.ts): trivially discharged — zero float math present per [[aspirational_mocks_signal_unimplemented_contract]] (Q-NEW4 lock).
- **Cementado**: C1 RED `1600fcf1` · GREEN `b32eb963`. 9α (α05–α13 — 5 read-method declarations + LedgerService 2-method + JournalLedgerQueryPort existence + α13b/α13c float-math parity). **α13b/α13c INVERTED at poc-money-math-decimal-convergence C1 GREEN** (OLEADA 7 POC #2) — now assert Decimal convergence (no float tokens; sumDecimals import + call present).

### §13.accounting.journal-ledger-core-C2-C3 auto-entry-generator-exporters-git-mv-exportVoucherPdf-fold
- **1ra evidencia**: POC poc-accounting-journal-ledger-core-hex C2+C3 (OLEADA 6 sub-POC #7/8)
- **Decisión**: **C2** — `auto-entry-generator.ts` git-mv'd `features/accounting/` → `modules/accounting/application/auto-entry-generator.ts` (orchestrates repo+ports → application layer); 5 sale/purchase runtime consumers (factory adapter, UoWs, composition-roots) + 3 integration tests' import targets repointed ATOMICALLY in one commit; barrel `auto-entry-generator` re-export repointed. **C3** — `features/accounting/exporters/` git-mv'd → `modules/accounting/infrastructure/exporters/` (voucher-pdf.{exporter,composer,types}, logo-fetcher, amount-to-words + __tests__) — completes the EX-D7 voucher-pdf deferral declared by sub-POC 6; `exportVoucherPdf` (the 6th journal method) folded onto the hex `JournalsService` (3 ctor deps wired in composition-root). With C1's 5 reads, all 10 journal methods now have hex use cases.
- **Cementado**: C2 RED `6b55692e` · GREEN `a6b9607c` (3α α14–α16); C3 RED `7eefab28` · GREEN `452b13d3` (3α α17–α19).

### §13.accounting.journal-ledger-core-C4 accounting-validation-merge-into-hex-presentation
- **1ra evidencia**: POC poc-accounting-journal-ledger-core-hex C4 (OLEADA 6 sub-POC #7/8)
- **Decisión**: the 8 journal/ledger zod schemas from `features/accounting/accounting.validation.ts` merged INTO hex `modules/accounting/presentation/validation.ts` (which held account-only schemas from POC #3d). Legacy `accounting.validation.ts` → thin `export *` re-export shim (barrel-facing; retires with sub-POC 8).
- **Cementado**: C4 RED `f09076a4` · GREEN `1428969e`. 1α (α20 — 8 journal/ledger schema exports present in hex validation.ts).

### §13.accounting.journal-ledger-core-C5 B2a-shim-ify-NOT-wholesale-delete-sourceType-aiOriginalText-fold-lazy-import-cycle-break
- **1ra evidencia**: POC poc-accounting-journal-ledger-core-hex C5 (OLEADA 6 sub-POC #7/8 — CLOSURE)
- **Decisión**: **B2a re-scope — Marco-approved after THREE escalations, SUPERSEDES the design's "wholesale-delete"** (c5-blocker #2410 / c5-blocker-2 #2411 / c5-blocker-3 #2412). The PRE-CN re-inventory gate surfaced that the legacy `JournalService`/`LedgerService` are FULL live runtime implementations consumed by ~10 `app/` files via the `@/features/accounting/server` barrel — and the spec's three constraints (wholesale-delete the files / keep the barrel working / leave the 26 barrel consumers untouched) were JOINTLY UNSATISFIABLE: the hex exposes `JournalsService` (factory `makeJournalsService()`), NOT a `new`-able `JournalService` class, so `export { JournalService } from <hex>` is impossible. Blocker #2: `createEntry` has a divergent return shape (legacy `JournalEntryWithLines` DTO vs hex `Journal` AGGREGATE) AND the hex `CreateJournalEntryInput` lacked `sourceType`/`aiOriginalText`. Blocker #3: shim-ifying the subject orphans ~27 it() of legacy tests coupled to the stripped impl. **B2a resolution**: (1) `journal.service.ts` 661→182 LOC + `ledger.service.ts` 113→63 LOC converted into THIN DELEGATING SHIMS over the hex — class names + public signatures PRESERVED so the barrel + 26 consumers stay byte-stable; ZERO duplicated impl logic. (2) `journal.repository.ts` KEPT (blocker-3 Finding C — no `app/` runtime consumer; barrel + tests only). (3) `sourceType`/`aiOriginalText` FOLDED into the hex `CreateJournalEntryInput` + `validateAndCreateDraft` forwards them to `Journal.create` (~6 LOC — the aggregate/DTO/repo already threaded them). (4) shim `createEntry`/`createAndPost`/`updateEntry`/`transitionStatus` do a hex call then `hex.getById()` RE-HYDRATION (a pure `Journal → JournalEntryWithLines` mapper is impossible — the aggregate carries no joined relations; create+getById = byte-identical DB write + byte-identical DTO at the cost of one extra read round-trip). (5) **CYCLE BREAK**: both shims resolve the hex factory via a lazy `import("@/modules/accounting/presentation/server")` cached in a module-level promise + `import type` for the type surface — a STATIC import would make the legacy barrel transitively pull the hex wiring graph at module-init = load-order cycle with `modules/organizations` (`LegacyAccountSeedAdapter` → `makeAccountsService`); all shim methods are already `async` so the extra `await` is free. (6) 5 orphaned legacy test files deleted as TEST-CEMENTACIÓN (27 it) — explicitly allowed even though the subject survives, justified because the hex `__tests__/` already cover the behavior; 3 assertions PORTED to `journals.service.test.ts`. **The `server.ts` barrel + 26 barrel consumers are DELIBERATELY UNTOUCHED — sub-POC 8 (`poc-accounting-shim-retirement`) owns the shim retirement + consumer repoint.**
- **Cementado**: C5 RED `118a7152` · GREEN `f908f5c6` (pushed) · D1 (this commit). 7α (α21–α27 — both shims EXIST + import the hex surface + carry NO duplicated impl-logic token + are small + `sourceType`/`aiOriginalText` fold + create+getById reconciliation + sub-POC-8 out-of-scope guard: barrel `server.ts` re-exports unchanged, 10 `app/` `new JournalService()`/`new LedgerService()` consumers untouched). **27α cumulative ALL PASS** (4α C0 + 9α C1 + 3α C2 + 3α C3 + 1α C4 + 7α C5). **α13a INVERTED at C5** (RED `toContain` legacy float-math → GREEN `not.toContain` + delegates-to-hex — honest documented prior-cycle collision: the legacy file became a shim; the real parity gate moved to α13b on the hex source, unchanged). **3 external prior-cycle sentinels updated atomically in C5 GREEN** (`poc-legacy-retirement-shape` Block B+G; `poc-document-signature-config` c4 α39/α44; `poc-org-profile-hex` c4 α42/α46 — the legacy files became shims; retirement invariant still enforced, stronger, by other blocks). **Verify VERDICT: PASS WITH WARNINGS** — zero CRITICAL. WARNINGs: α21/α22 LOC sentinel thresholds relaxed RED→GREEN (`<120`/`<60` → `<200`/`<90`) to fit the actual 182/63 LOC shims — defensible (the dup-impl token regex, the REAL shim gate, was NOT weakened; 182≪661 keeps the bound meaningful) but the goalpost move was not surfaced in apply-progress; test-count variance within flaky tolerance. Full suite at D1: 618 files pass / 1 skip; 6984 tests pass / 5 skip; tsc 0; `pnpm build` GREEN (Marco-run at `f908f5c6`). **OLEADA 6: 7/8 CLOSED** — journal/ledger core folded into the hex; legacy services are thin shims awaiting sub-POC 8 retirement; DEV-1/R-money float money-math preserved verbatim (un-converged Decimal invariant flagged, not silenced); import cycle broken soundly. Residual: sub-POC 8/8 (`poc-accounting-shim-retirement` — wholesale-retire the `@/features/accounting/` barrel + repoint all consumers).

## §13.accounting.shim-retirement — Canonical Homes Shim Retirement (NEW poc-accounting-shim-retirement — OLEADA 6 sub-POC #8/8 — FINAL CLOSURE)

> **AXIS-DISTINCT from sub-POCs 1-7** (all 7 prior sub-POCs created/migrated/folded hex code): this is a CONSUMER REPOINT + BARREL SLIM + SHIM DELETE (final cleanup). The legacy `journal.service.ts` + `ledger.service.ts` became thin shims in sub-POC 7; this POC deletes them and retires all re-exports from the barrel. Sentinel file: `modules/accounting/presentation/__tests__/poc-journal-ledger-core-hex-shape.test.ts` (C4 α21–α27 inverted to assert shim-GONE). **OLEADA 6 FULLY COMPLETE 8/8 — WAVE CLOSURE**.

### §13.accounting.shim-retirement-D1 barrel-slim-3-shims-deleted-26-consumers-repointed-canonical-homes-hex-factory
- **1ra evidencia**: POC poc-accounting-shim-retirement (OLEADA 6 sub-POC #8/8 — FINAL)
- **Decisión**: (1) **3 shim files DELETED**: `features/accounting/journal.service.ts` (182 LOC thin shim, no longer needed — consumers now call hex factory directly), `features/accounting/ledger.service.ts` (63 LOC thin shim), `features/accounting/accounting.validation.ts` (thin `export *` re-export, validation schemas merged into hex in sub-POC 7). (2) **Barrel `features/accounting/server.ts` SLIMMED**: removed 3 re-exports (`export { JournalService }`, `export { LedgerService }`, `export * from "./accounting.validation"`). RETAINED: `JournalRepository` (invariant collision — runtime payment adapter + 2 tests + sentinel α26), `journal.types`, `parseEntryDate` (from `journal.dates`), `AutoEntryGenerator`, `accounting-helpers`, `correlative.utils`, `document-lifecycle` (TrimPreviewItem + validateLockedEdit for payment + confirm-trim-dialog). (3) **26 barrel consumers repointed**: 4 pages (accounting/accounting, accounting/journal/new, accounting/journal/[entryId]/edit, accounting/ledger) + 5 routes (journal/route, journal/[entryId]/route, last-reference/route, correlation-audit/route, ledger/route) + agent/route + 8 vi.mock test files → hex factory imports `makeJournalsService()`/`makeLedgerService()` from `@/modules/accounting/presentation/server`. (4) **Canonical homes**: JournalService + LedgerService = hex factory functions at `@/modules/accounting/presentation/server` (NOT legacy barrel). The hex factory is zero-arg, returns fully-wired service whose methods are `async` with IDENTICAL sigs to the legacy shims — repoint is mechanical (swap `new JournalService()` → `makeJournalsService()`; swap import path). (5) **Invariant preserved**: `journal.repository.ts` RETAINED (design decision from sub-POC 7 escalation #2412 blocker-3 Finding C — no remaining `app/` runtime consumer; barrel re-exports + test imports only; FULL retirement is deferred dedicated POC).
- **Cementado**: C0–C4 RED/GREEN cycles COMPLETE; HEAD `f274300e` CLEAN; full suite 618 files / 6998 tests PASS / 0 FAIL; tsc 0. All 26 consumers tested via existing vi.mock + integration test suite. Sentinel α21–α27 inverted (C4 phase, atomic with deletion) — assert files GONE + barrel SLIMMED. **OLEADA 6: 8/8 FULLY COMPLETE (WAVE CLOSED)**. Accounting residual: only `server.ts` barrel (32 LOC net) + `journal.repository.ts` (94 LOC) + 2 test-coupled files (65 LOC) remain in features/accounting/; no live runtime service classes or validation re-exports. Next POC (out of scope): potential dedicated journal.repository.ts retire+migrate to hex (deferred, no active consumer pressure).

## §13.accounting.payment-journal-repo-cutover — Canonical Home `JournalRepository` (NEW poc-payment-journal-repo-cutover — OLEADA 7 POC #1/2 — CLOSED)

> **AXIS-DISTINCT from OLEADA 6 sub-POC 8** (consumer-repoint + barrel-slim + service-shim delete): this POC retires the LAST `features/accounting/` runtime FILE — `journal.repository.ts` — which sub-POC 8 explicitly KEPT (blocker-3 Finding C: payment adapter consumer + 2 test consumers). Paridad verbatim verified at explore (#2433 — 14/14 methods byte-identical between legacy and hex `JournalRepository`); naive import repoint is BEHAVIORALLY SAFE. Sentinel file: `modules/accounting/presentation/__tests__/poc-journal-ledger-core-hex-shape.test.ts` (α26 inverted + α04a/α04b retired + α27 retention-list shrunk + NEW ADAPTER-IMPORT-HEX block).

### §13.accounting.payment-journal-repo-cutover-C0 atomic-cutover-adapter-repoint-test-port-barrel-slim-file-delete
- **1ra evidencia**: POC poc-payment-journal-repo-cutover C0 (OLEADA 7 POC #1/2)
- **Decisión**: **CANONICAL HOME of `JournalRepository`** = `modules/accounting/infrastructure/prisma-journal-entries.repo.ts` (SOLE; legacy `features/accounting/journal.repository.ts` DELETED). (1) `modules/payment/infrastructure/adapters/legacy-accounting.adapter.ts` import repointed: `JournalRepository` from barrel `@/features/accounting/server` → hex `@/modules/accounting/infrastructure/prisma-journal-entries.repo` (split-import — `AutoEntryGenerator` + `JournalEntryWithLines` type STAY on barrel per Q2/Q3 locks). (2) **`git mv` × 3 mocked-Prisma tests**: `features/accounting/__tests__/{get-next-number,get-next-number-concurrency,journal.repository.origin-filter}.test.ts` → `modules/accounting/infrastructure/__tests__/`; per-file import edit `@/features/accounting/journal.repository` → `@/modules/accounting/infrastructure/prisma-journal-entries.repo`; test logic untouched (mocked Prisma — no real DB; constructor injection survives the relocation unchanged because `JournalRepository extends BaseRepository(db = prisma)` is identical in both classes). (3) **Barrel `features/accounting/server.ts` SLIM**: dropped the single line `export { JournalRepository } from "./journal.repository"`; the 13 remaining re-exports intact (`journal.types`, `parseEntryDate`, `AutoEntryGenerator`, `accounting-helpers`, `document-lifecycle`, `correlative.utils`). (4) **`git rm features/accounting/journal.repository.ts`** (465 LOC). (5) Sentinel α26 INVERTED (`toBe(true)` → `toBe(false)` + describe label `"journal.repository.ts DELETED"`). (6) Sentinels α04a + α04b RETIRED — file-content snapshot anchors on now-deleted legacy file (Collision-1 Option A from design #2436; hex-side α04c + α04d retain the snapshot anchor). (7) **NEW sentinel ADAPTER-IMPORT-HEX** asserts adapter imports `JournalRepository` from `prisma-journal-entries.repo` via `^import[^;]+JournalRepository[^;]+prisma-journal-entries\.repo` regex (`^...m` anchor mirrors α01 precedent per [[red_regex_discipline]]). (8) **α27 retention-list collision** (NOT enumerated in design #2436 — surface-honest finding at GREEN execution): the sub-POC 8 C4 α27 retention sentinel asserted `server.ts` still re-exports `./journal.repository`; spec R-02 explicitly drops that re-export. The `.toMatch(/from\s+["']\.\/journal\.repository["']/)` line was paired-sister stale; removed atomically from retention list per [[paired_sister_default_no_surface]] + D5 atomic precedent. Other 4 retentions intact (journal.types, journal.dates, correlative.utils, document-lifecycle). **D5 atomicity**: all 7 sub-substeps in ONE GREEN commit per sub-POC 8 C4 GREEN `f274300e` precedent — splitting violates [[shim_retirement_signature_drift]] (adapter type annotation + barrel re-export must drop together). **Mock hygiene** per [[mock_hygiene_commit_scope]]: ZERO `vi.mock` targets on `journal.repository` (verified explore #2433 + design #2436 D3) — no mock-target rewrite needed.
- **Cementado**: C0 RED `e7babab8` · GREEN `d47d5be4`. Baseline (`c4382aaa`): 619 files / 7005 tests PASS / 5 skipped / 0 FAIL. Post-GREEN: 619 files / 7004 tests PASS / 5 skipped / 0 FAIL. **Suite delta -1 net** = α04a + α04b retired (-2) + NEW ADAPTER-IMPORT-HEX (+1). Arithmetic EXACT — zero behavioral cascade. tsc `--noEmit` exit 0. `pnpm build` GREEN (Marco-run gate). **OLEADA 7: 1/2 CLOSED**. POC #2/2 = `money-math-decimal-convergence` (DEV-1/R-money convergence — 6 rebuilt reads + LedgerService + auto-entry float `Math.round(x*100)` cents-comparison → canonical `modules/accounting/shared/domain/money.utils.ts` Decimal `sumDecimals`/`eq` — deferred per sub-POC 7 escalation, scope-limited per [[invariant_collision_elevation]] deferred-follow-up convention).

## §13.accounting.money-math-decimal-convergence — Canonical Decimal pipeline TIER 1 (NEW poc-money-math-decimal-convergence — OLEADA 7 POC #2/2 — LAST · WAVE CLOSED)

> **AXIS-DISTINCT from OLEADA 7 POC #1** (legacy file retirement): this POC discharges the **DEV-1 / R-money** TEXTUAL named deviation — converging TIER 1 float `Math.round(x*100)` money-math to `Prisma.Decimal` arithmetic in the application layer, migrating monetary DTO fields `number → string` at the JSON boundary, rippling UI consumers with a `parseFloat` adapter, and revoking JSDoc + sigma-13 R-money paragraphs atomically. Sentinel file: `modules/accounting/presentation/__tests__/poc-journal-ledger-core-hex-shape.test.ts` (α13b/α13c INVERTED at C1 GREEN — see §13.accounting.journal-ledger-core-C1 above; NEW α13d at C2 GREEN). **R-money textual deviation DISCHARGED**.

### §13.accounting.money-math-decimal-convergence-C0 roundHalfUp-shared-canonical-YAGNI-EX-D3
- **1ra evidencia**: POC poc-money-math-decimal-convergence C0 (OLEADA 7 POC #2/2)
- **Decisión**: NEW export `roundHalfUp(d: Decimal): Decimal` in `modules/accounting/shared/domain/money.utils.ts` — `d.toDecimalPlaces(2, Prisma.Decimal.ROUND_HALF_UP)` (mode 4 = away-from-zero). YAGNI per Q3 lock: only `roundHalfUp` added — no `subDecimals`/`mulDecimals` (`LedgerService` uses `.minus()` chain idiomatic; `AutoEntryGenerator` uses `eq()` already present in shared). EX-D3 dependency direction honored: shared is dependency SOURCE; FS `financial-statements/domain/money.utils.ts` keeps its verbatim 2-line copy of `roundHalfUp` (sister precedent), shared MUST NOT import FROM FS. Duplication of the 2-line function is acceptable per design #2447 D1. NEW sentinel file `modules/accounting/shared/__tests__/money.utils.shared.test.ts` — 3 assertions covering positive `.x5` half-up (2.675 → "2.68"), negative `.x5` half-away-from-zero (-2.675 → "-2.68" DRIFT declared), and exact 2-place preservation (75.5 → "75.50").
- **Cementado**: C0 RED `d3001099` · GREEN `38477d58`. 3/3 PASS. tsc exit 0.

### §13.accounting.money-math-decimal-convergence-C1 atomic-7-substep-LedgerService-Decimal-DTO-string-UI-ripple-JSDoc-sigma-revoke-α13b-α13c-invert
- **1ra evidencia**: POC poc-money-math-decimal-convergence C1 (OLEADA 7 POC #2/2)
- **Decisión**: **CANONICAL HOME of TIER 1 money-math** = `Prisma.Decimal` via `modules/accounting/shared/domain/money.utils` (`sumDecimals`, `eq`, `roundHalfUp`); **CANONICAL HOME of monetary DTO serialization** = `string` at JSON boundary (precision wire-preserved). ATOMIC 7-substep GREEN per D2 precedent (POC #1 design #2436 D5; sub-POC 8 C4 grandparent — splitting violates [[shim_retirement_signature_drift]] tsc-gate; DTO + UI must drop together to avoid tsc-red intermediates): (1) `modules/accounting/application/ledger.service.ts`: imports `sumDecimals` + `roundHalfUp` from `shared/domain/money.utils`; `getAccountLedger` builds `deltas[]` per line as `new Prisma.Decimal(line.debit).minus(new Prisma.Decimal(line.credit))`, running balance is `sumDecimals(deltas.slice(0, idx+1))`, monetary fields serialized via `roundHalfUp(...).toFixed(2)`; `getTrialBalance` primary path uses `new Prisma.Decimal(b.debitTotal).minus(b.creditTotal)` + `roundHalfUp().toFixed(2)`; fallback path identical pattern over `aggregation._sum.debit/.credit`. Type-cast via `String(value)` (port shape declares `unknown` for `Decimal` columns; Prisma.Decimal accepts string). (2) `modules/accounting/application/ledger.service.ts:25-34`: DEV-1/R-money JSDoc paragraph REVOKED, replaced with "Decimal-converged" stub atomic per [[jsdoc_atomic_revoke]]. (3) `modules/accounting/presentation/dto/ledger.types.ts`: `LedgerEntry.{debit,credit,balance}` + `TrialBalanceRow.{totalDebit,totalCredit,balance}` `number → string`. Header JSDoc cites discharge. (4) `modules/accounting/domain/ports/journal-ledger-query.port.ts` JSDoc R-money paragraph at L31-34 REVOKED (additional cite site surfaced at apply — atomic per [[jsdoc_atomic_revoke]] symmetry; spec listed 2 cite sites, this is the 3rd). (5) `components/accounting/ledger-page-client.tsx`: shadow `interface LedgerEntry` re-typed (`debit|credit|balance: string`); `formatCurrency(amount: string)` body uses `parseFloat(amount).toLocaleString("es-BO", {...})`; comparison sites (`entry.debit > 0`, `entry.credit > 0`, `entry.balance >= 0`) parseFloat-wrapped. Precision is preserved on the wire (string DTO); UI re-floats for `toLocaleString` display ONLY (D6). (6) `docs/architecture/04-sigma-13-canonical-homes.md` §13.accounting.journal-ledger-core-C1 R-money paragraph REVOKED inline — replaced with `[DISCHARGED — poc-money-math-decimal-convergence C1 GREEN, OLEADA 7 POC #2]` block atomic per [[engram_textual_rule_verification]] symmetry + [[jsdoc_atomic_revoke]] applied to architecture catalogs. (7) Sentinels α13b INVERTED (asserts Decimal arithmetic tokens present + legacy float tokens GONE) + α13c INVERTED (asserts `shared/domain/money.utils` import present + `sumDecimals(` call present) per [[invariant_collision_elevation]]; α13a (legacy file DELETED at sub-POC 8) UNTOUCHED. **Mock hygiene** per [[mock_hygiene_commit_scope]]: `modules/accounting/application/__tests__/ledger.service.test.ts` fixture migration `.toBe(75.5) → .toBe("75.50")`; `.toEqual([100, 70, 75.5]) → ["100.00", "70.00", "75.50"]`; `totalDebit 200 → "200.00"`; `totalCredit 50 → "50.00"`; `balance 150 → "150.00"`; `balance -150 → "-150.00"`; `totalDebit 80.25 → "80.25"`; `totalCredit 20 → "20.00"`; `balance 60.25 → "60.25"`; `0 → "0.00"` — named in C1 RED body. **Parity matrix** NEW `modules/accounting/application/__tests__/money-decimal-parity.test.ts` — 7 cases (D4 design table): basic sum (1.10 + 1.20 = "2.30"), repeated fractional (0.1 × 3 = "0.30"), half-up positive (2.675 → "2.68"), half-up negative (-2.675 → "-2.68" DRIFT declared away-from-zero vs JS `Math.round(-267.5) = -267`), large exact sum (10000.00 + 0.01 × 3 = "10000.03"), subtraction (100.00 - 30.00 = "70.00"), mixed running balance (100 → 70 → 75.50). **Drift declaration** per D3: negative-half exact-`.x5` divergence is the ONE case. For TIER 1 the inputs are non-negative per DB `Decimal NOT NULL CHECK >= 0`; running balance subtraction produces signed values but never on exact `.x5` boundaries; final `toFixed(2)` rounds only at the return boundary. Risk = NIL for realistic data.
- **Cementado**: C1 RED `48a13dda` · GREEN `614e703c`. α13b + α13c GREEN post-C1. Suite: 7013 PASS / 5 skip; tsc exit 0. 1 pre-existing flake (`modules/ai-agent/__tests__/c3-presentation-shape — makeAgentService factory`) unrelated to money-math per [[diagnostic_stash_gate_pattern]] — different test fails across runs, auth-mock-related.

### §13.accounting.money-math-decimal-convergence-C2 AEG-balance-check-Decimal-eq-sumDecimals-α13d-NEW
- **1ra evidencia**: POC poc-money-math-decimal-convergence C2 (OLEADA 7 POC #2/2)
- **Decisión**: `modules/accounting/application/auto-entry-generator.ts` balance-check converged — `Math.round(totalDebit*100) !== Math.round(totalCredit*100)` → `!eq(sumDecimals(resolvedLines.map(l => new Prisma.Decimal(l.debit))), sumDecimals(resolvedLines.map(l => new Prisma.Decimal(l.credit))))`. Imports `sumDecimals` + `eq` from `@/modules/accounting/shared/domain/money.utils` (EX-D3 invariant honored). `Prisma` import promoted from `type` to runtime (needed for `new Prisma.Decimal(...)`). **EntryLineTemplate.amount: number STAYS** per Q4 TIER 2 deferral (explicit OUT — would ripple to sale/purchase/dispatch/payment callers; separate future POC scope). Coercion `new Prisma.Decimal(value)` handled at the AEG boundary. **Q-NEW3 collapse** per design D8: `auto-entry-generator.test.ts` does NOT exist (design grep) — C2 fixture work VOIDED; single sentinel α13d carries the C2 contract. **NEW sentinel α13d** in `poc-journal-ledger-core-hex-shape.test.ts` mirrors α13c shape per [[paired_sister_default_no_surface]] / [[red_regex_discipline]]: asserts AEG imports from `shared/domain/money.utils`, calls `sumDecimals(`, calls `eq(`, and source does NOT contain `Math.round(*100)`.
- **Cementado**: C2 RED `8256a6a3` · GREEN `bb7cc386`. α13d GREEN. Suite: 7014 PASS / 5 skip (delta +1 = α13d added); tsc exit 0.

### §13.accounting.money-math-decimal-convergence-D1 OLEADA-7-wave-close-rebuilt-reads-trivial-discharge
- **1ra evidencia**: POC poc-money-math-decimal-convergence D1 (OLEADA 7 POC #2/2)
- **Decisión**: Unified OLEADA 7 wave-close docs commit (per Q8 lock + sub-POC 8 D1 precedent — single docs commit). Updates 01-migration-roadmap.md (OLEADA 7 CLOSED banner; POC #1 + POC #2 rows finalized), 02-current-state.md (TIER 1 R-money DISCHARGED row; LedgerEntry/TrialBalanceRow DTOs string-typed; rebuilt-reads trivially discharged note), and this file (new §13.accounting.money-math-decimal-convergence section). **Rebuilt-reads trivial discharge** per [[aspirational_mocks_signal_unimplemented_contract]] (Q-NEW4 explicit close): the sub-POC 7 R-money lock cited "6 rebuilt reads" in `journals.service.ts` as candidates for convergence; design-time grep at #2447 found `journals.service.ts` contains **ZERO float math** (no `Math.round`, no `Number()` coercion of monetary fields, no running-balance arithmetic — the read use cases project entries through DTOs without arithmetic). Therefore the rebuilt-reads clause is TRIVIALLY DISCHARGED — no work, no PR, no follow-up scope. Stated explicitly here to prevent future re-investigation.
- **Cementado**: D1 (this commit). **OLEADA 7: 2/2 FULLY COMPLETE — WAVE CLOSED**. Pending: `pnpm build` GREEN gate (Marco-run, post-D1 cementación per repo convention).

## §13.accounting.money-math-decimal-convergence-TIER2 — Canonical Decimal pipeline TIER 2 (NEW poc-tier2-money-decimal-convergence — OLEADA 8 POC #1 — R-money-tier2 DISCHARGED)

> **AXIS-DISTINCT from §13.accounting.money-math-decimal-convergence (TIER 1)**: this POC discharges the **R-money-tier2** DERIVATIVE named deviation (per [[named_rule_immutability]]) — converging TIER 2 float `Math.round(x*100)` money-math to `Prisma.Decimal` arithmetic in domain builders (`modules/sale|purchase|dispatch/domain/`) + adjacent route fallbacks (`app/api/.../{sales,purchases}/[id]/route.ts`). **SHAPE-A-mirror**: `number` DTO contract preserved at `EntryLineTemplate.debit/credit` + `ComputedDetail.lineAmount` boundary (no `string` migration ripple); `.toNumber()` at builder edge. Sentinel file: `modules/accounting/__tests__/poc-tier2-money-shape.test.ts` (4 sentinel blocks — sale + purchase + dispatch + routes; all GREEN). **R-money-tier2 textual deviation DISCHARGED**.

### §13.accounting.money-math-decimal-convergence-TIER2-R-money-tier2-textual-rule (NEW derivative rule cementación)

- **1ra evidencia**: POC poc-tier2-money-decimal-convergence (OLEADA 8 POC #1)
- **Rule (verbatim — locked at proposal phase + design textual verification)**:

```
Rule: R-money-tier2

Derived from: R-money (OLEADA 7 archive #2452 — sdd/poc-money-math-decimal-convergence/archive-report)

Scope (textual):
  Float `Math.round(x * 100) / 100` cents-arithmetic in TIER 2 builders
  and their adjacent route fallbacks:
    - modules/sale/domain/build-sale-entry-lines.ts (ingresoNeto, itAmount, exentos)
    - modules/purchase/domain/build-purchase-entry-lines.ts (gastoNeto, exentos)
    - modules/dispatch/domain/compute-line-amounts.ts (NOTA_DESPACHO + BOLETA_CERRADA lineAmount)
    - app/api/organizations/[orgSlug]/sales/[saleId]/route.ts (lineAmount fallback)
    - app/api/organizations/[orgSlug]/purchases/[purchaseId]/route.ts (lineAmount fallback)
  EXCLUDED:
    - modules/dispatch/domain/round-total.ts (cooperative-rounding helper — Math.floor/ceil
      semantic, NOT cents-precision; not within R-money intent)
    - modules/shared/domain/value-objects/monetary-amount.ts (round2 in VO — distinct
      surface; rule sister R-money-vo applies if/when retired)
    - All TIER 3 presentation formatters
    - components/sales|purchases|dispatches/*-form.tsx UI line-preview math
    - components/accounting/(create-)?journal-entry-(form|detail).tsx balance-check
      (UI-only, distinct invariant)

Why a new rule (not extension of R-money):
  R-money's textual scope locks "rebuilt reads (journals.service.ts) + folded
  LedgerService + auto-entry-generator" — i.e. TIER 1 only. Per
  [[named_rule_immutability]], named rules are immutable historical contracts:
  R-money was DISCHARGED on 2026-05-15 at HEAD 50c45d96 with that exact textual
  scope. Extending R-money's text would mutate a closed contract. R-money-tier2
  preserves genealogy via the explicit Derived-from clause.

Status: DISCHARGED OLEADA 8 POC #1 (poc-tier2-money-decimal-convergence — this entry).
```

- **Cementado**: R-money-tier2 cementación commit (this D1). R-money entry at L507-510 + L545-567 textually UNTOUCHED — both retain their POC #2 OLEADA 7 closure markers verbatim per [[named_rule_immutability]] (pure-append discipline: derivative rule is a NEW section, not an edit). Genealogy chain locked: R-money (TIER 1 archive #2452, OLEADA 7 POC #2) → R-money-tier2 (TIER 2, this POC, OLEADA 8 POC #1).

### §13.accounting.money-math-decimal-convergence-TIER2-C1 sale-builder-Decimal-S1-S2-S3-α-tier2-sale-01

- **1ra evidencia**: POC poc-tier2-money-decimal-convergence C1 (OLEADA 8 POC #1)
- **Decisión**: `modules/sale/domain/build-sale-entry-lines.ts` Decimal-internal arithmetic at 3 sites — S1 exentos `roundHalfUp(new Prisma.Decimal(importeTotal).minus(baseIvaSujetoCf)).toNumber()`; S2 ingresoNeto `roundHalfUp(new Prisma.Decimal(baseIvaSujetoCf).minus(dfCfIva)).toNumber()`; S3 itAmount `roundHalfUp(new Prisma.Decimal(importeTotal).mul("0.03")).toNumber()` (string operand `"0.03"` avoids float-binary 0.0299999... precision drift — locked detail). Imports added: `Prisma` runtime from `@/generated/prisma/client` + `roundHalfUp` from `@/modules/accounting/shared/domain/money.utils`. EntryLineTemplate local type `{ debit: number, credit: number, ... }` UNCHANGED (SHAPE-A-mirror per Q2 lock). JSDoc updated to cite R-money-tier2 discharge. NEW sentinel α-tier2-sale-01 (2 assertions — import + call presence + `Math.round(*100)` absence) per [[red_acceptance_failure_mode]] / [[red_regex_discipline]] / [[paired_sister_default_no_surface]] mirroring POC #2 α13c shape. Negative-half drift declared in POC #2 D3 — TIER 2 inputs non-negative → risk NIL (cite, no re-declare per Q8 YAGNI).
- **Cementado**: C1 RED `d1248299` · GREEN `c07194f5`. α-tier2-sale-01 GREEN. Suite delta +3 PASS (2 sentinel + 1 monthly-close intermittent recovery — flake confirmed orthogonal); 0 NEW failures; tsc 0.

### §13.accounting.money-math-decimal-convergence-TIER2-C2 purchase-builder-Decimal-P1-P2-α-tier2-purchase-01-regex-line-bound-fix

- **1ra evidencia**: POC poc-tier2-money-decimal-convergence C2 (OLEADA 8 POC #1)
- **Decisión**: `modules/purchase/domain/build-purchase-entry-lines.ts` Decimal-internal at 2 sites — P1 exentos + P2 gastoNeto (mirror C1 shape per [[paired_sister_default_no_surface]]). NEW sentinel α-tier2-purchase-01 (symmetric to sale-01). **Regex hygiene fix** (named per [[mock_hygiene_commit_scope]]): negative-match regex changed `Math\.round\([^)]*\*\s*100\)` → `Math\.round\([^\n]*\*\s*100\)` (line-bound). POC #2 α13d precedent regex `[^)]*` failed on nested-paren expressions like `Math.round((a - b) * 100)` — character class terminates at inner `)`. TIER 1 (POC #2) expressions had no nested parens, the precedent worked; TIER 2 builders DO have nested-paren expressions at S1/P1/S2/P2. Line-bound `[^\n]*` catches all expression shapes. Applied to BOTH α-tier2-sale-01 (retroactive strengthening) and α-tier2-purchase-01.
- **Cementado**: C2 RED `9b916fcc` · GREEN `1f72cf31`. α-tier2-purchase-01 GREEN; α-tier2-sale-01 still GREEN. Suite 7028 PASS / 1 FAIL (ai-agent flake out-of-domain pre-existing); 0 NEW failures; tsc 0.

### §13.accounting.money-math-decimal-convergence-TIER2-C3 dispatch-builder-Decimal-D1-D2-α-tier2-dispatch-01-roundTotal-EXCLUDED

- **1ra evidencia**: POC poc-tier2-money-decimal-convergence C3 (OLEADA 8 POC #1)
- **Decisión**: `modules/dispatch/domain/compute-line-amounts.ts` Decimal-internal at 2 sites — D1 BOLETA_CERRADA lineAmount `roundHalfUp(new Prisma.Decimal(realNetWeight).mul(d.unitPrice)).toNumber()` + D2 NOTA_DESPACHO lineAmount `roundHalfUp(new Prisma.Decimal(netWeight).mul(d.unitPrice)).toNumber()`. `modules/dispatch/domain/round-total.ts` (D3 cooperative-rounding helper, Math.floor/ceil semantic) UNTOUCHED per R-money-tier2 textual EXCLUDED scope. R5 nuance: `Prisma` runtime import is for `Prisma.Decimal` value-type only (decimal.js re-export, NOT generated entity — sister precedent `money.utils.ts:25-29` "R1-permissible-value-type-exception"). NEW sentinel α-tier2-dispatch-01 (symmetric).
- **Cementado**: C3 RED `42d6c9ce` · GREEN `a030ddb9`. α-tier2-dispatch-01 GREEN. Suite 7030 PASS / 1 FAIL (out-of-domain); 0 NEW failures; tsc 0.

### §13.accounting.money-math-decimal-convergence-TIER2-C4 route-fallbacks-R1-R2-Decimal-α-tier2-routes-01-dual-arity

- **1ra evidencia**: POC poc-tier2-money-decimal-convergence C4 (OLEADA 8 POC #1)
- **Decisión**: `app/api/organizations/[orgSlug]/{sales,purchases}/[id]/route.ts` `computeNewTotal` fallback Decimal-internal (when `d.lineAmount === undefined`) — both routes identical shape: `roundHalfUp(new Prisma.Decimal(qty).mul(unitPrice)).toNumber()`. Imports added per route: `Prisma` + `roundHalfUp`. NEW sentinel α-tier2-routes-01 covers BOTH routes (dual-arity in single describe block — 4 assertions: 2 per route × 2 routes). Q4 lock honored (routes IN-scope; UI J1/J2/J3 explicitly OUT — distinct UI balance-gate invariant).
- **Cementado**: C4 RED `3ef0d0d6` · GREEN `8682a006`. α-tier2-routes-01 GREEN. Suite 7034 PASS / 1 FAIL (out-of-domain); 0 NEW failures; tsc 0. **All 4 cycles GREEN — R-money-tier2 scope fully discharged at source-text level**.

### §13.accounting.money-math-decimal-convergence-TIER2-D1 OLEADA-8-cementación-MonetaryAmount-VO-partial-convergence-75-percent-declared

- **1ra evidencia**: POC poc-tier2-money-decimal-convergence D1 (OLEADA 8 POC #1)
- **Decisión**: D1 cementación commit (this entry) — single docs commit per Q8 lock + POC #2 D1 precedent. Updates `01-migration-roadmap.md` (OLEADA 8 POC #1 row CLOSED), `02-current-state.md` (TIER 2 R-money-tier2 DISCHARGED row with explicit ~75% convergence declaration), and this file (new §13.accounting.money-math-decimal-convergence-TIER2 section). **MonetaryAmount VO partial-convergence honest surface**: `modules/shared/domain/value-objects/monetary-amount.ts:6` `round2 = Math.round((n+EPSILON)*100)/100` REMAINS float-internal — distinct surface from TIER 2 builders' compute-time arithmetic. VO upstream feeds TIER 2 builder inputs as `number` (via `.value` accessor); convergence at TIER 2 is ~75% partial. Future POC queued: `poc-monetary-amount-vo-retirement` (TIER 2.5 / TIER 3) — distinct VO retirement scope, orthogonal to R-money-tier2 textual scope. Surface-honest declaration per OLEADA charter. **Canonical homes**: TIER 2 money-math = `Prisma.Decimal` via `modules/accounting/shared/domain/money.utils` (`roundHalfUp` helper — reused from POC #2 C0); TIER 2 DTO monetary fields = `number` at builder boundary (SHAPE-A-mirror per Q2 lock); `.toNumber()` boundary preserves `number` contract end-to-end (no UI ripple). **R-money-tier2 DISCHARGED — OLEADA 8 POC #1 CLOSED**. Pending: `pnpm build` GREEN gate (Marco-run, post-D1 per repo convention).
- **Cementado**: D1 (this commit). Genealogy chain locked: R-money (TIER 1, archive #2452, OLEADA 7) → R-money-tier2 (TIER 2, this POC, OLEADA 8). 9 commits total (4 RED + 4 GREEN + D1).

## §13.accounting.money-math-decimal-convergence-VO — Canonical Decimal pipeline VO retirement (NEW poc-monetary-amount-vo-retirement — OLEADA 8 POC #2 — R-money-vo DISCHARGED)

> **AXIS-DISTINCT from §13.accounting.money-math-decimal-convergence (TIER 1)** + **AXIS-DISTINCT from §13.accounting.money-math-decimal-convergence-TIER2**: this POC discharges the **R-money-vo** DERIVATIVE named deviation (per [[named_rule_immutability]]) — converging the internal arithmetic of the `MonetaryAmount` Value Object (`modules/shared/domain/value-objects/monetary-amount.ts`) from float `round2` cents-arithmetic to `Prisma.Decimal` arithmetic. **SHAPE-A interno**: `.value: number` public boundary PRESERVED via `this.raw.toNumber()`; 88 RUNTIME-IMPORT consumer sites UNCHANGED (no ripple). Sentinel file: `modules/accounting/__tests__/poc-monetary-amount-vo-shape.test.ts` (1 sentinel block — α-vo-01 — GREEN). **R-money-vo textual deviation DISCHARGED**.

### §13.accounting.money-math-decimal-convergence-VO-R-money-vo-textual-rule (NEW derivative rule cementación)

- **1ra evidencia**: POC poc-monetary-amount-vo-retirement (OLEADA 8 POC #2)
- **Rule (verbatim — locked at proposal phase + design textual verification)**:

```
Rule: R-money-vo

Derived from: R-money (OLEADA 7 archive #2452 — sdd/poc-money-math-decimal-convergence/archive-report)

Scope (textual):
  Float `round2` cents-arithmetic (Math.round of float * 100, divided by 100)
  INTERNAL to the MonetaryAmount Value Object:
    - modules/shared/domain/value-objects/monetary-amount.ts:
        - private `round2(n: number): number` helper (DELETED — replaced by
          `roundHalfUp` import from `@/modules/accounting/shared/domain/money.utils`)
        - `parse(input)` validation pipeline (`Number()` coercion → `new Prisma.Decimal(value)`
          try/catch + `isFinite()` + `isNegative()` + `greaterThan(MAX_VALUE)` guards)
        - `minus(other)` direct `round2` call → Decimal `.minus()` + `roundHalfUp` + `isNegative()` throw
        - `MAX_VALUE = 9_999_999_999.99` float constant → `new Prisma.Decimal("9999999999.99")`
        - `equals` / `isGreaterThan` / `isLessThan` numeric comparators routed
          through Decimal `.equals` / `.greaterThan` / `.lessThan`
        - `private raw: number` field → `private raw: Prisma.Decimal`
        - `get value(): number` getter body → `return this.raw.toNumber()` (BOUNDARY preserved)

  EXCLUDED:
    - Public VO API contract: `.value: number`, `.equals(other): boolean`,
      `.isGreaterThan(other): boolean`, `.isLessThan(other): boolean`,
      `.plus(other): MonetaryAmount`, `.minus(other): MonetaryAmount`,
      static `MonetaryAmount.of`, static `MonetaryAmount.zero` — all
      preserve external `number`/`boolean`/`MonetaryAmount` shapes via
      `.toNumber()` boundary (SHAPE-A interno). NO consumer migration.
    - Caller-side float math OUTSIDE the VO (e.g. `modules/iva-books/domain/compute-iva-totals.ts:88`
      `MonetaryAmount.of(baseImponible.value * TASA_IVA)` — caller arithmetic on
      `.value` boundary `number`, NOT VO-internal). Future POC if ever needed.
    - All TIER 3 presentation formatters (toFixed2 family, voucher-pdf, amount-to-words).
    - components/accounting/(create-)?journal-entry-(form|detail).tsx
      balance-check UI math (UI-only invariant; queued POC #3).

Why a new rule (not extension of R-money or R-money-tier2):
  R-money's textual scope locks TIER 1 (rebuilt reads + folded LedgerService +
  auto-entry-generator). R-money-tier2's textual scope locks TIER 2 builders +
  route fallbacks and EXPLICITLY EXCLUDES the VO at sigma-13 L594-595
  ("rule sister R-money-vo applies if/when retired"). Per [[named_rule_immutability]],
  both contracts are immutable historical contracts. R-money-vo preserves
  genealogy via explicit `Derived from: R-money` clause and closes the
  placeholder named by R-money-tier2.

Status: DISCHARGED OLEADA 8 POC #2 (poc-monetary-amount-vo-retirement — this entry).
```

- **Cementado**: R-money-vo cementación commit (this D1). R-money entry at L507-510 + L545-567 textually UNTOUCHED. R-money-tier2 entry at L569-642 textually UNTOUCHED. Both retain their prior closure markers verbatim per [[named_rule_immutability]] (pure-append discipline: derivative rule is a NEW section, not an edit). Genealogy chain locked: R-money (TIER 1 archive #2452, OLEADA 7 POC #2) → R-money-tier2 (TIER 2, OLEADA 8 POC #1) → R-money-vo (VO internal arithmetic, this POC, OLEADA 8 POC #2).

### §13.accounting.money-math-decimal-convergence-VO-C1 monetary-amount-Decimal-internal-α-vo-01

- **1ra evidencia**: POC poc-monetary-amount-vo-retirement C1 (OLEADA 8 POC #2)
- **Decisión**: `modules/shared/domain/value-objects/monetary-amount.ts` Decimal-internal migration — `private raw: number → Prisma.Decimal`; `round2` helper DELETED (replaced by `roundHalfUp` import from `@/modules/accounting/shared/domain/money.utils`); `parse()` mirror sister `money.ts:9-25` verbatim (try/catch + `isFinite` + `isNegative`) + MAX_VALUE guard via `d.greaterThan(MAX_DECIMAL)`; `MAX_VALUE` float `9_999_999_999.99` → `new Prisma.Decimal("9999999999.99")` constant; `.value: number` getter body `return this.raw.toNumber()` (BOUNDARY preserved per SHAPE-A interno); `plus/minus` Decimal-internal with `roundHalfUp` wrap; `equals/isGreaterThan/isLessThan` routed through `.equals/.greaterThan/.lessThan` Decimal methods. NEW sentinel α-vo-01 (2 assertions — `roundHalfUp` import + call present; legacy cents-arithmetic absent) per [[red_acceptance_failure_mode]] + [[red_regex_discipline]] + [[sentinel_regex_line_bound]] mirroring sister α-tier2-sale-01 shape verbatim. **88 RUNTIME consumer sites UNCHANGED** (explore §6 enumerated; SHAPE-A interno preserves all public surfaces). **Existing `monetary-amount.test.ts` 18 assertions PASS unchanged** — pivotal L83 `expect(a.plus(b).value).toBe(0.3)` for `0.1 + 0.2` becomes deterministically exact under Decimal (no IEEE drift); L19 `toBeCloseTo(10.01, 2)` for `MonetaryAmount.of(10.005)` PASS via `roundHalfUp` half-away-from-zero mode 4. **Caller-side float math OUT** (e.g. `iva-totals.ts:88 baseImponible.value * TASA_IVA`) per R-money-vo EXCLUDED clause — caller arithmetic post-boundary, future POC if needed.
- **Cementado**: C1 RED `348bd6f9` · GREEN `c342d4ff`. α-vo-01 GREEN. Suite delta +2 PASS (2 sentinel assertions); 0 NEW failures; tsc 0.

### §13.accounting.money-math-decimal-convergence-VO-D1 OLEADA-8-POC-2-cementación-100-percent-convergence-declared

- **1ra evidencia**: POC poc-monetary-amount-vo-retirement D1 (OLEADA 8 POC #2)
- **Decisión**: D1 cementación commit (this entry) — single docs commit per [[paired_sister_default_no_surface]] POC #1 D1 precedent. Updates `01-migration-roadmap.md` (OLEADA 8 POC #2 row CLOSED), `02-current-state.md` (L89-91 "~75% partial" → "100% (TIER 1 + TIER 2 + VO retired)" — TIER 2.5 / VO row updated), and this file (new §13.accounting.money-math-decimal-convergence-VO section). **Convergence status post-D1**: TIER 1 (R-money) DISCHARGED · TIER 2 (R-money-tier2) DISCHARGED · VO (R-money-vo) DISCHARGED. Aggregate money-math convergence = **100%** across application + domain + VO layers (TIER 3 presentation formatters orthogonal). **Canonical homes**: VO internal arithmetic = `Prisma.Decimal` via `modules/accounting/shared/domain/money.utils` (`roundHalfUp` helper); VO public API = `number`/`boolean`/`MonetaryAmount` boundary (SHAPE-A interno per Q1 lock); `.toNumber()` boundary preserves `.value: number` end-to-end (zero ripple across 88 consumers). **R-money-vo DISCHARGED — OLEADA 8 POC #2 CLOSED**. Pending: `pnpm build` GREEN gate (Marco-run, post-D1 per [[git_commit_no_verify_default]]).
- **Cementado**: D1 (this commit). Genealogy chain locked: R-money (TIER 1, archive #2452, OLEADA 7) → R-money-tier2 (TIER 2, OLEADA 8 POC #1) → R-money-vo (VO, this POC, OLEADA 8 POC #2). 3 commits total (C1 RED + C1 GREEN + D1).

## §13.accounting.pagination-journal — Canonical Homes pagination-journal (NEW poc-pagination-journal — Journal/Libro Diario pagination replication — CLOSED)

> **AXIS-DISTINCT from POC pagination-sale (pilot) + POC pagination-replication Purchase/Payment**: replication target Journal/Libro Diario module — split-port 3-touchpoint asymmetry (port + adapter + repo) vs Sale/Purchase 2-touchpoint single-port. Filters scalar/enum (no Path 2 array unification needed — Journal asymmetry vs Purchase). Sentinel file: `modules/accounting/__tests__/c1-macro-pagination-additive-shape-journal.poc-pagination-journal.test.ts` (10α existence-only, T11 shadcn drop redundant heredado).

### §13.accounting.pagination-journal-split-port-findpaginated-three-touchpoints
- **1ra evidencia**: POC poc-pagination-journal C1-MACRO GREEN `45135b82`
- **Decisión**: **CANONICAL** — split-port READ routing through `JournalLedgerQueryPort` → `PrismaJournalLedgerQueryAdapter` → `JournalRepository` requires `findPaginated` in all 3 layers (vs Sale/Purchase/Payment single-port 2-touchpoint). The adapter remains thin pass-through (parity existing 7-method delegation: list/findById/getLastReferenceNumber/getNextNumber/findForCorrelationAudit/findLinesByAccount/aggregateByAccount). Plus `InMemoryJournalLedgerQueryPort.findPaginated` fake slice — atomic same C1-MACRO commit per [[mock_hygiene_commit_scope]] (TSC gate cascade across journals.service tests).
- **Cementado**: RED `fe63d671` · GREEN `45135b82`. 3 layers + 1 fake = 4-file findPaginated cascade landed atomic. Service `listPaginated` thin delegation (legacy `list` preserved dual-method transitional).

### §13.accounting.pagination-journal-include-lines-eager-hydration-accepted-paginated-tradeoff
- **1ra evidencia**: POC poc-pagination-journal C1-MACRO GREEN `45135b82`
- **Decisión**: **ACCEPTED TRADEOFF** — `journalIncludeLines` 3-JOIN eager hydration (lines + account + contact per line + root contact + voucherType) preserved in `findPaginated`. Pagination reduces row count but each page row still carries full lines[]. At 25 entries/page × ~10 lines avg = 250 line fetches + account/contact joins. Acceptable for current scale (vs Sale/Purchase whose includes are simpler). Lazy-load lines DEFER — separate concern, not blocking. Documented as known tradeoff for future scale conversations.
- **Cementado**: RED `fe63d671` · GREEN `45135b82`. `journalIncludeLines` const unchanged in `prisma-journal-entries.repo.ts:57-64`; `findPaginated` impl includes it verbatim in the `findMany` call.

### §13.accounting.pagination-journal-origin-filter-null-check-preserve-findpaginated-shared-where-builder
- **1ra evidencia**: POC poc-pagination-journal C1-MACRO GREEN `45135b82`
- **Decisión**: **CANONICAL** — `sourceType IS NULL / IS NOT NULL` origin filter (manual=NULL, auto=NOT NULL) extracted to shared `buildJournalEntryWhere(scope, filters)` DRY helper. Mirror Purchase `buildPurchaseWhere` pattern. Preserves the legacy `findAll` filter shape EXACT — `findAll` refactored to call `buildJournalEntryWhere` (behavior unchanged, internals DRY). `findPaginated` reuses same builder. Origin filter logic isolated to one source of truth; preserves Journal-specific SQL pattern (vs Sale `statusFilter` single-value / Purchase `purchaseTypeIn` array).
- **Cementado**: RED `fe63d671` · GREEN `45135b82`. Helper at `modules/accounting/infrastructure/prisma-journal-entries.repo.ts` (function declaration private al módulo, convention 9). Behavior test coverage via legacy `findAll` integration tests + existing `journal.repository.origin-filter.test.ts` (mocked Prisma) unchanged. **Heredados matures cumulative cross-POC**: dual-method-additive-transitional 4ta · shared-presentation-carve-out 4ta · vo-generic-paginated-result-shape 4ta · shadcn drop redundant T11 4ta.

## §13.dispatch.hubservice-retirement — Canonical Homes (NEW poc-dispatch-retirement-into-sales — RETIREMENT + cross-module unified presentation — CLOSED)

### §13.dispatch.hubservice-retirement-pre-cross-module-unified-presentation
- **1ra evidencia**: POC poc-dispatch-retirement-into-sales C1 GREEN (OLEADA 8 POC #2)
- **Decisión**: **RETIRED** — `DispatchHubService` class (`modules/dispatch/presentation/hub.service.ts`, 198 LOC) DELETED. `hub.types.ts` (HubItem discriminated union + HubFilters) DELETED. `hub.validation.ts` (`hubQuerySchema`) DELETED. `/api/organizations/[orgSlug]/dispatches-hub` route DELETED (internal-only — zero external consumers confirmed per [[retirement_reinventory_gate]] 5-class classification). Supersedes prior `§13.dispatch-presentation` decision (line 266) which had documented HubService as load-bearing arch debt acceptance. Categ C++ in-memory slice anti-pattern (mathematically incorrect pagination proxy) retired.
- **Cementado**: C1 RED `24ab9db4` · GREEN `c6938639`. 4α PASS (hub.service.ts absent, hub.types.ts absent, dispatches-hub/route.ts absent, server.ts barrel re-export absent). [[paired_sister_default_no_surface]]: HubService retirement paired sister #2428 (poc-accounting-shim-retirement) applied directly; surface honest divergence — test files DELETED not rewritten per design D5 ([[cross_module_boundary_mock_target_rewrite]] N/A path).

### §13.dispatch.cross-module-direct-read-presentation-layer-no-merge-service
- **1ra evidencia**: POC poc-dispatch-retirement-into-sales C0 GREEN (OLEADA 8 POC #2)
- **Decisión**: **CANONICAL** — `/sales/page.tsx` reads `modules/sale` + `modules/dispatch` DIRECT via `Promise.all([saleService.listPaginated, dispatchService.list])`. Merge happens in the RSC (presentation-layer composition); NO merge service (anti-pattern that HubService was). Source discriminator union `{source: "sale"|"dispatch"}` inlined as presentation-local types per design § 5. Marco-3 Option B lock honored: `modules/dispatch` + `modules/sale` stay separate code-side; cross-module composition lives in presentation only.
- **Cementado**: C0 RED `c91b340c` · GREEN `fb1374f6`. 4α sentinels (dispatch-import, no-hubservice, twin-call, three-types). Twin-call wired with concurrent Promise.all; cross-module composition in `app/(dashboard)/[orgSlug]/sales/page.tsx`.

### §13.dispatch.categC-app-level-slice-retirement-defer-pagination-correctness
- **1ra evidencia**: POC poc-dispatch-retirement-into-sales C1 GREEN (OLEADA 8 POC #2)
- **Decisión**: **RETIRED** — Categ C++ in-memory slice anti-pattern (load-all-then-slice across modules — #2474) RETIRED. Cross-module UNION pagination is mathematically incorrect (cannot UNION two paginated streams without violating ordering invariants). B5 LOCK = canonical home for the defer rationale; supersedes #1807 defer rationale (now RETIREMENT, not preservation).
- **Cementado**: C1 GREEN `c6938639`. HubService.listHub() slice logic DELETED with the class.

### §13.dispatch.dispatches-route-redirect-shim-preserve-detail-route
- **1ra evidencia**: POC poc-dispatch-retirement-into-sales C3 GREEN (OLEADA 8 POC #2)
- **Decisión**: **CANONICAL** — `/dispatches` LIST page becomes Next.js 16 `permanentRedirect(\`/\${orgSlug}/sales\`)` shim (308). `/dispatches/[dispatchId]` detail route PRESERVED (Marco-lock #3). `/dispatches/new` create route PRESERVED (defer create-route consolidation — separate POC). Sidebar `registry.ts:87` Ventas entry repointed `/dispatches` → `/sales`. R-HIGH-3 "Despachos" label audit: 3 LEGIT BC+ND subdomain labels at `monthly-close-panel.tsx:266,284` + `close-event/page.tsx:22` UNCHANGED — domain labels, not URL refs.
- **Cementado**: C3 RED `9923074f` · GREEN `9ceae7ef`. 3α sentinels (redirect-present, sidebar-href, no-data-fetch).

### §13.dispatch.transactions-list-component-3-type-discriminator-cross-module-read
- **1ra evidencia**: POC poc-dispatch-retirement-into-sales C2 GREEN (OLEADA 8 POC #2)
- **Decisión**: **CANONICAL** — `components/sales/transactions-list.tsx` (git-mv'd from `components/dispatches/dispatch-list.tsx`) discriminates Sale|ND|BC via `source: "sale"|"dispatch"` union. Presentation-local types (HubItem inlined per design § 5 — NOT domain). Coexists with `components/sales/sale-list.tsx` (Sale-paginated single-type for existing `/sales` flow context — design D4). Marco may consolidate (drop sale-list) in future POC.
- **Cementado**: C2 RED `027d3c5d` · GREEN `e3562bda`. 3α sentinels (dispatch-list-absent, transactions-list-exists, discriminator). JSDoc 1-2 lines action-first per [[jsdoc_adapter_calibration]] + [[jsdoc_adapter_framing]].

### §13.dispatch.sale-pagination-preserved-bc-nd-non-paginated-alongside-unified-view
- **1ra evidencia**: POC poc-dispatch-retirement-into-sales C0 GREEN (OLEADA 8 POC #2)
- **Decisión**: **ACCEPTED TRADEOFF** — Marco-resolved B5 side-effect: Sale rows preserve `saleService.listPaginated` (paginated as today); BC + ND rows fetched non-paginated alongside (`dispatchService.list` returns all). UI surfaces both in unified `TransactionsList`. NO cross-module UNION pagination math (mathematically incorrect — see §13.dispatch.categC-app-level-slice-retirement). Mixed paging semantics flagged honest for future UX consolidation; not blocking.
- **Cementado**: C0 GREEN `fb1374f6`. Implementation in `app/(dashboard)/[orgSlug]/sales/page.tsx` (Promise.all wrapper).
- **SUPERSEDED by**: §13.dispatch.sales-unified-pagination-union-cascade (poc-sales-unified-pagination D1 GREEN). Marco aclaración 2026-05-15 (#2503): "UNION pagination matemáticamente incorrecta" del B5 lock refería a **partial pagination** (paginar Sale-only mostrando subset incompleto del cross-source), NO al UNION verdadero (contar los 3, sumar totals, page-window con merge-sort cross-source). El UNION correcto es matemáticamente válido — solo más laburo. Sustituido en `poc-sales-unified-pagination` Approach 2.

### §13.dispatch.permanentredirect-308-vs-redirect-307-retirement-semantics
- **1ra evidencia**: POC poc-dispatch-retirement-into-sales C3 GREEN (OLEADA 8 POC #2)
- **Decisión**: **CANONICAL** — `permanentRedirect` (308) elected over `redirect` (307) for URL-consolidation RETIREMENT. Signals retirement intent + bookmark/SEO-friendly (browsers and bots cache aggressively). Source verified: `node_modules/next/dist/docs/01-app/02-guides/redirecting.md` (Next.js 16). Import: `import { permanentRedirect } from "next/navigation"`. Surface honest D3 OQ-1: spec wording said "redirect() (308 permanent)" — strict reading impossible; design ELECTED `permanentRedirect` per RETIREMENT semantics.
- **Cementado**: C3 GREEN `9ceae7ef`. `app/(dashboard)/[orgSlug]/dispatches/page.tsx` body = `permanentRedirect(\`/\${orgSlug}/sales\`)`. **Heredados matures cumulative cross-POC**: paired-sister-precedent #2428 (HubService is shim-retirement cousin) · cross-module-boundary-mock-target-rewrite #2470 honest divergence (N/A path — test files DELETED not rewritten) · [[retirement_reinventory_gate]] 5-class classification at C1 pre-GREEN · pure-append discipline preserved (zero deletion in this D1 cementación) · atomic cementación retirement for prior-CLOSED POC sentinels (c4-cutover-shape, c0-shape, c1-shape α18 baseline 84→82) per [[invariant_collision_elevation]].

### §13.dispatch.sales-unified-pagination-union-cascade
- **1ra evidencia**: POC poc-sales-unified-pagination D1 GREEN (Approach 2 — Marco lock #2503)
- **Decisión**: **CANONICAL** — `/sales` unified 3-type view (Sale + Boleta Cerrada + Nota de Despacho) paginates via UNION cascade: independent twin-call + summed totals + RSC-level merge-sort page window. Supersedes §13.dispatch.sale-pagination-preserved-bc-nd-non-paginated-alongside-unified-view (L759-762) per Marco aclaración 2026-05-15 (#2503).
  - **3-touchpoint dispatch cascade** (mirror Journal POC `45135b82`): `DispatchRepository.findPaginated` port + `PrismaDispatchRepository.findPaginated` adapter (`Promise.all([findMany({skip,take}), count({where})])` + DRY `buildDispatchWhere` helper, mirror `buildJournalEntryWhere`) + `DispatchService.listPaginated` thin delegation. Dual-method additive-transitional preserved: `findAll`/`list` UNCHANGED alongside (Journal/Purchase/Payment canonical).
  - **UNION total math**: `total = saleResult.total + dispatchResult.total`; `totalPages = max(1, ceil(total/pageSize))`. Each source paginates INDEPENDENTLY at `(page, pageSize)`.
  - **RSC merge-sort page window** (AD-2): `(createdAt DESC, id DESC)` tiebreaker — deterministic, source-agnostic; preserves "sin distinción" §13.dispatch.transactions-list-component-3-type-discriminator-cross-module-read lock. Implementation: `.sort((a,b) => b.date.getTime() - a.date.getTime() || b.id.localeCompare(a.id)).slice(0, pageSize)` in `app/(dashboard)/[orgSlug]/sales/page.tsx`.
  - **Over-fetch tradeoff (AD-3, honest)**: each source fetches up to `pageSize` rows; RSC renders `pageSize` of up to `2×pageSize` candidates. Acceptable at current scale (<100 tx/period per #2502 explore). NOT a globally-stable sort across page boundaries — stable within a page window only. Documented honest in spec residual risks; volume-bounded acceptable.
  - **TransactionsList paginated UI**: `PaginatedResult<HubItem>` intersection props (AD-5); `buildHref(orgSlug, page, status, type, periodId)` 5-param mirror `journal-entry-list.tsx` (AD-6); shadcn Pagination block + "Mostrando N-M de Total" counter verbatim from sister `sale-list.tsx:383-421`. Filter change drops `page` param (semantically resets to page 1, canonical pattern).
  - **PRE-1 routing bug fix** (preceding commit `cf3369e6`): `transactions-list.tsx:307,311` `applyFilter`/`clearFilters` routed to stale `/dispatches` (regression from C2-bis rename); fixed to `/sales`. Separate-commit-named per [[mock_hygiene_commit_scope]].
  - **Sentinel file**: `modules/dispatch/__tests__/c0-macro-pagination-additive-shape-dispatch.poc-sales-unified-pagination.test.ts` — 10α existence-only sentinels (drop T11 shadcn heredado per cumulative §13.7). RED 10/10 FAIL; GREEN cascade C1→α01-α06, C2→α07-α10.
  - **Heredado sister-cycle-update**: `c0-page-cross-module-unified.poc-dispatch-retirement-into-sales.test.ts` α-C0-twin-call regex broadened `list` → `list(?:Paginated)?` — heredado intent ("twin-call wired") preserved; named-rule semantics unchanged; per [[paired_sister_default_no_surface]] surface honest in C2 commit body.
  - **Honest divergence — no canonical `in-memory-dispatch.repository.ts` fake**: dispatch lacks the sister-canonical fake file (Sale + Journal have it). C1 atomic-extended inline `makeInMemoryRepo()` in `c1-application-shape.poc-dispatch-hex.test.ts:43-56` with `findPaginated` empty-default. Pre-existing asymmetry; queue follow-up POC. Per [[mock_hygiene_commit_scope]] named in C1 commit body.
- **Cementado**: PRE-1 `cf3369e6` · C0 RED `8ccdc22a` · C1 GREEN `ab406cf7` · C2 GREEN `37343faa` · D1 docs (this commit). Suite 7063/0/22 (baseline 1f pre-existing ai-agent timeout self-resolved during C2). TSC 0.

## §13.accounting.pagination-ledger — Canonical Homes pagination-ledger (NEW poc-pagination-ledger — Libro Mayor pagination with cumulative-state opening balance — CLOSED)

> **AXIS-DISTINCT from POC pagination-journal**: same module + same port (`JournalLedgerQueryPort`), distinct target method `findLinesByAccountPaginated` (journal-lines for one account vs whole journal entries). 3-query Promise.all asymmetry (rows + count + prior-rows for openingBalanceDelta) vs Journal 2-query (rows + count). Introduces NEW §13 rule for cumulative-state paginated DTOs. Sentinel file: `modules/accounting/__tests__/c1-macro-pagination-additive-shape-ledger.poc-pagination-ledger.test.ts` (10α existence-only).

### §13.accounting.pagination-ledger-cumulative-state-paginated-dto-pattern
- **1ra evidencia**: POC poc-pagination-ledger C1 GREEN `95cd49be`
- **Decisión**: **CANONICAL NEW** — paginated views requiring cumulative state across pages (running balance, opening delta) get a DEDICATED port DTO that EXTENDS the `PaginatedResult<T>` shape with domain-specific fields WITHOUT polluting the shared VO. Genealogy: child of `vo-generic-paginated-result-shape` (shared minimal shape extended by domain-specific stateful DTO). 1ra evidencia: `LedgerPageResult` adds `openingBalanceDelta: unknown` (monetary `unknown` mirrors `LedgerLineRow.debit/credit: unknown` port-edge pattern — adapter writes `Prisma.Decimal`, service coerces via `new Prisma.Decimal(String(...))`, DTO `LedgerPaginatedDto` adds `openingBalance: string` at API boundary). Architectural distinction preserved: port DTO carries raw monetary fields (`unknown`); presentation DTO serializes via `roundHalfUp+toFixed(2)` (R-money TIER 1 DISCHARGED, sigma-13). Running-balance accumulator in service is SEEDED FROM `opening` (NOT from `Decimal(0)`) — correctness invariant across page boundaries; page-1 → opening=0 → byte-identical to legacy `getAccountLedger`. **Heredados matures cumulative cross-POC**: `vo-generic-paginated-result-shape` EXTENDED-BY clause — `LedgerPageResult` is the first domain-specific extension demonstrating that the shared VO stays minimal while domain DTOs add stateful fields.
- **Cementado**: C0 RED `9bdafb32` · C1 RED `a4941257` · C1 GREEN `95cd49be` · C2 RED `f475184a` · C2 GREEN `0fe5a3ee`. `LedgerPageResult` interface at `modules/accounting/domain/ports/journal-ledger-query.port.ts`; `LedgerPaginatedDto` at `modules/accounting/presentation/dto/ledger.types.ts`; service impl at `modules/accounting/application/ledger.service.ts:getAccountLedgerPaginated`. JSDoc inline locks per [[jsdoc_adapter_calibration]].

### §13.accounting.pagination-ledger-split-port-three-touchpoint-find-paginated-2nd-evidence
- **2da evidencia matures**: POC poc-pagination-ledger C1 GREEN `95cd49be` (1ra evidencia Journal POC `45135b82`)
- **Decisión**: **MATURES** the canonical `§13.accounting.pagination-journal-split-port-findpaginated-three-touchpoints` rule (line 717 — original text UNTOUCHED per [[named_rule_immutability]]). Same `JournalLedgerQueryPort` now carries BOTH `findPaginated` (entries, Journal POC) AND `findLinesByAccountPaginated` (lines for one account, this POC) — split-port cumulative cross-method evidence. The 3-touchpoint port + adapter + repo atomic TSC gate held EXACT (4-file atomic landed `95cd49be`: port + adapter + repo + fake). `findLinesByAccount` legacy preserved via `buildLedgerLineWhere` DRY helper extraction (mirror `buildJournalEntryWhere` pattern from Journal POC) — `findLinesByAccount` refactored to call helper, behavior byte-identical (REQ-7 dual-method 5ta evidencia).
- **Cementado**: C1 GREEN `95cd49be`. 3-touchpoint cascade: `journal-ledger-query.port.ts` (interface + `LedgerPageResult` type) + `prisma-journal-ledger-query.adapter.ts` (thin pass-through, 8th method) + `prisma-journal-entries.repo.ts` (3-query Promise.all + `buildLedgerLineWhere` DRY) + fake `InMemoryJournalLedgerQueryPort.findLinesByAccountPaginated` atomic per [[mock_hygiene_commit_scope]].

### §13.accounting.pagination-ledger-twin-call-rsc-canonical-3rd-evidence
- **3ra evidencia matures**: POC poc-pagination-ledger C3 GREEN `7d2cf02e` (1ra evidencia sales-unified · 2da evidencia journal)
- **Decisión**: **MATURES** the canonical `§13.dispatch.sales-unified-pagination-union-cascade` RSC twin-call pattern. `/ledger` RSC: `Promise.all([accountsService.list(orgId), accountId ? ledgerService.getAccountLedgerPaginated(...) : Promise.resolve(null)])`. Twin-call asymmetry vs sales-unified UNION: ledger has ONE paginated source (lines for one account) — twin-call is `accounts` (full list, non-paginated) + `ledger` (paginated, conditional on `accountId`). Next.js 16 `searchParams: Promise<{ [key: string]: string | string[] | undefined }>` shape verified textually at `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md` per AGENTS.md mandate. `JSON.parse(JSON.stringify(...))` anti-pattern preserved per D7 defer (same posture as journal page.tsx). `buildHref` 6-param mirrors `journal-entry-list.tsx:104-120` sister-2 precedent (closer paridad for ledger's 5-filter shape than `sale-list.tsx:66-76` sister-1's 3-param). AccountSelector `onChange` writes draftAccountId LOCAL state ONLY — navigation only on Consultar form submit (REQ-8/SC-8). Filter submit resets to page=1 (SC-9). Opening balance banner above table: visible iff `page > 1 AND openingBalance !== "0.00"` (D5 SC-5).
- **Cementado**: C3 RED `bb070bd9` · C3 GREEN `7d2cf02e`. 3-file atomic: `app/api/organizations/[orgSlug]/ledger/route.ts` (D-Route LOCKED: always paginated when accountId; API contract LedgerEntry[] → LedgerPaginatedDto sole-consumer audit confirmed zero external consumers per design §4.1) + `app/(dashboard)/[orgSlug]/accounting/ledger/page.tsx` (RSC twin-call) + `components/accounting/ledger-page-client.tsx` (prop-driven, removes useState+fetch+Loader2; adds buildHref + Pagination + opening banner). page.test.ts mock surface expansion landed in C3 RED commit `bb070bd9` (mock with test that needs it semantics). **Heredados matures cumulative cross-POC**: `shared-presentation-carve-out` 5ta/6ta evidencia (parsePaginationParams + paginationQuerySchema reused) · `dual-method-additive-transitional` 5ta evidencia (`findLinesByAccount` + `getAccountLedger` preserved alongside their paginated parallels).

## §13.accounting.calendar-day-T12-utc-unified — Canonical Homes calendar-day persistence + display (NEW poc-date-calendar-vs-instant-convention — Bug #1 timezone resolution → T12 unification — CLOSED)

> **Scope**: All `DateTime @db.Timestamptz(3)` columns that represent calendar days — `JournalEntry.date`, `Sale.date`, `Purchase.date`, `Dispatch.date`, `Payment.date`, `AR.dueDate`, `AP.dueDate`. Does NOT apply to instant fields (`createdAt`, `updatedAt`, `audit_logs.timestamp`, etc.) — those remain real timestamps and use `formatDateTimeBO` / `formatTimeBO` for display.
>
> **Invariant**: Calendar-day fields MUST be stored as `T12:00:00.000Z` UTC. No write path may emit `T00:00:00.000Z` for these fields. UTC noon guarantees unambiguous calendar-day round-trip for any timezone in UTC-12 .. UTC+12 (no DST flips a noon-UTC instant across a day boundary in any inhabited TZ).

### §13.accounting.calendar-day-T12-utc-unified
- **1ra evidencia**: POC poc-date-calendar-vs-instant-convention C1..D1 GREEN (HEAD `faf26bca` baseline + this commit)
- **Decisión**: **CANONICAL NEW** — unified write helper + display helper + filter window convention across the entire accounting module:
  - **Write helper (canonical)**: `toNoonUtc(value)` — `lib/date-utils.ts:270`. Slices first 10 chars of ISO + appends `T12:00:00.000Z`. Throws `RangeError` on invalid. Pre-existing canonical for Sale / Purchase / Dispatch / IVA-books repositories. This POC joins `JournalEntry.date` (via `parseEntryDate` body T00→T12, mirroring `toNoonUtc` semantics inline) and `Payment.date` (`createPaymentSchema` + `updatePaymentSchema` `.transform(d => toNoonUtc(d))`). `parseEntryDate` (`modules/accounting/domain/journal.dates.ts`) intentionally does NOT call `toNoonUtc` directly (legacy ValidationError boundary preserved) but uses the same T12 literal and JSDoc cites the shared semantics.
  - **Display helper (canonical)**: `formatDateBO(value)` — `lib/date-utils.ts:58`. Pure ISO-slice (`value.slice(0,10)` then split on `-`), NO `Date` instantiation for the format step, NO Intl/locale call, NO `toLocaleDateString` call. Returns `"DD/MM/YYYY"` or `""` for null/malformed. TZ-safe by construction. Banned anti-pattern: `toLocaleDateString("es-BO", ...)` on calendar-day `Date` inputs (drifts D-1 in BO TZ UTC-4 for T00 inputs; also wrong for T12 inputs if the runtime TZ is east of UTC+12 — formatDateBO eliminates the whole class).
  - **Filter convention**: `dateRangeSchema.dateTo` (`modules/accounting/presentation/validation.ts:119`) post-processes via `.transform(d => endOfUtcDay(d))` where `endOfUtcDay(d)` returns a `Date` at `Date.UTC(year, month, day, 23, 59, 59, 999)`. Same-day filter window `{gte: T00, lte: T23:59:59.999Z}` includes BOTH legacy T00 rows AND new T12 rows (backward-compat preserved per SC-14). `dateFrom` left untouched (T00 is correct lower bound; T12 ≥ T00).
- **Banned-pattern source-grep sentinels** (α-1, α-2 — `modules/accounting/__tests__/c0-alpha-calendar-day-t12-sentinel.poc-date-calendar.test.ts`):
  - α-1: zero `toLocaleDateString("es-BO"` matches across 11 scoped files (3 components + 9 exporters incl. sheet.builder which has 3 call sites).
  - α-2: zero `function formatDate(` definitions in `components/accounting/{ledger-page-client,payable-list,receivable-list}.tsx`.
  - α-3..α-5: structural guards on `parseEntryDate` T12 literal, `dateRangeSchema` `.transform(` + `endOfUtcDay`, and `formatDateBO` import count ≥ 8 across the 11 scoped files.
  - SC-26: this `§13.accounting.calendar-day-T12-utc-unified` entry must be present in this doc (≥ 1 match for the literal slug).
- **Heredados cross-POC**:
  - `toNoonUtc` (`lib/date-utils.ts:270`) — pre-existing canonical write helper (Sale/Purchase/Dispatch/IVA). This POC adds 2 additional consumers (JournalEntry + Payment); does NOT mutate the helper.
  - `formatDateBO` (`lib/date-utils.ts:58`) — pre-existing canonical display helper. Already canonical in 6 non-accounting components after sister sweep (engram #2233 Bug B — `period-list.tsx`, `payment-form.tsx`, `payment-list.tsx`, etc.). This POC extends the sweep to 3 accounting components (ledger / payable / receivable) + 9 exporter files (atomic per G5 + G9).
  - `arch/§13/ui/date-display-regional-format-transform-axis-distinct` (`docs/architecture/02b-bridges-teardown-legacy.md:556`) — sister rule for UI date-display granjero familiar format transform via `formatDateBO`. This entry CONSOLIDATES the accounting half of the same display convention without mutating the sister rule (per [[named_rule_immutability]]).
- **Honest surfaces (declared in design #2594 §Honest Surfaces, preserved here)**:
  - H1: `prisma/seed.ts` has NO transactional fixtures — SC-27 (originally "seed emits T12") reframed at design time as write-path integration assertion ("test data created via Zod write paths post-fix emits T12 exclusively"). Source seed unmodified.
  - H2: `trial-balance-pdf.exporter.ts:167` + 5 others used `timeZone:"America/La_Paz"` rather than "bare no TZ" (Step 0 audit #2600 recount: actual banned-pattern total = 14, not the "11–12" estimated in design draft). La Paz TZ on T00 inputs drifts D-1 (UTC-4 → renders previous day local) — same fix path `formatDateBO` for both bare and La Paz patterns; no special-case. α-1 count expectation updated to 14 in narration only; the assertion `expect(count).toBe(0)` is unchanged.
  - H_INITIAL_BALANCE_FORMAT: `initial-balance-pdf.exporter.ts:41` and `initial-balance-xlsx.exporter.ts:61` previously emitted long format "DD de mes de YYYY" via `fmtDateLong`. G9 sweep makes those helpers return `formatDateBO(d)` → numeric `"DD/MM/YYYY"`. UX format change is intentional uniformity (all 9 exporters now numeric); helper name `fmtDateLong` retained for source-call-site stability — declared honest in G9 commit body.
- **Cementado**: C0 RED `3fabd2bb` (5α sentinels) · C1 RED `29177505` + GREEN `23d5a7f4` (parseEntryDate + Payment.date emit T12) · C2 RED `87aec216` + GREEN `484ac1cb` (ledger + payable + receivable formatDateBO sweep) · C3 RED `294500db` + GREEN `b856da5d` (dateRangeSchema.dateTo end-of-UTC-day) · C4 RED `07e226dc` + GREEN `e78ddcef` (9 exporter files formatDateBO sweep — sheet.builder + 8 PDF/XLSX exporters atomic) · D1 docs (this commit). 16 source files modified + 4 test files (T1 sentinel + T2 journal-dates + payment-date-t12 + T3 date-range-schema + T4 exporters-tz-safe). All 5 α-sentinels GREEN post-G9; SC-26 GREEN at G10. Suite + TSC verified at closure.
