# 01. Migration Roadmap — 5 Oleadas

> **Cementación**: POC docs-refactor (sesión 2026-05-11). OLEADA 4 retroactive cementación poc-organizations-hex (sesión 2026-05-13). OLEADA 5 retroactive cementación poc-dispatch-hex (sesión 2026-05-12). OLEADA 5 2/3 CLOSED poc-ai-agent-hex (sesión 2026-05-13). **OLEADA 5 FULLY CLOSED — 3/3 poc-financial-statements-hex (sesión 2026-05-13)**. **OLEADA 6 sub-POC 1/8 CLOSED poc-accounting-trial-balance-hex (sesión 2026-05-13)**. **OLEADA 6 sub-POC 2/8 CLOSED poc-accounting-equity-statement-hex (sesión 2026-05-13)**. **OLEADA 6 sub-POC 3/8 CLOSED poc-accounting-worksheet-hex (sesión 2026-05-13)**. **OLEADA 6 sub-POC 4/8 CLOSED poc-accounting-initial-balance-hex (sesión 2026-05-13)**. **OLEADA 6 sub-POC 5/8 CLOSED poc-accounting-iva-books-hex (sesión 2026-05-13)**.
> **Source**: Inventory cumulative cross-POC matures heredado [02-current-state.md](02-current-state.md).
> **Estimación cumulative migration completa**: ~60-80 sesiones cortas.

## 10-POC Framework — Cementación 2026-05-12 POC poc-accounting-routes-pages-cutover (POC #3d COMPLETE)

| POC | Objetivo | Estado | LOC | Consumers |
|---|---|---|---|---|
| **POC #1** | Public barrels (accounting + iva-books server.ts) | ✅ ARCHIVED | +11 | unblock #2+ |
| **POC #2a** | types-to-hex (3 DTO files moved to hex, SHIM barrel reconciled) | ✅ ARCHIVED | +193 | 6/6 REQs PASS |
| **POC #2b** | utils-pure-to-hex (account-code, correlative, accounting-helpers, journal.dates) | ✅ ARCHIVED | +150 | 6/6 REQs PASS |
| **POC #2c** | account-subtype-to-hex (utils + resolve) | ✅ ARCHIVED | +80 | 6/6 REQs PASS |
| **POC #2d** | ui-helpers-to-hex (journal.ui) | ✅ ARCHIVED | +43 | RED 57603898 · GREEN deb6872e |
| **POC #3a** | accounts-crud-port (domain port interface, 15 methods verbatim legacy) | ✅ DONE | +133 | RED 45568edf · GREEN 01656b96 · D1 863b6665 |
| **POC #3b** | PrismaAccountsRepo adapter (hex infra — implements AccountsCrudPort) | ✅ DONE | +302 | RED 02284e0d · GREEN 10df7d1e · D1 f739b609 |
| **POC #3c** | AccountsService app layer + composition root | ✅ DONE | +298 | RED 62d4728a · GREEN d58dd1a2 · D1 da8165d5 |
| **POC #3d** | routes + pages cutover + Zod schemas | ✅ DONE | +95 / -93 | RED 2e993d35 · GREEN 3062ba16 · D1 1b41fcfe |
| **POC #3e** | cross-module cutover (sale/purchase/dispatch/org-settings/ai-agent/payments) + AutoEntryGenerator ctor structural widening (Marco-locked Option 3) + 2 hidden importers fixed (W-#3e-01 inventory miss) | ✅ DONE | +68 / -524 | RED 37b57951 · GREEN c6af8468 · D1 0d3423c2 |
| **POC #3f** | legacy retirement — 2 sibling PORT-widens (ledger.service + journal.service → AccountsCrudPort + PrismaAccountsRepo) + accounts.repository.ts DELETE + SHIM trim (accounting.validation.ts L4–L9 removed); R-01 auto-closed; W-01 cross-cycle α34+α35 revoked atomic | ✅ DONE | -267 net | RED 3fdb737c · GREEN 20dce277 · D1 fb7dc642 |
| **POC #4** | accounts hex (replaces original broad #3 scope — now handled by #3a–#3f) | ✅ SUBDIVIDED | → #3a–#3f | accounts CRUD migration sub-divided |
| **POC #5** | auto-entry + lifecycle + validation | pending | ~600 | journal-driver |
| **POC #6** | reports paired-sister 4-batch (trial/equity/worksheet/initial) | pending | ~800 | journal-consumer |
| **POC #7** | financial-statements XL POC | pending | ~1500 | reporting-stack |
| **POC #8** | journal completion + parallel-impl reconciliation (JournalService→JournalsService) | pending | ~700 | core-rename |
| **POC #9** | account-balances + ledger paired (resuelve circular) | pending | ~400 | final-cycle |
| **POC #10** | server.ts barrel delete final | pending | -11+ | total cleanup |

### Principios cumulative cross-POC matures heredados

- **Paired sister reuse aggressive** per POC nuevo — EXACT mirror precedent matures cumulative
- **Bundling sesiones POC pequeño** (1 sesión = 1 huérfano delete viable)
- **Skip Step 0 expand verbose** en POCs simples (huérfanos sin tests + 0 consumers)
- **Single-axis atomic discipline** — POC #2 original (utils-types sweep B-batch) sub-dividido 4-way (types→hex, pure-utils→hex, account-subtype→hex, ui-helpers→hex) por pairing & reusability
- **Defer accounting big bang** scope dedicado ÚLTIMO (12.5k LOC + 128 consumers residual POC #10 shim cleanup)
- **Cycle-start cold próxima sesión**: read SOLO docs relevantes scope (NO architecture.md complete cumulative)

## OLEADA 1 — Quick wins (3 POCs ~1-2 sesiones cada uno)

| POC | Source | LOC | Tests | Consumers | Paired sister |
|---|---|---|---|---|---|
| `poc-quick-cleanup` | inline + delete `features/{rag, pricing}` CONSUMERS-LIGHT | 263 | 0 | **4** (rag→ai-agent+documents; pricing→ai-agent×2) | NOT TRUE-ORPHAN — mechanical inline cutover precedent #3e — ✅ **COMPLETE** (pricing: RED 11a00455 · GREEN 65f66e0c · D1 995e2ea9; rag: RED 0c1a52f7 · GREEN 285337ab · D1 ea47485c; W-01 706d3b84); 36α PASS (18 pricing + 18 rag); features/pricing + features/rag DELETED |
| `poc-expense-hex` ✅ CLOSED | `features/expenses` → `modules/expense` | 281 | 0 | 11 | farms+lots paired sister — 17 commits 6 ciclos 64α |
| `poc-operational-doc-types` ✅ DONE | `features/operational-doc-types` → `modules/operational-doc-type` | +428 hex / -256 legacy | 0 | 7 | farms+lots paired sister — 13 commits 6 ciclos 57α; C0 RED 4049a0af · GREEN dedf612c; C1 RED 3bee72b2 · GREEN 21cce2f2; C2 RED b2407b44 · GREEN 066b0146; C3 RED 528157da · GREEN 32223887; C4 RED b8ad5bf8 · GREEN 7be0bfed; C5 RED 62a505f3 · GREEN f2657943; D1 3218b520 |

## OLEADA 2 — Accounting utils sweep 4-way sub-divided + transversales (6 POCs)

| POC | Source | LOC | Tests | Consumers | Notas |
|---|---|---|---|---|---|
| `poc-accounting-types-to-hex` ✅ CLOSED | `features/accounting/journal/accounts/ledger.types.ts` → `modules/accounting/presentation/dto/` | +193 | 15α | 6/6 REQs PASS | POC #2a — types canonical home hex |
| `poc-utils-pure-to-hex` ✅ CLOSED | `features/accounting/{account-code, correlative, accounting-helpers, journal.dates}` → `modules/accounting/domain/` | +150 | 21α | 6/6 REQs PASS | POC #2b — paired sister #2a |
| `poc-account-subtype-to-hex` ✅ CLOSED | `features/accounting/{utils + resolve}` account-subtype | +80 | 23α | 6/6 REQs PASS | POC #2c — paired sister #2b |
| `poc-ui-helpers-to-hex` ✅ CLOSED | `features/accounting/journal.ui` | +43 | 13α | 6/6 REQs PASS | POC #2d — paired sister #2c; NEW barrel block Domain UI helpers |
| `poc-shared-canonical` ✅ CLOSED | `features/shared` → `modules/shared` expand | 418 | 6 | **230** | **4 sub-POCs ARCHIVED** (errors: dffaeb15 · middleware-auth: c56a1360 · audit: 69178f3f · base-repo: c242f858); 12 commits total; Option A/B SHIM strategic divergence; spy-consumer-namespace NEW invariant; 0 prod consumer edits (all via SHIMs, final deletion deferred POC #10) |
| `poc-permissions-hex` ✅ CLOSED | `features/permissions` → `modules/permissions` | 561 | 6 | **183** | **3 sub-POCs ARCHIVED** (domain: d537e015 · infra: dede98f7 · app: 0eead335); 9 commits total; Option B SHIM dominant (5 of 7 SHIMs — isolatedModules + spy gate); cross-module-boundary spy invariant NEW LOCKED (DIFFERS from poc-shared-audit closed-over-sibling); cross-module-boundary mock-target-rewrite invariant NEW LOCKED (B1 test mock target updated atomically with B3 cutover per [[mock_hygiene_commit_scope]] + [[diagnostic_stash_gate_pattern]]); 0 prod consumer edits; 84 vi.mock + 1 vi.spyOn preserved; final SHIM deletion deferred POC #10. **OLEADA 2 COMPLETE 6/6** |

## OLEADA 3 — Features small-medium sin POC histórico (6 POCs)

| POC | Source | LOC | Tests | Consumers | Notas |
|---|---|---|---|---|---|
| `poc-product-types-hex` ✅ CLOSED | `features/product-types` → `modules/product-type` (hex full: domain/application/infrastructure/presentation) | 219 | 0 | 9 | D1 cementación `108a0086` (pre-OLEADA-3-sweep migration); 6 hex sub-files + 4 layered __tests__ dirs; SHIM features/ already deleted (C5 wholesale-delete closed) |
| `poc-document-signature-config-hex` ✅ CLOSED | `features/document-signature-config` → `modules/document-signature-config` (hex full) | 222 | 3 | 5 | D1 cementación `8dd3a846` (joint D1 with org-profile `2959e3c6`); 6 hex sub-files + paired sister of product-type-hex EXACT mirror; SHIM features/ already deleted |
| `poc-org-profile-hex` ✅ CLOSED | `features/org-profile` → `modules/org-profile` (hex full + BlobStoragePort) | 174 | 3 | 10 | D1 cementación `2959e3c6` (joint with doc-sig-config) + C5 wholesale-delete `f1765571`; paired sister of doc-sig-config-hex EXACT mirror + BlobStoragePort NEW port + VercelBlobStorageAdapter; SHIM features/ already deleted |
| `poc-account-balances-hex` ✅ CLOSED | `features/account-balances` → `modules/account-balances` (application + infrastructure) | 150 | 0 | **7** | RED `2565077d` · GREEN `96aec947` · D1 (this row); 14α (13 FAIL + 1 PASS-lock α4 server-only); paired sister poc-users-hex EXACT mirror axis-distinct on (a) 5-file C1 atomic (+1 types file vs sister 3-file), (b) +1 REQ-004 bypass (JournalEntryWithLines DIRECT from `@/modules/accounting/presentation/dto/journal.types` — breaks type-level circular dep), (c) 7 consumers (5 runtime + 2 type-only test fixtures, vs sister 21), (d) roadmap closure line 65 OLEADA 3 6/6 CLOSED; Option A SHIM preserves `@/features/account-balances/server` mock-target path → 1 vi.mock decl (page-rbac.test.ts) + 7 consumers transit zero touch; REQ-004 canonical-import-bypass applied (2 bypasses: JournalEntryWithLines + BaseRepository direct from `@/modules/*`); SHIM deletion deferred retirement bundle (OLEADA 3 trio poc-auth-hex + poc-users-hex + poc-account-balances-hex ALL CLOSED — C5 wholesale-delete ready for retirement phase). **OLEADA 3: 6/6 CLOSED** (FINAL — wave complete) |
| `poc-users-hex` ✅ CLOSED | `features/users` → `modules/users` (application + infrastructure) | 82 | 0 | **21** | RED `6b41ce09` · GREEN `274ecd2e` · D1 (this row); 14α (13 FAIL + 1 PASS-lock α4 server-only); paired sister poc-shared-base-repo EXACT mirror axis-distinct on 2-layer split + single SHIM barrel named re-export; Option A SHIM preserves `@/features/users/server` mock-target path → 8 vi.mock decls + 21 runtime consumers transit zero touch; REQ-004 canonical-import-bypass applied (NotFoundError + BaseRepository direct from `@/modules/shared/*`); SHIM deletion deferred retirement bundle (poc-auth-hex + poc-users-hex + poc-account-balances-hex per OLEADA 3 pattern — poc-auth-hex NOW CLOSED, features/auth/sync-user.service.ts no longer imports through SHIM). Consumer count corrected 19 → 21 (recon discovered 17 app routes + 1 features/auth + 1 modules/organizations adapter + 2 misc; prior count stale) |
| `poc-auth-hex` ✅ CLOSED | `features/auth` → `modules/auth/application` (application-only flat) | 205 | 1 | **1** | RED `ac497dda` · GREEN `693a51c4` · D1 (this row); 14α (13 FAIL + 1 PASS-lock α4 server-only); paired sister poc-users-hex EXACT mirror axis-distinct on application-only flat (no infrastructure/) + 4-file atomic (+1 test relocation vs sister 3-file); Option A SHIM preserves `import "server-only"` + named re-export; REQ-004 canonical-import-bypass applied (UsersService + NotFoundError/AppError direct from `@/modules/*`); dual-mock wiring: `@/features/users/server` vi.mock PRESERVED + `@/modules/users/application/users.service` canonical mock ADDED alongside; SHIM deletion deferred retirement bundle (poc-auth-hex + poc-users-hex + poc-account-balances-hex per OLEADA 3 pattern). LOC retrofix: stale 57 → actual 205 (56 src + 146 test + 3 SHIM). **OLEADA 3: 6/6 CLOSED** (FINAL — poc-account-balances-hex CLOSED as last sub-POC) |

## OLEADA 4 — Features medianos sin POC histórico (4 POCs) — **4/4 CLOSED**

| POC | Source | LOC | Tests | Consumers |
|---|---|---|---|---|
| ✅ `poc-reports-hex` | `features/reports` | 330 | 2 | 4 | C0 `db46f0c0` · C1 `c5c2caa0` · C2 D1 (this commit)
| ✅ `poc-documents-hex` CLOSED (C5 deferred) | `features/documents` → `modules/documents` hex (C0–C4) | 422 | 1 | 5 | C0 RED `30390d43` · GREEN `2bcaee3e`; C1 RED `c81b3366` · GREEN `8127ec3e`; C2 RED `e003db19` · GREEN `c7e03d2d`; C3 RED `b7a7924b` · GREEN `168e9a33`; C4 RED `495ff6d2` · GREEN `e237c493`; D1 (this commit). 45α PASS (8 C0 + 10 C1 + 10 C2 + 9 C3 + 8 C4). BlobStoragePort upload+del FRESH documents-specific (DIFFERS from org-profile del-only sister). rag/ carved out (REQ-004 cross-module bypass — deferred to poc-rag-hex). features/documents/server.ts SHIM intact (rag/ blocks wholesale delete; C5 deferred). REQ-005 NEGATIVE sentinel enforced (domain + application server-only-free). REQ-007 pdfjs-dist processing library exception with comment lock.
| ✅ `poc-audit-hex` CLOSED (C5 executed) | `features/audit` → `modules/audit` hex (C0–C5 + D1) | 833 | 7 | 12 | 14 commits total; 48α PASS (8 C0 + 7 C1 + 7 C2 + 5 C3 + 12 C4 + 9 C5); C0 RED `a7036e43` · GREEN `bce5b981`; C1 RED `09ecb23e` · GREEN `154ff73f`; C2 RED `cb4f6ec0` · GREEN `2a74c69b`; C3 RED `5aebf296` · GREEN `9f78b9ac`; C4 RED `b68aa8f9` · GREEN `939934a9`; C5 RED `53758f0f` · GREEN `2da91e58`; D1 `fa445a87` (initial — sigma-13 retroactive update `67f437d0`/this commit). READ-ONLY pattern LOCKED (AuditInquiryPort + UserNameResolver port — NO mutation methods; axis-distinct from paired sister poc-documents-hex R/W BlobStoragePort). Raw SQL CTE preservation in PrismaAuditRepository (architectural exception — in-process correlated-event grouping). Tenant guard LOCKED security invariant (organizationId scoping in every query). 12 cross-feature consumers cutover atomic at C4 (2 RSC + 2 API + 4 client + 4 test files vs sister 3). **In-POC C5 EXECUTION** — first hex POC to delete `features/X/` wholesale within the POC (sister documents deferred C5 to poc-rag-hex due to rag/ carve-out). α36 sentinel guarded with existsSync (commit `67f437d0`) for C5-deleted file ENOENT per [[red_acceptance_failure_mode]]. SHIM NOT required (no deferred consumers). **OLEADA 4: 3/4 CLOSED** |
| ✅ `poc-organizations-hex` CLOSED (C5 executed) | `features/organizations` → `modules/organizations` hex (C0–C5 + D1) | 1,643 | 13 | 49 | C0 `f5705a19` · C1 `10552e56` · C2 `f8b498fb` · C3 `1d7ebed5` · C4 `3f2d4ad2` · C5 `a01b4d59` · D1 `3c75ddf7` (initial) + retroactive sigma-13 cementación (this commit). 139 tests PASS (13 test files; exceeded 13-file recon estimate — 6 additional tests discovered at GREEN). 7 adapter ports (ClerkAuth, UserResolution, AccountSeed, VoucherTypeSeed, SystemRoleSeed, PermissionCache, SystemRoleGuard) — HIGHEST port count in repo (axis-distinct from paired sister poc-audit-hex READ-ONLY 2-port). 3 domain aggregates: Organization, Membership/Clerk-saga, RBAC/CustomRole. Composition-root pattern: `makeRolesService` requires `getCallerRoleSlug` callback + `makeReadOnlyRolesService` for validation singleton. Client barrel `presentation/index.ts` exports `slugify` pure fn for `components/settings` consumer. features/permissions relay updated surgically: `requireOrgAccess` + `requireRole` repointed to `modules/organizations/presentation/server`. Zero new TS errors (27 total = all pre-existing dispatch/sale/org-settings). In-POC C5 EXECUTION (features/organizations wholesale delete — 14 source + 13 test + 3 fixture files). SHIMs NOT required (no deferred consumers). **OLEADA 4: 4/4 CLOSED** (WAVE COMPLETE) |

## OLEADA 5 — Big bang dedicated MAJOR scope (3 POCs) — **3/3 CLOSED (WAVE COMPLETE)**

| POC | Source | LOC | Tests | Consumers | Notas |
|---|---|---|---|---|---|
| ✅ `poc-dispatch-hex` CLOSED (C5 executed) | `features/dispatch` → `modules/dispatch` hex (C0–C5 + D1) | 2,199 | 7 | 13 | C0 RED `19345bcb` · GREEN `189492cc`; C1 RED `b2996874` · GREEN `e15b8d25`; C2 RED `4dcd9689` · GREEN `b9bdbcf3`; C3 RED `6dcf69be` · GREEN `5fd0cd42`; C4 RED `cb3f7a7d` · GREEN `e3e6ad8b`; C5 RED `80d92b47` · GREEN `0c546df4`; D1 `45f26c75` (initial — sigma-13 retroactive cementación this commit). **107α PASS** (33 C0 + 13 C1 + 8 C2 + 11 C3 + 13 C4 + 11 C5 + 18 correctivo poc-correctivo-dispatch-instantiation co-located sentinel). 7 ports total: DispatchRepository (R/W) + 6 external adapter ports (DispatchJournalEntryFactoryPort → LegacyJournalEntryFactoryAdapter bridging features/accounting AutoEntryGenerator; DispatchAccountBalancesPort → LegacyAccountBalancesAdapter; DispatchOrgSettingsReaderPort → LegacyOrgSettingsReaderAdapter; DispatchContactsPort → LegacyContactsAdapter; DispatchFiscalPeriodsPort → LegacyFiscalPeriodsAdapter; DispatchReceivablesPort → LegacyReceivablesAdapter). Axis-distinct from paired sister poc-organizations-hex on: discriminated dispatch types (NOTA_DESPACHO/BOLETA_CERRADA VO) + state machine DRAFT→POSTED→LOCKED→VOIDED + BC-path discriminated computations (shrinkage/shortage/realNetWeight) + HubService cross-module aggregation (Sale+Dispatch→HubItem[] co-located in presentation; **S: SaleServiceForHub interface imported at presentation layer from modules/sale — load-bearing arch debt, NOT port-isolated, accepted**) + Sale architectural mirror for state machine (vs organizations RBAC aggregate). Composition-root `makeDispatchService()` zero-arg (vs organizations `makeRolesService(getCallerRoleSlug)` callback). Consumer count 13 corrected from 15 roadmap estimate (7 runtime files + 3 vi.mock tests + 2 type-only tests + 1 app-route test). features/dispatch/ DELETED wholesale in-POC at C5 (15 files atomic — 10 source + 5 test; in-POC execution same as poc-audit-hex + poc-organizations-hex). SHIMs NOT required (no deferred consumers). **S-01**: 2 pre-existing TS2353 errors in c0-domain-shape sentinel (dispatchId — UNCHANGED, accepted). **S-02**: deferred duplicate vi.mock in `app/api/organizations/[orgSlug]/dispatches-hub/__tests__/route.test.ts` lines 54+72 (honest residual — NOT introduced by this POC). **OLEADA 5: 1/3 CLOSED** (WAVE INITIATED) |
| ✅ `poc-ai-agent-hex` CLOSED (C5 executed) | `features/ai-agent` → `modules/ai-agent` hex (C0–C5 + D1) | 9,501 | 21 | 16 | C0 RED `7a949b64` · GREEN `eb76f91e`; C1 RED `b1ef1a24` · GREEN `7d6e9ac8`; C2 RED `9df51130` · GREEN `8f84eb64`; C3 RED `a37f2c42` · GREEN `c16510ac`; C4 RED `a73c5978` · GREEN `ccab3a77`; C5 RED `e8b1565c` · GREEN `f84edceb`; D1 (this commit). **170α PASS** (38 C0 + 30 C1 + 11 C2 + 21 C3 + 16 C4 + 54 C5). FIRST hex with: (a) LLMProviderPort (non-Prisma external API port), (b) dual-barrel D5 with `"use client"` client barrel, (c) 6-port ctor injection in AgentService (deps-object pattern). 8 domain ports: LLMProviderPort, RateLimitPort, AccountsLookupPort, ContactsLookupPort, MemoryPort, AgentContextPort, FiscalPeriodPort, PricingPort. C4 drift: C1 changed AgentService from zero-arg to deps-object ctor — all 21 __tests__ migrations infeasible via git mv; deferred atomically to C5. C5 scope 54α vs spec ~33α (51 FILE-STILL-EXISTS + 1 REQ-001 + 2 runtime coverage). 10 closed-POC sentinel cascade fixed atomically in C5 GREEN (3rd sentinel-cascade category: readFileSync against features/ai-agent paths in other POC sentinels — NEW invariant). REQ-004 insulation: find-accounts.ts + parse-operation.ts use AccountsLookupPort (NOT direct PrismaAccountsRepo); legacy-accounts.adapter.ts is sole insulation point. `import type` pattern systematic — all hex cross-boundary factory refs use type-only imports (regex must accept `(?:type\s+)?`). features/ai-agent/ DELETED wholesale in-POC at C5 (51 files). SHIMs NOT required. **OLEADA 5: 2/3 CLOSED** |
| ✅ `poc-financial-statements-hex` CLOSED (C5 executed) | `features/accounting/financial-statements` → `modules/accounting/financial-statements` hex (C0–C5 + D1) | 6,684 | 14 | 23 | C0 RED `3a550df5` · GREEN `bafcec65`; C1 RED `4b11ba6c` · GREEN `0fe2f58c`; C2 RED `d01ea335` · GREEN `e7588066`; C3 RED `2073f1eb` · GREEN `b8b9dcf5`; C4 RED `03e6f6c1` · GREEN `5e854c7c`; C5 RED `e7b4f01f` · GREEN `f1f1d1a9`; D1 (this commit). **256α PASS** (42 C0 + 12 C1 + 11 C2 + 18 C3 + 23 C4 + 28 C5 + 122 pure-function tests migrated at C0/C1). 2 ports: FinancialStatementsQueryPort (4 methods narrow surface) + AccountSubtypeLabelPort (R7 single-method). **D5 INVERSE** (server.ts + index.ts; NO client.ts — dispatch-hex pattern, NOT ai-agent dual-barrel; financial-statements has zero React hooks). **R1 PERMISSIBLE VALUE-TYPE EXCEPTION LOCKED**: domain/money.utils.ts imports Prisma.Decimal RUNTIME (value-type arithmetic engine, NOT Prisma-generated entity); textual precedent shared/domain/value-objects/money.ts:4-10. **ai-agent CROSS-MODULE DEBT CLOSED** at C4 (5 files in modules/ai-agent/ repointed atomically — closes follow-up deferred by ai-agent C4 ccab3a77). C4 atomic cutover: 12 prod + 5 ai-agent debt + 6 vi.mock = 23 consumers (12 prod = 8 routes + 4 components TYPE-only). **Composition root extension** at C4 GREEN: presentation/server.ts added `serializeStatement` re-export (symmetric with old features/.../server.ts `export * from "./money.utils"`); presentation/index.ts added buildBalanceSheetTableRows + buildIncomeStatementTableRows RUNTIME + 5 new TYPE re-exports for ai-agent debt + page-clients. **C5 sibling-feature cutover (15 files — PRE-C4 grep gap closure)**: PRE-C4 grep `':!features/'` exclusion MISSED sibling consumers (equity-statement, initial-balance, trial-balance, worksheet, iva-books, exporters/voucher-pdf, 4 sibling tests). Caught at delete time by tsc cascade (+25 errors); fixed atomically in C5 GREEN. Sibling consumers repointed to @/modules/.../presentation/server (money.utils) + @/modules/.../infrastructure/exporters/ (pdf.fonts + pdf.helpers) + @/modules/.../infrastructure/prisma-financial-statements.repo (class renamed FinancialStatementsRepository → PrismaFinancialStatementsRepo) + @/modules/.../domain/ (builders + types). **5 service-coupled features tests + 1 exporters/__tests__/pdf.helpers.test deleted at C5** per [[API_breaking_change_C1_blocks_C4_test_migration]] — C1 ctor change (zero-arg → deps-object) blocked naive C4 test migration; -46 behavioral tests accepted loss, coverage maintained by 256α cycle sentinels + sibling integration tests still PASS. **NEW invariant catalog candidate [[retirement_reinventory_gate_features_inclusion]]**: PRE-CN grep MUST NOT exclude `features/` paths when migrating from features/ → modules/ (sibling features in same legacy tree are valid consumers needing cutover). SHIMs NOT required. **OLEADA 5: 3/3 CLOSED (WAVE COMPLETE)** |
## OLEADA 6 — accounting cleanup multi-sub-POC umbrella (8 POCs)

| POC | Source | LOC | Tests | Consumers | Notas |
|---|---|---|---|---|---|
| ✅ `poc-accounting-trial-balance-hex` CLOSED (C5 executed) | `features/accounting/trial-balance` → `modules/accounting/trial-balance` hex (C0–C5 + D1) | 2,814 | 18 | 1 | C0 RED `16b3a819` · GREEN `1a3bce65`; C1 RED `e6e26f56` · GREEN `47b98180`; C2 RED `96acb983` · GREEN `5c747026`; C3 RED `5aa9281a` · GREEN `2a50c2ca`; C4 RED `52288f52` · GREEN `e99f9c0a`; C5 RED `90f4b82f` · GREEN `12f85a44`; D1 `e2daff68`. **117α PASS** cumulative (25α C0 + 12α C1 + 11α C2 + 18α C3 + 23α C4 + 28α C5). Suite: 6575 PASS / 73 FAIL / 17 skipped. tsc: 30 EQUAL baseline. PAIRED SISTER poc-financial-statements-hex (EXACT mirror). 1 runtime consumer (route.ts) + 3 vi.mock test declarations. D4 Option A LOCKED: sumDecimals + eq copied to own domain/money.utils.ts at C0 (break cross-module coupling). D7 Option A LOCKED: exporter cross-module infra imports from FS kept + flagged tech debt (sub-POC 6 resolution). Composition root: `makeTrialBalanceService()` zero-arg factory. D5 INVERSE (server.ts + index.ts, NO client.ts). R1 exception (Prisma.Decimal RUNTIME in domain/money.utils.ts + builder.ts + repo.ts). 4-layer hex: domain/application/infrastructure/presentation. features/accounting/trial-balance/ DELETED wholesale in-POC at C5 (7 src + 7 test + 2 exporters + 2 exporters test). SHIMs NOT required. REQ-009: ZERO cross-import from FS domain post-C0 (sentinel). REQ-010: cross-module INFRA import tolerated (tech debt flagged). **OLEADA 6: 1/8 CLOSED** |
| ✅ `poc-accounting-equity-statement-hex` CLOSED (C5 executed) | `features/accounting/equity-statement` → `modules/accounting/equity-statement` hex (C0–C5 + D1) | 4,275 | 17 | 1 | C0 RED `6c923368` · GREEN `386021f6`; C1 RED `5c2f0898` · GREEN `486efe2d`; C2 RED `0ad99fd0` · GREEN `f031a966`; C3 RED `c139a981` · GREEN `34ee0005`; C4 RED `356fb338` · GREEN `061d74de`; C5 RED `3b57a9d8` · GREEN `68de447d`; D1 (this commit). **121α PASS** cumulative (25α C0 + 14α C1 + 13α C2 + 18α C3 + 23α C4 + 28α C5). Suite: 6675 PASS / 73 FAIL / 17 skipped. tsc: 30 EQUAL baseline. PAIRED SISTER poc-accounting-trial-balance-hex (EXACT mirror structure). 1 runtime consumer (route.ts) + 3 vi.mock test declarations. D4 Option A LOCKED (inherited): sumDecimals + eq copied to domain/money.utils.ts. D5 INVERSE (inherited): server.ts + index.ts, NO client.ts. D7 Option A LOCKED (inherited): exporter cross-module infra imports from FS flagged tech debt (sub-POC 6). R1 exception (inherited): Prisma.Decimal RUNTIME in domain. D9 Option A LOCKED (NEW): IncomeStatementSourcePort 2-method application-layer port + PrismaIncomeStatementSourceAdapter thin wrapper. D10 LOCKED (NEW): cross-module DOMAIN→DOMAIN (FS pure functions) tolerated at application layer. 2-adapter composition root `makeEquityStatementService()`. 4-layer hex domain/application/infrastructure/presentation. features/accounting/equity-statement/ DELETED wholesale in-POC at C5. REQ-011 NEGATIVE (NEW): ZERO FS PRESENTATION/INFRA in application/ (sentinel). REQ-012 (NEW): IncomeStatementSourcePort + adapter existence sentinel. **OLEADA 6: 2/8 CLOSED** |
| ✅ `poc-accounting-worksheet-hex` CLOSED (C5 executed) | `features/accounting/worksheet` → `modules/accounting/worksheet` hex (C0–C5 + D1) | 4,021 | 15 | 1 | C0 RED `c5d94070` · GREEN `1b4f6521`; C1 RED `b55f6f3b` · GREEN `cde818c5`; C2 RED `cf6d31bd` · GREEN `ab494aac`; C3 RED `980139cd` · GREEN `d2628891`; C4 RED `4a7b56b2` · GREEN `cbfa03ef`; C5 RED `bbaaf034` · GREEN `25e0deba`; D1 (this commit). **117α PASS** cumulative (25α C0 + 12α C1 + 11α C2 + 18α C3 + 23α C4 + 28α C5). Suite: 6582 PASS / 73 FAIL / 17 skipped. tsc: 30 EQUAL baseline. PAIRED SISTER poc-accounting-trial-balance-hex (EXACT mirror structure). 1 runtime consumer (route.ts) + 3 vi.mock test declarations. WS-D1 NEW: WorksheetMovementAggregation + WorksheetAccountMetadata extracted from infrastructure to domain/types.ts at C0 (port interface dependency). D4 Option A LOCKED (inherited): sumDecimals + eq copied to domain/money.utils.ts (4th copy total — consolidation MANDATORY sub-POC 6). D5 INVERSE (inherited + CLEANEST): server.ts line 1 = `import "server-only"` already present (NO ADD needed at C3). D7 Option A LOCKED (inherited): exporter cross-module infra imports from FS flagged tech debt (sub-POC 6). R1 exception (inherited): Prisma.Decimal RUNTIME in domain. Single-port architecture WorksheetQueryPort (3 methods). 1-adapter composition root `makeWorksheetService()` zero-arg factory. 4-layer hex domain/application/infrastructure/presentation. features/accounting/worksheet/ DELETED wholesale in-POC at C5. REQ-009: ZERO FS cross-import in domain post-C0 (sentinel). REQ-010: cross-module INFRA import tolerated (tech debt flagged). **OLEADA 6: 3/8 CLOSED** |
| ✅ `poc-accounting-initial-balance-hex` CLOSED (C5 executed) | `features/accounting/initial-balance` → `modules/accounting/initial-balance` hex (C0–C5 + D1) | 3,145 | 16 | 1 | C0 RED `07298ed7` · GREEN `ea900774`; C1 RED `768d537b` · GREEN `db8b4a71`; C2 RED `b88f4161` · GREEN `21ddbd3b`; C3 RED `c25a5d11` · GREEN `5f936c70`; C4 RED `41ac0115` · GREEN `704901b1`; C5 RED `288aa2cd` · GREEN `7baa839a`; Retrofix `f511ad57`; D1 (this commit). **148α PASS** cumulative (25α C0 + 12α C1 + 11α C2 + 18α C3 + 23α C4 + 28α C5 + 31α migrated). Suite: 6674 PASS / 73 FAIL / 17 skipped. tsc: 30 baseline preserved (retrofix `f511ad57` restored from C5 drift 33→30). PAIRED SISTER poc-accounting-trial-balance-hex (EXACT mirror). 1 runtime consumer (route.ts) + 3 vi.mock test declarations. IB-D1 NEW: NO type extraction at C0 (axis-distinct from WS-D1 — port interface uses application-layer dto shape, NOT domain extraction). IB-D4 NEW: service ctor refactor positional → deps-object port-typed. D4 Option A LOCKED (inherited): sumDecimals + eq copied to domain/money.utils.ts. D5 INVERSE (inherited): server.ts + index.ts, NO client.ts. D7 Option A LOCKED (inherited): exporter cross-module infra imports from FS flagged tech debt (sub-POC 6). R1 exception (inherited): Prisma.Decimal RUNTIME in domain. Single-port 1-adapter InitialBalanceQueryPort (4 methods). Composition root: `makeInitialBalanceService()` zero-arg factory. 4-layer hex domain/application/infrastructure/presentation. features/accounting/initial-balance/ DELETED wholesale in-POC at C5 (7 src + 7 test + 2 exporters + 2 exporters test). SHIMs NOT required. 10 REQs ALL PASS. **5th sumDecimals copy** — consolidation MANDATORY sub-POC 6. **OLEADA 6: 4/8 CLOSED** |
| ✅ `poc-accounting-iva-books-hex` CLOSED (C2 executed) | `features/accounting/iva-books` RETIREMENT+CUTOVER (hex pre-exists POC #11.0c) → 22 consumer cutover atomic | 680 | 2 | 22 | C0 RED `71c2393f` · GREEN `0f14c7c7`; C1 RED `71e75fa6` · GREEN `44cfabc7`; C2 RED `4132ef44` · GREEN `992b3feb`; D1 (this commit). **72α PASS** cumulative (18α C0 + 25α C1 + 28α C2 + 1 gate). **3-cycle structure** vs sister 6-cycle (retirement-only POC justifies compressed scope). tsc: 34 baseline preserved (pre-existing ambient 30→34 baseline drift documented; zero-drift post-POC). Suite: 6674 PASS / 73 FAIL / 17 skipped. PAIRED SISTER poc-accounting-initial-balance-hex (EXACT archive template). Hex pre-exists: 50 files / 8,667 LOC domain+application+infrastructure complete from POC #11.0c A1-A4 (production-used). **Retirement mission**: relocate features/accounting/iva-books/ (8 src + 5 test) type+const+exporter residual to modules/iva-books/; atomic cutover 22 consumers; wholesale delete. C0 scope: DTO/validation to domain (copy-exact); NEW `presentation/index.ts` client-safe barrel (IVA-D1 NEW); NEW `presentation/legacy-bridge-constants.ts` TASA_IVA Prisma.Decimal P3.4 textual lock (IVA-D2 dual-constant duality); server.ts re-exports. C1 scope: exporter git mv `features/...excel.exporter` → `modules/.../iva-book-xlsx.exporter.ts`; atomic cutover 22 files (4 routes + 2 entity-to-dto + 2 pages + 2 components + 4 cross-module + 6 test + 2 vi.mock rewrite). C2 scope: `git rm -r features/accounting/iva-books/` wholesale delete; 2 POC-siguiente-A2 sentinels retired collateral. NEW invariants: IVA-D1 (presentation/index.ts created this POC), IVA-D2 (TASA_IVA dual constant by layer — domain numeric + presentation Decimal), IVA-D3 (3-cycle compression). Inherited: D5 INVERSE (server.ts + index.ts NO client.ts), R1 (Prisma.Decimal RUNTIME in presentation legacy-bridge), [[cycle_end_tsc_enumeration_mandatory]] gate (tsc 34 zero-drift preserved). REQ coverage: 12/12 PASS. Features-scope: ZERO consumers of calcTotales + roundHalfUp utils (dies with features/ delete); behavioral loss -645 LOC (validation.test + iva-calc.utils.test deleted; NOT migrated — validation schemas exist domain, tests merged to modules/iva-books domain tests post-GREEN verification). Consumers: 22 = 14 runtime + 6 test + 2 vi.mock (reduced from 24 proposal estimate pre-exploration). Consumer count EXACT; vi.mock rewrites ATOMIC same commit C1 GREEN per [[mock_hygiene_commit_scope]]. Verification: 5 closed-POC sentinels (IB c5 + WS c5 + ES c5 + TB c5 + TB c4) pre-C1 GREEN [[low_cost_verification_asymmetry]] PASS. **OLEADA 6: 5/8 CLOSED** |
| ✅ `poc-accounting-exporters-cleanup` CLOSED (C2 executed) | `modules/accounting/shared/` NEW namespace — shared infra extraction (pdf.fonts + pdf.helpers git mv from FS-infra; 5× sumDecimals+eq consolidation to shared/domain/money.utils.ts) | — | 32 | 7 | C0 RED `fe4cde4f` · GREEN `864e9f4f`; C1 RED `02cf3d8c` · GREEN `451314ff`; C2 `784d69fb` (docs). **32α PASS** cumulative (10α C0 + 22α C1). Suite: 621 passed / 0 failed / 1 skipped. tsc: 0. EX-D1: shared/ namespace, NO barrel index. EX-D2: shared/ SOURCE-only dep-direction invariant. EX-D3: 5 standalone sumDecimals+eq copies → shared canonical (TB/ES/WS/IB → thin re-export shims; FS → re-exports + keeps 6 richer fns). EX-D4: git mv pdf.fonts/pdf.helpers FS-infra→shared (history preserved); 7-consumer repoint atomic. EX-D5: R1-permissible-value-type-exception — shared/domain/money.utils.ts runtime-imports Prisma.Decimal. EX-D6: 11 sentinel inversions (6 REQ-010 c2/c5: TB c2 α46, ES c2 α52, WS c2 α46, WS c5 α117, IB c2 α44, IB c5 α117 + 5 c0-domain-shape: TB α6/α7, ES α6/α7, WS α8/α9, IB α7/α8, FS α10/α11 — re-inventory gap [[retirement_reinventory_gate]]). EX-D7: features/accounting/exporters/ INTACT; voucher-pdf deferred sub-POC 7. REQ-010 RESOLVED. **OLEADA 6: 6/8 CLOSED** |
| `poc-accounting-journal-ledger-core` | `features/accounting/journals/ledger` → `modules/accounting/journal-ledger-core` hex (DIFFERS: money-math core, NOT pure-data mirror) | — | — | — | pending — sub-POC 7/8 |
| `poc-accounting-shim-retirement` | Retire `@/features/accounting/` SHIM barrels wholesale (final cleanup) | — | — | — | pending — sub-POC 8/8 |

## Token optimization tactics

| Tactic | Aplicación |
|---|---|
| Paired sister reuse aggressive | Cada POC nuevo cita precedent EXACT mirror cumulative cross-POC matures heredado |
| Bundling sesiones | 1 sesión = 1 huérfano delete (POCs ≤ 100 LOC + 0 consumers) |
| Skip Step 0 expand | POCs simples sin tests heredados + 0 consumers cross-feature |
| Read selective cycle-start cold | Solo docs scope-relevant (NO architecture.md complete) |
| Engram cementación canonical homes | §13.X engram NEW por POC sin retroactive doc bloat |
| Atomic single batch single-axis discipline | Paired sister precedent EXACT mirror cumulative cross-POC matures |
