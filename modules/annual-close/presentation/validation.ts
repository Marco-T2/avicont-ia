/**
 * Annual-close presentation validation — STUB (Phase 5.2).
 *
 * Real Zod schema lands at Phase 5.3 GREEN. Stub keeps tsc + barrel imports
 * clean while Phase 5.1/5.2 ship composition-root + server.ts.
 *
 * Per [[red_acceptance_failure_mode]]: this file intentionally exports
 * nothing — Phase 5.3 RED will assert `annualCloseRequestSchema` exists and
 * FAIL until 5.3 GREEN populates it.
 */
export {};
