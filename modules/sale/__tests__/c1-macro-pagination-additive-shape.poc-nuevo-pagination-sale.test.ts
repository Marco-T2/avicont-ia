/**
 * POC nuevo pagination-sale C1-MACRO RED — cross-layer additive shape (port +
 * adapter + service + HTTP route + UI page/list/shadcn). 1 RED test file 5
 * describes layered atomic single batch RED+GREEN macro-cycle (evidence-
 * supersedes-assumption-lock retroactiva intra-POC #1782 ~6 ciclos scope
 * collapsed → C0 + C1-MACRO + D1 per Marco lock Opción C ADDITIVE NEW método
 * paralelo, supersede L1 BREAKING retroactivo Hub callsite emergente Step 0
 * expand cycle-resume cold cascade analysis force reconsideración).
 *
 * Marco lock pre-RED ajustado IN-PLACE Step 0 expand cycle-resume cold
 * (Opción C lock + filename A.2 lock):
 *   L1 SUPERSEDED → Estrategia signature change = Opción C ADDITIVE NEW método
 *      paralelo. Port añade `findPaginated(orgId, filters?, pagination?):
 *      Promise<PaginatedResult<Sale>>` paralelo a `findAll(orgId, filters?):
 *      Promise<Sale[]>` legacy preservado. Service añade `listPaginated(orgId,
 *      filters?, pagination?): Promise<PaginatedResult<Sale>>` paralelo a
 *      `list(orgId, filters?): Promise<Sale[]>` legacy preservado. Dual-API
 *      transitional axis-distinct — Hub callsite legacy intacto, página +
 *      route consume NEW listPaginated. Cleanup pending engram canonical home
 *      NEW `feedback/sale-find-all-legacy-vs-find-paginated-dual-method-
 *      cleanup-pending` paired sister 3 heredados (4 cumulative cross-POC).
 *      L1 retroactivo evidence-supersedes-assumption-lock 9na matures
 *      cumulative cross-POC + 2da intra-POC mid-cycle.
 *   L2 Granularity α = 1 RED file 5 describes layered (~11 regex existence-
 *      only red-regex-discipline). Mirror C0 4α escalado cross-layer cohesivo.
 *   L3 Scope supersede #1782 ~6 ciclos confirmado — POC pilot revisado
 *      C0 CLOSED + C1-MACRO + D1 (3 cycles total post-supersede).
 *   L4 shadcn Pagination strategy = `npx shadcn add pagination` CLI install
 *      (Marco lock F #1782 alineado).
 *   L5 StatusFilter SaleList axis-η Sub-φ = mover server-side via URL
 *      searchParams. SaleList drop client-side useState filter. Coherent UX
 *      pre-pagination filter.
 *   L6 Test path = `modules/sale/__tests__/c1-macro-pagination-additive-
 *      shape.poc-nuevo-pagination-sale.test.ts` (filename A.2 lock pre-Opción-
 *      C adjustment IN-PLACE — semantic precise additive vs cutover ambiguo).
 *      NEW naming convention tag `c{N}-macro-...` 1ra evidencia matures
 *      cross-layer cohesion.
 *
 * §13 NEW emergentes pre-RED post-Opción-C-adjustment ledger (D1 cumulative
 * cementación target — total POC ~15 §13 NEW post-supersede):
 *   1. §13 NEW C1-MACRO ADDITIVE dual-method paralelo (findAll+findPaginated,
 *      list+listPaginated) transitional 1ra evidencia matures cumulative
 *      cross-POC — paired sister BREAKING signature change pattern abandoned
 *      retroactive Hub callsite emergente. Hex-correctness preserved dual API
 *      transitional axis-distinct.
 *   2. §13 NEW Prisma `skip/take` + `count` parallel ($transaction[fM,count])
 *      pattern adapter PaginationOptions 1ra evidencia (en findPaginated impl
 *      paralelo a findAll legacy unbounded preserved).
 *   3. §13 NEW HTTP route Response.json wrap PaginatedResult<T> shape 1ra
 *      evidencia (raw json wrap pattern divergence vs Sale[] flat).
 *   4. §13 NEW Service NEW método return generic VO `PaginatedResult<T>`
 *      paralelo a list legacy 1ra evidencia (consume shared VO downstream
 *      port findPaginated).
 *   5. §13 NEW UI shadcn Pagination + URL search params sync RSC pattern 1ra
 *      evidencia (server-side pagination state in URL, client-side wire-up).
 *   6. §13 NEW Page.tsx `searchParams: Promise<...>` Next.js 16 RSC pattern +
 *      filters/pagination wire 1ra evidencia (params + searchParams paired).
 *   7. §13 NEW Test file naming `c{N}-macro-...` cross-layer cohesion 1ra
 *      evidencia (extiende c{N}-{descriptor} POC #11.0a A3 precedent).
 *   8. §13 NEW evidence-supersedes-assumption-lock retroactiva intra-POC
 *      scope collapse 1ra evidencia (recon #1782 ~6 ciclos → 3 ciclos
 *      post-Marco-lock pre-Opción-C).
 *   9. §13 NEW evidence-supersedes-assumption-lock 9na matures + 2da intra-POC
 *      mid-cycle (L1 BREAKING superseded retroactive Hub callsite emergente
 *      Step 0 expand cycle-resume cold cascade analysis surface honest).
 *  10. §13 NEW HubService cleanup pending engram canonical home `feedback/
 *      sale-find-all-legacy-vs-find-paginated-dual-method-cleanup-pending`
 *      paired sister 3 heredados (json-stringify + contact-snapshot-types +
 *      radix-dialog-aria + esta NEW = 4 cleanup pending cumulative cross-POC).
 *  11. §13 NEW invariant-collision-elevation cross-feature consumer hub.
 *      service.ts retroactiva intra-POC 2da matures (1ra fue scope supersede
 *      #1782 ~6 → 3 ciclos pre-Opción-C).
 *  12. red-regex-discipline existence-only 8va aplicación matures cumulative
 *      cross-POC (mirror C0 4α 7ma + payment C0-pre 6α + paired-pr C7-pre +
 *      cross-cycle C0/C1b/C3-C4/C5-C6 poc-nuevo-a3).
 *  13. textual-rule-verification recursive structural conventions 8va matures
 *      (mirror C0 7ma 4 conventions verified — c{N} naming + readFileSync +
 *      poc-id suffix + path resolve REPO_ROOT pattern).
 *  14. pre-phase-audit-gate retroactive 6ta evidencia matures cumulative
 *      (cascade analysis post-RED-α 4a80c3a Hub emergente surfaceó adjustment
 *      scope IN-PLACE pre-GREEN — cost-benefit asimétrico justifica check).
 *  15. mock-source-identification-gate matures cumulative (3 archivos
 *      clasificados Step 0 — hub.service.test.ts + sale.service.test.ts +
 *      in-memory-sale.repository.ts; Opción C preserva NO adapt necesario
 *      hub.service.test.ts existing list mocks intactos legacy preserved,
 *      sale.service.test.ts list mocks intactos, listPaginated NEW separate).
 *
 * Cross-ref engrams cumulative:
 *   - `poc-nuevo-pagination-sale/c0-closed` #1785 (paired sister 1er ciclo
 *     CLOSED — VO + schema + helper canonical home consumido downstream).
 *   - `discovery/pagination-recon-fcdb399` #1782 (canonical home recon — 12
 *     listados + 7 axes Marco lock + VO shape canonical APROBADO + POC pilot
 *     scope ~6 ciclos pre-supersede + axis-η Categ C++ HubService DEFER).
 *   - `poc-nuevo-pagination-sale/c1-macro-red-alpha` #1787 (bookmark pre-
 *     adjustment — Hub callsite emergente NO anticipado Step 0 expand cycle-
 *     resume cold cascade analysis surfaceó force L1 retroactive supersede).
 *   - `baseline/fcdb399-enumerated-failure-ledger` #1780 (7f enumerated
 *     explicit pre-RED preserved {6,9} envelope membership).
 *   - `arch/lecciones/dispatches-hub-flake-recurrente` rev 8 (§13.A3-D4-α 31ª
 *     timeout pole stuck preserved across cycle).
 *   - `feedback_red_acceptance_failure_mode` (failure mode honest 11/11
 *     enumerated forward-looking pre-GREEN preserved post-adjustment).
 *   - `feedback_canonical_rule_application_commit_body` (cite + rationale +
 *     cross-ref applied RED body — 15 §13 NEW emergentes cementación targets
 *     post-Opción-C-adjustment supersede).
 *   - `feedback_textual_rule_verification` (4 conventions verified ≥3
 *     evidencias pre-RED commit — c{N} naming + readFileSync + poc-id suffix
 *     + REPO_ROOT path resolve).
 *   - `feedback_invariant_collision_elevation` (Hub callsite emergente
 *     surfaced honest pre-GREEN → Opción C ADDITIVE elevated NO silently
 *     resolved BREAKING cascade scope expand).
 *   - `feedback_pre_phase_audit` (6ta evidencia matures cumulative cascade
 *     analysis post-RED-α surface adjustment scope IN-PLACE pre-GREEN).
 *
 * Cross-cycle RED test cementación gate forward-only D1:
 *   - C1-MACRO RED targets paths SALE-MODULE + APP-DASHBOARD/API + COMPONENTS
 *     (cross-layer atomic). D1 = doc-only cementación architecture.md +
 *     lecciones engram (NO RED tests futuros). Gate satisfecho trivialmente.
 *
 * Granularity α RED scope post-Opción-C-adjustment (11α existence-only
 * forward-looking 5 layered describes):
 *   D1 Port (2): T1 findPaginated NEW signature accept pagination + T2 return
 *      PaginatedResult<Sale> (findAll legacy preserved sin tocar).
 *   D2 Adapter (2): T3 skip/take pattern (en findPaginated impl) + T4 count
 *      query (en findPaginated impl).
 *   D3 Service (2): T5 listPaginated NEW signature accept pagination + T6
 *      return PaginatedResult<Sale> (list legacy preserved sin tocar).
 *   D4 HTTP route (2): T7 import parsePaginationParams + T8 call invocation
 *      (route consume NEW listPaginated).
 *   D5 UI (3): T9 page.tsx consume `.items` (de listPaginated) + T10 sale-
 *      list.tsx PaginatedResult shape import + T11 components/ui/pagination.
 *      tsx file exists (shadcn).
 *
 * Expected RED failure mode pre-GREEN (per `feedback_red_acceptance_failure_
 * mode`) post-adjustment 11/11 FAIL preservado EXACT:
 *   T1-T2 FAIL: regex MISMATCH — findPaginated NOT YET in port (legacy
 *   findAll preserved retorna Promise<Sale[]> intacto).
 *   T3-T4 FAIL: regex MISMATCH — adapter sin skip/take/count (legacy findAll
 *   findMany unbounded preserved intacto).
 *   T5-T6 FAIL: regex MISMATCH — listPaginated NOT YET in service (legacy
 *   list preserved retorna Promise<Sale[]> intacto).
 *   T7-T8 FAIL: regex MISMATCH — no parsePaginationParams import/call yet.
 *   T9-T10 FAIL: regex MISMATCH — no .items access in page yet, no
 *   PaginatedResult ref in sale-list yet.
 *   T11 FAIL: file ABSENT — `components/ui/pagination.tsx` NOT yet created
 *   pre-GREEN (shadcn install deferred a GREEN phase).
 *   Total expected FAIL pre-GREEN: 11/11 enumerated explicit (10 regex
 *   mismatch + 1 ENOENT) — preserved EXACT post-Opción-C-adjustment.
 *
 * Source-string assertion pattern: mirror precedent C0 + payment C0-pre +
 * paired-pr C7-pre + cross-cycle poc-nuevo-a3 (`fs.readFileSync` regex match
 * + `fs.existsSync` for file presence — keep red-regex-discipline pattern.
 * Existence-only assertions verify shape source post-GREEN; runtime behavior
 * assertions defer post-cementación D1 si Marco lockea integration tests).
 *
 * Self-contained future-proof check (lección A6 #5): test asserta paths bajo
 * `modules/sale/{domain,infrastructure,application}` + `app/api/.../sales/
 * route.ts` + `app/(dashboard)/.../sales/page.tsx` + `components/sales/sale-
 * list.tsx` + `components/ui/pagination.tsx` que persisten post D1
 * cementación. Test vive en `modules/sale/__tests__/` — NO toca paths futuros
 * fuera scope cumulative. Self-contained vs D1 (doc-only) ✓.
 */
import { describe, it, expect } from "vitest";
import * as fs from "fs";
import * as path from "path";

const REPO_ROOT = path.resolve(__dirname, "../../..");

// ── C1-MACRO target file paths (5 layers + shadcn install) ──────────────────

const PORT_FILE = path.join(
  REPO_ROOT,
  "modules/sale/domain/ports/sale.repository.ts",
);
const ADAPTER_FILE = path.join(
  REPO_ROOT,
  "modules/sale/infrastructure/prisma-sale.repository.ts",
);
const SERVICE_FILE = path.join(
  REPO_ROOT,
  "modules/sale/application/sale.service.ts",
);
const ROUTE_FILE = path.join(
  REPO_ROOT,
  "app/api/organizations/[orgSlug]/sales/route.ts",
);
const PAGE_FILE = path.join(
  REPO_ROOT,
  "app/(dashboard)/[orgSlug]/sales/page.tsx",
);
const SALE_LIST_FILE = path.join(
  REPO_ROOT,
  "components/sales/sale-list.tsx",
);
const PAGINATION_UI_FILE = path.join(
  REPO_ROOT,
  "components/ui/pagination.tsx",
);

// ── Regex patterns existence-only (red-regex-discipline 8va matures) ────────
// Opción C ADDITIVE: regex target NEW métodos `findPaginated` (port) +
// `listPaginated` (service) — paralelos a `findAll` + `list` legacy
// preservados sin tocar (Hub callsite axis-η Categ C++ DEFER #1782 preserved).

const PORT_FINDPAGINATED_PAGINATION_RE =
  /findPaginated\s*\([\s\S]*?pagination\??:\s*PaginationOptions/;
const PORT_FINDPAGINATED_RETURN_RE =
  /findPaginated\s*\([\s\S]*?\)\s*:\s*Promise<\s*PaginatedResult\s*<\s*Sale\s*>\s*>/;

const ADAPTER_SKIP_TAKE_RE = /skip:\s*[\s\S]*?take:\s*/;
const ADAPTER_COUNT_RE = /(?:db\.sale\.count\b|sale\.count\s*\()/;

const SERVICE_LISTPAGINATED_PAGINATION_RE =
  /listPaginated\s*\([\s\S]*?pagination\??:\s*PaginationOptions/;
const SERVICE_LISTPAGINATED_RETURN_RE =
  /listPaginated\s*\([\s\S]*?\)\s*:\s*Promise<\s*PaginatedResult\s*<\s*Sale\s*>\s*>/;

const ROUTE_IMPORT_PARSE_RE =
  /^import\s*\{[\s\S]*?parsePaginationParams[\s\S]*?\}\s*from\s*["']@\/modules\/shared\/presentation\/parse-pagination-params["']/m;
const ROUTE_CALL_PARSE_RE = /parsePaginationParams\s*\(\s*searchParams\s*\)/;

const PAGE_ITEMS_ACCESS_RE = /\.items\b/;
const SALE_LIST_PAGINATED_RE = /\bPaginatedResult\b/;

describe("POC nuevo pagination-sale C1-MACRO — cross-layer additive shape (port findPaginated NEW método paralelo legacy findAll preservado + Prisma skip/take/count adapter findPaginated impl + service listPaginated NEW método paralelo legacy list preservado + HTTP route parsePaginationParams wire consume listPaginated + UI page.tsx items access + sale-list.tsx PaginatedResult prop + shadcn pagination component install, §13 NEW C1-MACRO ADDITIVE dual-method paralelo + Prisma skip/take/count + HTTP route Response.json wrap + Service NEW método paralelo + UI shadcn + page searchParams RSC + test naming c{N}-macro + evidence-supersedes-assumption-lock 9na + invariant-collision-elevation Hub + HubService cleanup pending engram NEW + pre-phase-audit-gate 6ta + mock-source-identification-gate 15 §13 NEW emergentes 1ra evidencia matures cumulative cross-POC, 11α existence-only forward-looking pre-GREEN 11/11 FAIL: 10 regex mismatch + 1 ENOENT)", () => {
  // ── D1: Port — modules/sale/domain/ports/sale.repository.ts (Tests 1-2) ──
  // §13 NEW C1-MACRO ADDITIVE dual-method paralelo pattern 1ra evidencia +
  // Service NEW método return generic VO PaginatedResult<T> 1ra evidencia
  // consumed downstream. Hub callsite axis-η Categ C++ DEFER preserved
  // (legacy findAll intacto).

  describe("Port — sale.repository.ts (ADDITIVE findPaginated NEW método paralelo, legacy findAll preservado)", () => {
    it("Test 1: port findPaginated NEW signature accepts `pagination?: PaginationOptions` parameter (consume shared VO from C0 canonical home modules/shared/domain/value-objects/pagination — paralelo a findAll legacy unchanged)", () => {
      const source = fs.readFileSync(PORT_FILE, "utf8");
      expect(source).toMatch(PORT_FINDPAGINATED_PAGINATION_RE);
    });

    it("Test 2: port findPaginated NEW return type `Promise<PaginatedResult<Sale>>` (ADDITIVE NEW método paralelo a findAll legacy retorna Promise<Sale[]> intacto — Hub callsite preserved DEFER axis-η)", () => {
      const source = fs.readFileSync(PORT_FILE, "utf8");
      expect(source).toMatch(PORT_FINDPAGINATED_RETURN_RE);
    });
  });

  // ── D2: Adapter — modules/sale/infrastructure/prisma-sale.repository.ts ──
  // §13 NEW Prisma `skip/take` + `count` parallel pattern adapter findPaginated
  // impl PaginationOptions 1ra evidencia (en NEW método paralelo a legacy
  // findAll findMany unbounded preserved).

  describe("Adapter — prisma-sale.repository.ts (Prisma skip/take + count en findPaginated impl)", () => {
    it("Test 3: adapter findPaginated uses Prisma `skip` + `take` pattern (offset/page-based pagination math via PaginationOptions.page + pageSize — en NEW impl paralelo a findAll legacy)", () => {
      const source = fs.readFileSync(ADAPTER_FILE, "utf8");
      expect(source).toMatch(ADAPTER_SKIP_TAKE_RE);
    });

    it("Test 4: adapter findPaginated calls `count` query (total computation for PaginatedResult.total + totalPages — likely $transaction[findMany, count] parallel pattern en NEW impl)", () => {
      const source = fs.readFileSync(ADAPTER_FILE, "utf8");
      expect(source).toMatch(ADAPTER_COUNT_RE);
    });
  });

  // ── D3: Service — modules/sale/application/sale.service.ts (Tests 5-6) ──
  // §13 NEW Service NEW método return generic VO PaginatedResult<T> paralelo
  // a list legacy 1ra evidencia.

  describe("Service — sale.service.ts (listPaginated NEW método paralelo, legacy list preservado)", () => {
    it("Test 5: service listPaginated NEW signature accepts `pagination?: PaginationOptions` parameter (forwards to repo.findPaginated mismo VO contract — paralelo a list legacy unchanged)", () => {
      const source = fs.readFileSync(SERVICE_FILE, "utf8");
      expect(source).toMatch(SERVICE_LISTPAGINATED_PAGINATION_RE);
    });

    it("Test 6: service listPaginated NEW return type `Promise<PaginatedResult<Sale>>` (ADDITIVE NEW método paralelo a list legacy retorna Promise<Sale[]> intacto — driver consumers Hub legacy preserved DEFER)", () => {
      const source = fs.readFileSync(SERVICE_FILE, "utf8");
      expect(source).toMatch(SERVICE_LISTPAGINATED_RETURN_RE);
    });
  });

  // ── D4: HTTP route — app/api/.../sales/route.ts (Tests 7-8) ─────────────
  // §13 NEW HTTP route Response.json wrap PaginatedResult<T> shape 1ra
  // evidencia (consume parsePaginationParams helper from C0 canonical home +
  // service.listPaginated NEW método).

  describe("HTTP route — sales/route.ts (parsePaginationParams wire-up + listPaginated consume)", () => {
    it("Test 7: route imports `parsePaginationParams` from C0 canonical home `@/modules/shared/presentation/parse-pagination-params` (consume shared HTTP boundary helper)", () => {
      const source = fs.readFileSync(ROUTE_FILE, "utf8");
      expect(source).toMatch(ROUTE_IMPORT_PARSE_RE);
    });

    it("Test 8: route invokes `parsePaginationParams(searchParams)` to extract pagination VO from URLSearchParams (HTTP boundary translation presentation concern — feeds service.listPaginated NEW método)", () => {
      const source = fs.readFileSync(ROUTE_FILE, "utf8");
      expect(source).toMatch(ROUTE_CALL_PARSE_RE);
    });
  });

  // ── D5: UI — page.tsx + sale-list.tsx + shadcn pagination (Tests 9-11) ──
  // §13 NEW UI shadcn Pagination + URL search params sync RSC pattern 1ra
  // evidencia + Page.tsx `searchParams: Promise<...>` Next.js 16 RSC pattern
  // 1ra evidencia + StatusFilter axis-η Sub-φ moved server-side.

  describe("UI — page.tsx + sale-list.tsx + shadcn pagination", () => {
    it("Test 9: page.tsx accesses `.items` (consume PaginatedResult shape from saleService.listPaginated NEW método — server-side pagination wire)", () => {
      const source = fs.readFileSync(PAGE_FILE, "utf8");
      expect(source).toMatch(PAGE_ITEMS_ACCESS_RE);
    });

    it("Test 10: sale-list.tsx references `PaginatedResult` (import or prop type — consume paginated payload shape downstream of page.tsx)", () => {
      const source = fs.readFileSync(SALE_LIST_FILE, "utf8");
      expect(source).toMatch(SALE_LIST_PAGINATED_RE);
    });

    it("Test 11: components/ui/pagination.tsx file exists (shadcn pagination component installed via `npx shadcn add pagination` per Marco lock F #1782)", () => {
      expect(fs.existsSync(PAGINATION_UI_FILE)).toBe(true);
    });
  });
});
